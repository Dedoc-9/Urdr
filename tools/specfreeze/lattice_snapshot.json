{
  "algorithm": "setattr-closure-pruned-v1 (identity-wide-equivalent, S8)",
  "articulation_nodes": [
    "clockauth",
    "compose",
    "heightfield",
    "jurisdiction",
    "layertheorem",
    "liveness",
    "membrane",
    "opcost",
    "perception",
    "rollstore",
    "sealwrit",
    "warden",
    "wavefield"
  ],
  "coverage": "79 enumerated / 13 bounded (import-depth < ceiling)",
  "degree_distribution": {
    "1": 22,
    "11": 1,
    "13": 1,
    "16": 1,
    "2": 12,
    "21": 1,
    "28": 1,
    "3": 7,
    "4": 6,
    "5": 3,
    "6": 3,
    "8": 1
  },
  "depth_ceiling": 13,
  "digest_binds": "edges + eligible + excluded + dynamic (a changed coverage set fails integrity)",
  "dynamic_withheld": {
    "auditgraph": "globals()",
    "frontier": "register"
  },
  "edge_digest": "af28a76a61f3b881e91298f7b748c5508f887998197ab4f851307d5d31db6543",
  "edges": [
    "adaptcite|anamorphosis",
    "adaptcite|byteacct",
    "adaptcite|citation",
    "adaptcite|perception",
    "anamorphosis|perception",
    "audible|perception",
    "budget|jurisdiction",
    "budget|voxlat",
    "byteacct|anamorphosis",
    "byteacct|perception",
    "byteacct|throttle",
    "chunkload|glide",
    "chunkload|heightfield",
    "chunkstate|glide",
    "chunkstate|heightfield",
    "chunkstate|persist",
    "chunkstate|storecost",
    "citation|anamorphosis",
    "citation|byteacct",
    "citation|perception",
    "clockauth|hitbox",
    "clockauth|lagcomp",
    "clockauth|perception",
    "clslo|glide",
    "clslo|govern",
    "clslo|horizon",
    "clslo|opcost",
    "cohort|budget",
    "cohort|inputset",
    "cohort|jurisdiction",
    "cohort|liveness",
    "cohort|tilemin",
    "cohort|voxlat",
    "commute|chunkload",
    "commute|heightfield",
    "commute|terraform",
    "compose|field",
    "compose|glide",
    "compose|heightfield",
    "compose|lockstep",
    "compose|persist",
    "compose|storecost",
    "compose|worldstep",
    "cpredict|glide",
    "cpredict|heightfield",
    "cpredict|lockstep",
    "cpredict|splice",
    "crossing|wavefield",
    "crosswarden|hand",
    "crosswarden|warden",
    "dirward|warden",
    "driftgaze|chunkload",
    "driftgaze|glide",
    "driftgaze|heightfield",
    "driftgaze|rannull",
    "driftgaze|wire",
    "drive|heightfield",
    "fpcap|heightfield",
    "ghostsnap|interest",
    "ghostsnap|warden",
    "glide|heightfield",
    "govern|glide",
    "govern|opcost",
    "hand|glide",
    "hand|heightfield",
    "hand|splice",
    "hitbox|perception",
    "horizon|cpredict",
    "horizon|glide",
    "horizon|lockstep",
    "horizon|splice",
    "inputset|budget",
    "inputset|jurisdiction",
    "inputset|liveness",
    "inputset|tilemin",
    "inputset|voxlat",
    "jurisdiction|disjoint",
    "jurisdiction|recirc",
    "jurisdiction|voxlat",
    "lagcomp|hitbox",
    "lagcomp|perception",
    "latencyest|clockauth",
    "layertheorem|drive",
    "layertheorem|gaze",
    "layertheorem|heightfield",
    "layertheorem|stance",
    "layertheorem|traj",
    "layertheorem|view_witness",
    "layertheorem|wavefield",
    "lease|chunkload",
    "lease|heightfield",
    "lease|rannull",
    "lease|terraform",
    "liveness|clockauth",
    "lookahead|adaptcite",
    "lookahead|anamorphosis",
    "lookahead|byteacct",
    "lookahead|citation",
    "lookahead|perception",
    "membrane|disjoint",
    "membrane|frontier",
    "membrane|voxlat",
    "mesh|chunkload",
    "mesh|lease",
    "mesh|migrate",
    "mesh|nway",
    "mesh|rannull",
    "mesh|terraform",
    "migrate|chunkload",
    "migrate|lease",
    "migrate|nway",
    "migrate|rannull",
    "migrate|terraform",
    "nway|chunkload",
    "nway|commute",
    "nway|rannull",
    "nway|terraform",
    "oobprior|clockauth",
    "oobprior|latencyest",
    "oobprior|pingpolicy",
    "opcost|glide",
    "opcost|wardhom",
    "panelight|glide",
    "panelight|heightfield",
    "panewire|chunkload",
    "panewire|driftgaze",
    "panewire|glide",
    "panewire|heightfield",
    "panewire|rannull",
    "panewire|wire",
    "partition|chunkload",
    "partition|lease",
    "partition|migrate",
    "partition|nway",
    "partition|rannull",
    "partition|terraform",
    "patience|auditgraph",
    "persist|glide",
    "persist|horizon",
    "persist|storecost",
    "pingpolicy|clockauth",
    "pingpolicy|latencyest",
    "predict|drive",
    "predict|heightfield",
    "predict|lockstep",
    "priogov|glide",
    "priogov|govern",
    "priogov|opcost",
    "quintessence|chunkload",
    "quintessence|commute",
    "quintessence|heightfield",
    "quintessence|lease",
    "quintessence|rannull",
    "quintessence|terraform",
    "quintessence|testament",
    "rannull|chunkload",
    "rannull|commute",
    "rannull|glide",
    "rannull|heightfield",
    "rannull|persist",
    "rannull|resurrect",
    "rannull|storecost",
    "rannull|terraform",
    "resurrect|glide",
    "resurrect|heightfield",
    "resurrect|persist",
    "resurrect|splice",
    "resurrect|storecost",
    "rollstore|field",
    "rollstore|lockstep",
    "rollstore|rollback",
    "rollstore|storecost",
    "schedule|anamorphosis",
    "schedule|perception",
    "schedule|throttle",
    "sealframe|glide",
    "sealframe|heightfield",
    "sealsession|chunkload",
    "sealsession|driftgaze",
    "sealsession|ghostsnap",
    "sealsession|glide",
    "sealsession|heightfield",
    "sealsession|interest",
    "sealsession|panelight",
    "sealsession|panewire",
    "sealsession|rannull",
    "sealsession|wire",
    "sealwrit|authinput",
    "sealwrit|chunkload",
    "sealwrit|heightfield",
    "sealwrit|rannull",
    "sealwrit|wire",
    "slo|glide",
    "slo|govern",
    "slo|horizon",
    "slo|opcost",
    "splice|glide",
    "splice|heightfield",
    "stance|heightfield",
    "storecost|horizon",
    "storm|chunkload",
    "storm|heightfield",
    "storm|rannull",
    "storm|wire",
    "terraform|chunkload",
    "terraform|glide",
    "terraform|heightfield",
    "testament|chunkload",
    "testament|heightfield",
    "testament|lease",
    "testament|rannull",
    "throttle|anamorphosis",
    "throttle|perception",
    "tilecert|jurisdiction",
    "tilecert|liveness",
    "tilecert|voxlat",
    "traj|drive",
    "traj|heightfield",
    "wire|chunkload",
    "wire|heightfield",
    "wire|rannull"
  ],
  "eligible_laws": [
    "adaptcite",
    "anamorphosis",
    "audible",
    "boundedhist",
    "budget",
    "byteacct",
    "cayley",
    "chunkload",
    "chunkstate",
    "citation",
    "clockauth",
    "clslo",
    "cohort",
    "commute",
    "compose",
    "cpredict",
    "crossing",
    "crosswarden",
    "dirward",
    "driftgaze",
    "drive",
    "fpcap",
    "fpface",
    "geoquorum",
    "ghostsnap",
    "glide",
    "govern",
    "hand",
    "hitbox",
    "horizon",
    "inputset",
    "interest",
    "jurisdiction",
    "lagcomp",
    "latencyest",
    "layertheorem",
    "lease",
    "liveness",
    "lookahead",
    "magicdiv",
    "membrane",
    "mesh",
    "migrate",
    "nway",
    "oobprior",
    "opcost",
    "panelight",
    "panewire",
    "partition",
    "patience",
    "perception",
    "persist",
    "pingpolicy",
    "predict",
    "priogov",
    "provbind",
    "quintessence",
    "rannull",
    "resurrect",
    "rollstore",
    "schedule",
    "sealframe",
    "sealsession",
    "sealwrit",
    "slo",
    "splice",
    "splitview",
    "stance",
    "storecost",
    "storm",
    "terraform",
    "testament",
    "throttle",
    "tierview",
    "tilecert",
    "traj",
    "warden",
    "wardhom",
    "wire"
  ],
  "excluded_laws": {
    "ashdepth": 2.461,
    "auditgraph": 0.505,
    "autoroute": 0.977,
    "blindscreen": 0.616,
    "bombtest": 0.52,
    "disjoint": 2.076,
    "divergence": 0.535,
    "edgeattr": 27.744,
    "frontier": 1.734,
    "horn": 1.014,
    "recirc": 3.846,
    "tilemin": 1.307,
    "voxlat": 16.347
  },
  "excluded_reason": "scene_result baseline >= 0.30s on the measurement host (too slow to sweep as a full-severance law)",
  "longest_chain": [
    "sealsession",
    "panewire",
    "driftgaze",
    "wire",
    "rannull",
    "resurrect",
    "persist",
    "storecost",
    "horizon",
    "cpredict",
    "splice",
    "glide",
    "heightfield"
  ],
  "magic": "URDRLAT-SNAP1",
  "monotonicity": {
    "baseline_34_preserved": true,
    "fast_221_preserved": true
  },
  "n_edges": 221,
  "n_eligible": 79,
  "post_seal_laws": {
    "attest": "minted after the seal (URDRATT1, the graduation law: a graduated claim cites a COMMITTED log the gate re-reads, with every number derived rather than typed). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply unchanged.",
    "confound": "minted after the seal (URDRCNF1, the schedule law: a treatment axis may not be a proxy for elapsed time, and a cell is not an experiment). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "contact": "minted after the seal (URDRCON1, ground contact as a certified state). NOT swept: the 79-law severance measurement is historical evidence at commit 2f13fa2 and adding a module that did not exist then would rewrite a record to fit the present. Its scene_result baseline is 0.06s, so it is eligible on cost and unswept on chronology alone; the depth obligations in (c)/(d) apply to it unchanged.",
    "deeper": "minted after the seal (URDRDPR1, the explanation law: a timing difference with no counted difference is UNEXPLAINED). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "entry": "minted after the seal (URDRENT1, the entry-point door law: an entry point that takes a path must refuse a flag-shaped token in that position). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "framing": "minted after the seal (URDRFRM1, the frame-coverage law). NOT swept, for the same reason `contact`, `stride`, `lift` and `vantage` are not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "indexed": "minted after the seal (URDRIDX1, the index-coverage law: a gated module appears in the tree's own index, and 'the counts are current' is not 'the document is'). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "lift": "minted after the seal (URDRLFT1, the preservation measurement family). NOT swept, for the same reason `contact` and `stride` are not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "measure": "minted after the seal (URDRMSR1, the performance-claim admission law). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "mould": "minted after the seal (URDRMLD1, the state-shaped snapshot record). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "pedigree": "minted after the seal (URDRPDG1, the provenance law: a claim may only consume an artifact whose provenance is admissible, derived from the artifact first). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "pixelcost": "minted after the seal (URDRPXC1, the resolution-decision law: the form and budget verdicts derive from two committed, digest-distinct probe records with declared conditions, and extrapolation to unmeasured resolutions is structurally impossible). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged \u2014 it is a LEAF importing only `hashlib` and `os`; live down-depth 1.",
    "probelog": "minted after the seal (URDRPBL1, the graduation law: the first \u00a73 log becomes evidence through sealframe's existing door, every figure derived from the committed record at claim time). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged \u2014 it is a LEAF importing only `hashlib` and `os`; the sealframe machinery it grades against is INJECTED by its callers, so its live down-depth is 1.",
    "reachable": "minted after the seal (URDRRCH1, the gate-reachability detector). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "reflow": "minted after the seal (URDRRFL1, the reflow law: a line break is not a claim, so no pattern in the count guard may consume an inter-word space it cannot consume a newline in). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged \u2014 it imports `doc_currency`, which lives OUTSIDE the swept universe (tools/terrain, tools/netcode, tools/physics) and is itself a leaf on `os` and `re`, so its live down-depth is 1.",
    "rehearse": "minted after the seal (URDRRHS1, the structural law: an admissible artifact must be reproducible from its declared plan). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "repeat": "minted after the seal (URDRRPT1, the variance-level law: a difference smaller than the between-execution spread is not a difference). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "retain": "minted after the seal (URDRRTN1, the snapshot ablation sweep). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "retire": "minted after the seal (URDRRET1, the retirement sweep: a retired law names its successor and nothing outside its own module may call it). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "rollbench": "minted after the seal (URDRRBN1, the rollback timing harness). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "stride": "minted after the seal (URDRSTR1, the 3D deterministic tick). NOT swept, for the same reason `contact` is not: the 79-law severance measurement is historical evidence at commit 2f13fa2, and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "vantage": "minted after the seal (URDRVAN1, the first-person frame). NOT swept, for the same reason `contact`, `stride` and `lift` are not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxsample": "minted after the seal (URDRVXA1, the construction law: two programs can name the same sample point and still disagree, because the point is not what is quantised \u2014 the object it is tested against is, and the earliest truncation in the pipeline dominates the latest). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxbaggage": "minted after the seal (URDRBAG1, the census law: before optimising further, ask which of the CHARGED operations the promoted artefact would actually perform \u2014 derive LIVENESS from the code and DECLARE category separately, never conflating a fact with an argument, and pair the finding with a CONTROL so a verdict of dead is a measurement rather than an inability). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxtrace8": "minted after the seal (URDRTR81, the corpus law: when a defect is found in the CORPUS a measurement was taken on, re-run the measurement on a corrected corpus and ship the result BESIDE the original rather than editing it \u2014 and obtain the correction BY DECLARED PROCEDURE, first-match in a fixed order, so the new datum cannot have been chosen for its effect on the numbers). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxtile": "minted after the seal (URDRVTL1, the parameter law: before concluding that an approach loses, VARY THE CONSTANTS IT WAS MEASURED AT \u2014 and when a sweep produces a winner, CHARGE EVERY TERM THE WINNING ARM PAYS THAT THE BASELINE DOES NOT, because the uncharged terms are worth most to the arm that is winning). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxschism": "minted after the seal (URDRVXX1, the census law: before building competing implementations, COST every candidate on every unit of the workload and ask whether the populations a real selector could READ actually separate them \u2014 and pair every reported ZERO with a PLANT that is allowed to memorise, so a null result is a measurement rather than an instrument that cannot measure). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxbreak": "minted after the seal (URDRVXZ1, the break-even law: an optimisation is scored by a LEDGER of separate accounts against a baseline that is an ALREADY-COMMITTED number, never a re-derivation of one \u2014 and a hypothesis stated before a measurement but never COMMITTED earns no pre-registration credit, because pre-registration is commit order or it is nothing). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxfriction": "minted after the seal (URDRVXY1, the friction law: a deliberate small computation whose only purpose is to prevent a larger one is COST-SHARED with the work it gates rather than compared against it, its payoff is a COUNTERFACTUAL that must actually be run, and its correctness asymmetry \u2014 a wrong decision either way costs performance and never the observable \u2014 is proved on both degenerate limits). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxmanifold": "minted after the seal (URDRVXV1, the kill law: a search experiment is entitled to answer NO, and an attractive analogy that fails three independent measurements is CLOSED rather than softened \u2014 with the ambiguity in the rung's own pre-registration scored on its LITERAL text rather than the reading that flatters the result). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxstate": "minted after the seal (URDRVXU1, the search-space law: a search experiment declares the space it searches in a SEPARATE rung from the search, so the answer cannot be circular \u2014 and a measured saturation that kills the rung's own first law is kept as the finding rather than tuned away). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxcond": "minted after the seal (URDRVXQ1, the certificate law: a fast path is scored against a prediction committed in an EARLIER commit and quoted by digest, its retirement is BASELINE MINUS EXECUTED so an untaken path can earn nothing, and the loop it sits on is reported beside it so a local win is never mistaken for a global one). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxpath": "minted after the seal (URDRVXJ1, the second-trace law: a question the frozen corpus was not designed to answer gets a NEW declared input rather than a reinterpretation of the old one \u2014 and the prediction for the rung that will use it ships one commit earlier, because commit order is the only mechanism that proves a prediction came first). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxsilo": "minted after the seal (URDRVXH1, the silo law: optimisation arms are separated deliberately and bound by a MECHANICAL lateral contract \u2014 byte-identical observable \u2014 and measured across the full combination lattice, because a stack of individually-positive optimisations can be beaten by a subset of itself). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxwork": "minted after the seal (URDRVXO1, the ruler law: a performance arc opens with an instrument PROVED not to move what it measures, and no wall clock enters a deterministic gate \u2014 counts on-gate, wall-clock off in a named-host record). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxcam": "minted after the seal (URDRVXB1, the promotion law: a candidate that WINS is still not the reference, because establishing a repair and promoting one are different acts \u2014 and a repair that moves the DEPTH half of the observable is a contract change and not a bug fix). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxproj": "minted after the seal (URDRVXP1, the candidate law: a repair is predicted before it is run and every prediction is scored, so a rung cannot report its hits and lose its misses \u2014 and a candidate that costs more than it buys is refused however good its motivation). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxwin": "minted after the seal (URDRVXW1, the complement law: a wrong pixel has two sides, and measuring only the loser leaves the mechanism unnamed \u2014 the winner must be chased separately, and set equality rather than a matching count is what makes two classifications agreeing evidence). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxslack": "minted after the seal (URDRVXK1, the margin law: a wrong decision taken ON a decision surface and one taken far from every surface are different defects, and only the signed distance tells them apart). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxgrid": "minted after the seal (URDRVXG1, the scale law: a claim about a sampled rate that carries no scale is a claim with room in it, and the ladder the rate was measured on is the axis that separates the artefact from the property). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxconv": "minted after the seal (URDRVXN1, the re-derivation law: a population selected under a convention is a measurement of that convention until it is derived again under the other, and the arm that reproduces the committed rungs is what licenses reading the arm that does not). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxfill": "minted after the seal (URDRVXL1, the convention law: an oracle DERIVED from the thing it audits cannot detect a convention error in it, so the deciding experiment varies both sides and a pairing that agrees with itself is evidence a pairing that does not cannot give). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxfate": "minted after the seal (URDRVXS1, the conditioning law: a fate distribution read off a mixed population is a mechanism for an average of unrelated mechanisms, so the population is conditioned before it is explained and the unconditioned run is pinned beside it). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxtie": "minted after the seal (URDRVXT1, the event-surface law: a disagreeing sample is a defect or a point on a visibility discontinuity, and one-sided limits decide which). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxcand": "minted after the seal (URDRVXD1, the repair-candidate law: a repair may be established without being promoted, and the reference it proposes to fix stays untouched until every declared fact passes). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxevent": "minted after the seal (URDRVXE1, the incidence and visible-surface census: a subdivision ladder that multiplies primitives while moving no world point). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxmicro": "minted after the seal (URDRVXM1, the oracle-qualification law: an elementary scene answers as declared, and every disagreeing pixel carries a named fate). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged. IT IS THE FIRST MODULE OF ITS ARC THE COVERAGE CLAUSE EVER ASKED ABOUT, and it asked because of an attribute NAME: `voxref`, `voxray` and `voxcoarse` pin conformance scenes through scene_result/golden and define no `SCENES`, so clause (e) never sees them \u2014 measured in voxmicro.the_sibling_modules_escape_the_coverage_clause.",
    "vouch": "minted after the seal (URDRVCH1, the witness replay certificate). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxrun": "minted after the seal (URDRRUN1, the census-precondition law: before building a mechanism that amortizes one proof over a SEQUENCE of work, measure whether the work arrives in runs AND whether those runs are knowable BEFORE the work they would retire \u2014 recording each cost variable separately and combining none of them, since a census that concluded with a business case would be the business it exists to decide on). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "attributed": "minted after the seal (URDRATB1, the measurement-attribution law: every quantitative percentage literal in shipped measurement prose must resolve to exactly ONE named live measurement accessor or be explicitly classified as non-measurement, with the declared values pairwise distinct, the exemptions disjoint from them, hedges no rescue, and every printed digit a TRUE digit \u2014 a rendering being the exact value truncated at its own precision and never rounded). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged, and it sits at depth ONE so that a shared law costs no depth to the modules that adopt it.",
    "voxreanchor": "minted after the seal (URDRRAN1, the control law: a candidate that exploits a structure must be measured against an arm that exploits the SAME structure without the input the candidate claims credit for, so a result is attributed to the mechanism that produced it rather than to the one that was proposed \u2014 and a pre-registration whose unit of analysis the experiment refutes is DISPOSED in a separate record, never edited, since a record rewritten to match what was learned proves nothing about what came first). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "shadowcut": "minted after the seal (URDRSHC1, the shadow-adjudicator law: a bounded production algorithm is questioned by an INDEPENDENT formulation run BESIDE it rather than in place of it, with the subject imported and unchanged, every answer witnessed by the subject's own primitive for sufficiency AND minimality, and the replace-or-retain decision DEFERRED to a later rung \u2014 because a remedy adopted because it is stronger, rather than because it answers the same question, is a re-baselining wearing a repair's clothes). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "voxin": "minted 2026-08-06, FOUR DAYS AFTER the seal at commit 2f13fa2 (URDRVXI1, the import boundary) \u2014 but it entered this register only now, when it acquired `scene_result` and `SCENES` at all. IT SHIPPED WITH NO CONFORMANCE CORPUS AND NOTHING SAID SO: `voxin:scenes` compared the occupancy digest to ITSELF and `voxin-placement` compared the Rust port to the LIVE Python, so every reader in the tree was live-against-live and a drift moved the whole population together. The pin exists now and this clause is what noticed the module was unregistered \u2014 a coverage partition that is TOTAL over current scene_result laws, working as designed on the first module to join them since the seal. NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. The depth obligations in (c)/(d) apply to it unchanged.",
    "blindabsolute": "minted after the seal (URDRBAB1, the absoluteness rung: `blindscreen`'s registered criterion tested and refuted \u2014 all five predictions MISSED, including the arm the record named in advance as the one that could refute it). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "chargecurve": "minted after the seal (URDRCHG1, the registered experiment run: `cohort`'s stated-and-unrun charge-curve protocol, scored against C1-C5 committed one commit earlier WITH the classifier, so the rule could not be chosen once the numbers arrived \u2014 and the finding is that the shipped one-line falsifier, taken at its word, would have adopted the peak). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "cutbound": "minted after the seal (URDRCBD1, the refusal-is-evidence law: a bounded search that declines to answer has PROVED a lower bound, so every consumer must read its refusal as the bound it is rather than as an absence \u2014 and a formulation that removes the bound in exchange for a complete answer trades a THEOREM for a WITNESS, which is a loss of provenance dressed as a gain in reach). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "cutpin": "minted after the seal (URDRCPN1, the pinned-derivation law: an expensive proof may be cached out of the hot path only when the cache binds a digest over the LIVE SOURCE of everything that could change its answer, refuses rather than answering once stale, can record only the one shape a cheap re-derivation could corroborate, and NAMES the exact increment taken on trust \u2014 caching evidence is admissible, caching it without saying what is no longer being proved is not). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "session": "minted after the seal (URDRSES1, the event-time membership law: when the set of authorized writers CHANGES over time, admission must be decided by the authorization in force AT THE EVENT rather than at the receiver \u2014 a check asking whether a peer is a member NOW makes admission depend on delivery order, which is not a stricter policy but a NON-DETERMINISTIC one, and it fails silently because every participant stays internally consistent). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "disposition": "minted after the seal (URDRDSP1, the forward-commitment law: a registered prediction is a DEBT, so every discoverable record must reach exactly one disposition that NAMES the mechanism discharging it \u2014 and the discharger is derived from a CROSS-MODULE call, which makes a registrar structurally unable to score its own record and so unable to back-date the commit order the whole pre-registration scheme rests on). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "ratchet": "minted after the seal (URDRRAT1, the direction law: a pin compared to a live reading at an INSTANT has no direction, so every declared monotone debt must have its direction enforced against a pinned historical baseline \u2014 and the population of such promises is read out of shipped prose rather than inferred from the shape of an integer, because a ratchet is a declared quantity plus a declared direction plus a baseline plus an enforcement and only the quantity has a syntax). NOT swept, for the same reason every other post-seal law is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot be added to it without editing a record to fit the present. The depth obligations in (c)/(d) apply to it unchanged.",
    "gamegen": "minted after the seal (URDRGEN1, the first game-layer vertical slice: seed + depth -> canonical level -> digest, admitted under D24 \u00a71 as CORE without amendment). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports nothing under tools/ at all \u2014 `hashlib` and `os`, read off its AST by `gamegen-layer` \u2014 so it sits at import-depth 1 and can host no REQUIRES chain; the depth obligations in (c)/(d) apply to it unchanged and are trivially met.",
    "descent": "minted after the seal (URDRDSC1, the topology witness: the stairs are connected, or they are not, and the witness is a PATH \u2014 the second game-layer vertical slice, D24 \u00a73's topology row made a law). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen` (import-depth 2) and stdlib and nothing else under tools/, read off its AST by `descent-witness`; the depth obligations in (c)/(d) apply to it unchanged and are met.",
    "move": "minted after the seal (URDRMOV1, the first authoritative transition D_n -> D_{n+1}: an entity steps one cell, MOVED or BLOCKED, and a wall step is the legal BLOCKED pair KINEMA's Plant A consumes). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen`, `descent` and stdlib and nothing else under tools/, read off its AST by `move`'s layer test; it consumes `descent.traversable` (import-depth 3 through descent) and can host no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met.",
    "entity": "minted after the seal (URDRETY1, the canonical entity as a content-addressed component in one identity vocabulary — `move` names it by its digest instead of an inline position, so there is no second serialization for `statecanon` to reconcile). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. Its RECORD imports nothing under tools/ — `hashlib` and `os`, read off its AST at MODULE SCOPE by `entity-laws` — so the record sits at import-depth 1 and can host no REQUIRES chain; the scene corpus reaches `gamegen` and `descent` only LAZILY (function-local), which is not a module-load edge, and the same row proves it (they appear in the full walk, not at module scope). The depth obligations in (c)/(d) apply to it unchanged and are met.",
    "rngstream": "minted after the seal (URDRRNG1, the canonical RNG stream — the first STATEFUL canonical component: rooted at the run's seed through a domain-separated root, advanced only by canonical actions, its digest entering canonical identity). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. Its RECORD imports nothing under tools/ — `hashlib` and `os`, read off its AST at MODULE SCOPE by the layer test — so the record sits at import-depth 1 and can host no REQUIRES chain; `gamegen` and `move` are reached only LAZILY in the laws (function-local), which is not a module-load edge (`gamegen` for the domain single-source and the not-a-gamegen-derivation cross-check, `move` for the isolation boundary). The depth obligations in (c)/(d) apply to it unchanged and are met.",
    "descend": "minted after the seal (URDRDEP1, the authoritative depth transition: from the down-stairs of the level at depth d to the level at d+1 from the same seed, the entity arriving at that level's stairs-up). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen`, `descent` and stdlib at MODULE SCOPE (read off its AST); `move` is reached only LAZILY in the laws (for the spawn-equivalence witness and to name the successor's identity in the one `move` state vocabulary), which is not a module-load edge. It consumes `descent` (import-depth 3 through descent) and `gamegen`, reinvents neither, and can host no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met.",
    "loot": "minted after the seal (URDRLOO1, the first canonical gameplay consumer of the RNG stream: source + stream -> drop, `(source, R_n) -> (drop, R_{n+1})`, the first real action that advances URDRRNG1). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen` (the Level type), `move` (the source identity `state_digest`) and `rngstream` (peek/advance) at MODULE SCOPE, and stdlib, read off its AST -- and NO `descent`, because traversability is not a loot rule. `move` reaches `descent` transitively, so loot sits at import-depth 4 through the move->descent edge, and it can host no REQUIRES chain of its own; the depth obligations in (c)/(d) apply to it unchanged and are met. `rngstream` imports no `loot`, so the RNG law and the loot law have separable failure surfaces.",
    "combat": "minted after the seal (URDRCMB1, the certified damage-resolution law: resolve(attack, defense) = max(0, attack - defense) over a declared byte domain, a derived result rather than a state transition). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `hashlib` and `os` ONLY at module scope (read off its AST) -- a stdlib LEAF at import-depth 0, with NO `entity`, `move`, `rngstream`, `gamegen` or `descent`, because the smallest combat law is deterministic (no stream) and a derived result (no entity mutation). It is a pure function that hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met trivially. Persistent health is deferred: `entity.FIELDS` is position-only and the chain entity.at -> entity.digest_at -> move.state_bytes means an `hp` field would break `move`, so it belongs at the rung that needs persistence.",
    "heirloom": "minted after the seal (URDRHEI1, the certified generational-growth law: heir(q) = q + (q * NUM) // DEN with NUM/DEN = 1/8, monotone non-decreasing, a derived quantity rather than a persistent state). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `hashlib` and `os` ONLY at module scope (read off its AST) -- a stdlib LEAF at import-depth 0, with NO `entity`, `persist`, `rngstream`, `move`, `gamegen` or the direction-and-baseline register, because the transition was settled quantity -> quantity' (a pure function). It hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met trivially. Where the quantity LIVES is deferred: the game layer has no persistence substrate (the game-layer persist rung is unbuilt, and the existing persist.py/URDRLAT5 is a different arc no game-layer module imports), entity.FIELDS is position-only so a progression field would break `move`, and the direction-and-baseline authority operates on source constants across git history -- so a container inside heirloom would be architectural invention, and the roadmap's predicted entity/direction-register dependencies are superseded by measurement.",
    "actionlog": "minted after the seal (URDRACT1, the authoritative recoverable action history: order composed via rngstream's fold from a declared seed-independent root, actions readable back where the stream discards them). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `rngstream` and stdlib (`hashlib`, `os`) ONLY at module scope (read off its AST), with NO `move`, `entity`, `gamegen`, `descent` or `persist` -- the first CONSUMER (not a leaf) since `loot`, at import-depth 1 through rngstream (itself import-depth 0), and it hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met. It COMPOSES rngstream's ordered fold rather than duplicating it: the log digest IS rngstream.apply from LOG_ROOT and append is one rngstream.advance, building no second hash chain (the ordering functions carry no hashlib; the only hashlib.sha256 is the root constant). The tokens stay opaque because no canonical action vocabulary has been earned, so it imports no move/entity; a typed vocabulary, the replay-sole-input claim, disk persistence and entity binding are deferred. rngstream imports no actionlog, so the two authorities have separable failure surfaces.",
    "savegame": "minted after the seal (URDRSAV1, the durable serialization of the earned canonical components: seed, depth, pos, (n,R_n) and the actionlog entries, each independently recoverable; D24 §3 persistence, chain role persist). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen`, `entity`, `rngstream`, `actionlog` and stdlib (`hashlib`, `os`) at module scope (read off its AST), and CRUCIALLY NO `persist`, `storecost` or `horizon` -- so the MMO rollback-window model of persist.py/URDRLAT5 is NOT inherited; only the tree's SHA(MAGIC|content) content-addressing vocabulary is reused, which ~25 modules share. It is a consumer of the component stack (import-depth 2 through actionlog->rngstream) and hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met. PERSIST STORES STATE; REPLAY DERIVES IT: each component is serialized independently and reconstructed DIRECTLY (the level regenerated from seed/depth, the entity from pos, the stream from (n,R_n), the log from its entries), never by folding the actionlog -- a log that folds to a different stream still restores the stored stream. It has its own module name because persist.py is occupied by the MMO arc. The record is a durable serialization of the earned components, NOT the assembled canonical D_n (statecanon's, rung 11); disk I/O, multi-entity rosters and manifests/windows are deferred.",
    "enact": "minted after the seal (URDRENA1, the typed action authority: a KIND, a payload and one dispatch binding each recorded action to the transition that owns it -- the prerequisite between actionlog and replay). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen`, `move`, `descend`, `loot`, `actionlog` and stdlib (`hashlib`, `os`) at module scope (read off its AST), with `savegame`/`entity`/`rngstream` reached only LAZILY inside laws; it is a consumer of the dispatch stack and hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met. The KINDS are EXACTLY the three canonical state transitions by code sweep (MOVE->move.step, DESCEND->descend.descend, LOOT->loot.loot); combat and heirloom are pure derivations (declared ints -> a number, holding no canonical component) and are EXCLUDED, because including them would manufacture a state authority rather than discover one. dispatch BINDS the authorities and reimplements no transition (read off the AST), RNG advances only on LOOT, and ordering is actionlog's append order; cross-peer canonical ordering is lockstep.canon's (tools/netcode), whose (tick, peer, seq) key game actions do not carry, so it is unearned here and deferred to replay. The neutral ruler is savegame, which stored the components independently of any dispatch; from-origin replay, cross-peer reconciliation, the assembled D_n and health/inventory binding are deferred.",
    "rerun": "minted after the seal (URDRRRN1, the from-origin replay certification: reconstruct the origin from earned state, fold the recovered action history through enact, and certify it reproduces the independently stored savegame; D24 section 3 replay). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen`, `move`, `rngstream`, `entity`, `actionlog`, `enact`, `savegame` and stdlib at module scope (read off its AST), and NO `lockstep` -- it is a certification layer over the earned stack and hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met. THE ORIGIN RECONSTRUCTS FROM EARNED STATE (Slice B, closed): depth0 = savegame.depth - (DESCEND count in the actionlog), because depth moves only via descend (+1); level0 = gamegen.generate(seed, depth0); pos0 = move.spawn(level0); stream0 = rngstream.root(seed) -- no origin field added to savegame, no hidden record minted. IT ADDS NO AUTHORITY: the fold IS enact.apply, so replay re-derives no transition and advances no RNG (the stream moves only through loot via enact); the discriminator is the mutable (pos, stream) and depth equality is tautological. The verdict is REPRODUCED/DIVERGED and the binding -- that the independently-stored log folds the reconstructed origin to the independently-stored state -- is the one savegame declines to make. SINGLE-PEER: cross-peer union is lockstep.canon's, whose (tick, peer, seq) key game actions do not carry (Slice A), unearned and deferred.",
    "statecanon": "minted after the seal (URDRSTC1, the assembled canonical identity D_n over the four earned component identities; D24 section 2 assembled state). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen`, `entity`, `rngstream`, `actionlog` and stdlib (`hashlib`, `os`) at module scope (read off its AST), and NO `savegame`, `rerun` or `lockstep` -- it composes the four earned component identities and hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met (import-depth 2 through actionlog->rngstream). IT EARNS EXACTLY ONE NEW AUTHORITY: the assembled identity D_n = SHA-256(MAGIC | lvl:<level_digest> | ent:<entity_digest> | rng:<stream_digest> | log:<actionlog digest>), one new SHA over the four already-earned identities, reimplementing none of them (it calls level_digest/entity_digest/stream_digest/digest, never canon_bytes/entity_bytes/stream_bytes/stream_of). The action history is IN the identity (actionlog.digest is a component), and no separate seed field is added because level_digest already commits to seed and depth. The authority/view boundary holds by construction: a view-only field cannot enter entity, so it cannot reach D_n. Read off the AST, the assembly reaches all four authorities and mints exactly one sha256 and reaches no raw serialization -- a mutant reproducing D_n while bypassing an authority reddens. Ownership stays crisp: savegame asks can-I-restore, rerun asks does-the-history-reproduce-it, statecanon asks what-is-the-identity; none absorbs another, and statecanon exposes no savegame/rerun API. SINGLE-PEER: cross-peer (tick, peer, seq) is Slice A, unearned and deferred; multi-entity rosters and hp/inventory are unearned; observers stay KINEMA's.",
    "kinema": "minted after the seal (URDRKIN1, the one-way observer-space refinement of an authoritative transition; D25's cinematic membrane, and the first game-layer VIEW module). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen`, `descent`, `field` (the frozen Q32.32 radix) and stdlib (`ast`, `collections`, `hashlib`, `os`) at module scope (read off its AST), and NO `move`/`descend`/`loot`/`enact`/`statecanon`/`rerun`/`lockstep` -- it is a VIEW consumer of canonical state and hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met. IT PRODUCES A VIEW, IT DOES NOT AFFECT STATE (D24 section 2 / D25 section 2): handed a real `enact` transition D_n -> D_{n+1} identified at both ends by `statecanon`, it refines the POSITION between the endpoints in Q32.32 -- refined = A*ONE + ((alpha*(B*ONE - A*ONE)) // ONE), landing alpha=0 on A and alpha=ONE on B exactly -- never the digest (a 64-hex SHA is un-interpolatable). Only a MOVED transition is spatially refined; a DESCEND is a discrete LEVELCUT and a LOOT/BLOCKED is a FIXED point. Containment consumes `descent.traversable`, the real topology; a forged wall-crossing or non-adjacent pair refuses KINEMA-REFUSE. THE MEMBRANE IS ONE-WAY: a Frame carries both endpoint witnesses verbatim (the D15 terrain_view pattern), the AST guard reads the FULL AST (function-local imports included, closing the escape hatch) and is direction-aware (no authority module, no .step/.dispatch/.apply mutator) with a positive control, no CORE module imports kinema, and the observer-presence differential (D25 section 14) shows the canonical transcript is byte-identical whether or not kinema is exercised. The arithmetic is reference on the frozen radix; a bounded-i64 model is a declared cross-placement obligation (D25 section 6). Multi-cell/curved trajectories, camera/pose, voxels, developer-mode authoring, network/replay connection and any frame budget are deferred.",
    "chorus": "minted after the seal (URDRCHO1, the composition of N disposable observers over one authoritative transition; WINDOW-0 of the observer-composition arc, the second game-layer VIEW module). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `gamegen`, `descent`, `kinema` and stdlib (`ast`, `collections`, `hashlib`, `os`) at module scope (read off its AST), and NO `move`/`descend`/`loot`/`enact`/`statecanon`/`rerun`/`savegame`/`lockstep` -- it is a VIEW consumer that composes `kinema` and hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met (import-depth one above `kinema`). IT COMPOSES OBSERVERS, IT DOES NOT AFFECT STATE: OBSERVER TOPOLOGY IS DISPOSABLE, CANONICAL SIMULATION IS NOT. compose(transition, specs) is PURELY repeated `kinema` application -- a Scene of per-observer Frame-sets -- so there is no N-window authority (N is a list length, N=0 the empty Scene). The endpoint witnesses (D_n values) enter opaque and are carried verbatim; it computes no D_n and the guard forbids `.d_n` ingestion outright. Agreement is provenance not pixels (panes of one transition share source_tick + endpoint witnesses + class while samples differ); permutation invariance is CANONICAL-ONLY (the provenance multiset is invariant, the ordered scene is deliberately not certified equal); the ObserverSpec window blob (bounds/focus/z-order/camera/zoom/layout) is disposable and inert this rung; staleness is provenance (a stale pane carries older witnesses, never a synthesised between-state). Topology is a list operation, and the CANONICAL topology differential -- mutating the collection, N=0 included, leaves the real enact/statecanon/rerun core byte-identical -- is proven at the gate. THE MEMBRANE IS ONE-WAY: the AST guard reads the FULL AST (function-local imports included) and is direction-aware (no authority module, no .d_n/.dispatch/.apply/.step) with a positive control, and no CORE module imports chorus. A forged transition refuses KINEMA-REFUSE (inherited); cadence independence is panelight's. window -> enact (WINDOW-1), OS windows, GPU composition, camera architecture, voxels, wireframes, developer authoring and any frame budget are deferred.",
    "cue": "minted after the seal (URDRCUE1, the one-way input membrane: a raw window/device event becomes a typed enact token; WINDOW-1 of the observer-composition arc, the input-side mirror of chorus, the third game-layer VIEW module). NOT swept, for the same reason every module from `contact` on is not: the 79-law severance measurement is historical evidence at commit 2f13fa2 and a module that did not exist then cannot appear in it. It imports `enact` (its token codec ONLY) and stdlib (`ast`, `collections`, `hashlib`, `os`) at module scope (read off its AST), and NO `move`/`descend`/`loot`/`statecanon`/`rerun`/`savegame`/`lockstep` -- it is a VIEW producer that turns a raw event into a token and hosts no REQUIRES chain of its own, so the depth obligations in (c)/(d) apply to it unchanged and are met (import-depth one above `enact`). IT BINDS INPUT, IT DOES NOT AFFECT STATE: FOCUS IDENTITY IS NON-AUTHORITATIVE, THE CANONICAL ACTION STREAM IS DETERMINED BY THE INPUT NOT BY WHICH WINDOW ORIGINATED IT. bind(event) is a PURE map from a raw Event(focus, key) to a typed enact token, reading no level/pos/stream/D_n (a sealed observer whose signature cannot receive canonical state); focus is a routing label consumed by nothing. Focus identity alone is non-authoritative: holding the binding table and the raw key sequence fixed and rerouting focus yields a byte-identical token stream, so canonical replay is byte-identical (checked at the gate against a real enact/savegame/rerun core); a different binding table may change the tokens (configuration is not focus authority, and the map is real). The token vocabulary is enact's via enact.encode (one vocabulary, two responsibilities); the binding table is data. Refusal is a three-tier ladder: an unbound/malformed event is CUE-REFUSE (this membrane's), a bad payload is ENACT-REFUSE (the codec's), and a valid-but-illegal token is refused downstream by the authority (DESCEND-REFUSE through enact.dispatch, the gate's). THE MEMBRANE IS ONE-WAY: the AST guard reads the FULL AST (function-local imports included) and is direction-aware (enact for its codec only, no transition/identity authority in any scope, no .dispatch/.apply/.d_n/.step) with a positive control, and no CORE module imports cue. Stateful/timed input, chords, key-repeat, modifiers, focus arbitration, a raw-event history channel, OS-native input, gamepads and any frame budget are deferred or rejected."
  },
  "post_seal_reason": "laws minted after the sealed sweep. LIVE and deliberately OUTSIDE the edge digest \u2014 the seal is a historical record and may not be edited to cover new work; what bounds this register instead is that every entry carries the same depth obligations as an excluded law, plus a reason, an existing module, and no overlap with the sealed partition.",
  "runtime_s": 450.4,
  "source_commit": "2f13fa2",
  "top_in_degree": [
    [
      "heightfield",
      28
    ],
    [
      "glide",
      21
    ],
    [
      "chunkload",
      16
    ],
    [
      "rannull",
      13
    ],
    [
      "perception",
      11
    ],
    [
      "terraform",
      8
    ],
    [
      "anamorphosis",
      6
    ],
    [
      "voxlat",
      6
    ]
  ]
}
