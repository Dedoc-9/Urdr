# URDROPC3 - priogov: the PRIORITY work governor (T3.31, MMO Stage H). Admit the highest-effective-priority
# prefix within the tick op-budget (effective = base + age_step*wait, so aging prevents starvation).
# SHA-256(MAGIC | name | budget | age_step | per-actor served ticks). 5 actors, base priorities 1..5.
#
# BOUNDARY, TRANSCRIBED from this module's own docstring rather than authored here. The claim was
# already graded in code and merely absent from the PINNED artifact, which is where a boundary has
# to live to be durable and scannable — this was placement debt, not absence. Transcribed from the
# module's GRADE section (its last does_not_show), because an earlier occurrence can be a reference
# to another module's boundary rather than this one's:
# does_not_show`: WALL-CLOCK (bounds WORK per tick; the time bound is `bench.py`'s,
# MEASURED-on-named-host); strict-priority WITHOUT aging (that CAN starve — this deliberately ages
# to bound the wait); pre-emption mid-actor; a budget-filling (skip-and-continue) packing (this
# keeps the clean priority PREFIX — a non-fitting actor stops the tick, it is not skipped for a
# lower one).
prio_two_per_tick f05a3802d5f189825f83f11e21edaf6b28f3e779fee95c661d4cd5f80eabbd6e
prio_one_per_tick ae58b0f74d9c31398fd43e7a31ecd2257e6a5c3c6af442a80be62b2fd4d86e6b
prio_no_aging 4c410882b6a1d3f261b0a02487fe3b30e3e4430c928621c36692580193bddee1
