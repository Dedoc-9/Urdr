# URDRVXK1 decision-surface slack census — emitted by voxslack.py, pinned by hand, compared by the
# gate.
#
# DIAGNOSTIC ONLY: no arm, no candidate, no altered renderer, no moved convention. One question —
# for each of the 378 stable disagreements, how far INSIDE or OUTSIDE each of the rasteriser's own
# decision surfaces does the oracle's face sit? A wrong decision taken ON a surface is a margin
# defect. A wrong decision taken far from every surface is not, and no threshold change can honestly
# rescue it.
#
# THE SIGN CONVENTION IS DECLARED, not inferred: POSITIVE means the predicate is satisfied with that
# much room, ZERO means the sample lies exactly ON the decision surface, NEGATIVE means it failed by
# that much. Every slack is an exact integer in the units the reference itself computes in — edge
# function for coverage, camera Q8 for depth — and a pixel that never reached a surface records
# `None` rather than a zero pretending to be a decision the reference never made.
#
#     318 not_covered      coverage slack       -1 exactly           215
#                                               within one pixel     103
#                                               beyond                 0
#
#     58 depth_rejected    depth slack          exact tie              2
#                                               under one cell         8
#                                               a whole cell or more  46
#                                               should have won        0
#
#     2 phantom            the oracle returns nothing at all
#
# EVERY COVERAGE MISS IS WITHIN ONE PIXEL OF THE SURFACE AND NOT ONE IS BEYOND IT. 215 fail by
# EXACTLY -1, which is the top-left bias and nothing else: the sample sits precisely ON the edge,
# and the only thing rejecting it is the convention `voxfill` and `voxconv` have now exonerated
# twice. The other 103 fail by a real but sub-pixel amount — the floored projected vertex. The split
# is between two MECHANISMS rather than two distances, and it maps onto `voxconv` exactly: under the
# aligned convention the 215 vanish and a sub-pixel remainder survives.
#
# THE FIRST VERSION OF THIS LAW DEMANDED A `beyond` CLASS AND REDDENED. The probe behind it bucketed
# on the raw edge-function magnitude and reported 95 pixels beyond a pixel; the edge function is an
# AREA, not a distance, and dividing by the edge length — `voxfill`'s exact squared comparison, no
# root taken — puts every one of them inside a pixel. The law refused to be satisfied by a structure
# that was not there, BEFORE the claim was written down. That is the first time in this arc the
# refusal has landed on the near side of a commit rather than the far side.
#
# AND THE 58 ARE NOT A MARGIN DEFECT AT ALL, WHICH REDIRECTS A WHOLE BRANCH OF THE DIAGNOSIS. Their
# depth slack is not near zero: 46 of 58 lose by a WHOLE CELL OR MORE, median 1.27 cells and maximum
# 6.06, only 2 are exact ties, and NOT ONE should have won. Meanwhile their COVERAGE slack is hugely
# POSITIVE — median 13552, deep inside the triangle — so they are nowhere near a coverage boundary
# either. THE DEPTH COMPARISON IS DOING ITS JOB. The oracle's face really is farther away and it
# loses honestly.
#
# SO WHAT IS WRONG AT THOSE 58 IS THAT A NEARER FACE COVERS THE PIXEL AT ALL. They are the SAME
# coverage defect seen from the winner's side rather than the loser's, which means the fate
# decomposition `voxfate` established — 318 coverage, 58 depth, 2 anomaly — is really 376 COVERAGE
# and 2 anomaly, with the 58 counted at the wrong end. A rung that had read `depth_rejected` as a
# depth problem would have gone looking for a defect in a comparison that is behaving correctly.
#
# DECLARED: the five decision surfaces in the order the rasteriser applies them; the sign
# convention; the coverage buckets in PIXELS (through `voxfill`'s exact squared comparison) and the
# depth buckets in CELLS, because a depth slack is a distance in the world and the cell is the
# world's own unit; and the fate vocabulary, inherited from the rung that owns it.
#
# DERIVED at claim time (L75): every slack, every bucket, the per-surface reach counts and the
# population digest.
#
# WHAT EACH GOLDEN COVERS
#   * buckets — both bucket distributions and how many pixels REACHED each surface. The reach counts
#     are the instrument's own coverage: 376 pixels reach the coverage test and only 58 reach the
#     depth test, because a face that does not cover a pixel never gets a depth comparison, and
#     reporting a depth slack for the other 318 would be inventing a decision.
#   * slack — the digest over every pixel's five slacks. THE POPULATION IS PINNED, NOT THE BUCKETS:
#     two different sets of 318 pixels would tally the same and digest differently.
#
# THE INSTRUMENT IS A SIXTH TRANSCRIPTION of the same rasteriser loop, kept because this rung needs
# the DEPTH BUFFER that `voxfate`'s does not return — and pinned to it in three ways at once: winner,
# stages and covered sets must all match on every declared frame.
#
# does_not_show, and these bound everything above:
#   * NOTHING ABOUT PERFORMANCE.
#   * WHICH face wrongly covers those 58. This rung measures the LOSER's distance to every surface
#     and does not chase the winner, because that is a different experiment and combining them is
#     how a diagnosis stops being one.
#   * NO MECHANISM for the sub-pixel 103.
#   * NO READING of the 2 phantoms — a population too small to carry one, refused as a law.
#   * NOTHING ALTERED. No arm, no candidate, no convention moved, no renderer changed; `voxref` and
#     `voxray` are untouched and the frozen census stays frozen.
buckets 4e64d4ca848aeef8066c18f3d86a4f9cf8b6df0fe9c9ef2b458c7a310947ce70
slack 6fc3c3a1689dc8a0dbc15804a998aa7bef68c4c4004981517f4b95d30afe0e4b
