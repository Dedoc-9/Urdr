# URDRLAT5 - persist: the persistent snapshot checkpoint (T3.36, MMO Stage H). The DURABLE realization of
# storecost's space bound. record = MAGIC | boundary | storecost payload | SHA-256 (the one digest: integrity,
# content address, filename), record_bytes(n) = 52 + 25*n; manifest binds the H+1 consecutive boundaries in
# order, manifest_bytes(H) = 47 + 40*(H+1); durable_window_bytes(H, n) = window_storage(H, n) +
# envelope_overhead(H). A tampered record is PERSIST-REFUSE; an over-budget durable window is STORAGE-REFUSE.
# SHA-256(MAGIC | name | n | horizon | record_bytes | durable_bytes | verdict).
#
# BOUNDARY, TRANSCRIBED from this module's own docstring rather than authored here. The claim was
# already graded in code and merely absent from the PINNED artifact, which is where a boundary has
# to live to be durable and scannable — this was placement debt, not absence. Transcribed from the
# module's GRADE section (its last does_not_show), because an earlier occurrence can be a reference
# to another module's boundary rather than this one's:
# does_not_show`: WALL-CLOCK or bandwidth of the writes (`bench.py` territory,
# MEASURED-on-named-host); CRASH-ATOMICITY of the filesystem (a torn or partial write is DETECTED
# on load — refused, not repaired — never PREVENTED; fsync/rename ordering is the OS's, not
# certified here); COMPRESSION or delta-encoding (the raw canonical size, the honest upper bound);
# CONCURRENT writers; and the FIELD's own storage (static and shared — until live world edits).
one_h4_durable 44ab484f6a1d8af1d792cf061c1dc36effe87dae255f774833c1f1d35a339295
five_h8_durable 4d17a0f6c9114764c5b40ebe8ba951a75e089da519a019a7103cc0a13a03961e
tampered ed4255523fe8401ab912e4a8fe37014472d77b75d03245cef6bb87ede108cc1c
