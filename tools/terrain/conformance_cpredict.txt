# urdr-terrain (T3.20, MMO Stage A × Stage B) — URDRCPRED1 continuous client-prediction reconcile canon:
#   `predict` on `glide`. Reconcile a client's guessed inputs against the authority on the CONTINUOUS Q32.32
#   trajectory (glide command boundaries), rolling back via `splice`'s resumption. Each digest binds (name,
#   scene, auth, pred, sub, mispredict_tick, reconstructed trajectory). `correct` = perfect prediction (k
#   None, no rollback). `mispredict` = a mid-window walk-vs-north divergence. `subcell` = THE REFINEMENT
#   WITNESS: a walk guessed where the authority sprinted into an east wall — same CELL (drive: no
#   mispredict, k None) but a different SUB-CELL pose (glide: k=1), so the continuous reconcile catches what
#   the grid misses. In every case reconstruct == glide_cells(authority) bit-for-bit, and floors to
#   drive(authority) — the continuous rollback refines the discrete one without contradicting it. Same host,
#   same bytes; no float, no clock.
# name  digest
#
# BOUNDARY, TRANSCRIBED from this module's own docstring rather than authored here. The claim was
# already graded in code and merely absent from the PINNED artifact, which is where a boundary has
# to live to be durable and scannable — this was placement debt, not absence. Transcribed from the
# module's GRADE section (its last does_not_show), because an earlier occurrence can be a reference
# to another module's boundary rather than this one's:
# does_not_show`: the input-PREDICTION policy (how a client guesses is a DECLARED heuristic — this
# certifies the reconcile for ANY guess); network transport / jitter / loss (the protocol, not
# this primitive); and **WALL-CLOCK latency and tick budget are `NOT_MEASURED`** — no timing or
# scale claim, none until the sealed bench (Stage H). Refusals are typed `CPRED-REFUSE` (window
# mismatch, empty window, bad transcript / subdivision): refuse, never smooth.
correct 47bc9b5de35f3ba07dfbef23f235f4e9edfb625b66cb489ab49c4001f5e047cf
mispredict 39e6b70ef2139e9d306456edfa96a7837c97c1452dbda30788c6dac3e20b55aa
subcell b4e03a041e8901149a63ff982b0f3f37aaf6814175fc2434d9aa42adb95328bf
