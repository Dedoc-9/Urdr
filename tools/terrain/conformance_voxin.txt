# URDRVXI1 conformance — THE IMPORT BOUNDARY HAD NO COMMITTED AUTHORITY, AND EVERY READER WAS LIVE.
#
# `voxin` shipped the arc's front end and pinned NOTHING. Four readers existed and all four compared
# a live computation to another live computation:
#
#     verify.py  voxin:scenes          d1 = digest(SCENE); d2 = digest(SCENE); d1 == d2
#     verify.py  voxin-placement       the Rust output == the LIVE Python digest
#     tests      permutation invariance digest(rotated) == digest(SCENE)
#     tests      determinism           digest(SCENE) == digest(SCENE), ten times
#     tests      two scenes differ     digest(SCENE) != digest(other)
#
# A repository-wide search for a committed hex literal of this digest returned ZERO. Determinism is a
# property of the RUN; permutation invariance is a property of the FUNCTION; neither is a claim about
# WHICH lattice the pinned scene imports to. So a change to `SCENE` or to the overlap rule moved the
# whole population together and the gate stayed green.
#
# DEMONSTRATED RATHER THAN INFERRED, AND WITH AN INERT CONTROL. A plant that leaves the measurand
# still proves nothing and is reported as proving nothing:
#
#     plant                      voxels   measurand moved   declared
#     one coordinate by one          51   NO                INERT — the control
#     one triangle dropped           39   yes               observable
#     one coordinate by eight        59   yes               observable
#
# Under EITHER observable plant the four voxin rows stayed GREEN and all eighteen unit falsifiers
# stayed GREEN. `the_pin_catches_what_the_old_reading_could_not` re-runs exactly that comparison on
# every gate pass: determinism still HOLDS under each observable plant and the committed authority
# does NOT, which is the difference the pin buys, kept as a live measurement rather than a paragraph.
#
# A SECOND COPY IS NOT A PIN, AND THAT IS WHY THE PLACEMENT DID NOT CLOSE THIS. `voxin_rs/voxin.rs`
# carries the scene under its own comment, "transcribed from voxin.SCENE" — so `voxin-placement`
# established agreement between two LIVE computations, which is a fact about the transcription rather
# than about the lattice. On a host without `rustc` that row records True, SKIPPED and honestly
# labelled, which is correct and also means such a host cannot falsify the relation at all. This is
# `armpair`'s and `retire`'s recurring shape: two hand-kept copies agreeing says only that the
# transcription was faithful on the day it was made. BOTH IMPLEMENTATIONS NOW READ ONE COMMITTED
# VALUE — `occupancy` below is the exact string each prints — so a drift on either side reddens
# against the authority rather than against the other side, and it reddens with no `rustc` present.
#
# THE SHARPEST FORM OF THE DEFECT: `voxin-placement-selftest` already pinned `41` — the voxel count
# the PORT'S PLANTED DEFECT produces. THE DEFECTIVE VALUE WAS COMMITTED AND THE CORRECT ONE WAS NOT.
# That literal is deliberately left where it is: it is a property of the port's own plant, and moving
# it here would be tidying rather than measuring.
#
# AND THE MODULE'S `does_not_show` HAD STOPPED BEING TRUE. It read "cross-placement — URDRVXI1 is a
# Python reference with no Rust or C99 port, so every figure here is single-implementation", and
# `voxin_rs` landed FOUR DAYS LATER with a live gate row. A boundary statement that denies a port the
# same tree ships is `claim != code` inside the sentence whose whole job is to bound the claim —
# `indexed`'s lesson, arriving in a module's own prose: a `does_not_show` is a promise about
# EVIDENCE, not a licence to stop reading it. Corrected, and the correction carried here as history.
#
# `occupancy` IS PINNED RAW and the others are scene digests, because the Rust emits an occupancy
# digest and cannot emit a scene case. The corpus is exactly what the module EMITS; it stays FROZEN
# rather than regenerated by the gate, since a golden the gate rewrites cannot detect drift.
#
# does_not_show: that the pinned lattice is the RIGHT one — it is the one this importer produces, and
# a wrong-but-stable import would be pinned just as firmly, which is what `voxin-law`, `voxin-property`
# and the voxlat oracle are for; that the scene RESEMBLES anything; that the Rust and the Python share
# no defect — they share a specification, and a fault in `voxlat`'s overlap law would move both and
# this pin with them on the day it was re-minted; and nothing about any scene other than `SCENE`.
census 527d00aaec525814f852bc3d4716c60ced62ff242de99d410fccb53c9d2ec229
plants 7a66a31c069f47b46d77138e9c9489c444fa3b87ce94830c975451feefd269b6
occupancy 22a7253516a202c87d34c6185792dd6ad870e119dc327c1f1c8aef5315ac7f47
voxin 0e5f82b6839732df05cc9b26fe1bef0fdc4719970bedfab7c7664bfb8dd331b9
