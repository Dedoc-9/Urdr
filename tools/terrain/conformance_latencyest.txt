# URDRLES1 conformance — THE LATENCY-ESTIMATOR that feeds clock-authority: measure the attested clock
# (lat, jitter) URDRCLK1 consumes from the acknowledgment / round-trip stream, and defend it against a
# slow-drip latency forge. From a window of ack samples (sent_tick, recv_tick): RTT = recv-sent; the one-way
# latency is the MINIMUM RTT // 2 (a cheater can delay an echo but never speed it up, so the min is the
# inflation-proof floor); the published estimate RISES by at most MAX_RISE per update (anti-drip) and FALLS
# freely (an improved ping tightens immediately — never a cheat); the jitter is the bounded spread capped at
# MAX_JITTER; an implausible RTT (negative or > MAX_RTT) is REFUSED, never folded in. The estimate feeds
# URDRCLK1 directly. Scene digests SHA-256(MAGIC | name | input | prev_lat | lat | jitter | status); the sweep
# digest is over a fixed-seed (20260725) 120-arena sweep asserting the min floor (an inflated window's latency
# equals the honest floor while the mean plant inflates it), the rate limit (a drip rises at most MAX_RISE
# while the no-ratelimit plant jumps), the jitter cap, the plausibility rejection, and the END-TO-END
# composition (the honest estimator feeding URDRCLK1 refuses a backdate a defective estimator's widened band
# would admit). Pinned AFTER the mean / no-ratelimit / no-plausibility plants were proven to bite and the
# sweep proven to redden under a mean-based estimator. The 88-byte published record is proof-carrying (bound
# to the ack window; a forged higher latency fails verify_record). Composition over URDRCLK1 (over URDRLAG1,
# URDRHIT1, URDRPCP1) — NO new glyph (kernel frozen). Declared: this bounds and slows band-widening, it does
# not make inflation impossible; the ping-scheduling / sample-selection policy is the declared successor.
honest 28b1c090edafbdf648b90d5bb806d272ce89ce36958664846af6fb7cf6926488
inflate 7f884f804627ce26ae376ba6f881f61ac964f415f8fb5295a6745d69f4189443
drip 930893751b9616109581efc6edff99fc5f655ac01016bd7330ad9846671985ba
improve 58cc63ebad5e2395438b67a485970126ae0f6005fe7c29bfa3c5dab5a38c22bb
implausible 01019ce48e41914e571cb43605abf0e2ad5a670679d1ccf33096c9c615742fd9
sweep 958ecddfad8722d804466d6470dbd1344b1af37ca34f77d37498d483a1735acc
