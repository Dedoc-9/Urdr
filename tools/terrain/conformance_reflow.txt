# SPDX-License-Identifier: AGPL-3.0-only
# conformance_reflow.txt — URDRRFL1 pinned scenes.
#
# A LINE BREAK IS NOT A CLAIM, AND A DEFAULT APPLIED ONCE IS A PREFERENCE.
#
# `doc_currency` is the tree's count guard. Its docstring records that on 2026-07-16 an escape was
# closed — a COMMA had broken the Rust word matcher and a stale count sat through two bumps — and
# states that "the self-defect plants exactly that shape so the escape can never silently reopen."
#
# It reopened through a NEWLINE. `hainuwele/README.md` hard-wrapped `2825 unit\nfalsifiers` across
# a line; the idiom carried a LITERAL SPACE between the two words; the scanner returned NOTHING for
# that document — not a wrong number, no number at all — while it also carried `896 gate rows`
# against a live 964 and `123 modules` against a live 125.
#
# AND THE CURE WAS ALREADY WRITTEN DOWN, IN THAT SAME FILE. The `_ABSENCE` note reads: "the third
# time in this repository that a checker missed a phrase because of where an author happened to
# break the line ... NORMALIZING IS NOW THE DEFAULT FOR PROSE MATCHING RATHER THAN A FIX APPLIED
# PER CASE." It was applied at exactly one call site — the one its author had just been bitten by.
# Seven of the module's fourteen patterns still carried a literal space when this rung opened.
#
# audit     — the pattern set DERIVED by walking `doc_currency`'s namespace (including the regexes
#             nested inside its pattern lists), the exact three-shape sensitivity test, and the
#             proof that a pattern added tomorrow enters the audit without this file being edited.
# behaviour — the pinned wrapped witness read as bytes; the repair proved NECESSARY by restoring
#             the literal spaces and watching the witness go unread; the 2026-07-16 comma escape
#             proved still closed; and the boundary demonstrated rather than confessed.
#
# does_not_show: ONE MODULE is audited — a wrap-sensitive matcher in `freeze_check`, `provenance`
# or `claimclass` would not be seen. WHITESPACE ONLY — the comma escape, markdown emphasis and
# every other inter-word token remain the province of the patterns themselves, and closing one
# class of escape is not closing the class of escapes. And nothing here says a matched number is
# CORRECT: this rung makes the guard able to SEE a claim; `doc-currency` decides whether it is true.
#
# THE FALSE RED THIS RUNG ALSO FOUND, RECORDED BECAUSE IT CUTS THE OTHER WAY. Normalizing exposed
# `README.md`'s "and two\n  detectors (toric, rigidity) are multi-runtime" to the word-form detector
# idiom, which means the D17 library size (10). The count idioms are REFERENT-AMBIGUOUS between a
# global claim and a local one, and the line break had been accidentally protecting that sentence.
# The prose was disambiguated rather than the guard weakened — but the ambiguity is a property of
# the idiom, it is still there in digit form, and the file whitelist is the only thing holding it.
#
# v1.2 (2026-08-13) — THE AUDIT'S SEARCH DOMAIN WAS LARGER THAN WHAT ITS INSTRUMENT COULD DISCOVER.
# v1.1 reported this module fully audited: 19 patterns, 0 wrap-sensitive, INVARIANT. The audit walks
# the module NAMESPACE, so it sees pattern objects that are BOUND. It cannot see one created inside a
# function body and thrown away after the call — and there were FOUR, all prose matchers, all the
# same `re.search(r"`%s(?:\.py)?`" % re.escape(m), text)` template. They happened to be wrap-safe.
# THE AUDIT HAD NO WAY TO KNOW THAT, and would have read INVARIANT just the same had they not been:
#
#     A BAD AUDITED ARTIFACT -> THE AUDIT CANNOT DISCOVER IT -> THE AUDIT PASSES.
#
# `sensitive()` returning () meant "nothing I found is sensitive" and was read as "nothing here is".
# The two differ by exactly the set the walk cannot reach, and nothing measured that set. That is a
# FALSE NEGATIVE in the instrument, not a documentation bound.
#
#     AN AUDIT CANNOT CLAIM COVERAGE OVER A CLASS OF OBJECTS ITS DISCOVERY MECHANISM CANNOT OBSERVE.
#
# discovery — a SECOND, independent mechanism (an AST walk of the audited module's source, which
#             finds every `re.*` call whether or not its result is ever bound) required to agree with
#             the first; the false negative DEMONSTRATED end to end on a planted source (inline ->
#             INVARIANT, lifted -> SENSITIVE, nothing else changed); the type boundary held against
#             three real source recognizers; and the repair proved to be a LIFT rather than a change,
#             by comparing the lifted `_MODULE_TOKEN` against its four inline originals over the
#             whole live corpus — 506 readings, 0 disagreements — before it was accepted.
#
# THE LAW IS NARROWER THAN "REGEXES SHOULD BE CONSTANTS", DELIBERATELY. `def (\w+)\(self\)` in
# verify.py, `STAGE_ORDER = \(` in indexed, `BRIEFS_REQUIRING_A_FALSIFIER = \(` in exempt all carry
# a literal space, are all wrap-sensitive by the test above, and are all RIGHT to be: a newline
# inside `def name(self)` is a syntax error, not a wrap. prose matcher -> declared and discoverable;
# source matcher -> ordinary implementation. `SOURCE_MATCHERS` is the named escape for a source
# recognizer inside the audited module and is EMPTY, because that module opens no source file.
#
# v1.3 (2026-08-13) — AN ERRATUM, AND THE MODULE CAUGHT BY ITS OWN PREVIOUS LESSON.
# v1.2 pinned `agreed` -- the number of (file, reading) pairs the lift comparison covered -- INTO
# this scene. It is derived from a walk of the WORKING DIRECTORY. It read 506 in the container the
# rung was written in and 512 on the author's machine, whose checkout carries three untracked drafts
# beside the repository, so the digest disagreed with its golden and the gate went RED THERE and
# GREEN HERE, on identical tree contents. Nothing about the repository was wrong. The instrument had
# pinned a fact about somebody's disk.
#
#     A PINNED FACT MAY NOT BE DERIVED FROM A POPULATION THE REPOSITORY DOES NOT FIX.
#
# It is L79 turned on the module that wrote L79. `docs_that_hide_a_count` carries the comment
# "MEASUREMENT, not law: ... reported rather than pinned, because it is a fact about the documents
# and the documents change" -- written SIX FUNCTIONS ABOVE the line that pinned one. And the walker
# was named `_tracked_md` while walking the filesystem, so the name asserted the very property the
# implementation lacked; it is `md_corpus` now, and says what it does.
# The verdict is pinned, the population is REPORTED in the row, and the scene is proved unchanged
# under an enlarged corpus while the count is proved to move -- both halves, because a check where
# neither moved would prove nothing.
#
# DECLARED: which module is audited, and the (empty) source-matcher escape.

audit 383d6f642ece2defb1ae56a044522f9bfc0e756adc5f42c5dfa7d39b45737808
behaviour 0d979796cba45983ff6b4b0b4d38485035028705d6cc794fef5d0cac0bd725bf
discovery f782a6a86d561c26739740fb6b8855908c2d8e8d0b48f6381fa38496327bacbc
reflow 4bb91cfa2aec584746ef4a3476b0d0aed4a88fe23266f36f0abab6669a08abae
