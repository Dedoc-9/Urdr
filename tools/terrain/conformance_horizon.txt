# URDRLAT1 - horizon: the rollback-horizon reconcile window (T3.32, MMO Stage H). A client correction is
# ADMITTED iff its rollback depth (n - k, the boundaries replayed) is within the snapshot horizon H, else a
# typed HORIZON-REFUSE. On admit, cpredict.reconstruct lands on the authority BYTE-EXACT (delta=0).
# SHA-256(MAGIC | name | depth | verdict). correct=0/ADMIT recent=1/ADMIT deep=5/HORIZON-REFUSE(H=2).
#
# BOUNDARY, TRANSCRIBED from this module's own docstring rather than authored here. The claim was
# already graded in code and merely absent from the PINNED artifact, which is where a boundary has
# to live to be durable and scannable — this was placement debt, not absence. Transcribed from the
# module's GRADE section (its last does_not_show), because an earlier occurrence can be a reference
# to another module's boundary rather than this one's:
# does_not_show`: WALL-CLOCK of the reconcile (bounds the WORK; the time follows via `bench.py`,
# MEASURED-on-named-host); the snapshot STORAGE cost of keeping H states (an operational
# parameter, not certified here); NON-MONOTONE or multi-authority corrections (this certifies the
# single-authority east-crossing reconcile `cpredict` covers); and the network TRANSPORT of the
# late input.
correct 31624edc1ea72d95923c73b63905d018638d205df03a3d6c28b83dccdce93c4a
recent 69bc9458e89060cd1f5ff3cdc850d58980b388c7724afc21adb4c4afc5c768e5
deep 279acacdf0087974f920899c13a251805887164682bb1f2064e835873b580fd3
