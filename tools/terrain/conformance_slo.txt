# URDRLAT2 - slo: the composite worst-case latency SLO (T3.33, MMO Stage H). worst_case_latency =
# admission_wait (ceil(N / floor(budget/actor_cost)), governor) + horizon (rollback window). slo_admit
# refuses a config whose worst-case exceeds the target (SLO-REFUSE). admission_wait is a SOUND upper bound
# on the real governor drain. SHA-256(MAGIC | name | n | budget | actor_cost | horizon | latency | verdict).
#
# BOUNDARY, TRANSCRIBED from this module's own docstring rather than authored here. The claim was
# already graded in code and merely absent from the PINNED artifact, which is where a boundary has
# to live to be durable and scannable — this was placement debt, not absence. Transcribed from the
# module's GRADE section (its last does_not_show), because an earlier occurrence can be a reference
# to another module's boundary rather than this one's:
# does_not_show`: WALL-CLOCK latency (this is in TICKS; wall-clock per tick is `bench.py`,
# MEASURED-on-named-host — multiply to get seconds); jitter / variance (this is the WORST-CASE
# bound, not a distribution); priority-class latency (uses the FIFO governor's uniform bound, not
# `priogov`'s per-class one — a follow-on); and any guarantee under an adversarial OS scheduler.
meets 94f910e9225e242852ed22e5e96df68f03b88a9d13a1d7b275cb6dfe6a4cd902
tight df59133d761047c4901c072a41994bf00f753aa006b9eb69b93a1deab9357986
fails a2c32635939f8c9471fe65484d25a9ec794472c753066a83805386cb86dfa1e8
