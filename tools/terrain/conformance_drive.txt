# urdr-terrain (T3.11) — URDRDRIVE1 movement-transcript canon: the authoritative trajectory is a pure
#   exact-integer function of (initial pose, input log) over the terrain. Each input is a direction +
#   gait (lower = walk 1 cell, UPPER = sprint 2 cells); each cell gated by stance's climb <= MAX_STEP.
#   The digest binds (start, input log, trajectory), so any forged/replayed/reordered command moves it.
#   Division-free; same (start, log, MAX_STEP) -> same bytes on every host. See the builder's docstring.
# name  digest
#
# BOUNDARY, TRANSCRIBED from this module's own docstring rather than authored here. The claim was
# already graded in code and merely absent from the PINNED artifact, which is where a boundary has
# to live to be durable and scannable — this was placement debt, not absence. Transcribed from the
# module's GRADE section (its last does_not_show), because an earlier occurrence can be a reference
# to another module's boundary rather than this one's:
# does_not_show`: continuous position / fixed-point rotation (mouse-look is the Q32.32 regime —
# `fpquat`/`fppose` — NOT this exact-integer grid); diagonal moves; the kernel lockstep
# cross-check. GRADE. The trajectory + its determinism + tamper-evidence are MEASURED (exact,
# reproducible, a defect diverges). The movement MODEL (turn-then-advance-gait-cells, a rise >
# MAX_STEP walls) is DECLARED, like buoyancy/crossing/stance. Refusals are typed `DRIVE-REFUSE`
# (unknown command, empty log, off-grid start, negative step, non-int): refuse, never clamp the
# path or invent a footing.
stroll c777a20b83c29d87c546f0ca2501e227d6ff3af3eb7ba15497c48bb6173d32bd
sprint_run 78b40c65ec4e701d9ad892945011b8090c016b4f23870fd3409840a9dd04bcc7
sprint_wall 0aa67e244bdcfa1509b5d9443cc51e07efc0273fbdfce33fefe7c5d87844ebf8
