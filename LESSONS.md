<!-- SPDX-License-Identifier: AGPL-3.0-only -->

# LESSONS — the discipline this repo inherits (as text, not as a dependency)

Urðr is a **standalone** project. It imports no code from Dentatus/Chronicle or Ursprung.
What it imports is the discipline those projects paid for. Each lesson below is stated as a
law of *this* repo, with where it is enforced. Lessons are cited as lineage, not authority:
`cited ≠ implemented` — the enforcement column is what counts.

| # | Lesson (paid for upstream) | Law here | Enforced by |
|---|---|---|---|
| L1 | Canonical bytes → SHA-256; identity *is* content. | Every value has one canonical byte form; `digest = sha256(canon(v))`. No digest is ever computed over non-canonical bytes. | `urdr/canon.py`; determinism falsifiers |
| L2 | No-inflation: evidence may never exceed maturity. Compile it to an unrepresentable state, don't police it after the fact. | Over-claiming source is **rejected by the static checker** (`URDR-INFLATE-STATIC`); `MEASURED` is unwritable in source (`URDR-EVIDENCE-UNEARNED`) and constructable only by the verify primitive ᛞ. A dynamic guard (`URDR-INFLATE-DYN`) remains as an unreachable-but-armed latch. | `urdr/check.py`, `urdr/values.py`; non-vacuous rejection tests |
| L3 | Determinism is an environment, not a hope: `PYTHONHASHSEED=0`, UTF-8 forced on Windows (`PYTHONUTF8=1`), no wall-clock, no unseeded RNG, no set/dict-order dependence, no absolute paths in results. | The interpreter core never calls a nondeterminism source; the gate re-runs programs in isolated subprocesses with a pinned env and compares digests bit-for-bit. | `verify.py` |
| L4 | Windows is a first-class host; Unicode dies at the console unless you force UTF-8 at every boundary. | All file I/O is explicit `encoding="utf-8"`; stdout is reconfigured to UTF-8; runner docs give the PowerShell incantation. | every `open()`; `urdr.py`; README |
| L5 | Validity, not outcome: a test that cannot fail proves nothing; a gate must contain a case that is *supposed* to go red. | The suite ships must-reject programs (checker must refuse them) and a deliberately tampered digest fixture the gate must fail. If the tamper case passes, the gate itself is broken and reddens. | `examples/rejected/`, `examples/must_fail/`, `verify.py` |
| L6 | Grade every claim; a green test certifies execution, never that a name means what it says. | Every capability is graded maturity × evidence in the boundaries ledger; README claims link to the ledger; no capability is described above its grade. | `spec/D5-ledger.md` |
| L7 | Capture nondeterminism at the boundary, never fake it away (clocks, RNG, floats, external reads are *inputs*). | The core stays pure and effect-free. Effects arrived (R4) exactly as promised: typed capabilities — reads recorded at the boundary as inputs inside content identity, writes planned in-language and executed at the līmes. | `urdr/capability.py`; `tests/test_capability.py`; D1 §16 |
| L8 | Bounded execution: an interpreter without a fuel meter is a nondeterministic halt oracle in disguise. | Evaluation carries a deterministic fuel budget; exhaustion is a *deterministic* error (`URDR-FUEL`), identical on every host. | `urdr/evaluate.py` |
| L9 | Build for extraction: every component must stand alone. | Lexer, parser, checker, evaluator, store, canon are separate stdlib-only modules with no circular imports; each is unit-tested in isolation. | `urdr/` layout; `tests/` |
| L10 | Arrival-order-independent canonical ordering is how you tame concurrency without lying about it. | Ported later as the deterministic-actor rung (canonical `(tick, actor, seq)` delivery order). Not implemented yet; graded `SPECULATIVE / N/A`. | ledger (R2 rung) |
| L11 | Integrity is not truth: reproducibility proves a record is unforged and re-derivable, never that the decision it records was wise. | Stated in README and ledger; the language's own vocabulary (`Grounded`, `MEASURED`) is defined as *verifier-passed*, not *true*. | README §honest boundaries; D1 §semantics |
| L12 | The membrane: if a read perturbs authoritative state, the change crossed the membrane and is wrong by definition. | ☽ (view) is a pure function of the store; the store is immutable; ☿ (edit) returns a *new* store with a new digest. There is no API through which a view can mutate. | `urdr/values.py` (immutable store); lens-law falsifiers |
| L13 | A bridge-mounted view of the working tree lies after out-of-band changes (paid for 2026-07-16, twice in one day): (a) files rewritten from outside serve NEW bytes truncated at the STALE cached length — `verify.py` arrived syntactically valid and exited 0 silently; (b) after a host-side `git gc`/repack, the mounted view's ref+object resolution split-brains (`show-ref` reads the ref, `rev-parse` can't find the object) — one commit attempt from that state would have orphaned history. | Sessions working through a mounted view verify byte-lengths after every out-of-band write (a rename round-trip busts the stale cache), and treat git as **read-only from the mount** after any host-side `gc` — commits and pushes happen on the host. | `tests/test_gate_guard.py` (ROWS_FLOOR + the truncation falsifier redden the gate on mode (a)); session playbook for mode (b) — procedural, not mechanical, and stated as such |
| L14 | A self-amplifying scheme's stability bound must be AUDITED over the full trajectory, never estimated from initial conditions (paid for 2026-07-17, the Marangoni sea S2): the naive CFL estimate `|κ·Δc| ≤ 1` from the initial gradients said κ=1/4 was safe; but Marangoni sharpening grows its OWN gradients, and κ=1/4 went negative mid-run. κ=1/16 was earned by a tick-by-tick monotonicity audit across the whole evolution, and the gate re-runs that audit every time. The estimate misleads exactly when the dynamics are interesting; the trajectory is the law. | `tools/terrain/sea.py` (the pinned κ); `sea-marangoni` (mass + monotone-30/30 + peak) and `sea-marangoni-selftest` (the over-bound κ overshoots negative yet conserves — the bound is load-bearing) |
| L15 | A falsifier is only evidence once its planted defect BITES — and the plant can be silently masked three ways (paid for 2026-07-18, three times in one arc): by DEFENSE-IN-DEPTH (a second mechanism catches the scenario, so gutting the first never reddens — the persist membership falsifier passed with both layers off because a swap leaves a *missing* file a third check catches; the honest falsifier was overwrite-not-swap), by an INSENSITIVE CORPUS (a ±1 step-gate drift survived every chunkload vector until a rise == max_step case existed), and by a HABITUAL ANCHOR (a sprint-gait mutation on latarith's all-walk corpus mutates nothing — the anchor must be chosen by READING the corpus, not by precedent). | Before any golden pins: plant the defect, watch the falsifier redden; where defense-in-depth exists, prove each layer individually redundant (one off → still green) AND jointly load-bearing (all off → red); pick corpora boundary-tight and anchors corpus-aware. | The per-rung red-first record in the D5 volumes and commit messages (`ff5cf15..cb14318`); the `*-placement-selftest` rows (in-gate source mutation that must diverge); `latarith_rs`'s in-binary corpus-count check |
| L16 | A number carried in prose is a mercenary: it deserts on contact with the source (paid for 2026-07-18/19: a "32-config / 345-config" soundness corpus quoted from prose was actually 24 when read from `verify.py`; live counts written in idiom VARIANTS — "25 Rust + 14 C99", "**25** single-file Rust" — slipped a manual sweep three commits running until doc-currency named them). | Every count is read from its artifact at claim time (`testsRun`, the row total, the filesystem, the corpus literal); live counts appear in tracked docs only in the digit idioms the checker scans; historical counts are pinned to a commit (the D5 Volume-II epoch-stamp convention), never left floating. | `doc-currency` + its non-vacuity selftest; the D5 volume convention; `latarith_rs` (`checks != 24` → `selfcheck FAILED`) |
| L17 | Resumable-in-memory ≠ recoverable-through-death, and the environment lies about process lifetime (paid for 2026-07-18/19, two modes): (a) a recovery claim needs a REAL process boundary — a successor whose only channel to its predecessor is the store — and the boundary finds laws memory hides (the k = 0 seed-facing mismatch was invisible to every in-memory test, because nothing in-process ever resumes at boundary 0; the falsifier caught it only because the resume law was tested from the restored record); (b) a "background" process in the bridge VM dies with its call (die-with-parent, per-call PID namespace), while a `pgrep -f` liveness poll MATCHED ITS OWN COMMAND LINE and reported the corpse RUNNING for fifteen minutes. | (a) The death boundary is modeled, never simulated: a fresh subprocess, run twice, byte-identical, knowing only the store + the static authority + the continuation request; (b) long work runs where execution persists (the workspace), results bridge back under L13's byte-verification, and a liveness probe must be structurally unable to match itself. | (a) `resurrect-death` (gate row, real successor ×2) + `tests/test_resurrect.py`; (b) session playbook — procedural, stated as such (the L13 mode-(b) genre) |
| L18 | **Polarity inversion on a hierarchy** — reading a comparison in the wrong direction against a tree, three separate times in one session. The 2-adic valuation (`ctz`) was adopted as octree LCA depth when hierarchy lives in the HIGH bits and the correct operation is `clz`; a handed-down predicate read `clz(x⊕y) < k` as "collision-relevant proximity" when that condition means FAR APART; and `lca_depth ≥ threshold` was proposed as disjointness when a deep common ancestor means the SAME subtree. All three are sound-looking and all three invert. | Any predicate over a hierarchy states, in its own docstring, which END of the word carries the structure, and ships the inverted form as a live plant with its measured failure rate. | `voxlat` (`_lca_by_ctz`, 3231/7140 = 45% correct, and INVERTED on leaf siblings — reports root divergence for a one-bit difference); `disjoint` (`_disjoint_by_lca_ge`, admits 402 NON-COMMUTING pairs as safe) |
| L19 | **Vacuity — the characteristic failure of this architecture** — five occurrences in one session, and it is *not* wrongness. An empty answer is indistinguishable from a correct one. A single-valued edit family makes every pair commute, so its census confirms ANY predicate including the inverted one it exists to refute; a 60-edit slice makes every greedy batch a SINGLETON, so an unsound grouping rule scores clean; a level comparison run on a corpus where every level scores zero distinguishes no levels and was one keystroke from "soundness never breaks at any level," proved on nothing; a proposed `max{k : P ⊆ γₖ(αₖ(P))}` bound passes at maximum burn because **a void is sound**; and a vacuity TRIPWIRE was itself insufficiently vacuous and stayed silent. | **Every census carries a non-vacuity precondition and RAISES rather than returning a quiet zero.** A comparison across configurations must first assert the configurations differ; a batching census must assert grouping happens; a scanner must be shown to fire on a planted defect. Silence must be loud. | `ashdepth` (`VacuityError`/`ASHDEPTH-VACUOUS`, `vacuity_witnesses()` pinning all four as constants, `EMPTY_CORPUS` tripwire); `frontier` (`greedy_census_is_not_vacuous`, `the_small_slice_was_vacuous`); `syntaxclean-selftest` |
| L20 | **A sample promoted to a universal** — the inflation this repo forbids, arriving four times by different routes: a single benchmark's 7.82 cm splat error quoted as *the* geometric error of a technique whose real spread is three orders of magnitude by capture condition; a cubic overflow exponent read off RANDOM SAMPLING whose own samples also supported quadratic between some bounds; a 66.7× collapse ratio carried over from an exploratory run at a superseded family size; a 27% fast-path yield quoted as an accelerator's value when it is an artifact of one corpus's density. | Worst-case-over-inputs is the measurand, decided by enumeration where the space allows it. A figure from a superseded run is recorded as stale rather than silently swapped. A quantity that varies with corpus is reported as a CURVE, never a point. | `voxlat` (4·B³ decided exhaustively, not sampled); `frontier` (`yield_curve`, `the_pinned_fraction_is_an_artifact`); `recirc` (the stale 6/66.7× pair recorded in-place); `horn` (`worst_relative_cost_bruteforce` as an independent oracle) |
| L21 | **Wrong units** — a defect measured in a space that cannot express it. Luminance delta proposed for information asymmetry (a uniform tint shifts 36 of 36 pixels for defect 0; culling one occluder shifts one cell for defect 3); a float z-score proposed for a fraud verdict, which is not reproducible across platforms; a flip *rate* proposed for quantization defect, which hides WHICH cells and the adversary picks cells. | A defect is measured in the codomain's own units, and for this arc that is **integers over sets**. Defects then compose by ordinary addition (`\|A △ C\| ≤ \|A △ B\| + \|B △ C\|`, decided over 32768 triples, 0 violations) and no float enters an authority path. | `tierview` (`\|Vis_high △ Vis_low\|` in cells, `luminance_delta_is_the_wrong_quantity`); `geoquorum` (integer cohort disagreement, not a z-score) |
| L22 | **Elegance that costs detection** — the most dangerous class, because the proposal looks *more* principled than what it replaces. A provenance certificate hashing only its own metadata is cleaner than one bound to geometry, and lets an attacker lift a permissive certificate onto a restricted capture. Fixed-point equality is a prettier integrity check than raw-lattice equality, and it COLLAPSES 400 distinct captures onto 5 fixed points — conflating an honest capture with a doctored one that dropped an obligation, which is precisely the omission attack the arc exists to catch. | Any simplification of an authority check is measured against the attack it is supposed to stop, before it is adopted. A check that admits more pairs as identical has raised false negatives, however elegant its derivation. | `provbind` (`_digest_metadata_only`, lift attack measured succeeding then failing); `recirc` (`_fixedpoint_as_integrity`, `doctored_collides_with_honest`) |

| L23 | **A checker that cannot fail is not evidence** — the sharpest failure this session produced, and it shipped THREE TIMES before an audit caught it. `auditgraph` sold "price == kappa, 771 of 771, 0 exceptions" on the grounds that *two computations agreeing is a measurement; one computation is a definition restated*. The two functions were the SAME loop over the SAME subsets calling the SAME `components`, separated only by a guard that provably cannot change the answer. It was refuted BY MUTATION rather than by argument: corrupt the shared primitive and the census still reports 0 exceptions, quietly shrinking its own denominator from 771 to 495. The numbers were right the whole time; the reason to believe them was not. | Any claim of the form "two methods agree" names the primitive each method uses and asserts they share none. Every cross-check ships a MUTATION PROBE that corrupts the shared dependency and demands the check go red — L15 (the plant bites first) applied to a CHECKER rather than to a claim. A check whose failure mode has never been observed is a hypothesis. | `auditgraph` (`menger_census` via max-flow vs subset enumeration; `cross_check_is_falsifiable` returning (circular 0, menger 181/10, family 24); `circular_check_cannot_fail` keeping the inert check as a named object rather than a comment) |
| L24 | **Hardening the numerator leaves the denominator** — the immediate sequel to L23, and it was found by a one-line question from outside. After the Menger cross-check de-circularized *what was measured*, the family it was measured OVER — `connected_graphs`, hence every census total — was still built entirely from the same flood fill. Under the identical planted fault the family at order 5 silently shrank from 728 to 476 and RAISED NOTHING, so a fault that narrowed the world without causing disagreements would still have passed. Separately, the same primitive silently DROPPED out-of-range and negative vertices, returning a plausible `False` to a caller who had asked about a different graph. | A cross-check covers the population as well as the statistic: the predicate that CONSTRUCTS the family is cross-checked by an independent route and carries its own mutation probe. Input that cannot be interpreted is a typed refusal, never a confident answer to a different question. | `auditgraph` (`is_connected_by_flow`, `family_census` 1099/0/1099/772, `family_check_is_falsifiable`, `validate_graph` raising `AUDITGRAPH-REFUSE` on out-of-range / negative / self-loop / malformed edges) |
| L25 | **A product written down instead of a counter read**, and **a code chosen without checking the namespace** — two cheap errors with the same root: asserting a quantity that was available to be measured. `bombtest`'s header claimed 24576 explored transitions, which is states × rules; the measured count of LEGAL transitions is 13824, because boundary-blocked moves never fire. And `auditgraph` was given MAGIC `URDRAUD1`, already held by `audible` — a digest is `H(MAGIC \| name \| payload)`, so a shared prefix defeats exactly the domain separation the prefix exists to provide. Neither was caught by 1725 falsifiers; the first was caught by reading the counter, the second by COMBING THE DOCS. | A count that an instrument can produce is read from the instrument, never derived from a formula in prose (L16, extended from doc counts to in-code claims). An identifier drawn from a namespace is checked against that namespace mechanically, and the check is a gate row rather than a habit. | `bombtest` (`never_claim_census` returning the read count; the gate asserting legal < states × rules); `magicuniq` + `magicuniq-selftest` (injectivity over all 90 MAGIC-bearing modules, with a planted duplicate proving the row can redden) |
| L26 | **A tuned constant is a theorem nobody looked for** — and the check on that suspicion is to ask what OBJECT the number is a property of. `THICK = 2` sat in the capture layer looking like a fitted parameter, carried a declared-regime caveat, and inherited a Guggenheim note admitting the 1/2 agreement bar was a fitted law with no derivation. It was neither: measured over spanning walls, min-cut(wall) is 1 at thickness 1 and 2 at thickness 2, so the constant was Menger's minimum vertex cut all along, computable from the wall's own geometry. Replacing the threshold with the theorem cost nothing and bought three things a threshold cannot have — a SCREENING LAW instead of a tolerance (below k the verdict provably cannot move, 32 tried, 0 flips), an IMPOSSIBILITY CHECK instead of a bar (a peer differing by fewer than k cells and reporting a different verdict is not an outlier to forgive, it is provably faulty), and the DISCHARGE of the declared regime rather than its inheritance. Four measurands were proposed across the arc and THREE died on contact with a witness: Jaccard overlap is blind to structure, longest-run is INVERTED relative to the truth (it over-flags the safe case, wrong in the dangerous direction), and the boundary reduction does not exist at all. | Before declaring a constant as policy, ask what mathematical object it is a property of and whether that object can be measured. A tolerance is a confession that the mechanism is unknown; where the mechanism is known, the same number arrives as a theorem and brings an impossibility proof with it. Every rejected measurand is PINNED as a live witness rather than deleted, so the reason a superseded predicate was wrong stays falsifiable and cannot quietly return. And a constant that genuinely stays declared says so with its falsifier attached: the criticality charge peak here is not adopted AND not refuted, because a charge curve was never measured, and claiming the measurement ruled it out would be the inflation this repo forbids. | `cohort` (`min_cut` / `gap_table` (3,1,1)(4,1,1)(4,2,2)(5,1,1)(5,2,2); `screening_law_census` (2,32,0); `sub_gap_disagreement_is_impossible` (2,32,0) with `at_or_above_the_gap_disagreement_becomes_possible` (2,496,16) proving the bound tight; `jaccard_is_blind_to_structure`, `run_length_is_inverted`, `boundary_does_not_determine_breach`, `hex_duality_fails_in_3d` as four pinned refutations; `the_peak_is_not_adopted` (12,12,False)) |
| L27 | **A positive answer from a search is family-relative FOREVER, and the reason is a hardness result rather than a coverage debt** — which finally names the caveat this arc has been carrying since `inputset`. What `determines` computes is exactly Nash-Segoufin-Vianu VIEW DETERMINACY: views V determine query Q iff V(D1) = V(D2) implies Q(D1) = Q(D2) FOR ALL instances. That is UNDECIDABLE for unions of conjunctive queries (reduction from the word problem for finite monoids) and open for conjunctive queries, and determinacy over FINITE instances diverges from determinacy over all instances — so enlarging the family cannot ever promote a family-relative positive to a universal one. Measured consequence, not theory: the search cleared `quorum_agreement` of needing `own_tile`, a fetch the quantity provably reads, and a router trusting it would have dropped the atom. **And the same session produced the mirror error in my own hand: I asserted that computing k over a wall SUBREGION was a wrong-denominator defect, and measurement said no — k(subregion) <= k(occupancy) always (160 occupancies, 0 violations), so it is CONSERVATIVE, and the real cost is that at the smaller k the sub-gap range is EMPTY and 0 of 5 peers get screened where the correct k screens 5 of 5.** | A NEGATIVE answer comes from one witness and is exact; a POSITIVE answer must come from SYNTAX — prove the quantity cannot read the input at all — because the semantic version is undecidable. Where both routes exist, act only on their conjunction, and pin the disagreement as a live witness. A correction to a handed-down design is itself a claim and gets the same treatment: state it so it can fail, then run it, and withdraw it in the module's own prose when it does. A checker's call-following filter is decided by the source FILE, never by `__module__`, or the verdict changes between `import` and running as a script — a digest that depends on the invocation is not a digest. | `autoroute` (`syntax_versus_search_census` (F,F)(F,F)(T,T)(T,T)(T,F)(F,F); `only_syntax_gives_a_universal_positive` (3,2,1); `search_alone_would_over_skip` -> (('quorum_agreement','own_tile'),); `the_syntactic_check_follows_calls` (True,True) as the checker's own plant; `subregion_k_is_conservative_not_unsound` (160,0,67) withdrawing my claim; `the_subregion_costs_the_whole_saving` (1,2,True,0,5); `_defined_under_here` and the invocation-independence assertion) |
| L28 | **Cheap-first is only sound where cheapness is backed by a determinacy proof; otherwise it is affordability wearing the costume of a decision** — and the whole family of cheap pre-screens can be closed with ONE pair instead of one refutation per candidate. Four invariants a cascade would be tempted to install ahead of the expensive measurand — cell count, boundary occupancy, tile prefix, occupancy defect — are EACH blind to the breach verdict, and so is their CONJUNCTION: two 16-cell occupancies differing in 2 cells agree on all four simultaneously and have opposite verdicts. Stacking cheap checks does not converge, because they are blind in the same direction. Measured operationally, a cell-count screen clears 8 of 8 peers and 3 of them WRONGLY. **The history that suggested the cascade actually argues against it:** the corrected sequence for safrole is FORMULA (Saint-Evre 1844) -> NAME (Grimaux and Ruotte 1869) -> IDENTITY-BY-SHARED-INVARIANT (Eijkman 1885) -> STRUCTURE (Bruhl 1888, the allyl assignment), so the structural answer came LAST, and not because cheap-first was sound but because the structural instrument did not exist yet. Eijkman's step is the defect itself, named: "same empirical formula plus similar properties, therefore the same compound" is identity from an invariant that cannot decide it, since safrole and isosafrole share C10H10O2. | Measure the COST order and the DECISIVENESS order separately and assert they are different orders; a tier earns its place in a cascade from a determinacy proof, never from being first. Refute a FAMILY with a single witness that defeats the conjunction, rather than one predicate at a time, so the next "add one more cheap check" proposal is already answered. Every cheap-screen refutation ships a POSITIVE CONTROL — the expensive measurand separating the same pair — or the witness shows a degenerate corpus instead of a blind invariant. A corpus built to contain its witnesses states that it was built, and asserts its size and its both-arm split, because a thin corpus that finds no witness reads exactly like a predicate that survived. | `blindscreen` (`blindness_census` 4 of 4 refuted; `the_conjunction_is_also_blind` (True,16,16,2,BREACHED,INTACT); `connectivity_separates_the_pair` as the positive control; `eijkman_identity_is_underdetermined` (True,True,True,True); `a_cheap_screen_would_clear_a_liar` (8,3,8) with `the_population_exercises_both_arms` (5,3); `cheapness_is_not_soundness` ((),('connectivity',),False); `the_router_takes_no_blind_invariant` (4,())) |
| L29 | **A refutation is a witness, never an impossibility theorem — and the status vocabulary is where that distinction has to live, because prose will not hold it.** Five candidate invariants for the breach verdict were refuted, and the temptation at that point is to write "no cheap invariant decides breach". What was established is five counterexamples. The fix is a durable FALSIFICATION RECORD per candidate — candidate, status, witness, failure mode, impact — with a status vocabulary admitting only FALSIFIED and OPEN and containing no CONFIRMED, PROVED or IMPOSSIBLE anywhere, checked mechanically rather than promised. **And the structural explanation must be graded separately from the refutations it explains:** measured, three of the four cheap invariants ARE valuations in the inclusion-exclusion sense (0 violations of 400 each) while the breach verdict is NOT (24 of 400) — a valuation assigns one number to one set, and breach asks whether TWO DESIGNATED FACES lie in the same free component, so there is nothing for inclusion-exclusion to decompose. That is a REASON pointing at an impossibility theorem, not the theorem; the theorem would need a Hadwiger-style classification of lattice valuations under this arc's symmetry group (cf. Hadwiger 1957; Ludwig-Reitzner 1999, 2010) followed by a check of the resulting short list. **The fifth refutation also proved the corpus insufficient, one invariant after the module warned about exactly that:** searching 545 occupancies for an equal-free-component / opposite-verdict pair found NOTHING, which read as a result would have said the candidate survived — every member was wall-like so its free space always touched both faces, and a hand-built pair refuted it at once. | A refuted candidate gets a record row, not a sentence: the witness so it can be re-checked, the failure MODE so the class is named, and an IMPACT saying what to do — which is what stops the same candidate being reconsidered later. Status words are checked against a closed vocabulary in the gate; a claim that a whole family is impossible is written only when a classification theorem is in hand. When a corpus sweep finds no witness, that is a fact about the corpus until a construction says otherwise — so record the insufficiency in the row itself, so the next candidate of that kind is not swept the same way. | `blindscreen` (`falsification_record` 6 of 6 FALSIFIED each with a witness; `the_record_claims_no_impossibility_theorem` (('FALSIFIED',), (), True); `STATUS_VOCABULARY`; `valuation_census` with cell_count/tile_prefix/occupancy_defect at 0 violations and verdict at 24 of 400; `breach_is_two_pointed` graded a reason; `the_corpus_missed_the_fifth_witness` (False, True, True) with `the_fifth_witness` (1, 1, INTACT, BREACHED, 35)) |
| L30 | **The discipline a repo most claims to be about is the one nobody mechanises — and measuring the debt is not the same as reading it correctly.** `doc-currency` guarded prose numbers, `doc-staleness` guarded status claims and `magicuniq` guarded identifier collisions, while CLAIM GRADING — the thing this repo says it is — was guarded by habit: `does_not_show` appeared in `verify.py` exactly twice, both as prose inside evidence strings, never as a check. Measured, 72 of 97 pinned corpora carried no boundary line. **Then the first reading of that number was itself wrong.** Reading the modules showed 66 of those 72 DO carry a boundary — in the docstring, not in the pinned artifact — so only 6 are orphans with no boundary anywhere. The debt was PLACEMENT, not absence, and reporting 72 ungraded claims would have been the inflation the rung existed to prevent, committed by the rung itself. A separate scan for unbounded language over the same 72 returned 4 hits, all already scoped in context — debt, not latent overclaiming. | Mechanise the discipline, not a snapshot of it: read the count from the filesystem at claim time (L16) and pin it as a CEILING, so a new violation reddens immediately while an existing backlog can only shrink. Pin the ceiling TIGHT as well as respected, so a backfill that forgets to lower it also reddens and slack cannot accumulate. Never bulk-backfill a boundary you have not earned by reading the module — a transcribed boundary cites its source and says it was transcribed; an authored one is a claim. And before reporting a debt figure, check whether the artifact you scanned is the only place the thing could live: a count over the wrong artifact is a measurement of your scanner. | `verify.py` (`grading-ratchet`: 97 corpora / 28 with a boundary / 69 without / 6 orphans / 60 undeclared, all three pinned as ceilings, scan asserted non-vacuous and orphans < without; `grading-ratchet-selftest`: an ungraded corpus injected in memory pushes the count one past the ceiling and breaks the predicate, a GRADED one leaves both counts unmoved, and the scan is asserted to write nothing to the tree) |
| L31 | **A model can be VERIFIED without being VALIDATED, and a repo that only ever verifies must say so in every boundary it writes.** The grading ratchet named six corpora with no boundary anywhere — `buoy`, `cross`, `sea`, `stance`, `terrain`, `wave`, the physics and terrain substrate that predates the whole discipline arc. Reading them showed why the omission was systematic rather than six oversights: each already states its model is DECLARED and its reproduction MEASURED, so the authors knew the distinction and simply had no vocabulary slot for it. AIAA G-077 supplies one — verification is "the process of determining that a model implementation accurately represents the developer's conceptual description of the model", solving the equations right, while validation asks whether they are the right equations. Every claim in this arc is verified and NONE is validated: a discrete Archimedes is not hydrostatics, a parabolic integer profile is not a Gerstner surface and reproduces no dispersion or breaking, a procedural generator detects nothing and resembles no landform, and a cardinal step gate is not locomotion. | Every boundary written for a model states the verification/validation split explicitly, because "MEASURED, exact, reproducible" reads as a physical claim to anyone who has not read the module. A boundary is EARNED by reading the module and grounded in what its code actually computes — the raft is rigid and flat so there is no roll or wake; the crossing agent is a sampler so no hydrodynamic force acts on it; the event resolution is one tick because the result is the first tick satisfying a predicate. Where a boundary already exists in a docstring it is TRANSCRIBED and marked as transcribed; where none exists it is written and marked as earned. The two are different acts and the artifact says which one happened. | The six corpora, each carrying an earned boundary citing the AIAA distinction; `grading-ratchet` with `GRADING_CEILING_ORPHAN` lowered 6 → 0 and asserted `orphans == 0`, so every corpus in the repo now carries a boundary somewhere and the remaining 63 are pure placement |
| L32 | **A ratchet that reaches zero becomes a wall, and the moment it does, its non-vacuity moves entirely onto its plant.** The grading backlog was paid in two acts that are NOT the same act and whose artifacts say which: six ORPHANS, whose modules carried no boundary either, were EARNED by reading each module and writing a boundary grounded in what its code computes; the other 63 were TRANSCRIBED from their own docstrings. Transcription is the lesser act and must be marked as such, or the repo ends up holding 63 boundaries nobody wrote. It also has its own failure mode, made once and caught here: cutting from the FIRST `does_not_show` in a docstring pulled 45 lines of unrelated prose, because an earlier occurrence can reference ANOTHER module's boundary — the extraction must come from the GRADE section, and a length guard (median 275 chars, nothing over 900 or under 40) is what turned that from a hope into a check. **And paying the backlog broke the row's own non-vacuity assertion:** `ungraded > 0` had been proving the scan sees both classes, and once every corpus is graded there is no live negative case left to point at. Asserting it anyway would have reddened a correct repo; dropping it silently would have left the row one step from a constant True. | When a ratchet reaches its floor, say so and re-derive what makes the check falsifiable — the plant, not a live negative. A backlog paid by two different mechanisms records which one applied to each item, because an EARNED boundary and a TRANSCRIBED one carry different evidence and a reader must be able to tell. Any bulk extraction from prose ships a plausibility guard on its own output (length bounds, a source anchor) before it writes, since a bad extraction is indistinguishable from an authored claim once pinned. | `verify.py` (`grading-ratchet` now 97 of 97 with a boundary, 0 without, 0 orphans, both boundary ceilings at ZERO so the ratchet is a wall; the DECLARED backlog stays a ratchet at 46 because a DECLARED list cannot be transcribed from anywhere; `grading-ratchet-selftest` carries the whole non-vacuity burden and is asserted to) |
| L33 | **A membership test is not a type check: `if x not in FROZEN_SET` admits `True`, because `True == 1`.** Three modules validated a frozen integer parameter that way — `glide._shift` over SUBDIV, `interest._k` over BUCKET_SIDES, `chunkload._check_grid` over CHUNK_SIZES — while every other integer parameter in the same files rejects bool explicitly via `type(v) is int` with the comment "bool excluded on purpose". The guard was inconsistent with the module's own stated discipline at exactly one spot each. **And it was not cosmetic:** measured, `glide(..., sub=True)` produced a trajectory IDENTICAL to `sub=1` and a DIFFERENT digest, and `interest.bucket(5, 7, True)` gave the identical bucket and a different `relevance_digest`, because both canons format the parameter as text — ONE BEHAVIOUR, TWO CONTENT IDENTITIES, which is L1 broken at the exact point both modules claim tamper-evidence, in modules 17 and 16 others import. `chunkload` was safe only because 1 is not in CHUNK_SIZES: luck, not design, and it stops being luck the day a smaller chunk size is frozen. **The lead that found it was itself wrong three times.** Regex counts of "refusal paths exercised" reported 1 for `glide` when the stage's loop exercises 4 and its suite 6 — the count was of `except` STATEMENTS, not paths. The defect surfaced only from a direct PROBE of candidate inputs against the running module. | A frozen-set membership guard on an integer parameter is preceded by an explicit type check, and the two are never conflated: membership answers "is it one of these values", typing answers "is it that kind of thing at all". Where a module states a refusal discipline in one place it is applied at EVERY parameter, and the gate asserts it rather than the docstring promising it. Prove such a guard load-bearing WITHOUT mutation where the arithmetic allows: `True in SUBDIV` is still True, so membership cannot be what refuses and only the type guard can be. And stop counting syntax when the question is semantic — a heuristic that has misfired is retired, not re-run with a wider pattern. | `boolport-refuse` (GLIDE-REFUSE / AOI-REFUSE / CHUNK-REFUSE on a bool, with the honest int still passing so the guard refuses bool rather than everything); `boolport-load-bearing` (`True in SUBDIV` and `True in BUCKET_SIDES` asserted TRUE, `True in CHUNK_SIZES` asserted FALSE so chunkload's accidental safety is recorded as accidental); `tests/test_glide.py::test_typed_refusals` gains the bool-subdivision case |
| L34 | **A plan that nothing consumes is a documentation promise wearing a router's clothes — and when the fabricated answer equals the honest one, no check can save you; only the TYPE can.** `autoroute` decided which inputs each quantity needs and then nothing stopped a caller evaluating a quantity without them: measured, `ledger_remainder` with the log returns 2 and WITHOUT the log returns 6, the full shard budget, so an under-populated situation reported "the ledger is pristine" — wrong in the dangerous direction. **The fix could not be a check.** `inputset`'s `situation` stored `history=()` for BOTH "fetched and empty" AND "never fetched", measured identical, so no gate could separate THIN from DEVIATE on a type that cannot express the difference. And the sharpest form: on an EMPTY log the honest answer is 6 and the FABRICATED answer from an absent log is ALSO 6 — the right answer and the wrong one are the same number. A value-level assertion would have passed forever. | Where a distinction cannot be expressed by the type, add an INHABITANT rather than a predicate: `NOT_FETCHED` is a new value, not a new test. An under-populated request refuses under a code DISTINCT from a malformed one, because they need different attribution. A gate over inputs asserts it changes WHICH inputs are required and NEVER the answer. And where such a gate covers only part of its census, the uncovered part is asserted with its reason — here 3 of 6 rows are CERT-tier with an empty plan, because the model derives the certificate FROM occupancy, which is a second representation defect named rather than hidden. | `autoroute` (`NOT_FETCHED`, `fetched_situation`, `guarded`, `MissingAtom` = AUTOROUTE-MISSING-ATOM and NOT a subclass of AUTOROUTE-REFUSE; `unguarded_evaluation_is_silently_wrong` (2, 6, 6, True); `the_representation_conflated_absent_and_empty` (True, True); `the_guard_refuses_absence_and_admits_emptiness` (True, 6, 2); `guard_census` all three planned rows refusing/evaluating/unchanged; `cert_rows_are_not_exercised_by_this_gate` (3, 3, 6)); gate row `autoroute-enforce` |
| L35 | **State the INVARIANT, not the implementation — an invariant survives the rewrite, and it catches what no unit assertion can.** Three were stated for the router and all three earn their place. PROVENANCE: no fetched state is representable as `NOT_FETCHED` and `NOT_FETCHED` is never produced by a successful fetch (6 values, 0 collisions, 13 built, 0 produced). GUARD TRANSPARENCY: for any FULLY POPULATED input, guarded and unguarded evaluation agree — quantified over a family, 24 pairs, 0 disagreements, with a populated input that REFUSED counting as a disagreement. ERROR PARTITION: the refusal classes are DISJOINT and EXHAUST all router failures — 42 attempts giving 18 missing-atom, 4 refuse, 20 legitimate successes and **0 escaped**. **That third one would have caught a defect this same rung shipped and then fixed:** an inert `NOT_FETCHED` let an undesignated read escape as `TypeError: '_NotFetched' object is not iterable` from inside `tilemin` — neither refusal class, naming neither atom nor caller, indistinguishable from a genuine bug. No unit assertion was missing; the EXHAUSTIVENESS property was. **And the capability reading is what found the ambient read in the first place:** checking a plan and then passing the whole situation is authority granted by SCOPE rather than DESIGNATION — measured, `guarded` admits 6 of 6 where `projected` admits 4 of 6, and the two that fall read `s['occupancy']` while their plan names nothing. | Where a property can be stated over all inputs rather than over a fixture, state it that way and quantify it over a family. A partition of failure classes asserts BOTH disjointness and exhaustiveness, and exhaustiveness is driven by a corpus that should fail while asserting nothing escapes as a third type — that is the only check that catches an untyped leak from a callee. Prefer PROJECTION over CHECKING wherever a plan exists: hand the callee exactly what it is entitled to, so an undesignated read fails by construction and nobody enumerates the reads. A sentinel that stands for absence refuses on USE, or it is not enforcement. | `autoroute` (`provenance_invariant`, `guard_transparency_invariant`, `error_partition_invariant`, `the_invariants`; `projected` / `projection_census` / `the_ambient_readers` naming exclusion_membership and prefix_disjointness; `_NotFetched` raising AUTOROUTE-MISSING-ATOM on all four use protocols with identity and hashing untouched); gate row `autoroute-enforce`; `AGENTS.md` §6a |
| L36 | **When a gate gets slow, MEASURE the distribution before parallelising it — the obvious fix bought 8% and the boring one bought 47%.** A full pass had grown to ~15 minutes and a rung costs two of them. The intuitive move was concurrency; measured, running both required passes at once took 1768s against roughly 2×16 min sequential, because the box has 2 cores and the gate already spawns subprocesses, so a second pass mostly CONTENDS. Profiling instead found the real distribution: `blindscreen` 70.35s and `autoroute` >130s against ~1s for their neighbours, and the cause was recomputation — every scene digest is computed once against its golden and again by `emitted_matches_pinned()`, with the falsifier suite computing it a third time, while `blindscreen` rebuilt a 545-occupancy corpus on each call. Memoizing the pure functions of pinned data took `blindscreen` to 1.19s and `autoroute` to 17.29s, and both passes together from 1768s to 935s. **And the acceptance test is the gate itself:** STDOUT is byte-identical before and after, so the cache provably changed no answer. | Profile before optimising, and state the speedup as a measured ratio rather than an expectation — a parallelisation that contends is slower than the arithmetic suggests. A cache over a pure function of PINNED data is safe by construction and its safety is PROVED by the byte-identity the gate already requires; a cache over anything else is a determinism hazard. Give the gate a SUBSET runner so nobody pays the full pass to check one stage, make it refuse to grade (`SUBSET RUN — NOT A GATE PASS`, with the vacuity floor and tamper selftest absent by construction), and keep the stage order as DATA so selection can never make the order a function of the invocation. Diagnostics go to STDERR so certified STDOUT stays byte-identical. | `verify.py` (`STAGE_ORDER` as data; `--only <substr>`; `--profile` to stderr) and `AGENTS.md` §6a; `lru_cache` on the pure hot paths of `blindscreen` and `autoroute`, with the pre/post byte-identity comparison as the proof |
| L37 | **A baseline is only evidence if a commit produced it — and a fast check earns trust by naming what it cannot see.** The proposal was a `--diff` on the subset runner comparing fresh rows against the pinned transcript, sold as "95% of the confidence in ~17 seconds". Three things were wrong. (a) **Provenance:** a `gate1.txt` lying in the tree has nothing binding it to any code, so a diff against it cannot distinguish *my edit is wrong* from *the baseline is old* — the exact defect class `doc-currency` exists to catch. The baseline is now built by `git worktree add --detach HEAD` and RE-EXECUTED there. (b) **Coverage:** measured, `--only autoroute --diff` sees 8 of 836 rows; `doc-currency` and `doc-staleness` need LIVE totals across the whole run and were this session's most frequent red, so a subset diff structurally cannot see the thing most likely to be broken. (c) **Cost:** the stage alone is 17.5s but the diff is 51.1s, because the baseline is a second real execution — against 579s for the two required passes that is ~1% of the rows for ~9% of the cost. A fourth point emerged only from building it: comparing raw evidence STRINGS would cry wolf on every reworded sentence, since L16 puts measured numbers *inside* prose, so the report extracts a NUMERIC SIGNATURE and reports `[NUM]` (a census moved) separately from `[TEXT]` (prose only, numbers unmoved). And the first run found its own bootstrap gap — HEAD predated `--emit-rows`, so the baseline returned nothing and the naive code would have printed "no drift". | A comparison against a baseline states where the baseline CAME FROM, and derives it from a commit rather than from a file. A partial check ships a `WHAT THIS CANNOT SEE` paragraph in its own output, naming the specific rows it structurally cannot compute, and quotes its coverage and cost as MEASURED numbers rather than as a percentage of confidence — "95% of the confidence" is not a measurable quantity and "8 of 836 rows in 51.1s" is. When a check loses resolution it says `[DEGRADED]` and says what it lost; silently reporting agreement at half resolution is worse than reporting nothing. Diff dimensions match the semantics of what is being compared: status, measured values and prose are three different events and collapsing them makes the tool noisy enough to ignore. | `verify.py` (`_drift_report`, `_numeric_signature`, `--emit-rows`, the `[DEGRADED]` bootstrap fallback) and `AGENTS.md` §6a; falsified by planting `CO.screening_law_census() == (2, 32, 1)` and watching `[RED] cohort-gap: 1 -> 0` appear |
| L38 | **A check whose failure is certain in advance is not evidence — it is training to ignore reds.** Building `--diff` meant running `--only doc_currency`, and it printed `[FAIL] doc-currency — README.md says fals=1931 (live -1) ... says rows=835 (live 4)` (a VERBATIM transcript of what the tool printed that day; 1931/835 were the live totals then, and a blanket count-refresh across the docs corrupted this quote once before it was caught — a recorded observation is not a claim about the present). The stage was not detecting stale docs; three of its five inputs (`fals`, `rows`, `det`) are totals accumulated across the WHOLE run, so in a subset they are `-1`, `-1` and the subset's own four rows. The red was GUARANTEED, carried no information, and — worse — the same missing input made the WORD-form half of `doc-staleness` compare against `-1` and pass VACUOUSLY, so the identical defect produced a false red in one row and a false green in its neighbour. The fix is neither: `Gate.subset_withholds` makes the stage decline to grade under `--only`, printing `[WITHHELD]` with the reason. **And a withhold needs its own falsifier, or it becomes the cheapest way to hide a red** — `subset-withhold-honest` asserts the predicate is OFF on the gate path, ON exactly once under `--only`, that the stated reason is TRUE of a fresh `Gate` (the run-scoped counts are absent attributes, not stale numbers), and that this full run supplies both, so what a subset lacks is the RUN and not the code. | Three verdicts, not two: PASS, FAIL, and *I cannot compute this here* — a tool that must answer in a binary will answer wrongly. Before trusting a red, ask whether it could have come out any other way; a red that could not is a bug in the harness. When one missing input feeds several checks, look for its OTHER shadow: the loud false red usually has a silent false green beside it, and the silent one is the dangerous half. Every licence to make no claim ships with a check that the licence was earned. | `verify.py` (`SUBSET_ONLY`, `Gate.subset_withholds`, the `[WITHHELD]` lines in the subset runner, and the `subset-withhold-honest` row inside `doc_currency`); found by running `--only doc_currency` while testing `--diff`, not by review |
| L39 | **The ruler was inside the thing it measured, and nothing scored the ruler.** A rigor pass over the freshly-shipped `--diff` asked one question the rung had not: *what scores the comparator?* Nothing did — `_drift_report` and `_numeric_signature` appeared in `verify.py` at their definition and call sites and NOWHERE else: no gate row, no unit falsifier, validated once by hand and then reverted. Two plants settled what that costs. Emptying `_numeric_signature` fails LOUD — every row reports `[NUM]`, benign. But a fail-OPEN edit, `if False and num_n != num_b:`, against a `cohort-gap` whose measured triple really moved (4,1,1) → (4,1,9), printed `rows 7 here / 7 at HEAD` and `no drift` with total confidence. The dangerous direction is the silent one. Fix: split the pure comparator `_classify_drift` out of the git/subprocess I/O so it can be falsified at all, then 15 falsifiers that each redden under that exact plant. The same pass caught two smaller things. The report claimed its blind spot was "the MOST FREQUENT red in this session's history" — true, but grounded in RECOLLECTION, which no reader can re-run; it now says `doc_currency` runs last and reads `len(self.rows)`, so every row in the gate is one of its inputs — the same conclusion, ESTABLISHED by construction instead of MEASURED from memory. And the manifest that this pass introduced was itself weaker than the CI it duplicates: `gate :: python verify.py` trusts the EXIT CODE, which is exactly the contract incident 2026-07-13 disproved. | Ask *what scores the scorer* before shipping any tool that grades a change — a ruler in the same tree as what it measures is blind to its own defects, and the boundary belongs in the tool's OWN output, not only in the docs. Prefer failing loud to failing open, and when you cannot choose, TEST which way it fails: the two directions are not symmetric and only one of them lies to you. Extracting a pure core from I/O is not tidiness — it is the difference between a component that can carry falsifiers and one that cannot. A claim grounded in what you REMEMBER is ungraded no matter how true; if a structural argument reaches the same conclusion, ship that instead. And when a new harness duplicates an existing contract, diff the two: the newer one is usually the weaker, because the incident that hardened the old one is not in the new one's memory. | `verify.py` (`_classify_drift` split out of `_drift_report`; the WHAT THIS CANNOT SEE note naming the comparator), `tests/test_drift_report.py` (15 falsifiers, incl. an AST purity check), `scripts/gate_once.py` + `gates.txt` (literal-tail contract, two passes not three), `AGENTS.md` §6a |
| L40 | **A blanket count-refresh across the docs corrupted a VERBATIM TRANSCRIPT, and the staleness checker would have been satisfied.** Adding 15 falsifiers moved the live count 1931 → 1946, so a one-line sweep rewrote `1931` everywhere — including inside L38, where the string `[FAIL] doc-currency — README.md says fals=1931 (live -1)` is not a claim about the present but a QUOTE of what the tool printed on the day. The edit made the lesson cite an error message that had never been emitted, and `doc-currency` — whose whole job is docs-versus-reality — would have reported green, because its rule is *docs must quote LIVE counts* and it has no notion of a recorded observation. The D5 ledgers are already exempted as "recorded history is not drift"; quoted transcripts inside LESSONS are the same category and were not. Caught by reading the diff hunk-by-hunk before committing, not by any check. | A number inside quotation marks is DATA, not a claim, and a refresh that cannot tell the two apart will quietly falsify your own record. Review mechanical sweeps hunk-by-hunk — the cost is minutes and the failure is invisible afterwards, because a corrupted transcript looks exactly like a correct one. When you must keep a historical figure, say so in the sentence ("the live totals at the time") so the next sweep and the next reader both stop. And note the shape of the near-miss: a green checker is not a defence when the defect is in the category the checker does not model. | `LESSONS.md` L38's restored quote, now labelled as a transcript; found by `git diff --unified=0` review during the L39 rigor pass |
| L41 | **The empty plan did not mean "needs nothing" — it meant "needs something nobody wrote down", and fixing it cost the module a witness.** `inputset.proj` computed `certify(s["occupancy"], s["tick"])` INSIDE the projection, so the CERT projection — the narrowest one, the entire point of the tier — read the very atom the tier exists to avoid. `projected` had already caught it (4 of 6 admitted against `guarded`'s 6) but the diagnosis stopped at "the model derives the certificate from occupancy"; the truth is that the certificate is not derived at all, it ARRIVES, and modelling it as derived encoded the fixture's convenience as authority. Making it an atom moved projection to 6 of 6 and ambient readers to 0 of 6. **Three things then had to be resisted.** (a) The cheap fix — add `occupancy` to the two failing plans — would have turned projection green while changing nothing; the check that it was avoided is that `occupancy_defect` and `ledger_remainder` did NOT widen, they gained `own_cert` in their DROPPED column. (b) The rewiring changed no value (`--only inputset --diff`: no drift), which is what makes it safe and equally what makes it unobservable — two functions agreeing everywhere is L23's shape — so a MUTATION PROBE forces them apart: hold occupancy fixed, forge the certificate's region 1 → 1001, and the quantity must return 1001. (c) Closing the defect DELETED the witness justifying `projection_is_stricter_than_checking`, now (6, 6, 6); rather than bank that as success, strictness is kept observable by a planted ambient reader the detector must still catch. **And the search appeared to go blind — the explanation shipped with this rung was WRONG and is corrected in L43.** All three CERT quantities reported search-INDEPENDENT and I blamed the family ("every situation derives its certificate from its occupancy"). The cause was a SECOND projection function, `autoroute._subproj`, carrying the identical defect this rung removed from `inputset.proj`. Syntax vetoed every bad drop throughout, so the plans were right the whole time. | An empty dependency set is a claim, and a suspicious one — ask what the code reads that the set does not name, because "needs nothing" and "needs something undeclared" look identical from outside. When a refactor provably changes no value, that is not the end of the verification but the START of it: force the paths apart with a mutation and demand they diverge, or you have shipped an edit you cannot distinguish from a no-op. When a fix closes a defect, check what WITNESS the defect was providing — a check whose only positive instance has just been repaired is indistinguishable from a broken one, and needs a plant to stay alive. And fixing a hidden dependency can make a SEARCH worse, because the family that hid it still cannot separate it; record the widened blindness as a measured number instead of enjoying the green rows around it. | `tools/terrain/inputset.py` (`situation(..., cert=...)`, `DERIVED` as declared data), `tools/terrain/autoroute.py` (`own_cert`, `the_quantity_follows_the_certificate_not_the_occupancy`, `an_absent_certificate_refuses`, `the_detector_still_catches_an_ambient_reader`), four re-derived goldens with the `screen` digest unchanged as the control |
| L42 | **A digest-binding scalar with no declared representation has its identity decided by `__eq__`, and that is not the same as its digest — measured, the two failed in OPPOSITE directions at once.** `commute_digest(name, parent, head, rank, nbytes, verdict)` validated `rank not in (0, 1)` and nothing else. `rank=True` was ADMITTED (because `True == 1`) and produced `34d6384b3500da4a…` where `rank=1` produces `32e3d2c45341be01…` — the same logical value, TWO digests, so two peers differing only in Python type would disagree. `rank=Fraction(1, 1)` was also ADMITTED and produced `32e3d2c45341be01…` EXACTLY — a different type laundered into the int's identity, ONE digest for two types. Split identity and laundered identity, from a single missing contract, because the preimage is built with `str()` and `str(True)` is `"True"` while `str(Fraction(1,1))` is `"1"`. `chunkstate` admitted `4.0` and `Fraction(4, 1)` as region sizes for the same reason; `clslo` let a priority reach a dict, where `True` would select the class keyed 1. **And the first guard I wrote was wrong**: `rank` is documented to accept the string `"-"` for a refusal, so a plain int check would have refused every refusal scene — the contract had to be read out of the code, not inferred from the parameter's name. | A membership test is an EQUALITY test, and Python's numeric tower makes equality cross types silently: `True == 1`, `4.0 == 4`, `Fraction(4,1) == 4`, and all three hash alike, so `x in FROZEN_SET` admits all of them. `type(v) is int` refuses all three, and bool must be excluded ON PURPOSE because it is a subclass. Declare the contract as DATA next to the scalar — `(admitted type, normalization, encoding)` — and put the guard AT THE DIGEST BOUNDARY rather than at some earlier validator that a second call path bypasses; the first attempt guarded `restore_cert` and left `commute_digest` wide open. Then exercise it: seven porous values, seven typed refusals, and the valid inputs' digests UNCHANGED, or the guard is a behaviour change wearing a safety costume. | `tools/terrain/{commute,chunkstate,clslo}.py` (`REPRESENTATION` as declared data, `_is_int`, `the_representation_contract_bites`), asserted in the `commute-refuse`, `chunkstate-refuse` and `clslo-refuse` gate rows |
| L43 | **The same defect lived in TWO projection functions, and I theorised about the data three times before reading the code.** L41 fixed `inputset.proj`, which derived the certificate from `s["occupancy"]` inside the projection. `autoroute._subproj` — the witness search's projection — did the identical thing and was missed: it stapled `certify(s["occupancy"], s["tick"])` onto EVERY projection regardless of the atom set and never consulted the new `own_cert` atom at all. So the "empty" projection carried `jurisdiction_region`, which IS `exclusion_membership`, and the search concluded that projecting onto NOTHING determined it; the 16-node lattice was really 8 nodes with a constant certificate stapled to each. **Three hypotheses died before I read it.** (1) "The family cannot separate `own_cert`" — refuted, 27 isolating pairs. (2) "It cannot separate the FIELDS the quantity reads" — refuted, `jurisdiction_region` varies in 648 pairs. (3) "Per-observed-input separating power is the metric" — refuted as toothless, since a quantity reading ONE input satisfies it vacuously. Every hypothesis was about the DATA; the defect was in a function whose name announced what it did. Corrected: two rows flip to search-False, over-skip is 2 not 4, the scorecard is (7, 5, 2), and `liveness_horizon/own_cert` stays True as the ONE honest family limitation — it takes a single distinct value across all 54 members, so the empty projection determines it by constancy. **And the fix made a check vacuous before anyone noticed:** `the_lattice_enumeration_overreached` established its "identical certificate" premise with `_subproj((), a) == _subproj((), b)`, which only ever compared certificates BECAUSE of the defect; once the empty projection became genuinely empty it compared two empty tuples and could not fail. | When a refactor removes a defect from a function, grep for the defect's SHAPE and not the function's name — `proj` and `_subproj` were siblings by intent and nobody wired the second one. Before hypothesising about your data, read the primitive: three rounds of increasingly clever theories about family expressiveness cost more than one reading of a twelve-line function would have. Measure the primitive directly ("is this quantity constant across the family?") before theorising about why a search over it behaves oddly. And after any repair, re-examine every check whose PREMISE was established through the repaired path — a fix can silently convert a real check into a tautology, and the resulting green is indistinguishable from the green it replaced. The digests that did NOT move are the evidence: `enforce` covers the plans, the projection census and the three invariants and is byte-identical, which is the mechanical proof that the plans never moved through any of this. | `tools/terrain/autoroute.py` (`_subproj` honouring `own_cert` and reading `s["cert"]`; the repaired premise in `the_lattice_enumeration_overreached`), the re-derived `autoroute-plan` and `-determinacy` goldens, `conformance_autoroute.txt` correction 8 |
| L44 | **The falsifier count is a four-digit number in the same range as every citation year in the repository, so a bare-number sweep is not risky — it is a collision waiting for its turn.** Refreshing 1948 → 1951 with a one-line replace rewrote **Loewner's 1948 *Annals of Mathematics* paper as 1951**, in four places including a references-table row `| 1948 | C. Loewner |`. Caught by reading the diff, not by any checker: `doc-currency` knows about the counts it tracks and has no concept of a bibliographic year, so the gate would have gone green over a falsified citation. The same review found that an EARLIER sweep had already corrupted L40's own historical figure — the lesson that says not to corrupt recorded figures, silently rewritten from `1931 → 1946` to `1931 → 1948` because 1946 was the count at the time. The two previous sweeps missed every citation only because no document happened to cite 1931 or 1946; the repo cites 1878, 1903, 1918, 1927, 1929, 1948, 1952, 1957, 1958, 1977, 1993 and 1999, and the count is now 1951. | Never sweep a bare number across documents. Anchor on the COUNT PHRASE — `1948 unit falsifiers`, `1948-test gate`, `FALS=1948`, `**1948**` — so a number can only move where it is being used as that count. A refresh that matches on digits alone cannot distinguish a measurement from a year, a DOI, an arXiv id or a quoted transcript, and every one of those is a claim you did not intend to change. Read the diff hunk by hunk on any mechanical edit: the cost is minutes, the failure is invisible afterwards, and a corrupted citation looks exactly like a correct one. And note which way the near-miss ran — the checker was GREEN throughout, because the defect was in a category the checker does not model. | `LESSONS.md` L40's restored figure; the anchored replacement patterns used for the 1948 → 1951 refresh; `spec/D19-abductive-gauntlet.md` and `docs/THEOREMS.md` where the Loewner citation lives |
| L45 | **Spanning is not a basis, and the incidence matrix that proves the first cannot decide the second.** The separation basis was accepted at rank 8 of 8 — every declared semantic axis had an isolating witness — and a minimality check applied afterwards immediately found a generator that was not needed: dropping the dedicated `cert.binding` fixture left the span at 8, because `_IN_TILE_FORBIDDEN` and `_IN_TILE_CLEAN` are the SAME tile, so they agree on prefix, region and token and differ only in the lattice digest — hence only in `binding`, which the OCCUPANCY fixture already covered. That axis had measured TWO isolating pairs where every other measured one, and the number was read as noise rather than as the redundancy it was reporting. **Then the obvious implementation of the new invariant was wrong too, and said so out loud.** Promoting the incidence matrix to a first-class artifact makes spanning a column-support question, so minimality looks like a row question: every fixture is the SOLE support of some column. Built, it reported five fixtures each the sole support of NOTHING — because isolation is a property of a PAIR and one end of every pair is a grid member, so the grid row supports every column and sole-support can never hold. A boolean row/column matrix has discarded which pairs the support came from. Irredundance has to be removal-and-recompute. | Distinguish the three properties and name the invariant for the weakest one you actually prove: COVERAGE (how many cases exist), SPANNING (every direction has a witness), IRREDUNDANCE (no single member is removable). And IRREDUNDANT is not MINIMUM — a set where no single removal breaks anything can still exceed minimum cardinality when two members jointly cover what a third covers alone; that does not bite in a small instance where each generator uniquely covers its own axis, but it is a fact about the instance rather than a theorem. When a summary statistic is anomalous in a boring direction — one column reading 2 where every other reads 1 — that is the cheapest possible signal and it costs nothing to chase. And a derived artifact is only as strong as what it retains: a matrix built by projecting pairs down to rows answers pair-free questions and silently returns nonsense for the rest. | `tools/terrain/autoroute.py` (`witness_matrix` with the caveat in its own docstring, `the_basis_spans` as a WALL rather than the `basis_rank` pin, `the_basis_is_irredundant` by removal, `a_redundant_fixture_is_caught` replanting the deleted member), `tools/terrain/inputset.py` (`BASIS_LABELS`, the binding fixture removed with its reason recorded), family 60 -> 59 |
| L46 | **The first composition law found that the code was more cautious than it needed to be — and the two smaller catches were both in my own machinery.** Stage 5 opens with the SEGMENTATION law: a run cut at ANY tick and resumed from that tick's snapshot must reproduce the tail exactly. That is what makes a checkpoint a checkpoint, and nothing asserted it, because the property lives in the SEAM between `worldstep` and `persist` — the gate already carried 26 rows across `rollback`, `lease`, `persist` and `boundary`, and ZERO for identity, associativity or replay, which are exactly the three that exist only between modules. MEASURED: 0 divergences over every cut of two independent worlds, 39 and 119. **And it settles a caution the code had written without checking**: `worldstep.simulate_trace` documents its per-frame snapshots as "DISPLAY-ONLY" and says "nothing here feeds back into the tick" — those snapshots ARE valid resumption points, and the caveat was stronger than the code required. Two plants, deliberately attacking from different directions: perturbing ONE WORD of a snapshot must diverge at exactly that cut (1 of 39), proving the comparison is sensitive to what it compares; carrying state OUTSIDE the snapshot must diverge at EVERY cut (39 of 39), which is the hidden-state defect the law exists to catch and the one a perturbation would never exercise. **Then two self-inflicted catches.** I pointed the new `<!-- remains: ROW -->` marker at the row that had just SHIPPED, and my own staleness class flagged it — the marker names the row whose EXISTENCE refutes the claim, so it belongs on what remains and must name a row that does not exist yet. And `hainuwele/README.md` carried `1948 unit\nfalsifiers` — a count phrase LINE-WRAPPED across a newline, invisible to every anchored sweep since the 1948 era, stale through three refreshes. | A property that lives between two components is invisible to every test inside either one, and 'both components pass' is not evidence about the seam. When code carries a caution, check whether it is load-bearing — an unnecessary caveat costs real capability, and nobody removes one without a measurement. Ship plants that fail DIFFERENTLY: a perturbation and a hidden-state defect break the same law in ways one plant cannot cover, and picking either alone leaves the other mode untested forever. Anchored replacement fixed the bare-number problem (L44) and introduced a new one — a phrase can be wrapped — so sweep for the wrapped form too, and then check each hit's CATEGORY before touching it: `hainuwele`'s wrapped count was a live MEASURED claim and `frontfps`' was an Observe section recording the counts at the time, and a wrap-aware blanket sweep would have corrupted the second while fixing the first. | `tools/netcode/compose.py` (URDRCMP1: `the_segmentation_law`, `the_identity_law`, `the_law_can_fail` with both plants), `tests/test_compose.py` (11 falsifiers), the `compose` gate stage (4 rows), `hainuwele/README.md` Stage 5 marked OPENED; and `tools/specfreeze/doc_currency.py`, where LESSONS.md joined the D5 ledgers as append-only history after THIS ENTRY reddened the gate — the successor rule is LINE-scoped, which is meaningful for prose and meaningless for a 2000-character table row that mentions a shipped module thirty clauses away from the phrase |
| L47 | **The absence of a function was doing the work of a design decision, and the plant that was supposed to fail silently refused instead — for a reason weaker than it looked.** The replay law was declared as "a checkpoint written by `persist` and read back reproduces the tail", and reading the code first shrank it twice. `splice` ALREADY gates resumption within `glide` for every log and interior split, so the uncovered property was never "can a glide resume" but whether the pose survives the ROUND TRIP THROUGH BYTES and still resumes. And `persist` cannot checkpoint a `worldstep` state at all — `storecost.serialize`'s contract is `(fx, fy, ground, facing)`, a GLIDE pose, while `worldstep` holds `(pos, vel)` in Q32.32. That was read as a gap and it is a CONTRACT: `persist` is the glide layer's durability, and the physics layer is transient because a world is re-derivable from an authored world plus a log. It is in D11 now, because a decision inferable only from a missing function is indistinguishable from an oversight. **Then the third planted axis did the opposite of what it was designed to do.** Permuting `ground_height` and `facing` was predicted to serialize cleanly and diverge on replay — a silent semantic fault. It REFUSES, at `storecost.serialize`, because `facing` carries a range guard of 0..3 and every ground height in the corpus is >= 24. Stronger outcome, WEAKER guarantee: the schema is protected by a range COINCIDENCE, not by structure, and a scene with heights in 0..3 would serialize the permuted pose without complaint. The gate pins the coincidence count at 0 so that such a scene reddens rather than quietly narrowing the plant's coverage. | Read the neighbouring modules before declaring a law — twice here the declared property was already covered in part, and the honest rung was the remainder rather than the headline. When a capability is missing, decide whether it is ABSENT or REFUSED and write the answer down; an unwritten boundary is re-litigated by every contributor who notices the hole. And when a plant fires for an unexpected reason, the reason is worth more than the plant: a defence that works because two ranges happen not to overlap is a defence with a precondition, and the precondition belongs in the gate as a pinned number. State a count as 'diverged of TESTABLE' rather than 'of total' when some cases are structurally untestable — 11 of 11 is a result and 11 of 14 reads as three silent misses. | `tools/netcode/compose.py` (`the_serialization_replay_law` as a commuting diagram, `the_replay_plants` over three axes), the `compose-replay` and `compose-replay-plants` rows, `tests/test_compose.py`, and `spec/D11-layer-contracts.md`'s durability boundary |
| L48 | **The dependency graph was topology until an edge could be BROKEN, and the first attempt at breaking had no resolution at all.** 320 AST edges across 134 modules say nothing about which of them a law rests on, so each declared edge is SEVERED — its target replaced by a sentinel that raises on use, `autoroute`'s trick for undesignated atoms lifted from atoms-inside-a-module to edges-between-modules — and the laws recomputed. **Severed against the STAGE, four different edges all reddened `compose:scenes` and nothing else**, because that row computes first and returns early: every edge produced an identical verdict and the test measured nothing. Against the individual law FUNCTIONS the picture separated immediately, and it PARTITIONS — the replay laws rest on glide + storecost + persist and nothing else, segmentation and identity on worldstep and nothing else, 0 edges carrying both. That is the D11 durability boundary, written one rung earlier from ARGUMENT, re-derived mechanically from the opposite direction; it is the first contract in this session that survived a measurement which could have refuted it. **And the declaration-versus-derivation check found a transitive carrier on its first run**: 7 declared edges, 6 direct imports. `storecost` is not imported by `compose` at all — it is reached through `persist` — yet severing `storecost.serialize` breaks BOTH replay laws. Severance measures REACHABILITY, the AST measures DIRECT IMPORTS, and the gap is exactly where a dependency carries a law while being invisible in the consumer's import list. | Before trusting a measurement that says every input matters equally, check whether the harness can tell them APART — an instrument returning the same answer for every probe is measuring the instrument. State the resolution you actually have: this one is family-level, not edge-level, because five edges share an identical vector, and claiming per-edge precision would repeat the granularity error that made an atom-level census read healthy while every field read zero. Absence of attribution is NOT dead code — it may equally be an implementation detail below the certification line or an evidence gap, and only one cell of the table is unconditionally wrong: a law depending on an edge that does not exist. A method that works by breaking things needs a plant that BREAKS NOTHING, or a harness silently failing to sever reports every edge as unattributed and looks exactly like a clean partition of nothing. | `tools/netcode/edgeattr.py` (URDREDG1: `sensitivity` by severance, `attribution_matrix`, `the_families_are_disjoint`, `minimal_responsible_set`, `every_family_has_two_edges`, `the_transitive_carriers`, `an_unbreakable_edge_is_caught`, `severance_leaves_no_residue`), the `edgeattr` gate stage (4 rows), `tests/test_edgeattr.py` (12 falsifiers) |
| L49 | **Instruments that COUNT evidence saturated; instruments that DESTROY it kept finding things — and generating the perturbations beat hand-picking them on the first run.** Three counting probes collapsed in one turn: stages clustered by dependency set (164 of 172 already distinct), falsification coverage by row name (48 stages looked uncovered; probing the SOURCE showed 45 carry defect machinery and only 3 stages / 4 rows genuinely lack one, all structurally exempt), and the perturbation-family census (7 of 7 populated, including a split-brain plant that predates this session). Meanwhile the destructive probe run over GENERATED candidates rather than a hand-written table produced 5 distinct sensitivity vectors where 7 curated edges produced 2 — in 18 seconds. **It separated two law pairs that had always moved together**, which is the difference between several facts and one fact wearing several green rows. `worldstep._fp_div` breaks segmentation and seg-plants but NOT identity, and the mechanism was measured rather than argued: the identity law runs an EMPTY event log, so no actor moves and fixed-point division is reached 0 times against 9 under segmentation. `persist.PersistError` breaks replay-plants but not replay, and is recorded as DEGENERATE — severing an exception CLASS makes `except PersistError` raise TypeError, so the plants break for a reason unrelated to what they test. And 41 of 68 candidates taught NOTHING. | Prefer probes that BREAK things to probes that count them: a census over a population the design already excludes measures the absence of a search space, not the presence of evidence. Generate the perturbations — a hand-written table is a guess about what matters and it under-reported twice, missing `storecost.serialize` and then `glide._fold`, `_grid_dims`, `_heights`. When two rows always move together, look for the minimal perturbation that separates them BEFORE trusting them as independent evidence, and grade the witness: a separation obtained by substituting an exception class is real and proves less than it looks. Keep the INERT count first-class and assert it is strictly between zero and the total — a sweep in which everything mattered is measuring the sweep, and one in which nothing did has broken. | `tools/netcode/edgeattr.py` (`severance_candidates` generated from module introspection, `the_vector_census`, `the_inert_share`, `the_separating_witnesses` with clean/degenerate grading, `the_identity_law_never_divides` measuring the mechanism, `the_declared_edges_are_a_subset`), the `edgeattr-sweep` row, `tests/test_edgeattr.py` |
| L50 | **A count of an ABSENCE drifts in the one direction work always moves, and the line-wrapped-phrase defect appeared for the THIRD time — inside the very claim the new checker was written for.** `hainuwele/README.md` said 87 modules have no design brief; five briefs had landed a rung earlier and the live complement was 82. Five staleness classes existed and none could see it, because every one of them watches something that EXISTS and can be counted where it lives — an absence has no file to inspect, so the checker has to recompute the complement. It goes stale only DOWNWARD, silently, as the work gets done. **And the claim was wrapped**: `— 87\nmodules have no ...`, so the first regex, written per-line like its four predecessors, matched nothing while computing the right answer. That is L46's wrapped count and the brief-boundary presence test making a third appearance, so whitespace normalization stopped being a per-case fix and became the default for prose matching. A second phrasing of the same claim (`87 of 103 modules have no design brief`) was found only because normalizing surfaced it. **The comb that found it was itself a near-miss**: the first candidate it produced was `97 pinned conformance corpora`, which looked stale against 136 conformance files tree-wide and is CORRECT — `tools/terrain` has exactly 97 and the ratchet scans that directory. Checking what a number counts before correcting it is the whole of the discipline. | Prose checkers match against WHITESPACE-NORMALIZED text, always: a check sensitive to where an author broke a line is testing the formatting, and this repository has now paid for that three times. When adding a checker class, ask which DIRECTION its claim can rot — a count of a presence can drift either way and someone notices; a count of an absence only ever shrinks, so nobody is surprised by it and nobody checks. And before correcting a number that looks stale, verify what it is counting: the comb's first hit was a correct subset count that a tree-wide total made look wrong, and 'fixing' it would have falsified a true claim with a green gate. | `tools/specfreeze/doc_currency.py` (`absence_count` recomputing the complement, `stale_absences` matching on normalized text, `absence_defect_text` as the sixth planted shape, `extension_defect_is_caught` now demanding all six), `hainuwele/README.md` 87 → 82 in both claims |
| L51 | **Writing a brief is an AUDIT of the code it documents, and these two caught three things no gate row was looking at.** The task was to document `partition` and `worldregion` — pure documentation, ostensibly — and it surfaced (a) a FABRICATED identifier: I titled the worldregion brief `URDRRGN`, and reading the module refuted it — worldregion mints NO new witness code, it recomputes the frozen `URDRLST1`/`URDRLSTT` law over reunified interiors, which is the entire point of the Seam Composition Theorem. An invented code in a title is the same defect class as an invented API. (b) A MISCOUNTED gated number: the plan said `82 → 80`, but the absence checker built last rung counts `tools/terrain` ONLY, and `worldregion` is a netcode module — so partition moves the gated count `82 → 81` and worldregion's brief, though real work, is outside the `82/103` terrain claim entirely. That is L50's 'check what the number counts' applied to the very checker L50 built. (c) STALE PROSE IN THE CHECKER ITSELF: extending `BRIEFS_REQUIRING_A_FALSIFIER` from 5 to 7 left the `brief-falsifiers` evidence string still saying 'each certificate-arc brief', which is now false since partition (terrain) and worldregion (netcode) are neither — fixed to read the set's live length. | Documentation is not a lower-rigor activity than code; a brief that restates a module HONESTLY has to read every claim the module makes, and reading is where fabrication and miscounting surface. Never carry an identifier, count, or citation into a doc without reading the source that defines it — a title is a claim. When a plan quotes a number, re-derive it against what the checker actually measures before believing it, even when YOU built the checker. And when you widen an enforcement set, the evidence string that describes it is prose that just went stale — update it in the same edit. | `docs/partition_brief.md` (CAP CP posture, Partition Prefix Theorem, falsifier `partition-law`), `docs/worldregion_brief.md` (Seam Composition Theorem, falsifier `netcode-region:seam2`, no invented code), `BRIEFS_REQUIRING_A_FALSIFIER` 5 -> 7 with the evidence string de-staled, terrain absence 82 -> 81 |
| L52 | **Across the current lesson corpus, the largest share of discoveries began by reading the artifact or measuring the implementation before extending the instrumentation — new mechanisms contributed fewer discoveries than correcting the working representation of the existing system.** Classified all 34 lessons (L18–L51) by the operation that produced each, using a fixed operator set chosen before classifying, with an OTHER bucket as the falsifier: a uniform distribution would have refuted the hypothesis. It did not. READ (source-reading contradicted prose/memory/plan) produced 9, MEASURE (a number overturned an assumption) 8 — together HALF of 34 — against SEVER, the session's shiniest new instrument, at 2. The humble operators dominate; the mechanisms are rarer. **What is NOT measured, stated so it is not promoted to a universal (L20):** I classified by a SINGLE primary operator, so I measured which operators PRODUCED discoveries, not their ORDER. The attractive 'READ→MEASURE→MUTATE→DERIVE→DECOMPOSE workflow' is a hypothesis this data cannot confirm, and one lesson cuts against a fixed order — L43 went MEASURE(×3, failed)→READ(succeeded), the reverse of the proposed phase 1→2. **The risky forward prediction, on record so future work tests it:** if the next 20–30 briefs produce essentially no corrections, the READ+MEASURE dominance was specific to Stage 6 rather than a general property; if briefs keep generating corrections at a materially higher rate than new instrumentation rungs, the finding strengthens. **First datapoint (this rung):** reading four well-documented modules caught one correction — and it was in MY scaffolding, not the modules: the `brief-falsifiers` evidence string went stale AGAIN the moment I extended the enforcement set (7→11), the same stale-prose class as L51's third catch. The modules read clean; the corrections are shifting toward the checker prose I keep hand-editing, which argues for making that evidence string DERIVE its description from the set rather than hardcode it (a named follow-on, not this rung). | The most productive research operator in a mature system may be the least glamorous one — confronting the actual artifact when prose, memory, or a plan claimed otherwise. State an operator's dominance as a share of THIS corpus, not as a law, and leave the distribution free to shift. Measure what you can defend (which operator produced a discovery) and name what you cannot (the order they compose in) rather than letting a plausible workflow narrative pass as measured. And watch where the corrections MOVE: when a documentation pass stops finding bugs in the modules and starts finding them in your own scaffolding, the scaffolding is the thing that now needs the derived-not-declared treatment. | `LESSONS.md` L18–L51 classified; the meta-study is itself a READ+MEASURE operation (read the lessons, measured the distribution), which is self-consistent but not proof; the four city/authority-arc briefs (`chunkstate`, `chunkload`, `migrate`, `rannull`) landed in the same rung as the first prediction datapoint |
| L53 | **The first module read of the resumed pass found the MODULE clean and the ROADMAP PROSE stale — the same maintained-number-beside-stale-sentence defect the previous rung had just eliminated in the gate, living one file over.** Adding `storecost`'s brief drops the terrain absence count 77 → 76, and updating that number in `hainuwele/README.md` surfaced that the sentence AROUND it was false: it claimed the ~20 city/authority/certificate-arc modules carry "NOT ONE" `docs/<name>_brief.md` and that "the newest arcs are the whole of the gap," but eleven of those arc modules were briefed THIS session — `blindscreen`, the very module named as the range endpoint, among them. The count had been kept current by the count-refresh sweep; the prose had not — precisely the pathology the immediately-prior rung fixed STRUCTURALLY for the `brief-falsifiers` evidence string, still alive in the narrative doc. **`storecost` itself read as a DERIVE ≠ DECLARE:** it declares the narrow Stage-H space scope, but severance (L48/L49's `edgeattr`) had measured `storecost.serialize` as a transitive carrier — `persist` composes it exactly, `durable_window_bytes(H,n) == storecost.window_storage(H,n) + envelope_overhead(H)`, gated in the `persist` stage — so the brief records the wider role as PROVENANCE and points the credit at `persist`, minting no law. **And enrolling `storecost` (11 → 12) was the first LIVE exercise of the derived evidence:** the `brief-falsifiers` count and enumeration updated with ZERO prose edits, the prior rung's fix working exactly as built. | L52's risky prediction gets its second datapoint and it holds — the brief pass keeps generating corrections — but sharpen it honestly: BOTH datapoints so far are stale prose I wrote MYSELF (the evidence string, then this roadmap narrative), not bugs in the modules, which keep reading clean. The READ operator's yield in Stage 6 is specifically catching a DERIVED number beside a stale DESCRIPTION, and the fix is the same every time — make the description derive. When a rung eliminates a defect class in one place, sweep the tree for the SAME class before calling it gone; here it had a sibling one file away. And when a brief's module declares a scope narrower than severance shows, name the wider role as provenance and gate it where it lives — never mint it in the doc. | `docs/storecost_brief.md` (URDRLAT4 space envelope, falsifier `storecost-soundness`, the derive≠declare carrier note crediting `persist`), `BRIEFS_REQUIRING_A_FALSIFIER` 11 → 12 (first live extension of the self-deriving evidence), `hainuwele/README.md` absence 77 → 76 in both claims with the stale "NOT ONE / newest arcs are the whole of the gap" prose corrected, `verify.py` `brief-falsifiers` now deriving 12 |
| L54 | **`persist` read CLEAN — the first no-correction brief of the resumed pass — and that null is worth recording precisely because dropping it would bias L52's prediction toward confirmation.** `persist` is the durable realization sitting directly above `storecost`; the read found the module consistent with its own honest grade, the composition already gated (the realization identity `durable_window_bytes == storecost.window_storage + envelope_overhead`), and NO stale prose, fabricated identifier, or miscount to fix. The only count that moved was MECHANICAL — absence 76 → 75 because a brief was added — not the correction of a false claim, unlike L53's genuine stale-roadmap catch. Recording a brief that found nothing is L49's inert-share discipline turned on the meta-study's OWN prediction: a log of brief-pass corrections that keeps only the catches and silently drops the clean reads is measuring the sweep, not the system, and would let L52's "briefs keep generating corrections" harden into a law by construction. | Count the nulls. A prediction is tested only if the datapoints that would REFUTE it are recorded as faithfully as the ones that confirm it — L52 said a run of no-correction briefs would mean the READ+MEASURE dominance was Stage-6-specific, so a clean-read brief is the MORE informative datapoint and belongs on the record, not quietly skipped. And when a module reads clean, resist manufacturing a finding to justify the rung: the honest output of a documentation pass is sometimes "the module was already right," and the brief still earns its place by making the reasoning readable rather than leaving it in the header. Running tally of the L52 prediction so far — L51/L53: two corrections (both stale prose I wrote); L54: one clean read. | `docs/persist_brief.md` (URDRLAT5 durable checkpoint, falsifier `persist-durable`, the realization identity as the composition's second link above `storecost`), `BRIEFS_REQUIRING_A_FALSIFIER` 12 → 13, `hainuwele/README.md` absence 76 → 75 (mechanical, not a catch) |
| L55 | **Writing the `rollstore` brief reddened the gate's OWN staleness net: `status_contradictions` flagged `docs/rollstore_brief.md` because its `does_not_show` used "not begun" while backtick-naming existing modules — and the module docstrings get away with the identical words only because they are `.py`, which the markdown checker never scans.** `rollstore` honestly defers an O(window) suffix-restore as a future rung its own docstring calls "stated not begun"; I carried that phrasing straight into the brief. But `status_contradictions` scans only markdown, flags any doc pairing an `_UNBUILT_MARKERS` phrase ("not begun", "no code yet", "nothing built") with a backtick-named module that EXISTS, and reports the FIRST alphabetically — here `bench`, named only in "wall-clock (`bench.py`)". The checker cannot tell that "not begun" referred to the suffix-restore rung and `bench` to an existing tool, so it conservatively refuses the pair. The fix was the DOC's, not the checker's — "not begun" → "deferred"/"not yet taken", meaning intact. This is the resumed pass's first catch that fired on NEW prose rather than stale OLD prose, and it came from the red-first gate itself, before the brief shipped. | The honest-scope vocabulary that is FREE in a module's `.py` header ("not begun", "no code yet") is a TRIGGER in a `.md` brief, because a brief lives under every prose checker the docstrings escape: express deferred work as "deferred" or "a future rung," never as an unbuilt marker sitting beside a backtick-named module. And read a gate catch before "fixing" it — `status_contradictions` was not over-eager: pairing an unbuilt marker with an existing module IS the stale-status shape it guards, so the fix is to stop making the pair, not to loosen the net. Tally for L52's prediction: the resumed pass's corrections are now L53 (stale OLD prose) and L55 (NEW prose tripping the gate) — both PROSE, still zero module bugs; `persist` and `resurrect` were the clean reads. | `docs/rollstore_brief.md` (URDRRBS1 durable rollback window, falsifier `rollstore-death`; three "not begun" → "deferred"/"not yet taken" rewordings), `BRIEFS_REQUIRING_A_FALSIFIER` 14 → 15 (netcode — the terrain absence count stays 74), the `doc-staleness` row that reddened on my brief and now passes |
| L56 | **"Integrity is not truth" (L11) is not one lesson — it is the special case of a law that governs every relation the evidence graph will ever hold: a claim's ENFORCEMENT may never exceed its EPISTEMIC CLASS.** Three rungs reached it from different sides before it was named. `REQUIRES` is re-derived from severance and its gate re-runs the derivation, so the gate certifies the value itself. `FORMULATED_FROM` is AUTHORED — no algorithm can infer *why* a theorem was formulated, because motivation is history, not a property of any artifact — so its gate can prove the edge consistent, symmetric, and pointing at real recorded observations, but never that the observation is the true or sole cause. The predictive experiment, when it exists, will be a frozen preregistration whose immutability is enforceable while its correctness is decided only by reality. Same shape three times (provenance, the lattice pin, genesis); stated once here so the registry rung DERIVES from a law rather than reinventing the distinction a fourth time. The recurrence is the evidence it is structural, not stylistic (L52's operator study, turned on the design principles themselves). | Every edge in the evidence graph DECLARES its epistemic class, and the gate certifies only the strongest guarantee that class admits — never confusing one class for another. **DERIVED**: mechanically re-derivable from artifacts; the gate re-derives and compares (`REQUIRES` by severance). **HISTORICAL**: an authored record whose integrity and internal consistency are checkable but whose truth and motivation are not; the gate verifies well-formedness, symmetry, and that it cites real recorded observations, never causation (`FORMULATED_FROM`). **PREDICTIVE**: a frozen hypothesis whose preregistration and immutability are enforced until reality decides its correctness (none yet). The schema grows ONLY when reality produces an instance a smaller basis cannot represent: ORDERING stays an EMERGENT consequence of existence (no theorem-side ordinal exists to separate a chronology violation from an existence failure, so mechanizing it now would enlarge the representation without adding expressive power — L45 spanning≠basis, L19 vacuity), and PREDICTIVE stays ABSENT until the first preregistered prediction exists. Asserting truth of an authored claim, or enforcement of an absent one, would be the inflation L2/L20 forbid. | NOT YET MECHANIZED, and graded so — the L10 pattern (a law stated a rung ahead of its checker). This rung STATES the law; the claim-class registry that checks each relation declares a valid class and names a live enforcing row is the NEXT rung, built only after this framing survives review. Two instances already conform without any new code: `REQUIRES` (DERIVED — the `lattice-depth`/`-snapshot`/`-conformance` rows) and `FORMULATED_FROM` (HISTORICAL — the `formulated-from` row), with L11 the seed the law generalizes. The honest enforcement column is "stated, one rung ahead of its checker," never a claim of a gate that does not exist |
| L57 | **L56 stated the law as a NEGATION — "integrity is not truth" — and the review hardened it into an AFFIRMATION that is stronger in three ways the negative framing hid.** (a) It separates the epistemic CLASS from the enforcement MECHANISM. The class is a property of the CLAIM'S SEMANTICS and fixes the strongest guarantee reality permits; the gate is merely ONE mechanism that certifies some or all of it. A relation moves HISTORICAL → DERIVED only if the RELATION itself becomes mechanically derivable — never because a checker got better — so the law outlives any future tooling. (b) It is CLOSED UNDER EXTENSION: the graph CURRENTLY holds three classes, not exactly three forever; the invariant constrains the class↔guarantee relation, not a fixed taxonomy, so a genuinely new category years from now leaves the law unchanged. (c) The limit is ONTOLOGICAL, not technical: truth is a property of the WORLD, not of records — a gate can certify consistency, derivation, reproducibility, immutability, or registration because those are properties of ARTIFACTS, but it can never certify that an authored causal history is the real one, because that property lives OUTSIDE the repository. | Every relation declares an epistemic class. That class specifies the STRONGEST GUARANTEE REALITY PERMITS for the relation, and every enforcement mechanism must certify NO MORE AND NO LESS than that guarantee. The three current classes are EXAMPLES of the rule, never the rule itself: DERIVED (guarantee — mechanical re-derivability from artifacts; `REQUIRES`), HISTORICAL (guarantee — integrity and internal consistency of an authored record, never its causal truth; `FORMULATED_FROM`), PREDICTIVE (guarantee — immutable preregistration until reality decides correctness; none yet). Class is SEMANTIC and stable; mechanism is IMPLEMENTATION and may only strengthen up to the class ceiling — a better checker cannot PROMOTE a class, only a change in what the relation IS. ORDERING stays emergent because it has NOT YET EARNED ITS OWN BASIS VECTOR: every ordering violation is presently also an existence violation (the theorem side carries no ordinal), so a theorem-order coordinate would add representation without expressive power (L45 basis≠span, L19 vacuity). A new class, or an independent ordering invariant, enters ONLY when reality supplies an instance the current basis cannot represent. | STILL NOT MECHANIZED — L57 refines the WORDING of the law L56 stated; it adds no checker, and it is a NEW entry rather than an edit because the ledger is append-only and a lesson is refined by a SUCCESSOR, never rewritten (the L23→L24, L41→L43 pattern). The claim-class registry remains the next rung and now implements the affirmative form: each relation names its class and a live enforcing row, the mechanism certifying EXACTLY the class's guarantee. `REQUIRES` (DERIVED) and `FORMULATED_FROM` (HISTORICAL) conform today; PREDICTIVE withheld until a real prediction exists. The column stays "stated, one rung ahead of its checker" |
| L58 | **The whole READ-2 line was obeying one law nobody had stated: the representation of justification must be exactly as rich as the DEMONSTRATED structure of justification — earned, never designed.** L56/L57 governed what a claim may CLAIM; this governs how much REPRESENTATION the justification may carry. Named now, as L52 named the operator distribution, so the next rung derives from it: provenance, the proof lattice, genesis, and the registry each independently refused structure ahead of evidence and shipped a falsifier with every primitive. It is a generalization of a measured pattern, not a fresh abstraction — reached, tellingly, by a review that first proposed a numerical conservation law, a proof↔execution homomorphism, an isomorphism, and a monotone-complexity master invariant, then pruned all four as unearned. The pruning IS the law operating on itself. | Representational structure is EARNED, not designed, in four clauses each already demonstrated. (1) Every increase in representational rank introduces new INDEPENDENTLY FALSIFIABLE structure — NOT a numerical conservation but a hard constraint: a primitive no plant can redden is unfalsifiable commentary (L23, L15); every READ-2 rung shipped plants (provenance 4, `formulated-from` 3, the registry 5). (2) A representation may never OUTGROW the repository's ability to falsify it — the capability poset, `SUPPORTED_BY`, `TESTED_BY`, and PREDICTIVE stay deferred precisely because no instance yet earns their falsifiers (L45 basis=rank, L2 no-inflation). (3) Every DERIVED relation stays structurally FAITHFUL to the substrate it is derived from and NAMES that substrate — `REQUIRES` from the severance graph, depth ≤ 13 from the import graph, `FORMULATED_FROM` from the recorded observations, brief counts from the live markers; the fidelity is structure-preserving PROJECTION, not runtime execution and not isomorphism (most derivations are projections or quotients, not bijections). (4) Every expansion must admit a mechanically verifiable COLLAPSE when its necessity disappears — orphaned edges left after a deletion prove the structure was hardcoded, not derived; the demonstrated instance is L45's irredundance check removing a redundant generator (basis 60 → 59), and the clause claims only that the CAPACITY to collapse must exist, shown once, not that collapse is richly exercised. Corollary, the READ-2 trajectory itself: PREFER DERIVED OVER AUTHORED — where new capability comes from replacing authored structure with derived structure, the derived form wins, so the harness grows in EXPLANATORY POWER, not necessarily in complexity. | NOT MECHANIZED — the L10 pattern; a discipline the rungs already obey, stated so the next one derives from it rather than rediscovering it a seventh time. WHAT IT DOES NOT CLAIM, because the repository has not shown these (L20 sample≠universal, L31 verification≠validation): it is NOT a numerical conservation law (`ΔX ≤ ΔY`) — the constraint is one falsifiable invariant per primitive, structural not counted; it does NOT require derived relations to map to RUNTIME execution — `FORMULATED_FROM`, provenance, the registry, and lesson lineage are historical, contractual, and documentary, faithful to substrates that are not code; it asserts no ISOMORPHISM; and it does NOT claim harness complexity is monotone in operational surface — READ-2 repeatedly increased capability while REDUCING authored representation (the registry replaced narrative with a checker), which refutes that outright. The law's own first live test is the capability poset from the last review: it stays a flat set until a sixth capability earns the order |
| L59 | **A pre-registration is only as blind as the session that authored it: freeze BEFORE any history scan of the target.** P1's provenance said "code UNREAD" and was true — yet the target's git-log SUBJECT line ("certified integer-work envelope") leaked into the session between the freeze and the witness commit during an unrelated lineage scan, so the commit alone could no longer prove blindness; only file mtime plus session order rescued the claim, and the contamination note is recorded in the ledger rather than the leak silently absorbed. P2 closed the hole STRUCTURALLY: the target was selected by import in-degree computed from import LINES alone (module bodies unread), the hypothesis authored from declared role prose only, secondhand exposure (nway's citation of terraform) disclosed in the frozen provenance, and NO git command touched the target before the freeze commit — blindness enforced by ORDER of operations, not by intention. | The rule: the freeze commit precedes every history scan of the target, and the selection procedure may read only what the frozen provenance declares it read. An operator's blindness is part of the apparatus (L23's cousin for the READ operator: a prediction no leak can contaminate is the analogue of a checker no defect can pass). Residual exposure is DISCLOSED, never assumed away — an undisclosed leak is a broken witness even when the prediction is right. | NOT MECHANIZED — no gate row can see a session, so enforcement is procedural, recorded per-prediction in the ledger's provenance and contamination fields. It does NOT claim P1 was invalid (the frozen text predates the leak, shown by mtime + session order) and does NOT claim role prose is leak-free — AGENTS.md role lines are DECLARED exposure, graded as such (L20 sample != universal). |
| L60 | **A frozen prediction must partition its outcome space exhaustively — every possible resolution maps to exactly one frozen class.** P3's second-order meta-prediction resolved INDETERMINATE BY ITS OWN TERMS: the actual outcome (predicate confirmed, a named sub-claim wrong) fell between its confirmation condition and both of its falsifiers, so the freeze could render no verdict — and a prediction that cannot classify what actually happened is unfalsifiable on exactly the outcomes that matter most, the partial ones. P4 applied the rule: four exhaustive primary classes (C0 / R1 / R2 / R3, the last a mandatory catch-all OTHER) and three exhaustive meta classes (M-0 / M-1 / M-2), each a total function over the resolution space; the meta predicted NOT M-1, and the READ resolved DECIDABLY — W-C0, meta M-0, the run's first second-order confirmation. The rule changed the verdict from indeterminate to decided in one rung. | The rule: at freeze time, enumerate outcome classes until they are TOTAL. The catch-all OTHER class is mandatory — it is the freeze's own admission that reality may exceed the named risks — and the meta-prediction's classes must partition the primary's. This is the total refusal battery (stance's 8/8, warden's 4/4) lifted to the epistemic layer: a partial outcome function invites the indeterminate verdict, and an indeterminate verdict is L23's checker-that-cannot-fail wearing a prediction's clothes. | NOT MECHANIZED — the totality of a prose partition is judged by the author at freeze time; the gate cannot enumerate semantic outcome spaces. One application so far (P4). It does NOT claim P3's meta was worthless — the informative failure is what BOUGHT the rule, and the failure stays recorded, never repaired (L2). And it does NOT claim exhaustiveness guarantees a CORRECT classification — only that a verdict EXISTS for every outcome; a wrong class assignment remains possible and falsifiable (L31 verification != validation). |
| L61 | **In this architecture wrong answers are rare and empty answers are common, and an empty answer is indistinguishable from a correct one unless something asserts non-emptiness.** Named by `ashdepth` after its FOURTH appearance in a single session and confirmed across the arc: `disjoint`'s single-value edit family (every pair commutes trivially — the census would confirm ANY predicate), `frontier`'s all-singleton greedy plant (an unsound rule scores harmless), the ash-depth level table first run on an empty-fast-path corpus (one keystroke from a false 'soundness never breaks'), the handed-down k* passing VACUOUSLY at maximum burn, and `auditgraph`'s denominator defect (a fault that narrows the census world without causing disagreements passes clean). Six carriers with the epistemics ledger's own interface instrument as the self-referential sixth: an instrument STARVED of the events it compares reads CONSISTENT, which is an empty answer in a confirmation's clothes. | The rule, in two clauses. (1) MECHANISM: every census, instrument, or comparison carries a NON-VACUITY PRECONDITION and raises rather than returning a quiet zero — `ashdepth`'s `VacuityError` + pinned `EMPTY_CORPUS`, `auditgraph`'s max-flow denominator cross-check, the provenance non-vacuity plant. The failure this architecture actually admits is the EMPTY fast path, never the WRONG one (coarsening is strictly conservative — a void is perfectly sound and perfectly useless), so the bound worth guarding is k_min (still distinguishes something), not the coarse-end k* the design was handed. (2) EPISTEMIC LIFT: every scoring instrument or prediction NAMES its starvation mode — what event stream feeds it, and that a starved resolution reads STARVED, never CONFIRMED; adopted at checkpoint 4, applied at the batch-4 and batch-5 freezes. | NOT MECHANIZED as one gate row — it is a DISCIPLINE distributed across every census's own non-vacuity check (each of which IS gated) and, at the epistemic layer, a freeze requirement the ledger records per-prediction. It does NOT claim empty answers are the ONLY failure (wrong answers exist, just rarely — L31 verification != validation) and does NOT claim a non-vacuity precondition proves USEFULNESS, only non-emptiness (a fast path of one is non-empty and nearly useless; k_min is a floor, not a policy — L20 sample != universal). |
| L62 | **A tournament between hypotheses reports which RIVAL is better; it never reports whether ANY of them is good — a basis that beats every rival while losing to the CONSTANT PREDICTOR has explanatory power that is entirely unearned, and no amount of head-to-head scoring can reveal it.** The discovery engine scored bases against each other from run 1 — Brier, posteriors, weight updates, MT eliminations, a frozen retirement — and never once against a trivial baseline. Checkpoint 8 built the deferred adversarial challenger and MEASURED the gap: two rivals constructed to attack the sole surviving basis at its weakest recorded point (C-AB, the modal outcome, priced by an auxiliary tie rule rather than a cell of the grid) scored 10/27 and 15/27 on the binary CONJ/SINGLE task, against a constant predictor's 16/27 — BOTH BELOW BASELINE. They were not weak rivals but anti-informative ones, and a tournament containing only them would have crowned a winner and reported convergence. The lesson is L61 one level up: L61 asked whether a rival can LOSE (a rival that cannot is theater); L62 asks whether a winner beats NOTHING-AT-ALL. | The rule: every scoring tournament seats a NULL ENTRANT from the start — the trivial predictor for that task (constant/majority class, or the maximum-marginal cell for a multi-class prediction) — scored by the same rule over the same joints, and a basis that fails to beat it is reported as UNEARNED regardless of its rank among rivals. The null is not a competitor to be defeated once and retired: it is the ZERO of the measurement scale, and a scale without a zero reports differences while concealing magnitudes. Corollary for construction: a challenger built by the same author who holds the incumbent must be scored MECHANICALLY (predictions computed from data the incumbent does not use — an import graph, a module's own self-description), because a rival invented and then judged case-by-case by its own inventor measures the inventor, not the rival. | MEASURED and WITNESSED, not merely stated — `exe_epistemics/nullbase.py` recomputes both challenger scores and the null baseline from the live import graph and the pinned joint list, stdlib-only, exhaustive (no sampling), rerun byte-identical. Its `challengers_fail_the_null()` and `non_vacuous()` decide the finding, the latter guarding L61's own trap (if only one class occurred, 'the constant predictor wins' would be an artifact of a one-class corpus, not a result). WHAT IT DOES NOT CLAIM: that no rival basis exists — two lenses of unboundedly many were tested and BOTH ARE STRUCTURAL (graph topology, declared phase position), with semantic lenses untested here precisely because scoring them needs the author's judgment, the bias the mechanization removed; an absence measured over two lenses is evidence, not proof (L20 sample != universal). And it does NOT claim the incumbent B-M' passed or failed the null: B-M' predicts a multi-class CELL scored by Brier, a DIFFERENT task, so the 59% bar does not transfer — that measurement is frozen as checkpoint 9's obligation and deliberately left unrun, since minting a lesson and choosing its first verdict in the same breath is the conflict the lesson itself forbids. |
| L63 | **EMPIRICAL ADMISSIBILITY. A diagnostic has NO ARCHITECTURAL STANDING. It earns standing only by repeatedly and preregisteredly improving out-of-sample performance on its declared objective over the currently SEATED incumbent, under the designated scoring rule; failure to maintain that improvement retires it from EXPLANATORY USE.** The wording is the lesson: a proposed diagnostic does not start with authority that failure then removes - it never acquires authority in the first place. Four carriers, one shape: the coupling/interface Gamma table was seated by adoption rather than by evidence and survived three rungs past its usefulness before checkpoint 3 suspended it; the two constructed challengers scored BELOW a constant predictor (10/27, 15/27 vs 16/27); v_D - load-bearing for nine runs - LOSES to the null out-of-sample under leave-one-batch-out (19036400 vs 17854727); and all four joint-level covariates lose to the seated null on the miss event (best 2560 vs 2380), the margin hypothesis failing DIRECTIONALLY. In every case the object had already been reasoned WITH before it was ever tested. **Architectural concepts are downstream of empirical winners, never upstream** - the single line that explains why Gamma was refused despite being mathematically appealing, why the census lost standing despite its intuitive appeal, and why Psi over a frozen probe corpus must be BUILT before any claim about joint-level behaviour may be made. | THREE STATUSES, never two. **EXPERIMENTAL** (proposed, unearned - may be computed and reported, may NOT be reasoned from); **SEATED** (current best performer on its declared objective); **RETIRED** (empirically displaced or failed). A brand-new idea therefore does not arrive competing with established instruments; it arrives with nothing. Each diagnostic REGISTERS exactly one objective, its scoring rule, and the incumbent it must beat - e.g. (objective: discrimination, scoring: held-out G, incumbent: the history predictor) or (objective: calibration, scoring: Brier, incumbent: isotonic recalibration) - which makes the law OBJECTIVE-INDEPENDENT and keeps it intact when calibration, efficiency or stability diagnostics arrive years from now. **RETIREMENT IS IRREVERSIBLE**: a retired diagnostic may NOT be reinstated by reinterpretation, parameter adjustment, or post hoc analysis; re-entry requires a NEWLY SPECIFIED diagnostic with a fresh preregistration. That blocks the commonest research failure - 'it failed, but perhaps with one coefficient tweaked...' - by making the failed object stay failed. **NO ZOMBIES**: retired diagnostics remain in the historical record but shall NOT appear as explanatory evidence in future arguments except as documented negative results. Paired with RST: RST separates STATE / BEHAVIOUR / HISTORY and is the THEORETICAL filter on what may represent; L63 separates REPRESENTATION / DIAGNOSTIC / THEORY and is the EMPIRICAL filter on what may explain - representation exists by construction, diagnostics exist by evidence, and theory exists only insofar as it is supported by SEATED diagnostics. | MEASURED and WITNESSED across three stdlib-only scripts, none a gate row: `nullbase.py`, `orbitprobe.py`, `prediction_residuals.py`, each exhaustive and rerun byte-identical; the live status of every instrument is registered in exe_epistemics/PREDICTIONS.md (INSTRUMENT REGISTRY). WHAT IT DOES NOT CLAIM: that a retired diagnostic is WORTHLESS - v_D remains STRUCTURALLY informative (it records what minted, a fact about the arc) and is retired only from PREDICTIVE/explanatory use, which is exactly the census-prediction separation; that these evaluations are significant (n = 7 batches, 22 joints, retrospective - descriptive only, L20); that SEATED is permanent, which is the precise error the recurring obligation exists to prevent; and it does NOT forbid computing an experimental diagnostic - only reasoning from one. |
| L64 | **AN EXAMPLE THAT CARRIES LIVE NUMBERS BECOMES A FORGERY UNDER MAINTENANCE.** Two disciplines this repo holds separately, each correct alone, COLLIDE on an illustrative transcript and the collision manufactures a measurement that never happened. The currency discipline says *sweep stale numbers so the docs match reality*. The record discipline says *never rewrite a measurement*. An example transcript is formally indistinguishable from both: it reads like a doc number (sweep it) and it IS a record of one run (do not touch it). Fourteen successive commits resolved that ambiguity the wrong way. `AGENTS.md`'s `<<<URDR` block was a REAL run on a named Windows host (2026-07-25) and said so in the prose beneath it. Its `FALS=` and `ROWS=` fields were then swept forward with the growing gate -- FALS=1915/ROWS=832 through FALS=1987/ROWS=848 -- while `HEAD=`, `BYTES=` and BOTH CRCs stayed frozen at the original run's values. Two passes at ONE head cannot produce different row counts AND identical output lengths AND identical CRCs, so by the end the block was a COMPOSITE: a genuine frame with fabricated contents, still labelled MEASURED, and stale anyway (848 against a live 854). **The exemption granularity was the defect.** `doc_currency._HISTORY` exempts append-only records FILE by file; `AGENTS.md` is correctly not exempt, because almost all of it is live instruction -- but the block INSIDE it was history, and a file-scoped rule cannot express that. No checker could have caught it either: the idioms match `N unit falsifiers` and `N/M rows`, never `ROWS=848`. Found by COMBING, not by any gate row -- the third time (L53, L55) a correction has lived in PROSE rather than in a module, and the first time the prose was pretending to be a measurement. | **An example may not carry live values.** Replace every varying field with a placeholder (`ROWS=<m>`, `BYTES=<b>/<b>`, `HEAD=<sha>`) so the block teaches the FORMAT, which is all it was ever for, and has no number left to sweep. This is deliberately a STRUCTURAL repair rather than a new checker, and the reason is that it is strictly stronger: a checker forbids the edit, whereas de-numeralization removes the thing the edit was reaching for -- there is no longer a target, so the class cannot recur here even unenforced. `example != observation`. **The general form:** any doc region that is simultaneously (a) inside a swept file and (b) a record of one specific past event is in this trap, and has exactly three exits -- de-numeralize it (best: needs nothing), move it into a `_HISTORY` file (good: file-scoped rules then apply), or mark the block itself as a record (weakest: relies on a reader honouring a comment). **What this does NOT fix, said plainly so it is not promoted (L20):** REINTRODUCTION. Nothing stops a future author pasting fresh live digits back into the block, and the frozen falsifier for the mechanized form is named in the enforcement column rather than claimed here. | DECLARED, not enforced -- and the distinction is load-bearing. The repair is structural: `AGENTS.md`'s `<<<URDR` block now holds placeholders and the prose records what happened to it, so no gate row is required for the block to stay honest. **NO ROW ENFORCES THIS**, and the arc has 854 rows precisely because unenforced claims are supposed to say so. The mechanization is frozen here and left UNRUN, so a successor tests the rule rather than inheriting it: a row asserting that no `.md` outside `doc_currency._HISTORY` contains a gate-transcript literal (`GATE=PASSED` followed by a digit-bearing `ROWS=`/`FALS=`/`BYTES=` field), planted red-first against this block's pre-2026-08-05 text, which MUST redden. Until that row exists the claim is DECLARED. does_not_show: that any other example in the tree is clean -- one instance was found by combing one file, and `sample != universal`. |
| L65 | **FIVE INSTRUMENT DEFECTS IN ONE SESSION, EVERY ONE INVISIBLE TO A GREEN GATE, AND NOT ONE OF THEM IN THE CODE BEING MEASURED.** The gate checks what the code does; nothing checked what the RULER does, and the ruler was wrong five different ways while every row stayed green and every selftest passed. **(1) LAYER** — `pixid.witness` was timed and reported as "the reference rasterizer"; decomposed, 90% of it is `serialize` building the byte string the frame digest is taken over, against ~5% in the draw loop. The number did not blur an answer, it NAMED THE WRONG SUBSYSTEM, and the cardinal invariant (replay stays byte-identical with observers ACTIVE) is precisely the claim that observers are separable. **(2) DOMAIN, of a predicate** — `named_host_ok` demanded §1's host string verbatim while the runner built its host line from `platform.node()`, so NO OUTPUT OF THE RUNNER COULD EVER SATISFY THE CHECK THAT GATED THE RUNNER'S OWN READINGS. Vacuous (L61), reddening nothing until an operator ran it on a real machine. Its selftest passed throughout, because a selftest proves a checker fails on SYNTHETIC input and says nothing about whether real input can reach either branch. **(3) AXIS FROZEN** — a two-axis cost (resolution x scene complexity) measured on one, with complexity pinned at a four-triangle fixture; "a 1080p frame" meant a 1080p frame of four triangles, which is not a frame. **(4) UNIT** — ns/PIXEL was the wrong denominator: across 64²–256² and 16–256 primitives it moves ~60x while ns/SAMPLE holds in a narrow band, because a rasterizer's work unit is the sample test and `samples != pixels` the moment complexity varies. **(5) AXIS CONFOUNDED** — "work is exactly linear in primitives" was an exact equality on a fixture that added a fresh patch of frame per primitive, so the law was linear in COVERAGE. **And the confounded law is PERFECTLY AFFINE** — a clean straight line through the data says nothing about whether the variable on the x-axis is the one doing the work. Four of the five were found only by re-measuring with one thing changed; the fifth by an operator running the instrument on hardware the author did not have. | **A MEASUREMENT ANSWERS FOUR QUESTIONS OR IT IS NOT ADMITTED: UNIT, AXIS, LAYER, DOMAIN.** What is the work unit (not the convenient denominator)? Which axis moves, and is there a fixture that moves it ALONE? Whose layer is being timed — CORE, VIEW or OBSERVER — and is the observer separable? Over what range was the law CHECKED AGAINST EXECUTION, outside which its answer is an extrapolation of a formula? Stated as a SCHEMA rather than a habit, because a habit is not inherited and a docstring is not enforced: a registered law declares all four and a caustic cannot be computed from one that does not. Three corollaries the arc paid for. **A unit cost derived by division is circular** — `frontbench` computes ns/division as tick-time DIVIDED BY the count, so multiplying it back returns the number it started from (L23); it carries information only transferred to a DIFFERENT count, and that transfer must be measured, not assumed (it drifts 2.2x below n~100). **Prove the work is identical, THEN compare the speed** — a ratio between two placements doing different amounts of work is a confound wearing a placement factor's clothes; the Rust raster placement is admitted on IDENTICAL EXACT SAMPLE COUNTS before its 153x is quoted. **Counts on-gate, wall-clock off** — a timing assertion inside a gate is nondeterministic and will be loosened until it cannot fail. | **MECHANIZED as a schema, and the schema can redden.** `caustic.answers_four_questions` requires unit, axis, layer in {CORE, VIEW, OBSERVER}, and a non-degenerate verified domain; `caustic()` REFUSES a law missing any, proved on four planted entries each missing exactly one (`tests/test_caustic.py::TheFourQuestions`), with `every_layer_is_populated` guarding L61 on the schema itself. Rows `caustic-refusal` / `caustic-laws`. The individual repairs are separately gated: `sealframe-layers` (the observer/renderer split and the unsatisfiable law's retirement, its unsatisfiability PINNED as a falsifier so the retirement stays honest), `sealframe-cost-surface` (both axes, gated as exact integer work), `sealframe-fill-floor` (the coverage confound and the floor that needs no scene), `sealframe-raster-placement` (identical work counts before any speed comparison). **WHAT IT DOES NOT CLAIM.** It does NOT mechanize defect (2)'s general form: nothing here checks that a checker's branches are REACHABLE FROM REAL INPUT, and a passing selftest still proves only synthetic failure — that detector is named and left unbuilt so a successor tests the rule rather than inheriting it. It does NOT claim the four questions are exhaustive; they are the four that were paid for. And it does NOT claim the surveyed repository is clean — the one `headroom x N` reading found outside this arc turned out to be circular AND regime-dependent, which is one instance combed, and `sample != universal`. |
| L66 | **A SEALED RECORD AND A LIVE TOTALITY CHECK WILL EVENTUALLY DEMAND THE SAME EDIT, AND ONLY ONE OF THEM MAY HAVE IT.** `lattice_snapshot.json` seals a 79-law severance sweep at a named commit — historical evidence, digest-bound over the edges AND the eligible/excluded partition, explicitly NOT a claim about the current graph. Its coverage check asks a different and equally correct question: does the partition still cover every law module in the LIVE tree? Adding one module minted after the seal, `contact`, reddened that check with no defect anywhere — the module is fine, the seal is intact, and the two requirements are simply incompatible on the same field. The available moves were all wrong in the way that matters: **re-run the sweep** and the commit stamp becomes a lie about which code was measured; **add the module to the sealed partition** and a record has been edited to fit the present, which is L64 exactly, one layer down and with a digest to make it look rigorous; **drop the coverage check** and the depth theorem silently stops being total over the tree it claims. The trap is structural rather than careless: any artifact that is simultaneously (a) a frozen record and (b) an input to a live totality check is in it, and the pressure only appears when new work lands — which is to say, always, and later, when the reasoning has been forgotten. | **SPLIT THE FIELD BEFORE THE PRESSURE ARRIVES: the record keeps its own scope, and a SECOND, LIVE register carries what the record cannot cover — under the same obligations, not weaker ones.** `post_seal_laws` sits OUTSIDE the edge digest by design, so the seal stays byte-identical forever; what bounds it is that every entry inherits precisely the obligations that made `excluded` safe (an unswept module may not host an import chain longer than the ceiling, so registering a module instead of sweeping it buys NOTHING), plus a mandatory reason, an existing module, and no overlap with the seal. That last clause is what separates this from a parking lot: the register cannot make a problem go away, only make an absence DECLARED — `#[expect]`, not `#[allow]`, the same shape `exempt` already uses one floor up. The general form: **when a frozen record is asked to grow, add a register beside it rather than a row inside it, and give the register the record's obligations rather than its own.** | **MECHANIZED, and the register is proved LOAD-BEARING rather than decorative.** `lattice.depth_proof` clause (g) requires a reason of real length, a module that exists in the tree, and no overlap with the sealed partition; clauses (c)/(d) now range over `excluded | post-seal` under one name, `unswept`, because the distinction between them is chronological and the obligation is not. Four plants in `lattice.plants_bite`, each proved to redden before the register was populated: a DEEP module parked instead of swept, an UNREASONED entry, an entry naming a module that no longer exists, and — the one that matters — EMPTYING the register, which must redden the coverage clause or the whole mechanism is decoration and coverage was already total without it. Row `lattice-depth`. **What it does NOT claim:** that a post-seal module has been swept (it has not, and the coverage string says so); that the register will stay small — nothing forces a successor to sweep rather than register, and the only pressure against a growing list is that it is enumerated, reasoned and visible in a diff. |
| L67 | **A DETECTOR NAMED AND LEFT UNBUILT IS INHERITED, NOT TESTED — AND THE SUCCESSOR REBUILT THE DEFECT INSIDE THE MODULE WHOSE JOB WAS HONESTY ABOUT IT.** L65 mechanized four of its five instrument defects and said of the fifth, in as many words, that nothing checks whether a checker's branches are REACHABLE FROM REAL INPUT, that a passing selftest proves only synthetic failure, and that the detector was being left unbuilt SO A SUCCESSOR WOULD TEST THE RULE RATHER THAN INHERIT IT. Six rungs later `rollbench` assembled a benchmark host string mechanically as `node | system release | note` and handed it to `sealframe.named_host_ok`, which requires the operator's verbatim declaration — a string containing NO `|` AT ALL. **No invocation, on any machine, with any note, could ever have passed.** Every falsifier was green: the log sealed, the digest bit, the provenance law refused an unnamed host correctly, and the one branch that mattered was unreachable. It reddened NOTHING until the operator ran the harness on a real machine and received a refusal that was the harness's fault, not the machine's. **The shape is exact and it is worth stating on its own: a gate whose only PRODUCER is a program, tested only against values a HUMAN typed, is untested.** A human can type the expected string; that is why the selftest passed. The machine could not; that is why the law was dead. | **A GATE MUST ADMIT SOMETHING ITS OWN PRODUCER CAN MAKE, AND THE WITNESS MUST BE PRODUCED RATHER THAN WRITTEN.** Register (producer, gate) pairs and obtain each witness by CALLING the producer, so a producer that cannot emit an admissible value has no witness to offer. Two verdicts, never one: UNREACHABLE (the producer's own output is refused — this defect) and VACUOUS (the counterexample is also accepted — L61's), because they fail in OPPOSITE directions and a register carrying one measures one failure mode while claiming two. **The repair on the other side is a separation, not a looser gate:** what is DECLARED by an operator and what is OBSERVED by a program are different fields, the law checks the declaration, the observation is recorded beside it as evidence, and an undeclared run falls back to the observation and therefore stays uncitable — so forgetting to attest cannot produce evidence by accident. Loosening `named_host_ok` to accept the machine's string would have made the law satisfiable and meaningless in the same edit. | **MECHANIZED as `reachable` (URDRRCH1), rows `reachable-census` / `reachable-plants`.** Eight pairs, all REACHABLE; both plants proved to bite AND to produce different verdicts; the hand-written-witness trap exhibited rather than argued, since the literal `NAMED_HOST` passes while the producer's output does not. **AND THE FIRST SWEEP CAUGHT THE REGISTER RATHER THAN THE CODE:** `contact.witness_digest` was registered as a gate and read VACUOUS — correctly, because it is a DIGEST and not a DOOR — so the detector was right and the registration was wrong, which is the same lesson one level in. **What it does NOT claim:** a survey. Eight pairs against 113 typed refusal codes in the tree, asserted checkably so the boundary cannot quietly stop being true; an unregistered gate is UNCHECKED, not proved reachable. `sample != universal`, and the entry that mattered was added because it had already gone wrong. |
| L68 | **A RETIREMENT WRITTEN IN PROSE DOES NOT TRAVEL, AND THE NEXT CALLER REBUILT THE DEFECT IT DESCRIBED.** `sealframe` found that its own `named_host_ok` demanded a verbatim host string its own runner could never emit, and the repair was COMPLETE: it wrote the paragraph explaining the defect, BUILT the replacement (`conditions_sufficient` — conditions are DATA, and each instrument class requires exactly the ones that CAN MOVE ITS READING, so a software timer needs machine/power/scheduler and not display), RETAINED the old law for the one scope that genuinely spans every condition, and SHIPPED A FALSIFIER pinning the old law's unsatisfiability so the retirement stayed honest. Nothing about that repair was missing except a way for it to reach anyone. Two rungs later `rollbench` imported `named_host_ok` and rebuilt the identical unsatisfiability on top of it, and `reachable` — the detector built for exactly this family — certified the pair REACHABLE, **correctly**, because a literal satisfies that door. Two instruments green, one door with an obituary six hundred lines above the call site. **A CALLER READS AN API, NOT A PARAGRAPH.** And the second half, found the same day: the v1.1 repair was itself UNREACHABLE FROM THE COMMAND LINE — `__main__` read `argv[i+1]` as a path, so `--bench --host "<decl>"` made `--host` the FILENAME and the declaration the NOTE, which was then appended to the checked field. **The register named the library call while the caller came through argv, and a register naming a producer nobody invokes measures a path nobody takes.** | **RETIREMENT IS DATA, SWEPT MECHANICALLY: `RETIRED[symbol] = (successor, reason)` in the owning module, and no other module may call the symbol.** Four verdicts, never fewer: CLEAN, STALE (a caller ignoring a retirement), UNNAMED (a successor that does not exist — 'do not use this' without 'use that' is an obstacle, not a repair) and VACUOUS (an empty register, L61). Read the AST, never the text: a name in a docstring is a MENTION and a call is a CALL, because the honest way to retire something is to explain it at length and a text sweep would punish exactly that documentation. Exempt the OWNER (the law may be retained for a narrower scope) and exempt falsifiers BY RULE (a test may call a retired law to pin why it was retired). **And the companion law: REACHABILITY IS ORTHOGONAL TO CURRENCY.** Whether a door opens and whether it is still the door the tree endorses are different questions, and a green reading on the first says nothing about the second. **What it does NOT claim:** that a law OUGHT to be retired. Retirement is the owner's declaration, so this sweep would certify a whole tree CLEAN the day before anyone writes the first register that catches something. `declared != discovered`. |
| L69 | **A FALSIFIER ANCHORED TO A MOVING REFERENCE PASSES ONLY FROM WHERE IT WAS WRITTEN.** `retire`'s strongest row was not a plant: it ran the sweep against the ACTUAL SHIPPED SOURCE that carried the defect, fetched with `git show HEAD:tools/terrain/rollbench.py`. It was green on the author's machine and green twice byte-identically, because THERE the rung was not yet committed and `HEAD` still held the defective file. The instant the rung landed, `HEAD` became the REPAIRED file, the sweep read CLEAN, and the assertion inverted — two rows red on the operator's machine, and **incapable of going red on the author's**, which is why two byte-identical gate runs certified nothing about it. `HEAD` is not a fact about the defect; it is a fact about the checkout. The same shape covers `now()`, `latest`, `origin/main`, `$USER`, a tempdir name, and any 'current' anything: **a test whose reference moves is measuring the environment it runs in, and the author's environment is the one place it is guaranteed to agree.** Note the tell — the defect was invisible to determinism checking, because determinism compares two runs FROM THE SAME PLACE. | **PIN THE REFERENCE BY IDENTITY AND SEAL IT BY CONTENT.** A historical artifact is named by a FIXED revision and carried with the SHA-256 of its bytes, line endings normalised, so it cannot drift and a SUBSTITUTED artifact REFUSES rather than passing — an artifact that has moved is not evidence about history. And **absence is a third state**: UNAVAILABLE (the object is not in this checkout — a shallow clone) must stay distinct from MISSED (the detector failed), reported in the row rather than collapsed into a quiet pass, or the strongest evidence in a module silently becomes the weakest. **What it does NOT claim:** that pinning makes the artifact right — only that it makes it FIXED. A wrong blob, pinned, stays wrong forever and reads green; the seal defends against drift, not against a bad choice. |
| L70 | **A TREATMENT AXIS ITERATED OUTERMOST IS A PROXY FOR ELAPSED TIME, AND THE FIRST REAL HOST LOG REFUTED THE HARNESS THAT PRODUCED IT.** `rollbench` timed three rollback representations and reported `narrowed` faster than `moulded` in 23 of 28 cells. But `narrowed` executes `moulded`'s timed path PLUS a widths tuple that replay never reads — a strict SUPERSET of the instructions — and a representation doing more work cannot be faster. The log was measuring something else: `cells()` iterated representation-outermost, so `flat` held run positions 0-27, `moulded` 28-55 and `narrowed` 56-83, perfectly aligned with clock ramp and thermal drift across 16 800 timed operations on a handheld under a turbo profile. **'Narrowed is fastest' and 'narrowed ran last' were the same measurement, and no number of extra iterations separates them.** The internal contradiction is what makes this MEASURED rather than suspected — the log refuted itself with its own arithmetic. And the same log carried a second defect of the same family: `depth` is what the harness ASKS for, the replay walk SATURATES against the world's length, so `all_airborne` replayed two ticks at depths 4, 8, 16, 32 and 64 — five rows, one experiment. 28 cells, 17 distinct, **11 duplicates**, so every count over that table carried a denominator that was 39% copies (L44, with the numerator disguised as the axis). | **GRADE THE SCHEDULE, NOT ONLY THE NUMBERS, AND CARRY THE WORK BESIDE THE REQUEST.** Visit cells on a stride co-prime to their count — a permutation by construction, so no cell is dropped or repeated — and require every level of every axis to sit within a declared tolerance of the run's midpoint. Three verdicts, not a scale: CONFOUNDED (disjoint contiguous blocks — the factor IS run position), SKEWED (interleaved but systematically early or late) and BALANCED. **Randomisation is refused**: it would balance just as well, and a seed is one more thing a result can depend on when determinism is the floor. The stride and the tolerance are DECLARED and the stride is RE-DERIVED BY SEARCH rather than trusted; the tolerance must be non-vacuous in BOTH directions — above the structural floor, below the shipped defect. Every row carries `depth`, `ticks` AND run position, so saturation and ordering are visible in the log rather than deducible from a docstring. **What it does NOT claim:** BALANCE IS NOT INDEPENDENCE. Spreading a factor across the run removes its CORRELATION with position; it does not remove position as a source of variance, and it cannot see a drift periodic at the stride. |
| L71 | **A POSITIONAL ARGUMENT READER CANNOT REFUSE, BECAUSE EVERY TOKEN IS A VALID PATH — AND THIS TREE HAD TWO FILES ON DISK PROVING IT.** After `rollbench` was repaired for reading `argv[i+1]` as an output path, listing the operator's repository root turned up `--host` (4.2 KB, a rollbench log filed under the flag) AND `--compare` (219 KB, a GATE log written by `scripts/gate_once.py`, months earlier, never noticed). Two runners, different authors, the same artifact. **Both programs reported success**, because the write went somewhere and nothing refused. `log = argv[1]` accepts `--compare` exactly as readily as a filename; there is no error state to reach. The defect was not carelessness in one line — it is what a positional reader IS. | **AN ENTRY POINT THAT TAKES A PATH MUST REFUSE A FLAG-SHAPED TOKEN IN THAT POSITION**, probed by feeding it one. Deliberately narrow: not 'use argparse', not 'enumerate every flag' — the ONE property both artifacts violated, stated so a probe settles it. Feed a REAL path FIRST, or a parser that raises on everything scores as the best door in the tree. And where the debt is wide — thirteen modules slicing argv across 40 sites — **name it as a RATCHET pinned AT the live reading** rather than repairing thirteen operator interfaces in one untested commit: it may fall, never rise, so the next positional reader reddens at once. A ceiling above the count is decoration. **What it does NOT claim:** a refusal probe is not a parser review — one property at one position, and a module configured from the environment is invisible to the census. |
| L72 | **VARIANCE HAS LEVELS, AND ITERATIONS SAMPLE THE WRONG ONE.** `rollbench` timed each cell 200 times and reported p50/p95/p99 — 200 samples of ITERATION-level variation and EXACTLY ONE sample of everything an interpreter fixes at startup: the hash seed, the address-space layout, where the allocator began, which core the scheduler chose. Kalibera and Jones state the consequence without qualification and it is not a matter of degree: **more iterations inside one execution CANNOT reduce execution-level variance** — the quantity is not being sampled at all, so `n` grows and the interval that matters does not move. `pyperf` spawns separate worker PROCESSES for exactly this reason. Every admissible log this repository has produced, including the two on the named host, is in this state: the numbers are real, the schedule has been balanced since L70, and whether any gap survives a second process **has never been asked**. | **A DIFFERENCE SMALLER THAN THE BETWEEN-EXECUTION SPREAD IS NOT A DIFFERENCE, AND WITH ONE EXECUTION THAT SPREAD DOES NOT EXIST.** Three verdicts, and the third carries the weight: SEPARATED, INDISTINGUISHABLE, and UNDETERMINED — which is NOT a polite INDISTINGUISHABLE, because answering the latter on one execution claims to have looked. Make a run a PROCESS, not a loop. Check that the verdict moves with the SPREAD and not only with the gap, or it is a threshold on the difference wearing a statistical name. Report within-execution and between-execution spread APART and never pooled: iterations shrink the first and cannot touch the second, and one number lets the cheap one hide the expensive one. Integer arithmetic, tie-break declared. **What it does NOT claim:** the spread of three executions is not a confidence interval — no distribution is computed or assumed — and a factor CONSTANT across every execution is invisible here exactly as run position was before L70. |
| L73 | **A TIMING DIFFERENCE WITH NO COUNTED DIFFERENCE IS UNEXPLAINED, AND UNEXPLAINED IS A VERDICT.** Under a balanced schedule the named host showed `moulded` beating `flat` at exactly ONE work level — ticks = 8 — in three of four workloads (-5.2%, -2.9%, -5.6%), with `flat` winning again at 11 and 12 ticks. Three independent workloads agreeing at one point is too orderly to file as noise and too narrow to explain by hand. And `measure` had ALREADY PROVED IN EXACT COUNTS that the three representations share a slope and differ only in the intercept — so a band that appears at one tick count and vanishes either side is a difference **the op model says cannot exist**. Either something below that model differs there, or the band is not real. What is not available is a story. | **MEASURE ONE LEVEL DEEPER, OR RECORD THAT YOU DID NOT.** Carry counted quantities beside the timing and grade the pair: EXPLAINED (a count moves where the time moves), UNEXPLAINED (none does), NOT_ASKED (no counts were carried — what every log here looked like, and a DIFFERENT finding from having looked and found nothing). Cover the symmetric case too, or the instrument only sees unexplained SLOWNESS. Put version-dependent counts in the LOG and NEVER in a golden — the gate runs on more than one interpreter, and a digested count reddens an operator's machine for a reason that has nothing to do with the tree. **And prove the probe BITES:** the first draft discarded the callable's return value before its second snapshot, so every transient allocation was already freed and it read ZERO for a function allocating five hundred objects — `getallocatedblocks` is a LEVEL, not a counter. A dead instrument reports 'no difference' exactly as convincingly as a working one reports the truth. **What it does NOT claim:** a counted difference is not a CAUSE — it is a correlation in one run, and causation needs an intervention this does not perform. |
| L74 | **A SEALED RECORD MUST OUTLIVE THE FORMAT THAT SUPERSEDED IT, AND A RECORD KEPT WHERE ITS PRODUCER WRITES IS ONE COMMAND FROM BEING OVERWRITTEN.** The arc finally produced an admissible measurement worth committing — and committing it exposed two ways it would have rotted. First: `parse_log` required every field in the CURRENT `ROW_FIELDS`, so the next rung to add one would have silently converted every archived log into an unparseable file. That is L64 at the artifact rather than the example — the record becoming a forgery under maintenance, with nobody editing the record. Second: `--bench` writes to `spec/attest/rollbench.txt` AND OVERWRITES IT ON EVERY RUN, so a record kept at its producer's own output path is one command away from being replaced by a DIFFERENT measurement wearing the same name — and nothing would have refused, because the new file is a perfectly valid log. | **VERSION THE FORMAT AND GIVE THE RECORD AN IMMUTABLE NAME.** Reading is PLURAL and writing is SINGULAR: a table of field lists per version, appended and never edited, with an unknown version REFUSED rather than guessed at — a row read against the wrong field list is a table of numbers under the wrong names. The sealed artifact carries host, executions and iterations in its filename, and the separation from the scratch path is CHECKED rather than remembered. **AND SHIP THE VERSIONING LAW WITH A REAL SUCCESSOR FORMAT IN THE SAME COMMIT** — a law never met by one is inherited rather than tested (L67), so the archived v1 record and the v2 writer land together and the gate proves the old bytes still read. |
| L75 | **A GRADUATED CLAIM'S NUMBERS MUST BE DERIVED FROM THE ARTIFACT, NOT TYPED BESIDE IT.** The measurement was real, the log was sealed, and the obvious next move — write the figures into a ledger sentence — is the one that fails silently. A typed number is a COPY, and a copy drifts from its origin the first time anyone edits either; nothing reddens, because a sentence cannot fail. **AND THE RESULT ITSELF IS WHY THE DISCIPLINE WAS WORTH IT:** `measure` predicted from exact op counts, before any host ran anything, that moulding moves the INTERCEPT and CANNOT MOVE THE SLOPE. Five independent executions on the named host measured the penalty as a CONSTANT, flat across a depth axis over which the replay work grows by nearly an order of magnitude. A prediction registered in integers, confirmed in nanoseconds, on a different machine, eight rungs later. | **RECOMPUTE EVERY FIGURE FROM THE SEALED BYTES AT CLAIM TIME**, and assert that no derived value appears as a LITERAL in the module — so an edited record moves a scene digest rather than leaving a sentence quietly false. Grade in BOTH directions and never upgrade the weak half: the experiments that SEPARATE under the between-execution floor are counted, the ones that read INDISTINGUISHABLE stay that way, and DIRECTION is reported apart from SEPARATION because they are different facts and neither stands alone. **What it does NOT claim:** one machine, one interpreter, one set of conditions — and those conditions are GAMEPLAY conditions rather than measurement conditions, which is defensible for a game engine and must not be a quiet choice. |

Roadblocks met upstream that this design routes around **by construction** rather than by
assuming they have vanished:

1. **Float drift across hosts/accelerators** — v0.x core is integer-only with defined wrap;
   floats, if they ever arrive, arrive as a differential-checked accelerator path, never as
   the source of truth (directive §5c; the trilemma is surrendered honestly: leak-explicit,
   not leak-free).
2. **Hash/dict iteration order** — canonical serialization sorts keys; digests never touch
   Python iteration order; `PYTHONHASHSEED=0` is set anyway (belt and braces).
3. **Actor message-order nondeterminism** — not inherited; deferred to R2 with the canonical
   logical order already proven upstream. Until then, no concurrency syntax exists at all.
4. **Windows console encoding** — UTF-8 forced programmatically; ASCII digraphs make every
   program typeable and diffable even where glyphs cannot render (`typeable ≠ renderable`).
5. **A green gate hiding a vacuous suite** — the gate red-tests itself (L5).
