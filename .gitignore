__pycache__/
*.pyc
*.exe
*.pdb
.pytest_cache/
*.egg-info/
.vscode/
.idea/
Thumbs.db
.DS_Store
tools/intla/urdr_math_rs/*.exe
tools/intla/urdr_math_rs/*.pdb
tools/editor/urdr_replay.json
tools/editor/runA.json
tools/editor/runB.json
tools/editor/wB.json

# Root working artifacts: patches, commit messages and gate logs are session scratch,
# not repository content. They had accumulated to ~200 files, so the pre-commit hook
# warned about untracked files on every single commit — a warning that always fires is
# a warning nobody reads, which is the same rot the exemption register exists to stop.
# `_gate?.log` and `_g*.log` were already here; the rest is what the pile actually was.
*.patch
*.msg
_g*.log
_g*.out
gate*.log
gate*.txt

# Replay scratch at the repo root: the demo writes a trace and a log per run, and a
# measurement series is a dozen of them. They are NOT repository content — a trace or a
# log becomes content only when a rung PINS it under spec/attest/ and a gate stage
# re-reads it, which is why these patterns are ROOT-ANCHORED and leave that path free.
/walk_*.txt
/ab_*.txt
/r60_*.txt

# Generator credentials and scratch (imagegen): the key file is read by the executor and by nothing in
# this repository; the executor and its history are per-machine scratch, never repository content.
.env
.imagegen/
.imagegen-history.jsonl

# Launcher photographs and the environment tools' derived intermediates: regenerated from the certified
# frame by `play.py --snapshot` / `envgen.py`, so never repository content. Generated treatments and their
# provenance/fit records are NOT ignored — keeping one is a choice, made per file.
launcher/assets/vista_*.png
launcher/assets/*_ref1536.png
launcher/assets/*_mask1536.png
launcher/assets/*_mapped1080.png
