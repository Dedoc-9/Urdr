# SURPRISES — the research-redirection ledger

Not `LESSONS.md` (transferable rules) and not a bug list. This is an append-only record of the times **reading
changed the planned work** — where the source, a measurement, or a gate contradicted the current mental model and
redirected what got built next. Kept separately because the eventual question it can answer is genuinely
predictive: *which kinds of assumption fail most often, and where do the failures cluster?* That is a candidate
for a non-vacuous operator (it predicts where future discoveries are likely), which an ordinary lesson is not.

Rules, mirrored from `LESSONS.md`: append-only; entries are dated by rung/anchor, not clock (determinism);
exempt from the prose staleness checkers (recorded history is not drift). Each entry states the PLAN, what
reading FOUND, and the REDIRECTION it forced — the smallest honest triple.

Earlier-arc redirections predate this ledger (e.g. worldstep replay, the session law, locality vacuity) and can
be backfilled from the D-series history; the entries below are the ones this ledger can attest firsthand from
`LESSONS.md` L37–L55 and the commit record.

| # | Planned | Reading found | Redirection | Anchor |
| - | ------- | ------------- | ----------- | ------ |
| S1 | Read module dependencies off the AST import graph — a declared edge is a real edge. | Severing edges (replace the target with a raising sentinel, recompute the laws) showed `storecost.serialize` is a TRANSITIVE carrier: 7 reachable dependencies against 6 direct imports. It carries a law while being invisible in the consumer's import list. | Attribute by SEVERANCE, not imports; a declared-vs-severed wall. Absence of attribution is not dead code — it may be a detail below the certification line. | L48 |
| S2 | Treat every fixture in the separation basis as load-bearing. | One basis member was REDUNDANT — the rank did not move when it was removed (basis 60 → 59). | A rank wall + a minimality invariant: the basis must be exactly as large as its rank, no larger. | L43-arc |
| S3 | Hand-maintain the `brief-falsifiers` evidence string's arc description. | The description went stale THREE times as `BRIEFS_REQUIRING_A_FALSIFIER` grew (5→7→11) — a derived count sitting beside un-derived prose. | DERIVE the whole evidence (count and enumeration) from the live markers/rows/bindings, so it cannot go stale; plant the five mismatch directions. | L52 |
| S4 | Bump `hainuwele/README.md`'s absence count 77 → 76 as a mechanical edit while writing the `storecost` brief. | The qualitative prose AROUND the number was false: it claimed the ~20 arc modules carry "NOT ONE" brief, but eleven had been briefed this session — `blindscreen`, the module the sentence named, among them. | Fix the stale narrative, not just the number. First datapoint that the corrections live in PROSE, not the modules. | L53 |
| S5 | Write the `rollstore` brief and gate it clean. | The gate REDDENED on my own prose: `doc-staleness` read a "not begun" marker in the `does_not_show` beside a backtick-named existing module (`bench`) as a false unbuilt-status claim. | Unbuilt-marker vocabulary is free in a `.py` docstring (unscanned) but a TRIGGER in a `.md` brief; express deferred work as "deferred". Second datapoint: correction in prose, not the module. | L55 |
| S6 | Read the next brief batch as B9 = `drive`, `stance`, `predict`, ordered by dependency-closure (which unbriefed module the existing briefs cite most). | The contradiction-density measure (imports + refusal paths + funcs + gate rows) de-prioritized the movement roots — `drive` is foundational but SIMPLE — and surfaced the attestation pair and the commutation/replay cluster at the top. | READ 2 batch 1 became the commutation cluster (`voxlat`, `nway`, `commute`) — high density AND it closes `disjoint`'s references AND coherent. The measurement moved the plan. | READ 2 b1 |
| S7 | Bump the gate's row count in the one doc that quotes it (`AGENTS.md`) after adding the `provenance` row. | `doc-currency` reddened on a SECOND file — `tools/README.md` also quoted "848 rows", twice — plus a third phrasing ("8 of the gate's 848 rows") the row-count regex does not even catch. My model of "the doc that quotes the count" was singular; the count lived in three places across two files. | The gate enforces the row count TREE-WIDE, not per-doc; update every quoting file, and note the non-slash phrasing the checker misses (a small coverage gap). Red-first caught the omission before it shipped — a CORRECTION caught by an existing ELIMINATION mechanism (`doc-currency`). | provenance rung |
| S8 | Sever a law's defining module with `setattr` (patch `M.f` on the module object) to catch every use — that is what "reachable" means. | A `from M import f` copied binding escapes it: `setattr` rewrites the module attribute but the consumer holds its own reference to the ORIGINAL object, so the severance misses it. The `setattr` algorithm is provably INCOMPLETE in general. But identity-wide severance (patch every repo binding by object identity) produced the SAME 221-edge set — URDR is uniformly late-binding (`import M; M.f()`), so the two agree here. | Keep the cheaper `setattr`-closure-pruned algorithm, but carry the identity-wide alias as a LIVE control (conformance plant 7) that must AGREE each run. The defect is real in principle, moot in practice, and the control is what keeps "moot" honest rather than assumed. | the pin |
| S9 | Predict full architectural depth in the 7–10 band, extrapolating the early module stack. | Datapoint 2 measured the enumerated lattice's longest REQUIRES chain at 13 — above the guessed band. The numeric guess was refuted on first real measurement. | Retire the number, keep the STRUCTURE: depth stabilizes while degree keeps climbing. Revised to "full architectural depth stabilizes near the observed 13-layer stack while degree continues rising" — a structural claim survives the numeric refutation because it never depended on the specific band. | datapoint 2 |
| S10 | Bound the lattice depth by running the full 92-law severance sweep and reading its longest chain. | The full sweep is infeasible as a gate step (~40 min; `voxlat` 16s and `edgeattr` 27.7s alone make the per-run cost impossible). But REQUIRES ⊆ transitive-imports, so max REQUIRES depth ≤ max import chain — and though the import diameter is 16, every import chain longer than 13 runs entirely through fast, already-swept modules (the union of slow/excluded modules on any >13 chain is EMPTY), while each excluded module's own import-depth is ≤ 11 < 13. | Prove `depth(REQUIRES) ≤ 13` CHEAPLY from the import graph as a live theorem re-derived each run (pin claim 1), instead of the infeasible sweep. The bound is structural, not sampled — an unswept module cannot host a deeper chain, so the theorem holds without the 439s measurement. | the pin |
| S11 | Test the READ-2 predictive hypothesis — that a module's structural CENTRALITY (articulation, REQUIRES fan-in) predicts whether reading it yields a correction — and, if it held, pre-register it and forward-test on `heightfield` to earn the PREDICTIVE class and a `TESTED_BY` edge. | The retrodiction over the 115-module terrain+netcode universe REFUTED it: corrections ANTI-correlate with centrality. P(correction \| articulation) = 0.154 (2 of 13 joints, and both soft — `compose` was a discovered seam law, `rollstore` was brief-prose, neither a module bug) against P(correction \| non-articulation) = 0.284; Fisher one-sided p for articulation being *enriched* in corrections = 0.92. A post-hoc scan found the lattice-EXCLUDED (slow, analytically complex) set enriched 12 of 13 (0.923 vs 0.186, p ≈ 0.000) — but that is p-hacking-adjacent (a second predictor tested only after eyeballing the roster) and CONFOUNDED by attention-selection: those are exactly the modules the L18–L29 adversarial arc scrutinized hardest, so the number may measure where the *effort* went, not where bugs *live*. | The ambitious path dies honestly: NO PREDICTIVE class, NO `TESTED_BY`, no schema invented ahead of a signal (L58, L20). The retrodiction bought the null cheaply — its whole purpose — and caught the confounded complexity lead one step from promotion to a false law. Proceed with the low-key, centrality-ORDERED brief: `heightfield` first (in-degree 28, the most-depended-on unbriefed module). The complexity predictor is a TRACKED CANDIDATE, earned only by its own pre-registered forward test with a clean metric and the confound addressed — never on this retrospective. | the retrodiction |
| S12 | Comb the tracked docs for stale COUNTS — numbers that drifted as the arc grew, the class `doc-currency` was built for. | A stale count, yes (`hainuwele/README.md` read 848 gate rows against a live 854) — but underneath it something worse: `AGENTS.md`'s `<<<URDR` example block, labelled in its own prose "a REAL run — MEASURED on the named Windows host, not a mock", had had its `FALS=`/`ROWS=` fields swept forward across FOURTEEN commits while `HEAD=`, `BYTES=` and both CRCs stayed frozen. The numbers were being maintained; the measurement they claimed to be had stopped existing. A currency refresh, doing exactly its job, had turned a record into a composite. | Stop treating the sweep as safe-by-default. De-numeralize the example so there is no target left rather than adding a checker to forbid the edit; complete the `_HISTORY` list in `AGENTS.md`'s refresh rule, which named two of six exemptions and would have had a literal reader sweep four append-only records; mint **L64** (`example != observation`) and freeze — unrun — the row that would mechanize it. The comb was looking for numbers that were WRONG and found one that was being kept RIGHT by falsifying what it was. | L64 |
| S13 | Bump `hainuwele/README.md`'s absence count if the comb showed it had drifted — the S4 edit, expected to be mechanical again. | The absence count was CORRECT (1, gate-derived and gate-enforced). The INDEX TABLE in the same file was not: 17 rows still showed `—` in the brief column for modules whose brief exists, so one document simultaneously asserted 1 unbriefed module (derived, right) and displayed 18 (hand-maintained, wrong). The gate enforces the COUNT and cannot see the ENUMERATION. | Restore the 17 links, and record this as a RECURRENCE of S3/L52 rather than a new law (L3: no promotion without preregistered recurrence) — the same shape, third instance: a derived number sitting beside an un-derived list of the same fact. Note the direction, which is new — S3 and S4 drifted toward OVERCLAIMING, this one UNDERSTATED completion by 17. A hand-maintained enumeration drifts whichever way the work moves, so the defect is the hand-maintenance, never the direction. | the comb |
| S14 | Evaluate eleven proposed extensions to the discovery engine (basis lattices, detector algebras, seam hypergraphs, fork ensembles, shadowing, ...) and rank them for a next phase. | Re-ran the EXISTING instruments first, on the principle that a proposal to extend an instrument is worth nothing until the instrument is sound — and `prediction_residuals.joint_level_beats_null` was naming the winning covariate by ALPHABETICAL ORDER (`min` over the key strings), so it returns `margin` for every corpus that could ever exist. It had been harmless for as long as nothing beat the null, because nobody reads the winner's NAME when there is no winner. The corpus had meanwhile grown 22 → 33 joints and `topmass` had become the first covariate ever to beat the seated null out-of-sample (2433 vs 2539) — so the run that made the label load-bearing was also the first run that could be wrong, and it crowned `margin`, which LOSES by 112. | Fix the argmin, add the red-first plant the line never had, and record the L63 status change WITHOUT promoting `topmass` (one retrospective win is not standing). Rule on the eleven by type rather than appetite: the basis lattice is admissible but must be frozen BEFORE the successor corpus or it is retrodiction; shadowing is cheap and already substrated; the detector 'algebra' does not type-check as stated; the fork ensemble is blocked on the same un-manufacturable independence as the unanchored Ψ floor. The law candidate (an output consulted only under a condition that has never held) has ONE carrier and is TRACKED, not minted — minting on a single instance is the error this rung was created by. | Rung 9 |
| S15 | Seal the v1.10 cap sweep expecting the container harness's recompute counts to equal the host demo's — "counts are trace-determined, identical on any machine" was the delivery-note claim. | The host demo's FIFO counts disagreed with the harness's at every eviction point (361,294 vs 348,417 at cap 65536) while every digest chain stayed identical: the demo prefills the ladder before walking, the harness filled on first rebase, and eviction counts are ACCESS-ORDER-determined even when values are not. | The claim was retracted in the delivery message and the discrepancy became the rung's strongest law: the harness was taught the demo's schedule, reproduced the host counts EXACTLY, and both schedules' counts are committed — the agreement and the difference each gate-checked (`capcost`'s one-schedule law). An instrument must speak the machine's schedule before it may claim the machine's costs. | capcost |
| S16 | Read the first three-cell resolution sweep and report 1080p's 60 Hz grade — run 1's worsts (14.8..15.9 ms) sat under the 16.67 ms slot, so the provisional reading was FITS by ceiling. | Run 2, same cells, same conditions, produced a 21.08 ms ceiling excursion in its final 1080p pass: the one-run FITS was a sampling artifact of a thermal tail the first run happened not to reach. The 120 Hz verdicts agreed between runs everywhere; only the 60 Hz ceiling claim was fragile. | The reader rung takes the CONSERVATIVE verdict of the pair (MARGINAL), records the disagreement rather than resolving it silently, refuses to speak at all where the pair disagrees at 120 Hz, and keeps the catch as a live plant (`a_one_run_fits_is_caught_by_the_pair`). A two-run protocol is not ceremony: it is the falsifier for one-run verdicts. | rescell |
| S17 | Read the host's v1.13 wanderer price pair, expecting the v1.13.1 prefill fix to leave a clean FITS at 120 Hz — the provisional reading from the contaminated first run was ~7.9 ms ceiling. | The fix WORKED (seg 0's 8.9 ms startup frame is gone, late 0) and the clean re-run REFUTED the provisional verdict anyway: lateness reappeared in the LAST segments, climbing 1 → 10 → 24, ceiling 12.59 ms. The wanderer is MARGINAL at 120 Hz, not FITS — a trailing-segment pattern the startup artifact had been masking. And the composition run (`--third --sky`) reported `focus_foreground false` while its lateness (p99 46 ms) far exceeded what its raster deltas (+0.9 ms median) could explain — a confounded condition, not a measured price. | Retract the provisional FITS publicly and grade the wanderer MARGINAL@120 / FITS@60 from the clean pair; refuse to price the composition at all until its conditions are known. And repair the instrument that let the condition hide: `focus_foreground` was sampled ONCE before the frame loop, so it could not distinguish a one-frame loss from a whole-run loss — now a per-frame COUNT (L85). The two-run discipline caught the verdict; the condition field caught the confound; neither would have fired on one clean-looking run. | L85 |
