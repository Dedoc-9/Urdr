<!-- SPDX-License-Identifier: AGPL-3.0-only -->

# `docs/` — briefs, papers, protocols, and transcripts (narrative, not normative)

## Index

Background and process material. Nothing here is a source of truth — the normative
documents are in [`../spec/`](../spec/) and the grades are in
[`../spec/D5-ledger.md`](../spec/D5-ledger.md). If a brief and a spec disagree, the spec
wins.

| Path | What it is |
|---|---|
| `THEOREMS.md` | **The theorem catalog** — short, scoped statements of what is *actually proved*, each tied to its evidence (gate stages, corpora, placements, hosts), with a design-targets section separating measured guarantees from planned capability. An index; D5 governs grades. |
| `PAPER.md` | **OSDI-style systems paper** for the deterministic execution pipeline — problem, layered design, implementation, and the cross-implementation reproducibility evaluation (29 Rust placements + 14 C99 runtimes reproducing the kernel/frame/physics/field/exact-math/fixed-point-dynamics/netcode-stack/regional-composition/detector digests behind a 925-test gate; current totals in `spec/D5-ledger.md`). Scoped to corpus agreement, not universal correctness. |
| `POSITIONING.md` | **Product positioning** (DECLARED, doc-only, grades nothing, not financial advice) — the consolidation of Urðr + Ursprung + the sealed engine into one stack; the two-audience moat (verifiable simulation for researchers / a deterministic world to build and play on); annotated pioneering uses with honest gating; and the OODA pick of the first presentation slice (the T3.2+T3.3 wave seam). The boundary is the product. |
| `presentation_doctrine.md` | **The presentation doctrine** (T3 and every renderer) — the boundary as first law (`semel`/`et iterum`/`idem`: authority measured, presentation declared, boundary fixed); the budget doctrine D20–D23 (PFAL / Polygon Reconciliation / TFAL / Reality Debt) as **declared weighting models walled from D5** (`model ≠ verified structure`, silicon-falsified on record); the T3 slice ladder; the renderer-as-information-firewall pivot. Declared doctrine, deliberately outside `spec/`. |
| `terrain_studio_brief.md` | **The terrain-studio insertion plan** (the zyfod goal under this repo's laws) plus its REALIZED-LADDER addendum: the plan's T1/T2/T3/T4 rungs, the heat diagnosis behind the idle law — and the status section recording how far the ladder actually went (T3.9–T3.41: movement, observers, anti-cheat, the Stage-H latency guarantee on both axes, durability/recovery, the Stage-I streaming + regional-cut openers, the mutable chunked world, and the commutation certificate, each with its gate stage named). The plan sections are history; the addendum is the current map. |
| `bench_protocol.md` | **The sealed latency benchmark protocol** — the named host, display segregation, and metric a performance number must be produced under before it may read `MEASURED`. §4a/§4b carry the first ROG-Ally-X readings (the sim-tick graduation). Until §3 runs, all fps / input→photon latency numbers are `NOT_MEASURED` by law. |
| `presentation_layer.md` | **The D15 presentation-layer design note** (DECLARED) — how UE5 / three.js / Godot pair as replaceable layer-3 consumers behind the one-way firewall: the no-feedback + byte-exact guardrails, coordinate rebasing, interpolation, view-stream jitter buffering, content-addressed assets, and deterministic replay triage. Everything `NOT_MEASURED`; the renderer is unbuilt. |
| `roadmap_engine.md` | **The engine roadmap** (design contract, nothing MEASURED): the hybrid execution model (fixed-point simulates, exact algebra arbitrates), the pixels-vs-state boundary, the three tracks, and the Track-1 mutation-proposal contract — with a dated STATUS note tracking which of its environment assumptions events have since overtaken. |
| `fraud_proof.md` | **The optimistic-verification rung** (DECLARED) — verify a run without re-executing it: publish the `URDRLST1`/`URDRLSTT` trace commitment, bisect a dispute to one tick, and settle it by re-executing t