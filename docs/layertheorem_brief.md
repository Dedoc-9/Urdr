# The Integer Scalar Potential Layer Theorem (URDRISPL1, T3.22): a design pass

<!-- brief-falsifier: ispl-conservation -->

`layertheorem` is the third articulation joint read under the centrality order (after `heightfield` and
`jurisdiction`), and it is the one where reading the source *corrected the model* — a clean illustration of
`claim ≠ code`. A plausible reading of "Integer Scalar Potential Layer Theorem" is a statement about a potential
FIELD: conservative circulation (`∮∇Φ·dr = 0`), monotone equipotential strata `Φ_{k+1} ≥ Φ_k`, type-guarded
values. The source shows something else. The module measures none of those; the "layers" are not field-value
strata but seven ARCHITECTURAL roles, and the theorem is about AUTHORITY FLOW across them.

## OODA

**Observe.** The terrain studio's seven layers are Authority (`heightfield`, the exact-integer scalar potential
Φ: ℤ² → ℤ), Consumer (`stance`), Presentation (`terrain_view3d.html`, DECLARED), Firewall (`view_witness`),
Observer (`gaze`), Transcript (`drive`), Horizon (`traj`). The scalar potential is real — it lives in the
*authority* layer, `heightfield`, whose gradient over a scalar potential is conservative by construction — but
`layertheorem` neither computes a circulation nor asserts equipotential monotonicity. It certifies the CROSS-layer
properties no single stage checks.

**Orient.** Three MEASURED facts, and together they are one-way authority transfer. SINGLE SOURCE: the firewall's
cited authority digest (`hf_witness`) equals the authority's own live digest — one Φ, cited by the presentation
and consumed by every observer. OUTWARD FLOW: a one-cell perturbation of Φ moves EVERY downstream layer's digest
(Consumer, Observer, Transcript, Horizon), so a layer that ignored the authority would be caught. MEMBRANE (no
feedback): the full downstream pass leaves the authority field bit-identical, and a forged presentation is refused
by the firewall, never reaching authority. One source, strictly outward, nothing flows back.

**Decide.** That is exactly the principle `ANCESTRY.md` names as the conceptual center of the Ursprung `tre/`
boundary work — authority is admitted at one source and flows one way, and no downstream witness authenticates
what produced it. `layertheorem` is the Urðr *certified instance* of it: `tre/` states one-way authority transfer
as a design contract; this measures it across seven live layers and binds them into a composite theorem digest
that reproduces deterministically and moves if any bound layer drifts. The module is a CONFIRMATION — clean and
honestly graded — and the read's value was correcting the potential-field mis-reading, not fixing a defect (L54).

**Act.** The law is enforced live by `ispl-conservation` (the composite URDRISPL1 digest reproduces its pin; an
unknown manifold is a typed `ISPL-REFUSE`), with `ispl-single-source` and `ispl-outward-flow` carrying the
one-way-authority core (single source; a one-cell perturbation moves all four downstream layers while the
authority stays bit-identical and a forged citation is refused). Terrain absence drops 65 → 64.

## What this does not show (`does_not_show`)

The module states its boundary and this brief inherits it. It certifies the URDR terrain INSTANTIATION, not the
general theorem over an arbitrary integer manifold. The Presentation layer's pixels are DECLARED — certified only
by citation through the firewall, never by a measured digest of their own — and nothing is claimed beyond the
seven bound layers. The "division-free" epilogue is narrowed honestly: the manifold is float-free, exact-integer,
and deterministic throughout, every downstream layer is `//`-free (shift-only, tokenizer-asserted), but the
authority's own normalization is one exact-integer `raw*height_scale // rawmax` (rawmax not a power of two), so
"entirely division-free" holds downstream, not at the authority. Type strictness (`int` only, bool excluded) is
inherited from `heightfield`, the authority — it is not a guard `layertheorem` re-imposes at its own boundary.
Grade: MEASURED on `ispl-conservation` for the single source, outward-flow sensitivity, membrane, and composite
determinism.
