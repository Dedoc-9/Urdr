<!-- SPDX-License-Identifier: AGPL-3.0-only -->
<!-- brief-falsifier: panewire-concord -->
# `panewire` — design brief (URDRPNW1, T3.53, V2)

**Read**: 2026-08-04, the centrality-ordered READ pass — P26 of batch 6
(`../exe_epistemics/PREDICTIONS.md`), the batch's genuine discrimination. Outcome: **C-AB** — the
whole arc composed under play; the promoted basis won, and this loss retired the challenger. This
resolution's third scoring zero closed run 7. Reading grade: **CONFIRMATION**.

## What it is

**The wired window** — the whole arc composed in one live loop. V1 `panelight` drove the certified
world as a game; this rung makes that world **replicated and streamed**: movement (panelight's
tick) + replication (wire's equal-or-refuse admission) + streaming (driftgaze's verified
acquisition) meet to drive one avatar over a world it does not hold whole and did not author alone.
The first playable networked world: two windows, one authority, an edit made in one view appearing
in the other — every byte admitted rather than trusted.

## The core law (what `panewire-concord` certifies)

**Two windows, one authority**: the same (input, edits) run twice lands the *identical* composed
witness — the edit made in one view is seen in the other — while a different edit stream diverges;
and a tampered edit woven into the stream refuses mid-loop with the replica byte-unchanged and the
avatar's walk unperturbed. Equal-or-refuse **under play**. `panewire-resident` holds the resident-set
correctness across the live loop; `panewire-live` holds the interactive tick. The module mints
nothing — it is the composition of panelight, wire, and driftgaze, proved to hold *together* under
adversarial edits during interactive play.

## The seam (P26's finding)

The interactive-loop identity (`panelight`'s interactive == batch) **fused** with the wire's
equal-or-refuse admission — a two-law C-AB join. The promoted basis priced this police-×-and-AB
reading over the challenger's representation-first reading and won; that loss was the challenger's
third consecutive on a genuine discrimination, retiring it from the tournament (recorded in the
ledger). The V-phase's thesis is discharged: the playable networked world earns trust by *composing*
already-certified laws under play, not by a new mechanism.

## does_not_show

The render (pixels behind the firewall, declared); wall-clock / bandwidth / frame latency
(`bench.py`); multi-authority beyond two windows (the mesh phase's territory); who computes demand
or interest (operational). A concordant window is not a *correct* world — it faithfully replicates
what the shared authority signed. `integrity ≠ truth`.

## Falsifier

This brief cites `panewire-concord`: two-windows-one-authority + malice-under-play. If the two
windows ever diverged on the same (input, edits), or a tampered edit admitted mid-loop, that row
reddens and this brief's central claim dies with it.
