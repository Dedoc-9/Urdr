<!-- SPDX-License-Identifier: AGPL-3.0-only -->

# D17 — The invariant-detector admission law (what survives?)

Status: **META-CONTRACT.** D17 does not define a subsystem. It names the *admission law* that
every gated detector in this project has already been following, and makes that law explicit and
falsifiable. Its correctness test is **retro-admission**: the existing detectors (D14, D15, D16,
rigidity, criticality) must each satisfy it — *if even one cannot be expressed under D17, D17 is
wrong.* That test was run against the committed gate (`e390f42`) and all five pass; the mapping is
§6. This is an unusually strong specification because it is falsifiable by code that already exists.

The organizing question is not a prover's ("is proposition P true?") nor a CAS's ("compute f(x)?")
but:

> **Which exact statements about an object remain true under a declared class of transformations?**

An invariant is a value that is *constant on the orbits of that class*. A detector computes one, on
a domain where it can, and **refuses** everywhere else.

## 1. The detector

A detector is a tuple

    𝒟 = (Dom, Inv, W, ~, R)

- **Dom** — a **decidable** domain. Membership is checkable.
- **Inv : Dom → V** — an **exact** invariant map (no rounding, or bounded-regime honesty).
- **W** — an **independently checkable witness** that recomputes / certifies the value.
- **~** — a **declared equivalence relation** on Dom, generated by a declared class of *admissible
  transformations* (morphisms). It need NOT be a group action: a group (invertible) is the special
  case; monoids (non-invertible evolution, presentation edits) and bare equivalences (provenance,
  partition choices) are the general case. `Inv` is invariant iff it **factors through the quotient
  `Dom/~`** — i.e. `Inv(x) = Inv(y)` whenever `x ~ y`.
- **R** — a **total refusal relation** outside Dom: every object not in Dom gets a *typed* refusal,
  never a wrong answer.

## 2. The admission law

A detector is **admitted** iff:

1. **Membership is decidable** — `x ∈ Dom?` is computable.
2. **REFUSE is total outside the domain** — `x ∉ Dom ⇒ R` fires, typed.
3. **The invariant is exactly computable** — `Inv` is deterministic and exact.
4. **The witness recomputes the invariant** — (4a) self-verifies the value; and, for the *cross-placed*
   grade, (4b) an independent placement reproduces it (the D8 move).
5. **The invariant is constant on a declared corpus of `~`-transformations** — `Inv(T·x) = Inv(x)`.
6. **A deliberately non-invariant quantity reddens the gate** — a `Q` with `~ ⊄ ker(Q)` must fail,
   proving the invariance is load-bearing and not vacuous.

Nothing more, nothing less — and nowhere does it mention topology, physics, graphics, or
networking. That silence is why it generalizes.

**Every theorem ships with its counterexample.** Conditions 2 and 6 are first-class, not
afterthoughts: admission is measured as a **positive corpus + a negative corpus + typed refusals**,
which is precisely what a well-formed gate stage in this repo already records —
`golden` · `invariance property` · `-selftest` (non-invariance) · `-refusal`.

## 3. Two relations, and completeness as a graded claim

A detector carries two relations: the **declared** `~` (what it must identify) and the **induced
kernel** `ker(Inv) = {(x,y) : Inv(x) = Inv(y)}` (what it actually identifies). Admission requires
`~ ⊆ ker(Inv)`.

The detector is **complete** iff `ker(Inv) = ~` — the invariant separates *exactly* the orbits, so
`Inv(x) = Inv(y) ⟺ x ~ y`. Completeness is a **separate** claim from admission and is frequently
undecidable (deciding orbit-equality can be harder than computing the invariant). It is therefore
**graded** like everything else — `PARTIAL`, `MEASURED-complete` (on a corpus), or `PROVEN-complete`
— and never a free label. Complete detectors are terminal for their `~`: every weaker detector on
the same domain **factors through** them.

## 4. The lattice of detectors

Order detectors by their induced kernel: a finer kernel distinguishes more. If `~₁ ⊆ ~₂` then
`Inv₂` is *coarser* (`Inv₂(x)=Inv₂(y)` is a weaker statement — more is identified). This is a genuine
partial order: some detectors distinguish more, some deliberately forget more, and complete
invariants sit at the bottom of their domain's order. Organizing the library *is* building this
lattice.

## 5. The invariant profile (the API)

An object does not have *an* invariant; it has a **profile** — the table

    { detector → value | REFUSE }

Each detector answers where it is competent and refuses where it is not. Example (illustrative):

    URDROBJ2 object
      canonical (D14):   digest 3c36067…
      rigidity:          RIGID (dof 0)
      topology (H₁):     REFUSE  (not a chain complex)
      criticality:       REFUSE  (not a population field)
      symmetry:          REFUSE  (no detector admitted yet)

Nothing speculative, nothing inferred — exact detectors answering only where competent. This is the
honest form of "map the invariant landscape of an object": **not** universal introspection (which is
undecidable — no algorithm returns the symmetry group or conserved quantities of an *arbitrary*
object), but a **finite library of detectors with decidable domains and total REFUSE**, whose union
of non-refusing answers is the profile. REFUSE does the same work here that `FIELD-REFUSE` and
`WORLD-REFUSE` do everywhere else: it lets the engine say *"I can't tell, from here,"* which is what
keeps *"what survives?"* honest.

## 6. Retro-admission (the proof, run against `e390f42`)

Every existing detector satisfies §2. The transformation class is a **group only for rigidity**;
the rest are monoids or equivalences — which is why §1 declares `~` as an equivalence relation, not
a group.

| Detector | Dom (+ REFUSE) | Inv | W | `~` (kind) | positive · invariance · non-invariance · refusal | grade |
|---|---|---|---|---|---|---|
| **D14** frontend | canonical designs (non-integer → `CONTRACT-REFUSE`) | `URDROBJ2` id | canon bytes, reproduced by 3 impls | provenance / representation (**equivalence**) | `:reference`,`:tracer` · `-provenance` · `-selftest` (provenance-fold) · non-integer refuse | cross-placed |
| **D15** view | authority state + presentation (bad body-count → `VIEW-REFUSE`) | carried witness | binding check + independent viewer | presentation edits (**monoid**) | `:canonical` · `-observational` · `-observational` fold-defect · `-refusal` | cross-placed |
| **D16** region | valid integer-seam partitions (malformed → `REGION-REFUSE`) | `URDRLSTT` | reunified chain, 3 placements | partition / recomposition (**equivalence**) | `:seam2` · `-invariance` (6 partitions) · `-boundary`,`-nonvacuity` · `-refusal` | frozen |
| **rigidity** | canonical frameworks (overflow → `REFUSE`) | RIGID/FLEXIBLE + dof | self-stress / infinitesimal-flex certificate | Euclidean motions (**group** — the special case) | `:shapes` · `-flex` · `-selftest` (full-rank misclassifies) · `-refusal` (overflow REFUSE) | measured |
| **criticality** | population fields, bounded regime (overflow → `FIELD-REFUSE`) | conserved total (+ eigenvalue) | trace digest / running total | transport evolution (**monoid**, non-invertible) | `:galton`,`:doppler` · `-conserve` · `-eigenvalue`,`-selftest` · `FIELD-REFUSE` | **reference** (4b DECLARED) |

The last row is the honest catch: criticality meets conditions 1–6 with self-verification (4a) but
has no independent placement yet (4b), so it is *reference-admitted*, not *cross-placed* — D17's
grade is exactly D5's MEASURED-vs-FROZEN ladder.

## 7. Self-application

**"Evidence Against C8" (D5) is D17 applied to the engine's own primitive set.** The object is the
sealed alphabet; the admissible transformation is *"admit a new domain"*; the invariant under test
is *"the primitive set is unchanged"*; the ledger is the corpus of orbits checked. The engine
already runs its central question on itself — and every clean composition is one more orbit on which
the alphabet proved invariant.

## 8. Admission ladder

1. **State the law** (this document) and **retro-admit** the existing detectors (§6 — done, and the
   validation *is* the proof the law is right).
2. **A structural lint** (`invariant_detectors` gate stage) — **LANDED.** Each admitted detector
   *declares* (in an explicit manifest, never inferred from row-name conventions) which recorded rows
   fill its four roles; the lint checks every role is present, every named row was recorded, and every
   such row passed, with a non-vacuity self-test that rejects a broken declaration. Building it fixed a
   real gap — rigidity gained a `rigidity-verdict-refusal` row (overflow → `REFUSE`) — so all five
   detectors are mechanically D17-compliant. The admission law is now a gate row, not just a table.
3. **The first genuinely new admission: the toric code.** `Dom` = stabilizer codes on a cellulated
   surface; `Inv` = code dimension `= dim H₁` (a topological invariant, computed over 𝔽₂ by the
   existing chain-complex / Betti machinery); `W` = stabilizer group + logical-operator (homology)
   representatives; `~` = local stabilizer-preserving operations; `R` = ill-formed / non-CSS input
   refused. Admitted by the *same* six conditions as everything else — "no special treatment."
4. Thereafter, every new domain (a symmetry-group detector, a persistent-homology detector, a
   representation/character detector, a conserved-quantity detector) is a plugin answering the one
   question, and the C8 discipline governs whether any of them ever needs a new *primitive* rather
   than a new *detector*.

## See also

- `spec/D14-frontend-contract.md`, `spec/D15-view-contract.md`, `spec/D16-regional-authority.md`
  (the three one-way boundary contracts D17 abstracts)
- `spec/D13-glyph-probe.md` §C8 and `spec/D5-ledger.md` § "Evidence Against C8" (the self-application)
- `tools/frontend/rigidity_verdict.py`, `tools/physics/criticality.py` (detectors whose `~` is a
  group and a monoid, respectively — the two poles of §1)
