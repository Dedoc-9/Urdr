<!-- SPDX-License-Identifier: AGPL-3.0-only -->

# D5 — Boundaries ledger (every claim graded) — VOLUME II (live)

Continuation of [`D5-ledger.md`](D5-ledger.md) (Volume I, sealed at commit `cb14318`). Same directive,
same grading law, same discipline — a new file only because a ledger that grows a dense paragraph per rung
had reached the size where reading it cost more than writing it. Volume I is immutable history except for
corrections of record; **all new entries append HERE**. A capability's current grade is its LATEST entry
across the two volumes; the capability inventory table lives in Volume I and is superseded entry-by-entry
by whatever lands below.

Grades: **maturity** `IMPLEMENTED` / `SCOPED` / `SPECULATIVE` × **evidence** `MEASURED` / `DECLARED` /
`N/A`. Evidence never exceeds maturity's ceiling. `MEASURED` means: a falsifier exercising the capability
is green in `verify.py` on a named host; it never means universally proven. This volume, like Volume I, is
deliberately NOT doc-currency tracked — it records historical `x → y` count steps stamped to commits,
which the live-count idioms would wrongly read as current totals.

## State at opening (the epoch stamp, pinned to `cb14318` — historical, not live)

The terrain placement batch closed with the ledger at zero: every rung of the Stage-B..I streak —
storecost, persist, glide, chunkload, chunkstate, resurrect, opcost, govern, priogov, horizon, slo,
clslo — carries an independent std-only Rust placement re-verified LIVE by the gate each run
(`latstore_rs`, `glide_rs`, `streamstate_rs`, `latarith_rs`, joining `heightfield_rs`). At the seal the
gate stood at 906 unit falsifiers / 559 rows / 29 Rust / 14 C99 / 10 detectors, run twice byte-identical
on Linux (rustc live) and Windows. Standing debt carried FORWARD from Volume I, restated so it cannot
hide in a sealed file: refusal-MESSAGE parity across placements (verdicts and digests are placed; the
refusal prose is Python's); the C99 axis for the terrain-arc families (Rust-only so far, as
`heightfield_rs` before them); wall-clock everything (`bench.py`, MEASURED-on-named-host, forever
off-gate); and the netcode-layer unification named in `resurrect`'s does_not_show (N2 `rollback`'s
in-memory window vs the durable window — a future rung, stated not begun). The next capability rung
queued at the seal: the MUTABLE CHUNKED WORLD (live edits — every storage rung's shared "until live
world edits" boundary), to open on a fresh OODA with the D16 dynamic-repartitioning falsification
attempts behind it.

<!-- New entries append below this line, newest LAST (chronological, as Volume I). -->

**Design-context note (no new capability, no count change): the cross-partition invariance of the regional cut — a free theorem, recorded so it stops living only in session chat.** Because `chunkstate`'s reunification reproduces the C-INDEPENDENT monolithic `persist` window byte-for-byte (URDRCHS1's same-witness law, Volume I), cuts taken at DIFFERENT region sizes — even different C at consecutive boundaries — trivially reunify to the SAME witness: the monolith is the fixed point, and every admissible partition round-trips to it. Consequence for the C8 seal (Volume I, "Evidence Against C8", designed falsification attempt #1 — dynamic repartitioning): the SNAPSHOT half of that attempt is answered BY CONSTRUCTION — re-partitioning a saved world across cuts cannot move the witness, because no partition ever entered the witness. What remains genuinely open in attempt #1 is live AUTHORITY migration (which region SIMULATES which actor, mid-window, under load) — the part `chunkstate` explicitly declared out of scope. GRADE: the invariance is ESTABLISHED by composition of two MEASURED laws (reunify == state; persist records are partition-free) — no new falsifier is strictly required, though a one-line cross-C falsifier (cut at C=8 and C=16, both reunifying to the identical record bytes) would make it MEASURED directly and is noted as cheap future hardening. This entry also records, for takeover completeness, where the session's red-first finds live: the persist membership-law strengthening, the chunkload ±1 boundary-tight corpus repair, the resurrect k=0 seed-facing law, the chunkstate identity-loss plant, and the latarith vacuous-anchor find are each documented in their Volume I entries and their commit messages (`ff5cf15..cb14318`) — the commit-message record is the authoritative narrative for those.

**The mutable chunked world (URDRTFM1) — T3.40, MMO Stage I — MEASURED (exactly-one-slot manifests checked EQUAL to direct mutation; CAS refuses; replay bit-for-bit; blast radius certified by demand sets).** `tools/terrain/terraform.py` + gate stage `terraform`: Volume II's first capability entry, and the rung every storage rung DECLARED as its boundary — "until live world edits" — now closed. THE EDIT RECORD: 96 bytes = MAGIC | parent manifest digest | x | y | old_h | new_h | SHA-256, digest-first restore, exact-length law, EVERY single-byte flip and truncation of a pinned record refused (the exhaustive sweep, the house corruption discipline). THE ☿-LAW (the membrane's identity law applied to terrain): `apply_edit` verifies the record, requires the PARENT to be exactly the live world's manifest address — a stale parent is a typed `TERRAFORM-REFUSE` ("an edit authored against another world is refused, never rebased") — and the OLD height to match the live cell (the compare-and-swap; a wrong old height refuses, as does an off-grid target, checked at apply time because the grid is the WORLD's property, not the record's), then mints the NEW world: the new manifest differs from the parent in EXACTLY the containing chunk's slot (gate-checked over a corpus of island/blank/mountains fields × corner/center/chunk-boundary cells × C=8/16, and checked EQUAL to direct field mutation), every untouched chunk keeps its content address, and the PARENT world still reassembles byte-for-byte under its old manifest — ANAMNESIS: both worlds coexist in one store; history is an address, not an undo. THE CHAIN: `edit_chain` threads parent → head; `replay` of the edit log reproduces the head manifest BIT-FOR-BIT, with order STRUCTURAL via the parent chain — a reordered log refuses on the stale parent itself, no sequence numbers to forge. THE BLAST RADIUS, certified not estimated: by `chunkload`'s own demand sets — a walk whose certified demand set contains the edited chunk DIVERGES (the pinned walled_walk scene: a wall raised at (5,8) stops the near walk), while a walk whose demand set misses it is BIT-IDENTICAL before and after (the far walk, same bytes) — the edit's consequence surface is exactly the chunks that read it. THE COST: `edit_cost_bytes(C, w, h)` = chunk_bytes(C) + the new manifest + 96, a closed form checked against real bytes under `storecost`'s `STORAGE-REFUSE` budget law — an edit prices ONE new chunk, ONE new manifest, ONE record; every untouched chunk is SHARED between parent and child because the content address IS the deduplication. THE STALE SNAPSHOT, by composition with `resurrect`: a durable actor snapshot whose ground an edit has contradicted REFUSES on revive (`check_states` — derived data is checkable data, L11's law doing new work unchanged), checked in BOTH directions — an edit under the actor's floor cell refuses the old snapshot; an edit elsewhere revives green. Three pinned scenes: single_dig (island C=16, one dig, exactly-one-slot + parent intact → ADMIT), replay_chain (three edits threaded and replayed to the same head → ADMIT), walled_walk (the blast-radius witness → ADMIT). Gate stage `terraform` (4 rows: scenes / edit / chain / refuse) + red-first `tests/test_terraform.py` (9 falsifiers, each proven to BITE via planted defects — a leaky neighbor-mutating edit, a gutted parent-CAS, a reordered replay, record corruption, and a cost off-by-one all redden; stage non-vacuity proven three ways — a drifted golden reddens scenes alone, the gutted CAS reddens refuse, the leaky edit reddens edit). Unit falsifiers 906 → 915; rows 559 → 563; NO placement change. GRADE: the record law and exhaustive corruption refuse, the exactly-one-slot locality (vs direct mutation), the parent-CAS and old-height refuses, the replay equality and structural order, the both-worlds anamnesis, the certified blast radius (both directions), the cost closed form and budget refuse, the stale-snapshot refuse (both directions), and determinism are MEASURED (exact, reproducible, a defect diverges). DECLARED, honestly: CONCURRENT editors (this is a single-author CAS chain — two edits racing one parent is a merge POLICY question, authority territory, not a data law; the CAS guarantees the race is DETECTED, not arbitrated); edit AUTHORIZATION (WHO may terraform — `authinput`/`fraud` territory); the edited world is a DESCENDANT of the URDRHF1 canon, not a re-seeded canon (the seed law certifies genesis; the chain certifies history). `does_not_show`: multi-cell brush edits (a composition of single-cell CAS records, not begun); wire replication of edits to interested clients (interest RELEVANCE is certified, replication stays declared — the standing Stage-C boundary); a depicting client; and cross-placement (Python reference only — the URDRTFM1 record family joins the placement frontier alongside winding/toric/rigidity/math/netcode live re-verification).

**The commutation certificate (URDRCMU1) — T3.41, MMO Stage I — MEASURED (the diamond discharged constructively over the corpus; forgeries refused under full re-derivation; the permutation closure reject-whole).** `tools/terrain/commute.py` + gate stage `commute`: the PROOF-OBJECT turn, and the first rung built under the ratified private-slice consensus — `terraform` closed the mutable-world boundary with a total refusal (two edits racing one parent, the loser DETECTED, never arbitrated); this rung converts detection into a CALCULUS. Two sibling edits either carry a first-class, content-addressed, independently re-verifiable proof that order cannot matter, or they refuse — concurrency stops being a policy bolted beside the data laws and becomes a theorem derived FROM them (terraform's exactly-one-slot locality, chunkload's demand sets, and the CAS are the premises; the certificate is the composed conclusion). THE CERTIFICATE: cert = MAGIC | rec_a | rec_b | rank | SHA-256 — CERT_BYTES = 233, a closed form; the embedded records keep their OWN digests (defense in depth: the outer digest catches transport corruption, the inner digests catch a forger who re-seals the outside around a tampered inside — both layers proven to bite); EVERY single-byte flip and truncation of a pinned certificate refuses. THE DIAMOND (the theorem the certificate records): for sibling edits on DISTINCT cells, apply A then explicitly-rebased B EQUALS apply B then explicitly-rebased A — field AND manifest address — and both equal the direct two-cell mutation, checked over a corpus (far cross-chunk pair / same-chunk pair / seam-diagonal pair × island/blank/mountains × C=8/16); the proof obligation is discharged CONSTRUCTIVELY — certify builds both paths and refuses if they diverge, so nothing is assumed from geometry that isn't re-checked in bytes. THE RANK (the graded conflict dimension): 0 = different chunks — fields commute AND manifest slots are disjoint AND the blast radii are proven NON-INTERFERING (a demand-disjoint probe walk is bit-identical across the composed pair; A's divergence is unperturbed by B) — parallel execution CERTIFIED, every serial order provably equivalent; 1 = same chunk, distinct cells — the world commutes exactly while that chunk's consumers honestly serialize; the SAME cell is no rank at all — COMMUTE-REFUSE in TWO INDEPENDENT layers (certify's cell law; the old-height CAS on the rebased loser), individually redundant, jointly load-bearing. PREDICTION (scheduled, not repaired): `predict` is PURE chunk geometry — no field, no records, no editing — and the gate checks it EQUAL to the rank every certificate embeds over the corpus, so an authority can schedule writers BEFORE any edit exists; predict proposes, certify disposes. THE EXPLICIT REBASE: `rebase_edit` is a MINTING act — new record, new parent binding, new digest, same claimed transition — and the original still refuses on the moved world; terraform's law stands UNWEAKENED beneath the calculus (gate-checked: an un-rebased sibling still stale-refuses). CLOSURE (the batch theorem): every pairwise certificate minted, EVERY permutation replayed to ONE head, or the batch refuses WHOLE. THE SESSION'S RED-FIRST FIND, recorded per L15: the repairing-closure plant (skip refusing pairs, skip refusing permutations) was VACUOUS against the naive contested-batch falsifier — every permutation of an ordinary contested batch dies on the CAS, so even a repairer refuses by accident; the unmasking construction is the NO-OP CONTESTED EDIT (same cell, dh=0), which leaves the CAS satisfied in exactly the orders where the no-op goes first — a repairing closure finds a consistent-looking subset of orders and ADMITS an order-DEPENDENT batch; the strengthened falsifier pins this construction and the plant now bites. Four pinned scenes: twin_dig (rank 0, the parallel-certified pair → ADMIT), quarry (rank 1, same chunk → ADMIT), caravan (the closure — three edits, six permutations, one head; the rank slot carries the verified permutation count → ADMIT), contested (the same cell twice, refused in both layers → COMMUTE-REFUSE, the verdict IS the refusal). Gate stage `commute` (4 rows: scenes / cert / diamond / refuse) + red-first `tests/test_commute.py` (10 falsifiers; five planted defects — a gutted rank law, gutted sibling/world binding, a transposed rebase, a skipped outer digest, and the repairing closure — each proven to BITE before the goldens pinned; stage non-vacuity three ways — a drifted golden reddens scenes alone, the gutted rank reddens cert, the transposed rebase reddens the stage). THE PROOF IS PRICED: CERT_BYTES under `storecost`'s STORAGE-REFUSE budget law — evidence has a byte cost like everything else in the arc. Unit falsifiers 915 → 925; rows 563 → 567; NO placement change. GRADE: the certificate closed form and round-trip, the exhaustive corruption refuse, the diamond (field + manifest + direct-mutation equality, both orders, full corpus), the two-layer contested refuse including the no-op-masked construction, the rank law and its pure-prediction agreement, the explicit-rebase laws, the permutation closure and reject-whole, the rank-0 blast independence, forgery refusal under full re-derivation, determinism, and the priced proof are MEASURED (exact, reproducible, a defect diverges). DECLARED, honestly — the arc this rung OPENS rather than lands: the independence LATTICE as a first-class queryable structure (certificates already induce it; nothing yet queries it), a general REGION ALGEBRA over demand sets (union/closure/projection — the commutation condition as region disjointness), CAUSAL WITNESS-SETS (rebase validity as "is the witness still true" rather than "is the parent still current"), and proof-carrying repo commits; WHO may author remains `authinput` territory. `does_not_show`: n-way simultaneous certificates beyond pairwise + closure (at this scale the pairwise set + permutation sweep IS the batch proof; a closure certificate RECORD is future work), cross-process scheduling and transport, wall-clock (`bench.py`), and cross-placement (Python reference only — URDRCMU1 joins URDRTFM1 on the placement frontier).

**RAN-0, the authority-nullity certificate (URDRRAN0) — T3.42, MMO Stage I — MEASURED (the four-way head equality discharged constructively; the frame property and the minimal-knowledge coordinator checked in bytes; certificate transport both directions).** `tools/terrain/rannull.py` + gate stage `rannull`: the composition of the two proof domains, built to the ratified RAN-0 brief — `chunkstate` certifies OWNERSHIP (which region owns which state), `commute` certifies SEMANTIC INDEPENDENCE (order cannot matter); NEITHER alone certifies distributed execution, and the reason is the structural find this rung is built around: TERRAFORM'S GLOBAL CAS IS ITSELF A SHARED AUTHORITY — every edit binds the world's manifest address, which touches every chunk (why even rank-0 commute pairs needed explicit rebases: the shared binding moved under them). THE REGIONAL EDIT RECORD rebinds the CAS to the edit's own authority: MAGIC | parent CHUNK digest | kx | ky | x | y | old_h | new_h | SHA-256 — RAN_RECORD_BYTES = 104; the record names exactly what it binds — one chunk's content address, one cell transition; nothing global appears in it, so nothing global can be held by it. THE AUTHORITY, first-class and certified minimal: authority(E) = closure of REGION (chunkstate's floor law), BLAST (MEASURED per edit by lifting to the global law and diffing manifest slots — terraform's exactly-one-slot theorem re-derived, never assumed from geometry), and DEMAND (the cells the regional CAS reads); at this granularity the three sources must AGREE on one chunk — misalignment (a record claiming a region its cell is not in) is RAN-REFUSE, the annexation law applied to writes. THE NULLITY, a proof of ABSENCE at three levels of informational absence: (1) THE SHARD — shard_apply(chunk_record, record) is a pure function whose inputs do not contain the world; the FRAME PROPERTY is checked in bytes (the same chunk embedded in two DIFFERENT worlds + the same record → byte-identical outputs — influence from outside the authority has no channel to arrive by); (2) THE COORDINATOR — parallel_head runs from the parent MANIFEST plus the two parent chunk records ALONE (addresses, not state: the gate feeds it a store LACKING every other chunk's bytes and it still yields the head — a coordinator that secretly reassembles the world is a planted-defect target and reddens); (3) THE PROVER — nullity discharges Execute(EA||EB) == Execute(EA;EB) == Execute(EB;EA) as a FOUR-WAY head equality bit-for-bit: the parallel head, both global serial heads (terraform lift + explicit rebases), and the commute-domain diamond head — the two proof domains AGREE, and the certificate (MAGIC | rec_a | rec_b | SHA-256, CERT_BYTES = 248) embeds the ORIGINAL records unchanged: ZERO rebases, because no shared authority moved. Synchronization is not omitted; it is shown to be unnecessary by construction. THE SESSION'S RED-FIRST FINDS, two, both recorded per L15: (i) the wrong-world forgery falsifier — written to the commute-style GLOBAL expectation — did not fail, and was not wrong to not fail: check_nullity against a world differing only OUTSIDE both authorities correctly RE-VERIFIES, because the certificate binds chunk addresses, not the world — THE CERTIFICATE IS BOUND TO ITS AUTHORITIES AND TRANSPORTS across authority-preserving worlds (landing on THAT world's head), while a world that moved INSIDE an authority refuses; the portability IS the absence of shared authority made visible at the proof level, and the falsifier now pins both directions; (ii) the stale-chunk CAS plant was MASKED by the old-height CAS for same-cell staleness — the unmasking construction is a chunk moved at a DIFFERENT cell (the height CAS satisfied, only the digest CAS can refuse), now pinned. OVERLAP: same chunk → RAN-REFUSE in two layers proven persist-style — the authority-disjointness check and the head comparison are INDIVIDUALLY REDUNDANT (either gutted alone still refuses: a doubly-written slot diverges the heads; disjointness catches before heads exist) and JOINTLY LOAD-BEARING (both gutted admits and the falsifier bites); the commute rank-1 fallback still certifies the lifted pair — nullity is STRICTLY stronger than commutation, refusing it contradicts nothing. RECOVERY INHERITS THE NULLITY: an actor parked in a region disjoint from BOTH authorities revives green across the parallel pair (RS.check_states on the reunified world); an edit under the actor still refuses — the consistency law unweakened. THE COSTS split along the roles: the SHARD's increment = one chunk + one record (NO manifest — it never mints one; that is the point); the COORDINATOR's = one manifest + one certificate (NO chunk — it never touches state; that is the point); both under storecost's STORAGE-REFUSE law. Four pinned scenes: twin_shards (the certified parallel pair → ADMIT), frame_witness (the same chunk, two worlds, identical shard outputs → ADMIT), annexed (overlap → RAN-REFUSE with the rank-1 fallback certified — the verdict is the refusal), healed_region (recovery inherits the nullity → ADMIT). Gate stage `rannull` (4 rows: scenes / authority / nullity / refuse) + red-first `tests/test_rannull.py` (10 falsifiers; five plants — a gutted regional CAS, a padded authority, a reassembling coordinator, the two-layer overlap pair, a chunk-dropping reunify — each proven to BITE before the goldens pinned; stage non-vacuity three ways, each plant reddening a DIFFERENT row). Unit falsifiers 925 → 935; rows 567 → 571; NO placement change. GRADE: the record and certificate laws with exhaustive corruption refuses, the authority alignment and its misalignment refuse, the frame property, the minimal-knowledge coordinator, the four-way head equality, the zero-rebase witness, the two-layer overlap refuse with the rank-1 fallback, the regional CAS refuses including the far-cell stale and cross-magic both directions, forgery refusal under full re-derivation, certificate transport both directions, the recovery composition, the split costs, and determinism are MEASURED (exact, reproducible, a defect diverges). THE ARC AS OF THIS ENTRY, stated as the composed narrative: persistence proves REPLAY; terraform proves LOCALITY; commutation proves SEMANTIC INDEPENDENCE; chunkstate proves OWNERSHIP; RAN-0 proves AUTHORITY NULLITY — distributed WRITE ADMISSION is now a theorem obtained by composing proofs, not a new subsystem. DECLARED: the full authority ALGEBRA (difference/projection/closure over transcripts and actor demand), n-way nullity beyond pairwise, LIVE authority migration mid-window (which shard SIMULATES which actor — the C8 attempt-#1 live half; this rung answers the write-admission half), scheduling policy, and the PHYSICAL half of isolation (the shard's isolation is INFORMATIONAL — the inputs do not contain the world; cross-process transport, mid-parallel failures, and wall-clock are bench/future territory). `does_not_show`: WHO may author (`authinput`); eviction/prefetch (chunkload's boundary); cross-placement (Python reference only — URDRRAN0 joins URDRTFM1 and URDRCMU1 on the placement frontier).

**The standing lease (URDRLSE1) — T3.43, MMO Stage I — MEASURED (interval commutation over the corpus; amortized == reproved bit-for-bit; the lost-update two-layer law proven persist-style with the anamnesis trap demonstrated).** `tools/terrain/lease.py` + gate stage `lease`: the TEMPORAL extension of RAN-0, built under the ratified slice — rannull proved the nullity certificate is bound to its AUTHORITIES, not the world (spatial transport); the lease makes the INTERVAL first-class: an 80-byte write capability (MAGIC | chunk digest | kx | ky | SHA-256) minted against one chunk STATE, valid from mint until that authority moves — proof as an interval, not a moment; optimistic distributed admission with proofs instead of locks (an authority hands a client a lease; the client authors offline; the edit admits later against ANY head the interval reaches; conflict is EXPIRY, typed and honest). VALIDITY, state-free: valid(manifest, lease) reads ONE manifest slot — no store, no field, no chunk bytes; a coordinator answers "is this lease live?" from the 55 + 32·n manifest alone. THE DESIGN DECISION THE ANAMNESIS STORE FORCES (the rung's central find, caught at design time before any falsifier ran): the content-addressed store retains the OLD chunk forever — anamnesis is an address, not an undo — so an admission that fetched the chunk BY THE LEASE'S DIGEST would find the stale bytes, apply cleanly (the shard CAS SATISFIED against the stale state), and silently REVERT every edit the interval landed in that chunk: the classic LOST UPDATE, hiding inside the store's own virtue. `admit` therefore fetches by the CURRENT manifest slot: the shard CAS against the LIVE chunk is the deep guard, valid() the cheap pre-check — two layers proven persist-style (valid gutted alone → the shard CAS still refuses; fetch-by-lease-digest alone → valid still refuses; BOTH gutted → the stale admission lands and the falsifier detects the reversion — the lost update is IMPOSSIBLE, not avoided), with the trap demonstrated in-suite: the stale chunk IS still in the store when the refusal fires. INTERVAL COMMUTATION (the keystone): over a chain of authority-disjoint leased edits evolving the world lawfully (each step itself a lease-admit — the world evolves BY the law being proven), the leased edit admits at EVERY insertion position with its bytes UNCHANGED, and every position lands ONE final head — RAN-0's diamond iterated across an interval without re-proving. AMORTIZATION measured: the cheap admission (slot check + one shard apply + address-substituting reunify) EQUALS the full global reproof (terraform lift on the reassembled world) at every interval head, bit-for-bit — the proof was paid once at mint; admissions inherit it. SELF-EXPIRY + RENEWAL: a lease dies at its OWN use (the edit moves the very authority the lease names), so leases are single-shot; renewal is lease_from_chunk(new chunk), and the lease chain IS the region's write history — each link a content address (the transcript law on write capability). TRANSPORT INHERITED: the same lease + record admit on a DIFFERENT world sharing the chunk, landing the SAME new chunk address in the leased slot (heads differ — they bind whole manifests; the leased slot agrees — the lease reaches exactly as far as its authority). Four pinned scenes: long_watch (the interval commutation, all four insertion positions → one head → ADMIT), relay (three self-expiry/renewal links — the write history → ADMIT), amortized (cheap == reproved at two interval heads → ADMIT), expired (the interloper at ANOTHER cell of the leased chunk — height-CAS blind, digest evidence only — refused in both layers with the stale bytes still in the store → LEASE-REFUSE, the verdict is the refusal). Gate stage `lease` (4 rows: scenes / validity / interval / refuse) + red-first `tests/test_lease.py` (10 falsifiers; plants — valid-gut alone and fetch-by-lease alone (each proven REDUNDANT), both together (the lost update lands, the falsifier BITES), reunify-skip (four falsifiers redden), lease-digest-skip (the exhaustive sweep bites) — all proven before the goldens pinned; stage non-vacuity three ways). Unit falsifiers 935 → 945; rows 571 → 575; NO placement change. GRADE: the record law and exhaustive corruption refuse, state-free validity both directions, interval commutation, the amortized == reproved equality, the two-layer lost-update refuse with the demonstrated trap, self-expiry and renewal, admission honesty (foreign state / region mismatch / missing current chunk / tampered lease), inherited transport, the split and amortized cost closed forms under the budget law, and determinism are MEASURED (exact, reproducible, a defect diverges). DECLARED: lease ISSUANCE (who gets a lease — `authinput`/capability territory); DURATION policy (expiry is authority-motion, not wall-clock; TTLs are an operational overlay); n-way lease scheduling and the independence lattice as an allocator (the arc); the physical client (offline authoring is modeled by byte-stability across the interval, not a process boundary). `does_not_show`: revocation-before-expiry (mint a competing edit and the CAS expires the lease naturally; explicit revocation lists are future work); wall-clock (`bench.py`); cross-placement (Python reference only — URDRLSE1 joins TFM/CMU/RAN0 on the placement frontier). THE ARC AFTER THIS ENTRY: persistence proves replay; terraform proves locality; commutation proves semantic independence; chunkstate proves ownership; RAN-0 proves authority nullity; the lease proves the INTERVAL — write capability now has a spatial law, a temporal law, and a history, all content-addressed, all refusing rather than repairing.

**The testament (URDRTST1) — T3.44, MMO Stage I — MEASURED (the through-death admission via a REAL successor subprocess; exactly-once with speaking refusals; the substitution refuse; executor purity).** `tools/terrain/testament.py` + gate stage `testament`: DURABLE INTENT, the write that survives its writer — built under the ratified slice, named for its double meaning: a last WILL (written intent surviving its author, executed by a successor only under the conditions it names) and TESTIMONY (the record as evidence, content-addressed, corruption-refusing). `resurrect` proved the READ side of death — a successor replays a saved world from the store alone; this rung proves the WRITE side: a client may die holding an unadmitted edit, and the intent crosses the death as a first-class object. THE TESTAMENT (144 bytes): MAGIC | regional edit record | SHA-256 — nothing more; the lease it was authored under is DERIVED from the record's own parent digest and region (never carried — carrying it would invite incoherence, and derived data is checkable data); the one digest is integrity check, content address, and on-disk filename (the persist law, now covering intent). PROBATE: the successor derives the lease and runs the LEASED ADMISSION, so everything the lease law proved is INHERITED — the lost-update impossibility, amortized == reproved, interval transport — and EXACTLY-ONCE IS FREE: the admission moves the very authority the testament names, so a second probate refuses; gate-checked that the refused attempt perturbs NOTHING (the world before probate replays to the identical result). THE REFUSALS SPEAK, three flavors each earned from evidence, never guessed: "EXECUTED" — the current chunk state IS the state this intent produces (adjudicated by deriving the expected child from the RETAINED parent state — the anamnesis store doing honest READ-ONLY work — and comparing content addresses); the successor may rest. "DISTRIBUTED" — a foreign edit moved the authority; the intent conflicted; re-author. "UNADJUDICABLE" — the parent state is no longer retained; no flavor is guessed from missing evidence. THE DEATH BOUNDARY IS REAL (the resurrect discipline): testament.py is its own successor — `python testament.py <store_dir> <testament_addr> <manifest_addr>` — knowing NOTHING but a directory and two addresses, the disk its only channel; the gate runs it twice and the outputs are bit-identical and EQUAL the never-died admission head; on a conflicted estate it prints the typed refusal and the directory is BYTE-IDENTICAL after (executor purity, gate-checked file-by-file). THE FILENAME LAW with its sharpest tooth: every estate object must hash to its filename — so an INTACT, internally-valid testament placed under the WRONG address is a SUBSTITUTION and refuses; the inner digest cannot see this attack (the bytes are a perfectly good testament — just not the requested one); the address check carries it alone, and the plant that guts it bites. Four pinned scenes: phoenix_scribe (probate == living admission == the from-disk successor path → ADMIT), twice_told (exactly-once: the second probate refuses AS executed; the refused attempt perturbs nothing → ADMIT), legacy_race (the foreign edit lands first: refused AS distributed, never as executed → TESTAMENT-REFUSE, the verdict is the refusal), sealed_scroll (the filename law on a real directory: bit-exact round-trip, flip refuses, substitution refuses → ADMIT). Gate stage `testament` (4 rows: scenes / death / probate / refuse) + red-first `tests/test_testament.py` (10 falsifiers; five plants — a skipped outer digest, a gutted flavor adjudication, an ECHOING successor (prints its argv instead of computing — the through-death falsifier catches it because the never-died head is never in the argv), a tombstone-writing refuse path, and a skipped filename check — each proven to BITE before the goldens pinned, each on its OWN falsifier; stage non-vacuity three ways, each reddening a different row). Unit falsifiers 945 → 955; rows 575 → 579; NO placement change. GRADE: the record law and exhaustive corruption refuse, probate == living == the global reproof, the through-death admission (twice, bit-identical, disk-only), exactly-once with the unperturbed refused attempt, the three speaking flavors, the filename/substitution law, executor purity (gate-checked byte-by-byte), the estate cost closed form under the budget law, and determinism are MEASURED (exact, reproducible, a defect diverges). DECLARED: testament ISSUANCE (who may leave one — `authinput`/capability); retry policy after "distributed" (re-authoring is the client's choice; the law guarantees only that the flavors are TRUE); batched wills (many bequests = a chain of these primitives); store compaction (anamnesis keeps everything; GC is operational). `does_not_show`: a crash DURING the caller's persist-back of probate results (the successor prints, the caller persists — torn writes are persist's crash-atomicity boundary, inherited); wall-clock (`bench.py`); cross-placement (Python reference only — URDRTST1 joins TFM/CMU/RAN0/LSE1 on the placement frontier). THE ARC AFTER THIS ENTRY, the composed narrative now seven laws long: persistence proves REPLAY; terraform proves LOCALITY; commutation proves SEMANTIC INDEPENDENCE; chunkstate proves OWNERSHIP; RAN-0 proves AUTHORITY NULLITY; the lease proves the INTERVAL; the testament proves INTENT THROUGH DEATH — the write path now has a spatial law, a temporal law, a history, and an estate, every object content-addressed, every failure a typed refusal that tells the truth about why.

**The durable rollback window (URDRRBS1) — N2.5 / T3.45 — MEASURED (restored == never-died in every observable; rollback across a REAL process death; the checked-evidence refuse; the surviving defect anchor) — A DEBT SETTLED.** `tools/netcode/rollstore.py` + gate stage `rollstore`: the N2-window/durable-window UNIFICATION — the deferral resurrect's does_not_show recorded ("unifying its window with this durable one is a future rung, stated not begun") and every subsequent storage rung inherited; jumped four times this sitting by ratified challengers, then bound by covenant and repaid FIRST, before quintessence — the placement-debt discipline holding: recorded debt becomes scheduled repayment becomes closed ledger. THE UNIFICATION: the terrain arc's window law (one digest = integrity check + content address + filename; restore-or-refuse; the priced window; the REAL death boundary) applied to N2 rollback's in-memory K/H snapshot window, so the repo's two window disciplines become ONE law. THE RECORDS: snapshot_record = MAGIC | tick | n | (pos, vel as signed int64 Q32.32)* | SHA-256 (52 + 32·n); log_record = the event transcript (47 + 48·m); window_manifest = head | K | H | the (tick, digest) list IN ORDER + the log's address (95 + 40·s) — all three closed forms checked EQUAL to real bytes, all three under exhaustive flip + truncation sweeps. THE SOURCE LAW (resurrect's, verbatim): the event log is the REWINDABLE SOURCE — restore_peer replays it from the world's start — and the saved window is CHECKED EVIDENCE, never trusted state: every restored snapshot must equal the replay's regenerated window bit-for-bit (tick, positions, velocities, count, head) or the restore refuses. Integrity is not truth (L11): a CRAFTED-BUT-DIGESTED snapshot — integral bytes, wrong physics — is caught by the cross-check, not welcomed by the digest (the pinned crooked_window scene; the plant that guts the cross-check bites). THE THEOREMS, all MEASURED: (1) restored == never-died in EVERY observable (state, head, chain, known set, window — attribute by attribute) and forever after (both peers finish the run identically, equal to the canonical N1 timeline); (2) ROLLBACK CROSSES DEATH — a real successor process (rollstore.py as its own __main__: argv = store dir, manifest address, ONE post-death late event; the disk its only channel) restores, REWINDS across the death boundary on the held-back input, and converges to lockstep.simulate bit-for-bit, twice; (3) THE LAW SURVIVES — the horizon refuse (reject whole, state untouched), the identity conflict, duplicate absorption, and K-invariance hold IDENTICALLY on the restored peer; (4) THE DEFECT SURVIVES — the apply-at-head defect diverges on a RESTORED peer exactly as on a living one: the convergence invariant is not weakened by the round-trip (non-vacuity through death); (5) THE PRICE — window_cost = Σ snapshots + log + manifest, checked EQUAL to the real directory bytes, gated by storecost.within_storage_budget: the N2 window priced exactly as the terrain window was. THE HOUSE REFUSALS: a disordered manifest REFUSES, never re-sorts (the repairing-restore plant bites); an intact object under the wrong address is a substitution, refused; every flip and truncation refuses. Four pinned scenes: mirror_window (save mid-run, restore, finish both — one trace, the canonical one → ADMIT), phoenix_peer (the rollback that crosses death → ADMIT), crooked_window (the checked-evidence refuse → ROLLSTORE-REFUSE, the verdict is the refusal), priced_window (K=4/K=8 both restore to ONE trace; the cost closed form equals the real directory bytes under the budget law → ADMIT). Gate stage `rollstore` (4 rows: scenes / death / law / refuse) + red-first `tests/test_rollstore.py` (10 falsifiers; five plants — a skipped record digest, UNSIGNED velocity decoding (bites seven falsifiers at once: every bounced body carries a negative velocity), the gutted consistency cross-check, an echoing successor, and a re-sorting restore — each proven to BITE before the goldens pinned; stage non-vacuity three ways, each reddening a different row). Unit falsifiers 955 → 965; rows 579 → 583; NO placement change. GRADE: the three record laws, the restored == never-died equality, the through-death rollback, the checked-evidence refuse, disorder/substitution refusals, horizon/conflict/duplicate/K-invariance preservation, the surviving defect anchor, the priced window, and determinism are MEASURED (exact, reproducible, a defect diverges). DECLARED, honestly: the restore path is O(history) — replay from the world start with the window as cross-checked evidence; an O(window) suffix-restore reproducing the FULL witness chain needs a durable frame chain (a future rung, stated not begun — the new deferral this rung leaves, recorded here so the covenant can find it); save/restore AUTHORITY is `authinput` territory; K/H remain operational (proven non-semantic, now through the restore too). `does_not_show`: cross-peer window exchange (each peer saves its own; a shared store is transport territory); wall-clock (`bench.py`); cross-placement (Python reference only — URDRRBS1 joins the placement frontier). WHAT THIS CLOSES: resurrect's does_not_show line is updated in place (the debt line now names its settlement); the netcode README's "Remaining DECLARED" list shrinks by one; and the repo now has ONE window law — byte-priced, content-addressed, death-crossing — on both its layers. The queued next rung is quintessence (URDRQNT1): the ID-0 representation theorem.

**Quintessence, the ID-0 representation theorem (URDRQNT1) — T3.46 — MEASURED (extractor totality with full-tuple injectivity; the scope finding predicting transport; the five-axis conservation ablation; one lineage modulo certified commutation) — THE CALCULUS CLOSES.** `tools/terrain/quintessence.py` + gate stage `quintessence`: built to the ratified ID-0 brief, in the order the covenant fixed (the N2.5 debt repaid first) — and qualitatively unlike every rung before it: THIS RUNG MINTS NOTHING. No record format, no magic-prefixed bytes of its own beyond the scene canon — its entire content is theorems about the five landed families (URDRTFM1 global edits, URDRCMU1 certificates, URDRRAN0 regional edits, URDRLSE1 leases, URDRTST1 testaments). The system stops expanding and starts closing. THE REPRESENTATION: every lawful authority is characterized by its FIVE-AXIS EVIDENCE TUPLE — historical (the parent address: what must have come before), spatial (the cells claimed + the claim's scope: global | a named region), semantic (the transitions old→new, or None for a pure capability — a lease claims NO transition and its axis says so, stated never invented), temporal (the validity predicate: scope + address — under what condition the authority still stands), identity (the object's own content address: the evidence of intent). `essence_of` is TOTAL over the five families, deterministic, injective on the full tuple over the corpus (4 edits × 5 embodiments, 20 distinct essences), and REFUSES everything else — a chunk record, a manifest, raw bytes, a forged sixth magic: no essence is ever guessed. THE SCOPE FINDING (the tuple's first dividend, surfaced at design time): within EVERY family, the historical and temporal axes carry the SAME address at a SCOPE — a global record's parent IS its validity condition (world-manifest-current); a regional record's parent IS its lease (chunk-current). VALIDITY IS "MY HISTORY IS STILL CURRENT" — and the scope of that binding is exactly what RAN-0 changed; the tuple does not merely describe the transport theorem, it PREDICTS it (gate-checked: the world-scoped form refuses on an elsewhere-moved world while the chunk-scoped form of the SAME logical edit admits — decidable from the tuple before any execution). CONSERVATION OF AUTHORITY, both halves MEASURED: evidence SUFFICES — behavior is determined by the first four axes (a regional record and its testament share axes 1–4, differ on identity alone, and admit/refuse IDENTICALLY over the world corpus: mint world, same-chunk-moved, elsewhere-moved — with the essence predicting which); NO EVIDENCE, NO AUTHORITY — `conservation_check` degrades exactly one axis at a time (a wrong parent / a foreign region / a wrong old height / a moved authority / a flipped byte) and admission REFUSES for every axis: each individually load-bearing, one law across the families; process lifetime appears on neither side of the ledger. ONE LINEAGE (the theorem the user's brief named, honest in both its refinements): `lineage` admits an edit set in a given order and returns (head, essence set); over the corpus, EVERY order carries the SAME essence set to the SAME head — the lineage is the EQUIVALENCE CLASS, not the path (uniqueness MODULO the commutation the repo's own certificates certify: the naive "exactly one lineage" would be falsified by long_watch's own interval commutation) — and a DIFFERENT essence set lands a DIFFERENT head: heads in bijection with lineages over the corpus. Honesty twice more: (1) lawful histories degenerate multisets to sets — an exact duplicate essence cannot admit twice, because its own admission moved the CAS it binds; (2) the bijection's injectivity rides on SHA-256 collision-resistance, THE one DECLARED pillar under the whole calculus, stated here and never claimed as proven. THE LEASE RECONSTRUCTS FROM THE ESSENCE bit-for-bit (`lease_of_essence`): evidence axes are not commentary — they rebuild the capability they describe. Four pinned scenes: distilled (one logical edit, five embodiments, one extractor — agreement where the law says agree, difference where it says differ → ADMIT), one_lineage (six orders, one head, one essence set; a changed transition changes both → ADMIT), five_wounds (five axes, five degradations, five refusals → QUINTESSENCE-REFUSE, the verdict is the refusal), closed_form (totality + the boundary refusals → ADMIT). Gate stage `quintessence` (4 rows: scenes / essence / lineage / refuse) + red-first `tests/test_quintessence.py` (11 falsifiers; five plants — a guessing extractor, a conflated temporal scope, a non-degrading ablation, an essence-dropping lineage, a verdict-blind digest — each proven to BITE before the goldens pinned; stage non-vacuity three ways, each reddening a different row). VACUITY HONESTY, recorded: the ablation reporter's own guard is the undegraded control (which must admit, or the check refuses itself as void) plus the home-rung falsifiers that already prove each refusal law bites — a reporter hardcoding True on a lawful corpus is extensionally indistinguishable there, and the claim rests on the home rungs, not on this stage alone. Unit falsifiers 965 → 976; rows 583 → 587; NO placement change. GRADE: extractor totality/determinism/injectivity, the scope finding and its transport prediction, behavior-determined-by-essence, the five-axis ablation, lineage uniqueness modulo commutation with the differing-set separation, the joint essence of certificates, the None-semantic honesty of leases, the lease round-trip, and determinism are MEASURED (exact, reproducible, a defect diverges). DECLARED: SHA-256 collision-resistance; THE CLOSURE LAW as a standing commitment — a future capability is lawful iff its evidence EMBEDS (essence_of extended by one dispatch arm, its axes checked by the same conservation ablation) rather than by introducing a new kind of authority — recorded here so every future ledger entry can be held to it; essence over the READ-side families (persist/resurrect/chunkstate windows — mechanical future work, stated not begun). `does_not_show`: authority POLICY of every kind (`authinput`); wall-clock; cross-placement (Python reference only). THE ARC AT THE CLOSE OF THIS SITTING, eight laws and a closure: persistence proves replay; terraform proves locality; commutation proves semantic independence; chunkstate proves ownership; RAN-0 proves authority nullity; the lease proves the interval; the testament proves intent through death; rollstore proves the window law is one; and quintessence proves the whole write calculus is FIVE KINDS OF EVIDENCE and nothing else. What began as "can the field change?" ended as a representation theorem — the boundary conditions determined the interior, as the house always said they would.

---

## THE WRITE CALCULUS SEALS (epoch stamp, pinned to `0929d72` — historical, not live)

The area that opened at `c813f81` (terraform, "the mutable chunked world") closes at `0929d72`
(quintessence, "the calculus closes"): eight laws and a closure, one sitting, every rung red-first,
every gate x2 byte-identical on both hosts, every push green. The sealed span: T3.40 terraform
(URDRTFM1) · T3.41 commute (URDRCMU1) · T3.42 rannull (URDRRAN0) · T3.43 lease (URDRLSE1) · T3.44
testament (URDRTST1) · N2.5/T3.45 rollstore (URDRRBS1, the settled window debt) · T3.46 quintessence
(URDRQNT1, the representation theorem). The composed narrative, as the entries above record it:
persistence proves replay; terraform proves locality; commutation proves semantic independence;
chunkstate proves ownership; RAN-0 proves authority nullity; the lease proves the interval; the
testament proves intent through death; rollstore proves the window law is one; quintessence proves
the write calculus is five kinds of evidence and nothing else. POSITIONING vs the field, from the
sealing OODA's research sweep: the CRDT literature verifies convergence of MERGE FUNCTIONS at the
type level (commutativity designed in, proven once); this calculus certifies commutativity PER
INSTANCE from evidence and REFUSES otherwise — no merge function exists to verify, because conflict
is detected, never arbitrated. The standing exit condition is quintessence's closure law: a future
capability is lawful iff its evidence embeds. THE EXIT DEBT, recorded with its boundary (the
covenant discipline — recorded debt becomes scheduled repayment): PLACEMENT BATCH #2 — six families
are Python-reference-only (URDRTFM1, URDRCMU1, URDRRAN0, URDRLSE1, URDRTST1 on the terrain
substrate; URDRRBS1 on the netcode substrate), twenty-three pinned scenes with no independent
placement. The boundary: THE WIRE PHASE DOES NOT OPEN UNTIL THE BATCH CLOSES — ratified in session,
batch-first by choice, not by default. THE RATIFIED NEXT PHASE, named so the ledger can hold us to
it: THE WIRE — certified replication, the slice the sealing OODA surfaced and consensus ratified:
EQUAL-OR-REFUSE REPLICATION, where every wire update carries its essence and the receiving client
ADMITS rather than applies (the client as verifier — composing interest's certified relevance,
quintessence's evidence tuple, and the admission laws; no production or academic system found in
the sweep does this). Reopening this sealed area requires what Volume I's seal required: a new
entry, never an edit — these words stay as written.

**PLACEMENT BATCH #2 CLOSES — writecalc_rs (URDRTFM1 + URDRCMU1 + URDRRAN0 + URDRLSE1 + URDRTST1) and rollstore_rs (URDRRBS1), RE-VERIFIED LIVE — the seal's exit debt, repaid in one commit.** The write-calculus seal recorded its debt with a boundary: six Python-reference-only families, twenty-three pinned scenes, the wire phase closed until the batch closes. This entry closes it. THE TERRAIN HALF, `tools/terrain/writecalc_rs/writecalc.rs`: one std-only Rust file, the FIVE terrain families — terraform's edit records + CAS apply + chains + the certified blast radius; commute's certificate + diamond + rank + permutation closure; rannull's regional records + shard_apply + reunify + the FOUR-WAY nullity (parallel == both serials == the diamond, re-derived in Rust); the lease's state-free validity + admission + the full interval commutation (four insertion positions, one head, re-run in-binary); the testament's probate with all three speaking flavors AND the filename law exercised on REAL DISK (std::fs, a temp estate, the substitution refuse included) — NINETEEN scenes against the LIVE conformance goldens, ALL REPRODUCED ON THE FIRST SUCCESSFUL RUN, twice per gate run, byte-identical. The general fold, the URDRHF1 generation, and the chunk/manifest law lifted verbatim from streamstate_rs/glide_rs (established practice); in-binary selfchecks: determinism ×2, exactly-one-slot, diamond == direct mutation, the lost-update two-layer law, corruption refuse. THE DEFECT ANCHOR, chosen and then honestly re-shaped by what it revealed: minted_height new_h → _old_h (the no-op-edit bug) — the terraform and commute scenes print DIVERGED digests (all seven, including the CONTESTED refuse scene, whose second layer flips because a no-op leaves the CAS satisfied), and then the AUTHORITY-ALIGNMENT LAW ITSELF KILLS THE BINARY at the first nullity scene: a no-op moves no manifest slot, so the measured blast refuses the alignment — the law catching the defect IS the defense, and main was restructured to STREAM scenes so the pre-kill divergence evidence prints (a lesson applied, not just recorded: the first defect run's evidence was an empty file compared vacuously, caught before it could pin anything). THE NETCODE HALF, `tools/netcode/rollstore_rs/rollstore.rs`: URDRRBS1 on the rollback_rs substrate (the frozen Q32.32 physics, the URDRLST1/URDRLSTT witness laws, the peer — lifted verbatim); the FOUR scenes with REAL disk round-trips and the filename law on read-back — mirror_window, phoenix_peer (the rollback crossing a save/restore boundary on the held-back late input), crooked_window (the crafted-but-digested snapshot refused — checked evidence, in Rust as in Python), priced_window (K=4 and K=8 both restoring to ONE trace, the cost closed form EQUAL TO THE REAL DIRECTORY BYTES — the tempdir file sizes reproduce across the language boundary) — all four reproduced on the first run. In-binary selfchecks: restored == never-died attribute by attribute, the apply-at-head defect DIVERGING on a restored peer (non-vacuity through death, now cross-placed), horizon/conflict/duplicate surviving the restore reject-whole, corruption refuse. Defect anchor: the restitution coefficient (−3/4 → −2/4) diverges the canonical trace and every trace-bearing scene digest (the zeros-bound refuse scene honestly excepted). GATE STAGES `writecalc-placement` + `rollstore-placement` (2 rows each: live recompile ×2 vs the LIVE goldens + the source-mutation selftest; stage non-vacuity: a drifted live golden reddens each placement row — the live-recompile discipline is load-bearing, a re-pinned canon FORCES the ports to keep up); SKIPPED rows without rustc, honestly labelled, row count host-stable. Rows 587 → 591; Rust placements 29 → 31 (the filesystem count the gate enforces); falsifiers unchanged at 976. GRADE: MEASURED on this session's Linux host (rustc, live-compiled by the gate, twice, byte-identical), re-verified live wherever rustc exists — the owner's Windows included, every run. THE BATCH LEDGER: opened at six Python-only families, closed at zero, one commit — the seal's boundary is satisfied and THE WIRE PHASE IS OPEN. What remains outside both batches, unchanged and honest: refusal-MESSAGE parity (verdicts and digests are placed; prose is Python's), the C99 axis for the terrain arc, the older frontier (winding/toric/rigidity/math/netcode live re-verification), and wall-clock everything (`bench.py`, MEASURED-on-named-host, forever off-gate).

**THE WIRE PHASE OPENS — equal-or-refuse replication (URDRWIR1) — T3.47 — MEASURED (per-step byte equality of replica and authority; the no-sequence-numbers laws; interest soundness and detected-necessity; refuse purity).** `tools/terrain/wire.py` + gate stage `wire`: the ratified phase slice, landed as the phase's first rung on the batch-closed tree — and the second rung in the repo's history to MINT NOTHING (the quintessence discipline): no new record format, no wire envelope, no sequence numbers, no snapshots. Pure composition, and every ABSENCE is a theorem the sealed calculus already paid for. THE STANCE, against both fields the sealing OODA surveyed: production replication ships derived state and asks the client to trust it; CRDTs design types whose merges cannot conflict. This wire does neither — every update IS the essence-bearing object (the 104-byte URDRRAN0 regional record, verbatim: quintessence's tuple riding the wire as itself), and the receiving client ADMITS it under the same laws the authority used, against its OWN replica. THE CLIENT IS A VERIFIER, NOT A BELIEVER: a malicious or buggy server produces a typed WIRE-REFUSE with the replica byte-unchanged — the silent desync, production replication's characteristic failure, is made IMPOSSIBLE rather than monitored for (both silent-desync defects — the ignoring client and the absorbing client — are planted and proven to bite). NO SNAPSHOTS ON THE WIRE: the client DERIVES the new chunk by shard_apply — 104 bytes per edit regardless of chunk size, because derived state is recomputed, never shipped; RAN-0's frame property is what makes the recomputation exact (the shard function's inputs are the chunk and the record, both of which the client verifiably holds). NO SEQUENCE NUMBERS: within a region, order is STRUCTURAL — each record's parent is the previous chunk state's address (terraform's chain law on the wire), so out-of-order refuses on the stale parent, in-order retry admits, and an exact duplicate refuses (at-most-once free: its own admission moved the parent it binds); ACROSS regions, order is provably IRRELEVANT — every interleaving of disjoint-authority updates lands the identical replica (RAN-0's nullity is the wire's cross-region ordering law, gate-checked over the interleavings). THE INTEREST FILTER, one frozenset test on the essence's SPATIAL axis (the tuple doing protocol work): SOUND — an irrelevant edit cannot touch a resident chunk (exactly-one-slot), so the unsent client stays byte-equal, gate-checked; NECESSARY WITH THE VIOLATION DETECTED — a withheld relevant update leaves the client's chunk stale, and the next in-region update refuses on the CAS: drift is caught by the client's own admission law, never absorbed (the silent_drift scene pins the detection). Four pinned scenes: faithful_mirror (five updates, four regions, per-step byte equality → ADMIT), narrow_gaze (a walk's certified demand set as the interest; the relevant update admits, the irrelevant one filters, the client stays byte-equal → ADMIT), crooked_wire (the adversarial server: tamper / out-of-order / unheld each refuse with the replica byte-identical, then the in-order retry admits and the duplicate refuses → WIRE-REFUSE, the verdict is the refusal), silent_drift (the necessity witness → ADMIT). Gate stage `wire` (4 rows: scenes / replicate / interest / refuse) + red-first `tests/test_wire.py` (9 falsifiers; five plants — the IGNORING client (unheld updates silently absorbed), the gutted interest filter, the ABSORBING client (refusals swallowed, the replica returned unchanged — the exact defect production replication tolerates), the gutted subscription check, the verdict-blind digest — each proven to BITE before the goldens pinned; stage non-vacuity three ways, each plant reddening different rows). Unit falsifiers 976 → 985; rows 591 → 595; NO placement change (URDRWIR1 joins the frontier). GRADE: the update-is-the-record law, per-step replica/authority byte equality, the derived chunk, structural in-region ordering with at-most-once, cross-region interleaving invariance, interest soundness and detected-necessity, the verifier refusals with refuse purity, verified subscription, and determinism are MEASURED (exact, reproducible, a defect diverges). DECLARED, honestly — the phase's remaining rungs, named so the ledger can hold us to them: TRANSPORT itself (loss, reordering, duplication are MODELED by the delivery-order falsifiers, not carried over sockets — the laws are stated where a transport must meet them); SENDER AUTHENTICITY (`authinput`'s Lamport signatures — this rung certifies STATE LAW: a lawful update admits whoever relays it, an unlawful one refuses whoever signs it); INTEREST SHIFT (acquiring/releasing regions mid-session — chunkload's verified fetch is the mechanism); server-side fan-out scheduling (`govern`'s territory). `does_not_show`: wall-clock and bandwidth (`bench.py`); the depicting client; cross-placement. THE PHASE LEDGER AT ITS OPENING: the wire's first law is landed — a replica that cannot silently drift, fed by updates that prove themselves, ordered by the calculus instead of counters. What the seal promised, the wire now begins to spend.

**The storm — the deterministic adversarial-transport loom (URDRSTM1) — T3.48, W2 — MEASURED (convergence-under-chaos with exactly-once; the typed-chaos invariant over a measured primary-reordering floor; the prefix property under measured loss; malice-under-chaos; the becalmed control).** `tools/terrain/storm.py` + gate stage `storm`: W2 of the wire-phase brief, invoked by name and landed — the DST discipline (the FoundationDB-simulation / turmoil lineage the brief researched) as a GATE STAGE. The wire's laws were stated where a transport must meet them; the storm is that transport, misbehaving REPRODUCIBLY: frozen, seeded schedules of loss, duplication, reordering, and burst delivery, every draw a SHA-256 digest-stream decision (no clock, no RNG, no platform hash) — the same seed mints the same storm on every host, so the gate stays byte-deterministic while the network inside it does not. THE MODEL'S HONESTY: the client is `wire.client_admit` UNMODIFIED — the storm tests the landed law, it does not get a friendlier one; the retry loom (refused deliveries requeued, passes to fixpoint) is the transport's redelivery and the ONLY repair in scope. THE LAWS: CONVERGENCE-UNDER-CHAOS — every loss-free schedule lands every client on the authority's witness bit-for-bit with each update admitted EXACTLY once (at-most-once holds under duplication because a duplicate's parent has already moved); three seeds, one truth (cross-seed agreement). TYPED CHAOS, the invariant with two teeth: a schedule with MEASURED reorderings must produce refusals — zero refusals convicts either the storm (vacuous) or the client (the 'helpful' defect: buffering out-of-order updates and applying silently — production's reflex, the house's poison); one assertion reddens both. THE SESSION'S RED-FIRST FIND (L15, again doing its work): the vacuous-storm plant (primary delays gutted) was MASKED — duplicate copies still reorder, so the naive reordering floor passed on secondary chaos while the PRIMARY delivery stream flowed in order and the stale-parent law went unexercised; the unmasking measure is PRIMARY_REORDERINGS (reorderings among first deliveries only), now the floor the falsifier stands on, and the re-planted defect bites. THE PREFIX PROPERTY under measured loss: every region equals some authority prefix at every moment — no state ever exists that the authority never had; the first gap freezes the region and the post-gap deliveries stall as COUNTED, TYPED refusals (detected, named, never silent; repair is W4's verified-fetch law, declared). MALICE UNDER CHAOS: tampered copies and foreign records woven into the storm all refuse while convergence proceeds unharmed. THE CONTROL: the becalmed (identity) schedule converges with ZERO refusals — refusals are caused by chaos, never by the loom. Four pinned scenes: tempest (three clients, three storms, one truth → ADMIT), becalmed (the control → ADMIT), gale_loss (the prefix property + the detected stall → ADMIT), maelstrom (chaos plus malice → ADMIT). Gate stage `storm` (4 rows: scenes / chaos / loss / refuse) + red-first `tests/test_storm.py` (10 falsifiers; five plants — the helpful loom (refusals hidden), the vacuous storm (delays gutted — vacuous against the naive floor, biting against the primary floor), the single-pass loom (no retries), the gapless oracle, the verdict-blind digest — each proven to BITE before the goldens pinned; stage non-vacuity three ways). Unit falsifiers 985 → 995; rows 595 → 599; NO placement change (URDRSTM1 joins the frontier). GRADE: convergence-under-chaos with exactly-once, the typed-chaos invariant with its measured primary floor, cross-seed agreement, the prefix property and detected stalls over measured drops, malice-under-chaos, the becalmed control, schedule and outcome determinism, and the lawfulness of the emission log are MEASURED (exact, reproducible, a defect diverges). DECLARED: the schedule FAMILY is a pinned corpus — real networks are worse in ways no corpus exhausts; the corpus is falsifiable coverage, not a proof of all weathers; GAP REPAIR (W4); sender authenticity (W3); REAL sockets (W5's off-gate attestation — this rung is the reason that attestation can be JUDGED: the laws reality must exhibit are pinned here first). `does_not_show`: wall-clock/bandwidth/congestion (`bench.py`); partitions between MULTIPLE authorities (single-authority storms — the mesh phase's problem); cross-placement. THE PHASE LEDGER: W1 landed, W2 landed; W3 `sealwrit` is next — the network now misbehaves inside the gate and the replica does not care; the remaining question is who may speak, and that is a signature's job.

**The sealwrit — the signed wire (URDRSWT1) — T3.49, W3 — MEASURED (eligibility precedes admission; sign-cannot-launder; every writ byte load-bearing; the first-admission-seals-the-keypair one-time law with free identical retry; the tail-collision forgery vs the first-byte defect verifier).** `tools/terrain/sealwrit.py` + gate stage `sealwrit`: W3 of the wire-phase brief — WHO may write, composed onto WHAT may change, closing the wire's declared authenticity hole with `authinput`'s own Lamport machinery (the same SHA-256 every placement hand-rolls; no new primitive). THE WRIT: the 104-byte regional record rides VERBATIM inside a 24,712-byte writ — MAGIC | i64 writer | i64 seq | record | pubkey (16,392 B) | signature (8,192 B) — whose message digest BINDS (writer, seq, record), so a writ states its own speaker and is meaningful in isolation; no trailing SHA is needed because every byte is covered by the signature chain (a flip in ANY section — writer, seq, record, pubkey, signature — refuses typed with replica and ledger byte-identical: the every-byte falsifier sweeps all five). ELIGIBILITY PRECEDES ADMISSION (N3's own ordering, kept): parse → roster membership → pubkey-to-pin → all 256 revealed preimages → the seal ledger → and ONLY THEN the state law, `wire.client_admit` UNMODIFIED (the storm's discipline: compose, never reimplement); the ordering is PROVEN, not narrated — the writ that is BOTH mis-signed and state-unlawful refuses SEAL-REFUSE, and the admission-first plant turns that refusal into WIRE-REFUSE and bites. SIGN-CANNOT-LAUNDER: a perfectly signed STALE record still refuses as WIRE-REFUSE — the state law's own voice, unweakened beneath the signature — and consumes nothing. THE SESSION'S DESIGN FIND — the one-time rule, retooled: authinput enforced one-keypair-one-message through N2's identity-uniqueness law (a second envelope under a used (peer, seq) was ROLLBACK-CONFLICT), but a retry-friendly transport (the storm's loom) requires the SAME writ to be redeliverable for free, so that anchor is gone; the replacement is THE FIRST ADMISSION SEALS THE KEYPAIR TO ITS DIGEST — the ledger binds each (writer, seq) to the message digest it first ADMITTED; an identical redelivery passes eligibility and dies at the CAS (at-most-once, the wire's own theorem, untouched); a verified-DISTINCT record under a sealed keypair — the reuse leak's EXACT exploit, state-lawful by construction in the fixture — refuses on the ledger; a state-refused writ seals NOTHING (eligibility is consumed by admission, never by attempt — reordering costs nothing); and the ledger records only VERIFIED admissions, so a garbage signature can never block the honest writ (no poisoning by anyone who cannot sign). NON-VACUITY, authinput's probe lifted to the writ: the first-byte defect verifier ACCEPTS the tail-collision forged writ (a different record whose writ digest shares its first byte, carrying the genuine signature) that the real 256-bit verifier refuses — all 256 digest bits load-bearing. Four pinned scenes: signet (three writers, six sealed writs, the replica mirroring the authority with all six keypairs sealed → ADMIT), impostor (five forgery shapes refuse pure, the genuine writ then admits → SEAL-REFUSE), burnt_seal (the retooled one-time law end-to-end → SEAL-REFUSE), precedence (the ordering proof + wire-voice + nothing-sealed-by-attempt → SEAL-REFUSE). Gate stage `sealwrit` (4 rows: scenes / provenance / reuse / order) + red-first `tests/test_sealwrit.py` (14 falsifiers; five plants — the trusting client (signature loop skipped), the first-byte verifier promoted to real, the amnesiac ledger (reuse law dropped), the admission-first ordering defect, the verdict-blind digest — each proven to BITE before the goldens pinned; the stage reddened per-row: golden drift → scenes, first-byte-real → provenance AND order, amnesiac → reuse). Unit falsifiers 995 → 1009; rows 599 → 603; NO placement change (URDRSWT1 joins the frontier — batch #3 falls due when the phase seals). GRADE: the writ format with every byte load-bearing, eligibility-precedes-admission, sign-cannot-launder, first-admission-seals with free identical retry and no poisoning-by-garbage, the forgery/defect-verifier separation, refuse purity on replica AND ledger, and determinism are MEASURED. DECLARED, honestly: fixture keys derive from PUBLISHED seeds (the gate pins MECHANISM, not secrecy — authinput's scope, inherited); a writer who signs two distinct messages under one keypair has leaked forgeability and the ledger only BOUNDS the damage (at most one admission per keypair — if a forger lands first, the honest writ refuses; the writer's violation, contained); the 24,712-byte writ does not fit a datagram (Lamport is the house scheme, not the wire-efficient one — the LAW certified here is scheme-independent, and W5's attestation may carry a compact signature under the same law). `does_not_show`: key distribution/rotation; wall-clock; the depicting client; cross-placement. THE PHASE LEDGER: W1 landed, W2 landed, W3 landed — the wire now refuses the unlawful AND the unauthorized, each in its own voice; W4 `driftgaze` is next: the client that MOVES, acquiring and releasing regions under its own certified demand, equal-or-refuse preserved across a changing resident set.

**The driftgaze — interest shift (URDRDGZ1) — T3.50, W4 — MEASURED (verified acquisition with five refusal shapes pure; clean release with never-guess; the mover equal across the shift; interest follows the gaze; re-acquisition carries history; stale acquisition detected; the gap repair paid both ways).** `tools/terrain/driftgaze.py` + gate stage `driftgaze`: W4 of the wire-phase brief — the client that MOVES. The wire certified the static resident set, the storm certified chaos against it, the sealwrit certified who may write to it; this rung makes the set itself LAWFUL MOTION, and it MINTS NOTHING — the client is the wire replica plus its grid dims, acquisition is `chunkload`'s fetch law holding records, admission is `wire.client_admit` untouched, the witness is the wire's own; every law is composition. THE LAWS: NEVER-UNVERIFIED — there is no path into residency except a verified subscribe, a verified admission, or a verified fetch; the acquisition refusal shapes (tampered bytes, a RE-SEALED coord-forged manifest — the integrity SHA honest, the coords check the one that refuses, proving it load-bearing — a missing store entry, an off-grid key, a dims-mismatched manifest) each refuse typed with the replica byte-identical. EQUAL-ACROSS-THE-SHIFT — the mover CHUNK-REFUSEs on the unloaded demand and, after the verified acquire, runs on the resident view EQUAL to the full-field glide bit-for-bit (chunkload's theorem, now under a resident set that changes beneath the walk, driven by the walk's own demand — the wayfarer scene walks three legs with acquire-ahead and release-behind computed from `demand_chunks` on the authority's evolving field). INTEREST FOLLOWS THE GAZE — `wire.relevant` against the LIVE resident set: a released region's update is irrelevant, an acquired one's admits and mirrors the authority. RE-ACQUISITION CARRIES HISTORY — the fetched chunk is the authority's CURRENT one, so missed updates are never replayed: they arrive as ALREADY-HISTORY and refuse at the CAS; catching up is a fetch, not a replay (the homecoming scene: release, two authority edits, re-acquire, the missed update refuses, the next live one admits). THE STALE ACQUISITION IS DETECTED — a lagging server's internally-consistent old world verifies at fetch time (the fetch checks integrity, not currency — currency is STATE) and the next live admission's CAS refuses: wire's necessity-with-detection, extended to acquisition. THE GAP REPAIR, the storm's declared debt PAID — the loss-stalled region (the storm's counted, typed stall) is repaired by a verified re-acquire of the head, BOTH WAYS (release-then-fetch and refresh-in-place — the gate's repair row exercises both, and the lazy-refresh plant that skips resident-key refresh reddens it); the redelivered stalled update refuses as history after each; the next live update admits; the replica lands on the authority's head with nothing trusted. Four pinned scenes: wayfarer (three legs, 2 acquired / 1 released / 1 admitted, the mover equal on every leg → ADMIT), crooked_fetch (five forgery shapes refuse pure, the genuine acquire lands the head → DRIFT-REFUSE), homecoming (→ ADMIT), stormrepair (→ ADMIT). Gate stage `driftgaze` (4 rows: scenes / fetch / shift / repair) + red-first `tests/test_driftgaze.py` (11 falsifiers; five plants — the trusting fetch (verification skipped), the sticky gaze (release keeps the region), the lazy refresh (resident keys never refetched — the session's OWN stage-hardening find: the first repair row released before re-acquiring, so this plant ESCAPED the stage; the row now exercises refresh-in-place and the plant reddens it), the polite refusal (ChunkError swallowed), the verdict-blind digest — each proven to BITE before the goldens pinned; per-row stage reddening: golden drift → scenes, the swallow → fetch, a transposed resident view → shift, the lazy refresh → repair). Unit falsifiers 1009 → 1020; rows 603 → 607; NO placement change (URDRDGZ1 joins the frontier — batch #3 falls due when the phase seals). GRADE: all seven law families MEASURED (exact, reproducible, a defect diverges). DECLARED, honestly: the demand POLICY (which regions to want, when to acquire-ahead/release-behind) is operational — the scenes drive it from `demand_chunks`, the law certified is the replica's evolution under ANY policy; WHO computes demand and whether a server may refuse a fetch (authorization of READS — the sealwrit's write-side twin) is declared to the mesh phase; fetch bandwidth and prefetch latency are `bench.py`'s. `does_not_show`: wall-clock; the depicting client; cross-placement. THE PHASE LEDGER: W1 landed, W2 landed, W3 landed, W4 landed — the replica now survives chaos, forgery, and its own motion; W5 is the attestation: the same laws carried over REAL transport on a named host, off-gate by law, judged by the Python laws after the fact — the phase's last rung, and the reality boundary crossed honestly.

**The wireattest — the reality attestation (URDRWAT1) — T3.51, W5 — MEASURED (the checker's laws with seven refusing forges; trace integrity; the mechanized named-host law; determinism) + MEASURED-named-host (the pinned trace itself) — THE WIRE PHASE SEALS.** `tools/terrain/wireattest.py` + gate stage `wireattest` + the pinned trace `spec/attest/wire_attest.txt`: W5, the phase capstone — the reality boundary crossed honestly. Sockets and wall-clock are nondeterministic, so by the house's own law they may not live inside the gate; the split: the RUN (off-gate, `--run` on a NAMED host) spawns real client and relay SUBPROCESSES over real loopback UDP — the relay a real chaos middlebox (seeded duplication, reorder by delayed forwarding, corrupt-duplicate malice, and, in the tempest, REAL drops), every client running the UNMODIFIED wire loom (refused deliveries requeued to fixpoint), the tempest's loss-stalled client repairing by a VERIFIED TCP fetch (the acquisition law over reality) — and leaves behind a SELF-DIGESTED TRACE; the CHECK (in-gate, pure) REPLAYS every recorded delivery through `wire.client_admit` and every recorded fetch against the authority's replayed head — reality's claims (outcomes, witnesses, addresses, even the authority's own update log) must MATCH what the law replays, or the attestation is UNLAWFUL. The gate certifies the laws; the attestation certifies reality met them; the trace is where they shake hands, and neither pretends to be the other. THE NAMED-HOST LAW, MECHANIZED: bench_protocol's rule (an unlogged MEASURED still reddens) becomes structure — a trace whose host line is empty or missing REFUSES, and the gate row NAMES the pinned trace's host in its output on every run. THE CHECKER'S LAWS, each with a refusing forge: a forged admission (targeted at a reorder-early refusal — the one lie only the outcome law can catch, chosen deliberately after the duplicate-targeting forge was found MASKED by the at-most-once layer: the layers are individually redundant and jointly load-bearing, rannull's discipline in the checker); a drifted client witness (silent drift, the wire's one poison); a double admission; an untyped outcome; a corrupt delivery claiming admission; a stalled client claiming the authority's witness (the prefix law over reality); and a CONSISTENT wrong-address fetch (the forge installs the wrong region's chunk AND records the witness that wrong fetch actually produces, so only the address law can refuse it — the first textual version was masked by the witness backstop and was unmasked at plan level, L15's find for this rung). THE PINNED RUN (real numbers from real sockets): 245 deliveries, 200 typed refusals, 58 corrupt-duplicate malice refusals, 3 stalls, 1 verified repair fetch, across gale + tempest, verdict LAWFUL — the storm's laws holding over an actual kernel network stack. Gate stage `wireattest` (4 rows: laws / forges / trace / selftest; per-row reddening proven — a malice-gutted synth builder → laws, the COHERENT trusting checker → forges, a tampered pinned trace → trace, the anonymous checker → selftest) + red-first `tests/test_wireattest.py` (13 falsifiers, synthetic traces only — the gate stays byte-deterministic; five plants each proven to BITE, with the address-blind-fetch plant's first fixture found masked and rebuilt). Unit falsifiers 1020 → 1033; rows 607 → 611. GRADE: the checker's laws, trace integrity (any byte flip refuses), the named-host rule, and check determinism are MEASURED; the pinned trace's verdict is MEASURED (named host) — the trace IS the host log. DECLARED, honestly: the run exercises loopback UDP on one machine (real sockets, real processes, real kernel buffering — not cross-machine networks, NAT, or the open internet); wall-clock latency is NOT claimed (`bench.py` §3's job); the relay's misbehavior is seeded and bounded (real networks are worse in ways no relay exhausts — the storm's declaration, inherited); the scaffold trace was minted in the cloud and is REPLACED by the named host's own run at ship time. `does_not_show`: throughput/bandwidth; the depicting client; cross-placement. THE PHASE LEDGER — THE WIRE PHASE IS SEALED: W1 the verifier client, W2 the deterministic storm, W3 the signature, W4 the moving gaze, W5 reality itself — equal-or-refuse replication now holds from the calculus to the kernel, and every rung earned its refusals red-first. THE COVENANT CALLS ITS DEBT: placement batch #3 (URDRWIR1, URDRSTM1, URDRSWT1, URDRDGZ1, URDRWAT1's checker) fell due at this seal — it is the next rung, before Phase V (the visible world: the WebGPU firewall client, bench §3) and Phase M (the mesh) open.

**Placement batch #3 — the wire phase, independently placed (`wirephase_rs`) — MEASURED (sixteen scene digests + two synthetic-attest report digests reproduced bit-for-bit, twice, defect-caught) — THE COVENANT'S DEBT PAID.** `tools/terrain/wirephase_rs/wirephase.rs` + gate stage `wirephase-placement`: the exit debt the wire phase's seal called due, closed. ONE std-only Rust file (no crates, no cargo, hand-rolled SHA-256) places all FIVE wire-phase families on the shared substrate (the URDRHF1 field, the chunk/manifest law, the URDRRAN0 record + shard_apply, the general fold — lifted verbatim from writecalc_rs): URDRWIR1 wire (equal-or-refuse replication, the four scenes faithful_mirror/narrow_gaze/crooked_wire/silent_drift), URDRSTM1 storm (the seeded schedule + retry loom + the primary-reordering measure, tempest/becalmed/gale_loss/maelstrom), URDRSWT1 sealwrit (the full Lamport machinery — keygen, the 16,392-byte pubkey, the 256-bit reveal signature, roster pins, the tail-collision forge — with eligibility-before-admission and the SEAL/WIRE typed split, signet/impostor/burnt_seal/precedence), URDRDGZ1 driftgaze (verified acquisition with all five refusal shapes, clean release, the mover EQUAL to the full-field glide over a changing resident set, the gap repair, wayfarer/crooked_fetch/homecoming/stormrepair), and URDRWAT1 wireattest — NOT merely the scene digests but the CHECKER RE-DERIVED: the Rust reproduces the synthetic gale/tempest traces (the shuffle + fixpoint loom that builds the gale plan, the tempest's real-loss + verified-fetch plan) and runs an independent port of `check_trace` over them, reproducing the two report digests (`67bac7bf…` gale, `6b33a132…` tempest) that Python's checker mints. All EIGHTEEN outputs reproduce bit-for-bit, twice, on rustc 1.95 in the cloud AND (at ship time) on the named Windows host; in-binary selfcheck (wire determinism, refuse purity, the becalmed control, the SEAL/genuine split) green. NON-VACUITY, the writecalc anchor discipline: the `minted_height` defect (new_h → _old_h, the no-op edit) makes shard_apply a nullity — and the gate's re-verification proves it load-bearing: FIFTEEN of the sixteen scene digests diverge (crooked_fetch alone is minted-independent — a pure fetch with no applied edit, correctly so) AND the attestation checker CRASHES on the no-op trace (the admitted-everything convergence law rejects it, so the binary exits nonzero → the gate reads empty output → caught). The one scene that cannot diverge is DECLARED, not hidden — its invariance is a property (fetch touches no minted cell), and fifteen-plus-a-crash is decisive. Placement count 31 → 32; rows 611 → 613 (the two `wirephase-placement` rows); unit falsifiers UNCHANGED (a placement is gate-verified, not a new unit suite — the writecalc precedent). GRADE: MEASURED both the cloud host (rustc 1.95.0) and the named host. DECLARED, honestly: the attest placement reproduces the SYNTHETIC-trace report digests (host-independent fixtures) — the REAL named-host trace's report digest is host-specific by construction and is not a cross-placement target (the trace itself is the cross-medium artifact the gate already re-verifies); the Lamport writ is the house scheme, not the wire-efficient one (the LAW is scheme-independent, as sealwrit declared). THE COVENANT: recorded ambitions become scheduled rungs or are honestly re-declared — batch #3 was scheduled at the wire phase's seal and is now PAID IN FULL. THE ROAD OPENS: with the debt closed, the window ladder (`AGENTS.md` §12) is unblocked — V1 `panelight`, the windowed loop (input → certified tick → witness → declared view → pixels), is the next NOVEL vertical slice; V0 was deliberate redundancy (the admission ladder's cross-placement rung), never a novel slice, and is now behind us.

**panelight — the windowed loop (URDRPNL1) — T3.52, V1 — MEASURED (interactive==batch; the accumulator's exactly-once + alpha-bound + total laws; the render/authority decoupling; the interpolation firewall) — THE VISIBLE WORLD OPENS.** `tools/terrain/panelight.py` + gate stage `panelight` + the declared window `tools/terrain/panelight.html`: the first rung of Phase V and the first NOVEL vertical slice after the wire phase sealed — the certified terrain driven not as a batch fold but as a LIVE INTERACTIVE GAME: input -> a fixed-timestep authority tick -> the witness -> a declared, interpolated view -> pixels, closed in a loop. THE MODULE MINTS ITS MOTION FROM `glide` (the quintessence discipline — a tick advances the avatar by one command from a RESUMABLE Q32.32 pose via `glide._fold_from`, the same fold `splice` resumes, so the loop is the certified mover CLOCKED, not a new mover). What panelight ADDS is three laws no batch fold has, and this is what makes it a genuinely distinct slice rather than glide re-dressed: (1) INTERACTIVE == BATCH — `run` clocked one command per tick reproduces `glide.glide_cells` BIT-FOR-BIT on every pure-move log, so a live game and its fold agree (the theorem that lets a played world be trusted); (2) THE ACCUMULATOR (frame/tick decoupling — the Source/Overwatch discipline made exact) — given a per-frame dt-log and TICK_MS=16, a frame runs floor(acc/TICK_MS) ticks and carries the remainder as alpha in [0,TICK_MS); each input is consumed EXACTLY ONCE (a schedule short of the log refuses PANEL-REFUSE, no silent skip), total ticks == floor(sum dt / TICK_MS), and THE DECOUPLING LAW holds — two different render cadences over the same input land the IDENTICAL authority witness while their DECLARED frame streams differ (render rate never moves the authority); (3) THE INTERPOLATION FIREWALL (D15 on TIME, the space firewall's temporal twin) — a frame renders a DECLARED pose lerped between two tick poses by alpha, an exact integer interpolation bounded strictly within the certified segment, and the witness is STRUCTURALLY blind to it (loop_witness's domain is the tick transcript alone — no frame or alpha can enter it; the firewall is enforced by the function's type, not a runtime check, which is why the naive alpha-into-witness plant is UN-writable and the firewall's non-vacuity is carried instead by the interpolation-bound plant plus the decoupling law). THE DECLARED WINDOW `panelight.html`: a self-contained keyboard-driven canvas game — WASD walk / Shift sprint, a real `performance.now()` accumulator feeding the same fixed-timestep loop, interpolated avatar rendering, the idle law (zero ticks while at rest), and — the honesty seam — it CITES the loop-witness it recomputes LIVE in JS (a byte-exact BigInt integer fold + a hand-rolled SHA-256), pressing '1' replays the pinned stroll scene and shows the scene digest EQUALS the gate's golden (`d04d1b2e…`, verified in node in-session): the pixels are declared, the citation is measured, and a viewer can check the window depicts the certified authority. Four pinned scenes: stroll (one tick per frame -> ADMIT), sprint (jittery cadence, authority unmoved -> ADMIT), wall (the terrain gate holds in the loop -> ADMIT), restful (idles rest the avatar byte-identically -> ADMIT). Gate stage `panelight` (4 rows: scenes / equiv / accum / firewall; per-row reddening proven — golden drift + walk-through-wall -> scenes, walk-through-wall -> equiv, lost-input -> accum, interpolation-overshoot -> firewall) + red-first `tests/test_panelight.py` (12 falsifiers; FIVE plants — walk-through-wall, lost-input, idle-drift, interpolation-overshoot, verdict-blind digest — each proven to BITE before the goldens pinned; the sixth candidate (alpha-into-witness) was found VACUOUS by construction and dropped with its reason recorded — the firewall's type forbids the leak, measure-don't-remember catching a plant that could not bite). Unit falsifiers 1033 -> 1045; rows 613 -> 617; suites 116 -> 117; NO placement change (URDRPNL1 joins the frontier). GRADE: all four law families MEASURED. DECLARED, honestly: WALL-CLOCK dt is nondeterministic and lives OFF-GATE — the gate certifies the accumulator over a PINNED dt-log, the window feeds it real deltas; the fps/latency NUMBERS are bench §3's named-host claim (V4), NOT made here; the RENDER is presentation behind the firewall; multiplayer ghosts are `ghostsnap` (V3, declared). `does_not_show`: fps/latency; the window's own correctness (declared); cross-placement. THE LADDER: V0 (batch #3) paid the covenant's debt; V1 `panelight` is the first NOVEL slice — the certified substrate is now SEEN and PLAYED. Next: V2 the wired window (the loop becomes a wire client — live terraform edits mid-play, driftgaze acquisition as the avatar crosses regions; two windows, one authority), then V3 `ghostsnap`, V4 the sealed frame (bench §3 input->photon), V5 the attested session. The OODA review's named vertical slice is landed; the next belongs at the V2/V3 seam where the windowed loop meets the wire.

**panewire — the wired window (URDRPNW1) — T3.53, V2 — MEASURED (resident-or-refuse + acquire-on-cross; live-edit-changes-the-walk; two-windows-one-authority; equal-or-refuse under play) — THE PLAYABLE NETWORKED WORLD.** `tools/terrain/panewire.py` + gate stage `panewire` + the declared two-window demo `tools/terrain/panewire.html`: V2 of the window ladder, and the rung the whole arc has been building toward — MOVEMENT (panelight's tick), REPLICATION (wire's equal-or-refuse admission), and STREAMING (driftgaze's verified acquisition) meeting to drive one avatar over a world it does not hold whole and did not author alone. THE MODULE MINTS NOTHING: the avatar folds with `glide`'s EXACT micro-step law, but through a RESIDENT getter (`chunkload.height_at`, which refuses on an unloaded chunk) instead of a full field — so the fold is the certified mover reading a replica; edits admit through `driftgaze.gaze_admit` = `wire.client_admit` unmodified; acquisition is `driftgaze.acquire` = `chunkload`'s verified fetch. The novelty is that the three only compose correctly under four new invariants, each a gate row: (1) RESIDENT-OR-REFUSE — the avatar reads the resident REPLICA, not the field; a step into an unresident region refuses (CHUNK-REFUSE, never walks on unloaded terrain) until the region is ACQUIRED by verified fetch from the CURRENT authority (interest follows the avatar — a walk east crossing the (0,1)->(1,1) seam refuses with streaming off and completes, equal to the full-field glide bit-for-bit, with streaming on); (2) LIVE EDIT CHANGES THE WALKED WORLD — a terraform edit admitted mid-play raises a wall in the avatar's path and STOPS a walk that would otherwise pass (the avatar halts west of the raised cell) — the world is live authority, read fresh each tick, not a backdrop; (3) TWO WINDOWS, ONE AUTHORITY — the composed witness (avatar transcript + replica) is a pure function of (input, edits): two independent runs land the IDENTICAL witness (the edit made in one view IS seen in the other, every byte admitted rather than trusted) and a different edit stream diverges; (4) EQUAL-OR-REFUSE UNDER PLAY — a tampered edit woven into the stream refuses mid-loop with the replica byte-unchanged AND the avatar's walk equal to the malice-free walk (malice moves neither the world nor the player — wire's verifier discipline under a live game). THE DECLARED WINDOW `panewire.html`: two canvases rendering ONE shared authority — an edit made with 'E' (a raised wall) appears in BOTH windows at once, the avatar (WASD) stops at it, and each pane shows the world-hash it computes live, staying EQUAL (the authority is shared, not trusted per-window). Four pinned scenes: crossing (streams in the seam region -> ADMIT), reshape (a wall stops the walk -> ADMIT), concord (two runs, one witness -> ADMIT), besieged (malice refused, walk unperturbed -> ADMIT). Gate stage `panewire` (4 rows: scenes / resident / live / concord; per-row reddening proven — golden drift + residency-bypass + dead-world + malice-admits -> scenes, residency-bypass -> resident, residency-bypass + dead-world -> live, malice-admits -> concord) + red-first `tests/test_panewire.py` (10 falsifiers; FIVE plants — residency bypass, dead world, credulous verifier, world-blind witness, verdict-blind digest — each proven to BITE before the goldens pinned; a sixth candidate NV plant that merely counted a refusal without admitting was found VACUOUS for the concord row (a no-op leaves the world clean, so the row correctly stayed green) and replaced with a malice-ADMITS plant that perturbs the world — measure-don't-remember, again). Unit falsifiers 1045 -> 1055; rows 617 -> 621; suites 117 -> 118; NO placement change (URDRPNW1 joins the frontier). GRADE: all four law families MEASURED. DECLARED, honestly: the demand/acquire POLICY is operational (the law certified is the loop's evolution under this policy — acquire the step's demand from the current authority); wall-clock frame timing is `panelight`'s accumulator, OFF-GATE; WHO may edit is `sealwrit`'s signature (composable, not re-proven here); ghosts (other actors) are `ghostsnap` (V3). `does_not_show`: fps/latency (bench §3, V4); the depicting client's own correctness (the two-window demo is declared); cross-placement. THE LADDER: V0 paid the debt, V1 made the world SEEN and PLAYED, V2 makes it REPLICATED and STREAMED — the brief's flagship demonstration lands (two windows, one authority, an edit in one appearing in the other, every byte admitted rather than trusted). Next: V3 `ghostsnap` (the actor wire — other players appear as certified ghosts, interpolation declared), then V4 the sealed frame (bench §3 input->photon MEASURED), V5 the attested session. The OODA review's named slice for the V2/V3 seam is reached; V3 is where the world becomes multiplayer.

**ghostsnap — the actor wire (URDRGHS1) — T3.54, V3 — MEASURED (equal-or-refuse ghosts; chain order + at-most-once; interest follows the observer; two clients one truth with a chaotic relay converging; the interpolation firewall) — THE WORLD MADE MULTIPLAYER.** `tools/terrain/ghostsnap.py` + gate stage `ghostsnap`: V3 of the window ladder — `wire` for ACTORS instead of terrain, the §10 ghostsnap layer landed. The industry's ghost-snapshot pattern (Unity's Netcode-for-Entities) streams server-authoritative entities to clients as per-tick snapshots the client TRUSTS, interpolating and predicting from bytes it cannot check; Hainuwele's counterpart makes a ghost a CONTENT-ADDRESSED per-tick POSE RECORD chained by parent digest (terraform's chain law, on the movement plane), admitted under the SAME equal-or-refuse discipline the terrain wire uses — the differentiated claim unchanged: not a cheaper ghost, a ghost that CANNOT LIE. THE GHOST SNAPSHOT (112 bytes): MAGIC | actor_id | tick | parent_addr | fx | fy | facing | SHA-256, content-addressed (the tail digest is its address AND its integrity check), chained (each snapshot's parent is the actor's previous snapshot address; the genesis chains from all-zeros — the spawn); NO sequence numbers, order is structural exactly as in the terrain wire. THE LAWS, each a gate row: (1) EQUAL-OR-REFUSE GHOSTS — a snapshot admits iff it VERIFIES (digest), the actor is IN INTEREST (within the observer's Chebyshev AoI radius — `interest.aoi`'s ground-truth distance, REUSED), and it CHAINS from the client's current ghost for that actor (parent CAS: genesis if unseen); a forged, tampered, stale, duplicated, or out-of-interest ghost is a typed GHOST-REFUSE with the ghost map byte-unchanged — never a silent teleport; (2) CHAIN ORDER + AT-MOST-ONCE — an out-of-order snapshot refuses on the parent then admits on the in-order retry, and a duplicate refuses (its admission moved the parent); (3) INTEREST FOLLOWS THE OBSERVER — an actor outside the radius is filtered (not relevant), a delivered out-of-interest ghost refuses; (4) TWO CLIENTS, ONE TRUTH — two clients (even at different observer positions, same interest) admitting the same ghost stream reach the IDENTICAL ghost witness, and a storm-style CHAOTIC RELAY (shuffled delivery through a retry loom) converges to the in-order witness with typed refusals for the out-of-order (the storm's convergence-under-chaos, on actors); (5) THE INTERPOLATION FIREWALL — a rendered ghost is interpolated between two snapshots (declared, exact integer lerp, bounded strictly between them), and the ghost witness is STRUCTURALLY blind to it (ghost_witness's domain is the ghost map alone — no interpolated frame can enter it, the firewall enforced by the function's type; D15 on actors — ghosts smooth the VIEW, never the witness). Four pinned scenes: muster (three actors' chains admitted in-order -> ADMIT), interloper (five forgery/interest violations refuse pure, genuine admits -> GHOST-REFUSE), relay (shuffled delivery converges -> ADMIT), concord (two clients one witness -> ADMIT). Gate stage `ghostsnap` (4 rows: scenes / admit / relay / firewall; per-row reddening proven — golden drift + chain-blind + interest-blind -> scenes, chain-blind + interest-blind -> admit, chain-blind -> relay, unbounded-interpolation -> firewall) + red-first `tests/test_ghostsnap.py` (10 falsifiers; FIVE plants — credulous verifier, chain-blind, interest-blind, unbounded interpolation, verdict-blind digest — each proven to BITE before the goldens pinned). Unit falsifiers 1055 -> 1065; rows 621 -> 625; suites 118 -> 119; NO placement change (URDRGHS1 joins the frontier). GRADE: all five law families MEASURED. DECLARED, honestly: WHO authors a ghost is `sealwrit`'s signature (a ghost admits whoever relays a lawful chain — the signed-ghost successor is composition, not re-proven here); PREDICTION of the LOCAL actor is `predict`/`cpredict`/`splice` (certified already — the local player is predicted, remote actors are ghosts); the interest POLICY (radius, buckets) is operational; wall-clock interpolation timing is `panelight`'s accumulator, OFF-GATE. `does_not_show`: fps/latency (bench §3, V4); the depicting client; cross-placement. THE LADDER: V0 paid the debt, V1 SEEN, V2 REPLICATED+STREAMED, V3 MULTIPLAYER — the world now carries other players, each a ghost that cannot lie. Next: V4 the sealed frame (bench §3 input->photon MEASURED on the named host — the fps/latency numbers become a claim), then V5 the attested session (a play session recorded as a self-digested trace the gate replays — the demo becomes a proof). The visible-world phase is one measurement and one attestation from its capstone.

**sealframe — the sealed frame (URDRSFR1) — T3.55, V4 — MEASURED (the op-cost envelope, deterministic + fits-the-budget; the budget honesty with the unlogged-MEASURED defect caught; the host-log integrity + named-host graduation law).** `tools/terrain/sealframe.py` + gate stage `sealframe`: V4 of the window ladder — the windowed loop's PERFORMANCE, graded honestly, answering "how fast" without lying about it. The house keeps two halves apart, exactly as `bench_protocol` and `frontbench-budget` demand: (1) THE WORK ACCOUNTING (MEASURED, host-independent, GATED) — `frame_ops` is the EXACT integer count of the micro-steps and height reads one frame's authority tick performs (deterministic, pinned, a wrong count diverges — and cross-checked model==execution against `glide`'s own micro-trajectory length, so a miscounted envelope reddens); this is what BOUNDS the wall-clock (the opcost discipline, on the visible loop), and it is a CHECKABLE INEQUALITY — the op envelope fits the 60Hz budget under the MEASURED native tick rate (bench §4b's ~0.073ms) with vast headroom — the "high fps is architecturally cheap" claim made as arithmetic, not a stopwatch; (2) THE WALL-CLOCK (NOT_MEASURED until a §3 run, then MEASURED-on-named-host) — fps and input->photon latency are nondeterministic and may not live inside the gate; the FRAME_BUDGET manifest grades each frame component, and THE HONESTY BOUNDARY is mechanized — every entry graded MEASURED must cite a named-host log; a MEASURED-without-a-log is the dishonesty the gate forbids (the `frontbench-budget` rule, applied to the frame; the unlogged-MEASURED defect on `input_to_photon` is the non-vacuity control, caught); `input_to_photon` stays NOT_MEASURED, `authority_tick` is MEASURED because it cites the real sim-tick log (bench §4b, ROG Ally X). THE HOST LOG — a self-digested named-host record (host line, native tick ns, input->photon median/p95); the off-gate `--bench` runner times the real loop (via `time.perf_counter_ns` — why it is OFF-GATE) and writes it; a byte flip refuses, and an ANONYMOUS log cannot graduate a claim (the named-host law, mechanized: `frame_budget_measured` refuses an unnamed log and returns honest-False for a missing or over-target reading). Four pinned scenes: walk / sprint / restful / budget (the op-envelope digests + the honest-budget verdict). Gate stage `sealframe` (4 rows: scenes / envelope / honesty / selftest; per-row reddening proven — golden drift + miscount -> scenes, miscount -> envelope, dishonest-budget -> honesty, digest-blind-log -> selftest) + red-first `tests/test_sealframe.py` (13 falsifiers; SIX plants — miscounted envelope, dishonest budget, anonymous-log graduation, over-target graduation, digest-blind log, verdict-blind digest — each proven to BITE before the goldens pinned; the reads-miscount is a SCENE-digest plant, correctly caught by the pinned envelope rather than a pre-pin unit assertion, and the model==execution cross-check was made genuinely INDEPENDENT (glide-trajectory length) so a micro-step miscount bites directly). Unit falsifiers 1065 -> 1078; rows 625 -> 629; suites 119 -> 120; NO placement change (URDRSFR1 joins the frontier). GRADE: the op envelope, the budget honesty, the host-log integrity + named-host law, and the fits-the-budget inequality are MEASURED. DECLARED, honestly and centrally: the WALL-CLOCK NUMBERS (fps, input->photon ms) are NOT claimed here — this rung is the MACHINERY that lets their graduation be honest, not the measurement; they become MEASURED only when the named host runs `sealframe.py --bench` and the operator graduates the FRAME_BUDGET entry citing that log (and the render/photon-capture path is the operator's to run — the op envelope covers the AUTHORITY work, not the pixels). The cloud `--bench` produced a ~13µs native loop for the four-command "EEEE" scene with input->photon correctly NOT measured; that scaffold is NOT committed (the gate re-verifies the op envelope + honesty rule, never a host-specific log — so a shipped scaffold would only rot, unlike wireattest's gate-re-verified trace). `does_not_show`: any ms/fps number as MEASURED without a host log (structurally forbidden); the render/capture path; cross-placement. THE LADDER: V0 paid the debt, V1 SEEN, V2 REPLICATED+STREAMED, V3 MULTIPLAYER, V4 the frame graded HONESTLY — the performance question answered the only way the house allows, work gated and wall-clock deferred to the named host. Next: V5 the attested session — the visible-world capstone: a play session recorded as a self-digested input+outcome trace the gate replays to the same witnesses (the wireattest discipline for a windowed session), the demo becoming a proof. One rung from the capstone.

**Named-host graduation (no new capability, no count change): the sealed frame's `native_loop` row is MEASURED (Ally X).** The V4 `sealframe` off-gate `--bench` runner was run on the named host (`DanielDillberg · Windows 11 · ROG Ally X`, 2026-07-20): the four-command sprint reference loop ("EEEE", 4 authority ticks, 32 micro-steps) clocks a **median 8 800 ns (0.0088 ms)** native loop — ~2.2 µs per authority tick, ~0.05% of a 60 Hz frame, confirming the gated op-envelope's fits-the-budget inequality against real wall-clock with ~1900× headroom on this machine. Recorded as `bench_protocol.md §4c` (the host log) and the `native_loop` FRAME_BUDGET row graduates to MEASURED citing it — the same discipline that graduated the sim tick at §4b. THE BOUNDARY HOLDS: this is the AUTHORITY-tick wall-clock only; **input→photon stays NOT_MEASURED** until the layer-3 renderer + photon capture exist, and the `sealframe-honesty` gate still enforces that any MEASURED frame entry cite a host log (the unlogged-MEASURED defect still reddens). No unit falsifier, gate row, or placement count changes — the scene digests are unchanged (the honest verdict is unmoved by a MEASURED-with-log entry); only the manifest and the protocol gain a named-host reading. The performance answer, made honest and now carrying one real number.

**sealsession — the attested session (URDRSSN1) — T3.56, V5 — MEASURED (the checker's laws each with a refusing forge — avatar / world / ghost / malice; trace integrity; the named-host rule; determinism) — THE VISIBLE-WORLD PHASE (V) IS SEALED.** `tools/terrain/sealsession.py` + gate stage `sealsession`: V5, the capstone. `wireattest` proved the NETWORK met the laws; this proves a PLAY SESSION did. A session composes the whole visible world Hainuwele built — the loop (V1 `panelight`), the wired world (V2 `panewire`: live terraform edits over a streamed, region-acquired terrain), and the ghosts (V3 `ghostsnap`: other actors as equal-or-refuse pose snapshots) — and records it as a SELF-DIGESTED TRACE: the input the player pressed, the edits that arrived, the ghost stream, and the THREE witnesses the session produced (avatar, world, ghosts). THE SPLIT (the wireattest discipline, on a session): the RUN is OFF-GATE (a human plays; the frame cadence is wall-clock, nondeterministic — it may not live in the gate), and what crosses the boundary is the trace (the INPUT, a deterministic authority driver, plus the witnesses reality produced); the CHECK is PURE — the gate REPLAYS the recorded input through the UNMODIFIED laws (`panewire.run_wired` for the avatar + world, `ghostsnap.ghost_admit` for the ghosts) and verifies every recorded witness MATCHES what the law replays; reality's claims must equal the law's, or the session is UNLAWFUL. The gate certifies the laws; the attested session certifies a PLAYTHROUGH met them; neither pretends to be the other — the demo stops being a video and becomes a PROOF. THE LAWS, each a gate row with a refusing forge: a forged AVATAR witness (a session claiming a movement outcome the recorded input does not produce) refuses; a forged WORLD witness (claiming a terrain the recorded edits do not produce) refuses; a forged GHOST witness (claiming an actor cohort the recorded stream does not produce) refuses; a CHEATER'S MALICE-CLAIMED EDIT (an illegal edit dressed as admitted, its world witness left as the lawful world's — the exact exploit a session attestation must catch) refuses because the replay refuses the malice and the world witness mismatches; an UNNAMED session refuses (the named-host law, mechanized); a TAMPERED trace refuses on the self-digest. THE MALICE FIND: panewire GRACEFULLY refuses malice during play (it does not crash — a refused edit, counted), so a session merely containing malice is LAWFUL; the session-level attack is a cheater who KEEPS the lawful world witness while swapping the edit for malice, claiming the illegal edit took effect — caught by the world-witness replay, not by a crash. Four pinned scenes: stroll (movement only), wired (live edits + streaming), multiplayer (+ a two-actor ghost stream), witnessed (the multiplayer report's host-independent core — the whole visible world in one number). Gate stage `sealsession` (4 rows: scenes / lawful / forge / selftest; per-row reddening proven — golden drift -> scenes, credulous world/avatar -> forge, anonymous -> selftest) + red-first `tests/test_sealsession.py` (13 falsifiers; FIVE plants — credulous avatar / world / ghost verifiers, anonymous session, digest-blind parse — each proven to BITE before the goldens pinned). Unit falsifiers 1078 -> 1091; rows 629 -> 633; suites 120 -> 121; NO placement change (URDRSSN1 joins the frontier). GRADE: the checker's laws, trace integrity, the named-host rule, and determinism are MEASURED; a real playthrough's attestation is MEASURED-on-named-host (the trace IS the host log — the off-gate `--record` runner captures it). DECLARED, honestly: the RUN's wall-clock frame cadence is OFF-GATE (fps/latency are V4 `sealframe`'s territory, not re-claimed); the RENDER (the pixels the player saw) is presentation behind the D15 firewall, never in the witness — a session proves the AUTHORITY evolution was lawful, not that the pixels were pretty. `does_not_show`: fps/latency; the render; cross-placement. THE LADDER, COMPLETE: V0 paid the covenant's debt, V1 the world SEEN, V2 REPLICATED + STREAMED, V3 MULTIPLAYER, V4 the frame graded HONESTLY (and carrying the Ally X native-loop number), V5 the playthrough ATTESTED — a recorded session whose every authority byte is admitted rather than trusted. THE VISIBLE-WORLD PHASE IS SEALED. The road ahead, declared: Phase M — the certified mesh (authority migration as lease transfer between shards, n-way nullity as the write scheduler, the authority algebra as the allocator; the answer to server meshing that cannot lie), and the standing debts (the authority algebra, essence extended to the read-side families, the placement batches that fall due as phases seal). The whole stack now stands from the sealed language kernel to an attested playthrough of a certified, adversarial, multiplayer world — every rung red-first, every claim graded, every byte admitted rather than trusted.

**Named-host confirmation (no new capability, no count change): the V5 attested session reproduced bit-for-bit on the Ally X.** The `sealsession.py --record` runner was run on the named host (`DanielDillberg · Windows 11 · ROG Ally X`, 2026-07-20): the capstone playthrough (6 ticks, 1 live terraform edit, a 5-snapshot two-actor ghost stream) verified **LAWFUL**, producing avatar witness `7c52f344…`, world witness `be44a814…`, and ghost witness `3129ee27…`. Those three witnesses are BYTE-IDENTICAL to the ones this repo's laws produce in the cloud (Linux) from the same session — a CROSS-HOST reproduction of the sealed capstone across two OSes: the whole visible world (avatar + live world + ghosts) attested to the same authority on both machines. Because the session witnesses are deterministic given the recorded input (host-independent by construction — the wall-clock frame cadence lives off-gate and never enters the witness), the gate's `sealsession:scenes` row already proves them on every host; the named-host `--record` run confirms the Ally X reproduces them exactly. The recorded trace is the operator's own (not committed — like the sealframe host log, the gate re-verifies the deterministic laws, not a host-specific file; the witnesses it would carry are already the pinned ones). No unit falsifier / gate row / placement count changes. The demo, made a proof, now confirmed on the named host: a playthrough of a certified, adversarial, multiplayer world whose every authority byte is admitted rather than trusted, reproducing across two machines.

**Pre-mesh hardening review (no new capability, no count change): a web-researched adversarial debate, and the Tier-1 honesty consolidation.** At the sealed visible-world capstone (V1–V5, `224e61e`), a deep OODA + web-research + adversarial debate was run before opening Phase M (the mesh): three INDEPENDENT skeptics (a distributed-systems engineer, a formal-methods researcher, a game-security engineer) each read the repo and produced their strongest genuine objections, crediting the honest `DECLARED`/`does_not_show` blocks rather than double-counting them. THE CONVERGENT VERDICT (all three, independently): the code and this ledger are exceptionally honest; the HEADLINE RHETORIC consistently sat one notch above what corpus-plus-single-mutant-plus-fixed-seed evidence delivers — an epistemic-scope gap, not a correctness failure. Two of the three, from different disciplines, independently named the SAME technical #1: property-based/randomized falsifiers over an oracle (which the repo already patterns in `test_physics_properties` / `test_interest`). The full brief is `docs/hardening_brief.md`. THE ONE GENUINE UN-DECLARED CORRECTNESS GAP found: `ghostsnap.ghost_admit` checks a snapshot's bytes + chain + interest but NOT its kinematics — a remote ghost can teleport / wall-clip and still admit ("a ghost that cannot lie" holds of its bytes, not its motion). TIER 1 (DONE, this rung, doc-only): the headline aligned to the ledger — the README gains a "game-world threat model" (state-cheats DETECTED; information/input/collusion cheats NOT prevented, the server-authority ceiling stated; the AoI radius is not occlusion and is server-owned; authentication is a keyless mechanism; a safety-only availability posture — no liveness under partition/Byzantine, that being Phase M's burden), the verification-scope stated (∀-laws MEASURED existentially on a pinned corpus; plants are single curated mutants — catchability, not a kill-score; no coverage metric yet), the `ghostsnap` docstring declares the kinematic gap, and the baldest overclaim (AGENTS §11 "cheating is structurally impossible") is requalified to "STATE-cheats structurally DETECTED". TIER 2 (queued next rung): the property-based / metamorphic falsifier stage — generalize the repo's own seeded-generator + brute-oracle pattern to the write-calculus / wire / ghost ∀-laws, fixed-seed in-gate + an off-gate reseeded explorer feeding counterexamples back as pinned scenes (the wireattest off-gate→pinned discipline, for input exploration). TIER 2b (LANDED in this same rung — see the follow-on entry): the ghost kinematic check — `warden` reachability wired into `ghost_admit`. TIER 3 (declared): off-gate mutation-score + coverage as DECLARED metrics. Tier 1 alone is doc-only; FOLDED with Tier 2b in this pre-mesh hardening rung the gate moves 1091 / 633 → 1098 / 634 (see the follow-on entry). The covenant records these tiers as scheduled hardening before Phase M opens; the honesty that was the arc's method is now also its documented boundary.

**Tier 2b LANDED — the ghost kinematic law (`ghostsnap`, URDRGHS1; folded into the pre-mesh hardening rung): a warded ghost that cannot lie about its MOTION. 1091 → 1098 unit falsifiers, 633 → 634 rows.** The one genuine un-declared correctness gap the debate found is closed. `ghostsnap.subscribe_ghosts` now optionally holds a TERRAIN REPLICA (walkable `field` + step law `max_step`); a client that holds terrain is WARDED, and `ghost_admit` runs the new `ghost_reachable` between the held parent snapshot and the incoming one. THE KINEMATIC LAW, reusing `warden` verbatim (the movement certificate the LOCAL actor already obeys): (i) TIME — Δtick ≥ 1; (ii) REACHABILITY — the destination cell is in the parent cell's walkable component (β₀, `warden.reachable`) — a wall-clip into a separated region refuses even when SLOW enough to pass the speed bound (the topological cheat a per-tick replay cannot cheaply catch); (iii) GAIT+STEP — `|Δx|+|Δy| ≤ MAX_JUMP·Δtick`, and for a single-tick move the exact `warden.step_kind` (cardinal, ≤2 cells, every sub-step's rise ≤ max_step). A teleport, speed-hack, or wall-clip is a typed GHOST-REFUSE with the ghost map BYTE-IDENTICAL (equal-or-refuse, on motion). RED-FIRST (L15): the baseline gap was demonstrated (a 76-cell teleport admitted today), then four warded scenes pinned AFTER the disabled-check plant was proven to bite — `warded_muster` (honest 1-cell/tick walk ADMITS — the non-vacuity control), `warded_teleport` (12-cell jump REFUSES — gait), `warded_wallclip` ((6,8)@t0→(10,8)@t4 REFUSES — passes the speed bound, crosses the β₀ wall), `unwarded_teleport` (a terrain-less client ADMITS the teleport — THE TERRAIN-GATED BOUNDARY made executable). Plus a `ghostsnap-kinematic` gate row (per-row reddening proven with the defanged check) and 7 falsifiers. BACKWARD-COMPATIBLE: `field=None` clients are bytes-only exactly as before — the four original scenes reproduce their goldens unchanged; the residual (terrain-gated admission; directed reachability, inheriting `warden`'s undirected-components `does_not_show`) is DECLARED and, for the terrain-gating, executable. The flagship "a ghost that cannot lie" now covers MOTION as well as BYTES for a warded client. WHEN THE REST LANDS (the schedule, `docs/hardening_brief.md`): Tier 2 (property-based falsifiers) = next rung, pre-mesh; the AIMBOT/ANTI-CHEAT posture = a THREE-BAND phased roadmap — Band A server-driven AoI delivery WITH Phase M (required; bounds ESP/wallhack by what the server reveals), Band B behavioural/statistical detection post-Phase-M (DECLARED off-gate — a probabilistic detector may not be graded MEASURED), Band C input-rate/humanness bounds post-Phase-M (partial) — because aimbot/botting via LAWFUL inputs is unaddressable at the authority layer and must never be claimed as structurally prevented; Tier 3 (mutation-score + coverage) = off-gate, DECLARED, no rung dependency. Verified: gate ×2 byte-identical, GATE PASSED at 1098 / 634; doc-currency green (counts cascaded across the seven tracked docs + AGENTS.md).

**Tier 2 PART 1 LANDED — the property-based falsifier stage (`commuteprop`, URDRCPS1): the commute diamond faces a seeded adversary. 1098 → 1106 unit falsifiers, 634 → 636 rows.** The pre-mesh hardening review's technical #1 (two of three independent skeptics): the flagship ∀-laws are verified existentially on tiny curated corpora, so digest-pinning cannot distinguish "correct" from "consistently wrong on the sampled points." This rung promotes property-based / randomized falsifiers with an INDEPENDENT ORACLE to a first-class discipline, applied to the write-calculus commute diamond (the review's first-named example). `commuteprop.sweep` runs a fixed-seed (20260720) integer-LCG generator over 200 scenarios — random 8×8 worlds, random 2–3 distinct-cell edit tuples at C=4 — and asserts the ∀-law against oracles the module-under-test CANNOT read (the anti-Goodhart / neutral-ruler rule): (i) THE DIAMOND — a brute-permutation ORBIT (apply every order of the explicitly-rebased edits from the base world) must land ONE head manifest AND one field, using only `terraform.apply_edit` + the explicit rebase, never `commute`; (ii) CLOSURE AGREES — `commute.closure`'s head equals the brute orbit's single head; (iii) RANK — `commute.predict` matches chunk geometry recomputed independently (`(x//c,y//c)` comparison), NOT `certify`'s internal predict — the circularity that first let a `predict`-always-0 mutant slip past the sweep until the oracle was made external (an engineering-rigor find, recorded honestly); (iv) CONTESTED — a same-cell pair must raise COMMUTE-REFUSE. NON-VACUITY is asserted, not hoped: rank0>0 AND rank1>0 (both 0 would be a degenerate generator), every scenario changed the world, and all 200 contested pairs refused — a vacuous sweep raises `COMMUTEPROP-FALSIFIED` itself. RED-FIRST (L15): proven to bite mutants of `predict` (rank), `closure` (head), and `certify` (contested-arbitration) — each makes `sweep` RAISE rather than return a digest; the aggregate digest (SHA-256 over the ordered head|sorted-ranks of every scenario) was pinned only AFTER the bites, and a `commute-property-selftest` gate row reddens under a planted `predict` defect and returns to green after the revert. THE SPLIT (wireattest discipline, on a search): the IN-GATE sweep is fixed-seed (byte-identical, digest pins); the OFF-GATE `--explore` reseeds across many seeds and files the first counterexample as a new pinned scene — existence-on-a-corpus becomes confidence-over-a-sampled-space. DECLARED: this is SAMPLING, not a proof over all worlds — MEASURED stays "the ∀-law survived N seeded adversarial scenarios on a named host," never universal (`intautology` is the road to decided-for-all). does_not_show: the off-gate explorer's reseeded search (declared, not gate-run); `reunify == monolith` and the storm prefix-property (the stage's next flagship targets — QUEUED before Phase M on the same harness); cross-placement (URDRCPS1 is Python reference only). New: `tools/terrain/commuteprop.py`, `tools/terrain/conformance_commuteprop.txt`, `tests/test_commuteprop.py` (8 falsifiers), gate stage `commuteprop` (2 rows: property / selftest). Verified: gate ×2 byte-identical, GATE PASSED at 1106 / 636; doc-currency green.

**Anti-cheat home — a PLANNING DROP (no code, no count change): `tools/anticheat/README.md`.** A web-researched design drop opening the anti-cheat subsystem the pre-mesh review's aimbot roadmap named. Researched against the 2026 state of the art (cited in the doc): the systematic review of technical defenses (arXiv 2512.21377 — the server-side ceiling is explicit: no visibility into overlays, render-only wallhacks, or external vision-based aimbots, and none against lawful inputs); server-owned AoI / occlusion as shipped by Valorant's Fog of War and the CS2FOW project (a real information-cheat mitigation, but costed — peeker's advantage, resident hitboxes leaking via collision, audio leakage, static-geometry-only, and the smooth-vs-fair tradeoff Valve abandoned in Source 2); and server-side behavioural detection (arXiv 2607.04336 — 29 server-observable features, stacked-LSTM 88.6% acc / 0.97% FPR, "temporal context is the essential signal") which is irreducibly statistical. THE ARCHITECTURE (the decision that makes anti-cheat belong in this repo): split what can be PROVEN from what can only be DECLARED. (1) The deterministic **evidence membrane** — an exact, replayable function folding the `sealsession` accepted-action trace into a content-addressed feature stream (delta view-angles, aim-arc, reaction-time windows, hit distributions) behind the D15 firewall — is GATE-GRADABLE / MEASURED (reproduces bit-for-bit; a trace tamper moves the feature digest; a detector version reruns deterministically on the frozen stream — the proof-carrying substrate the industry lacks). It is the first queued rung inside the folder, buildable now on the existing trace format. (2) The **statistical detectors** that consume it stay OFF-GATE, DECLARED, never MEASURED — grading a probabilistic detector (FPR ≠ 0) as MEASURED would be the exact dishonesty the review exists to kill; their value is auditability + replayability on the immutable stream, scored on precision/FPR on a named host. Band A occlusion lands WITH Phase M (the mesh owns delivery). The honest boundary, restated: this is an EVIDENCE-producing subsystem — it cannot certify human intent, stop a lawful-input aimbot, or see an external bot; it accumulates deterministic, replayable, auditable evidence. Structure chosen (with the operator): a SUBFOLDER on main (one gate; the membrane graded, the detectors declared off-gate), scope a design-doc drop (no code). No falsifier / row / placement count change — the gate stays green at 1106 / 636. Cross-referenced from `docs/hardening_brief.md`'s hardening schedule.

**Tier 2 COMPLETE — the property-based falsifier stage extended to the last two flagship ∀-laws (`regionprop` URDRRGP1 + `stormprop` URDRSTP1). 1106 → 1116 unit falsifiers, 636 → 640 rows.** With the commute diamond already swept (URDRCPS1), this rung finishes Tier 2 before Phase M by sweeping the two remaining flagship universals against INDEPENDENT oracles. **reunify == monolith (`tools/netcode/regionprop.py`, URDRRGP1):** the Seam Composition Theorem (D16) says any valid spatial partition reunifies to the monolithic witness bit-for-bit; the gate checked six hand-chosen seams. `regionprop.sweep` mints 200 random valid partitions (strictly-increasing integer x-seams, 1..4 regions) of the in-scope `seam2` world and asserts each `worldregion.region_simulate` equals `worldstep.simulate` — the MONOLITH, the independent oracle that never partitions, never admits a ghost, never consults `worldregion`. SCOPE (declared): the sweep varies the PARTITION not the world, because `worldregion`'s per-region contact pass is exact only for the single-cross-seam-pair regime — randomising worlds could mint out-of-scope scenes and falsely falsify a DECLARED law; the theorem's own quantifier (∀ valid partition) is what is swept. Non-vacuity: ≥3 distinct region counts and an evolving monolith (>1 frame). Red-first: the dropped-boundary defect (`defect_drop_ghost`) diverges from the monolith and a malformed partition is REGION-REFUSEd — both make the sweep raise REGIONPROP-FALSIFIED before the golden pinned. **The storm prefix property (`tools/terrain/stormprop.py`, URDRSTP1):** `stormprop.sweep` mints 200 random storms `(seed, loss%, dup%, delay_max)` and drives one honest wire client (`storm.run_client`, the landed `wire.client_admit` UNMODIFIED); loss-free storms must converge to the authority's own witness (`storm.authority_log`'s `want`) exactly-once, and lossy storms must equal `storm.prefix_witness` — per region the authority's prefix up to the first dropped in-region update, computed WITHOUT the retry loom (the independent oracle). LOAD-BEARING: the sweep asserts a strict-prefix case occurs (`prefix_witness != want` under a real gap), so a broken client that admitted past a gap (converging to `want`) would be caught — else the two branches would be indistinguishable. Non-vacuity: both branches, and nonzero total drops / reorderings / duplicates / detected stalls (104 lossy, 96 loss-free, 258 drops, 632 stalls, 93 strict prefixes on the pinned seed). Red-first: a wrong prefix oracle (the full-log witness) makes the sweep raise STORMPROP-FALSIFIED on a lossy storm. ENGINEERING-RIGOR FIND (recorded): the integer LCG's LOW bits cycle with tiny period, so `nxt() % k` for small k is degenerate — it made every generated partition the same shape until the generator was switched to the HIGH bits (`nxt() >> 16 % k`); commuteprop (already shipped) is unaffected because its asserted non-vacuity — both ranks — was genuinely exercised regardless. Both sweeps: fixed-seed in-gate (byte-identical, an aggregate digest pins) + an off-gate `--explore` reseeder that files counterexamples. Two gate stages, 4 rows (region-property / -selftest, storm-property / -selftest), per-row reddening proven; 10 falsifiers. DECLARED: SAMPLING, not a proof over all worlds/storms — MEASURED stays "the ∀-law survived N seeded adversarial scenarios on a named host." New: `tools/netcode/regionprop.py`, `tools/netcode/conformance_regionprop.txt`, `tools/terrain/stormprop.py`, `tools/terrain/conformance_stormprop.txt`, `tests/test_regionprop.py`, `tests/test_stormprop.py`. Verified: gate ×2 byte-identical, GATE PASSED at 1116 / 640; doc-currency green. Tier 2 is complete; the road ahead is Phase M — the certified mesh.

**Phase M design pass — a DESIGN DROP (no code, no count change): `docs/mesh_phase_brief.md`.** Opening the certified mesh design-first, because the challenge is no longer implementing a mechanism but showing the mechanisms already built COMPOSE into a protocol whose guarantees can be stated and falsified — `rannull`'s own words: "distributed execution becomes a theorem obtained by composing proofs, not a new subsystem." THE COMPOSITION (each primitive → its mesh role): `lease` (URDRLSE1) → authority migration as lease transfer (single-writer-per-region structural via the CAS, its own declared successor "n-way lease scheduling / the independence lattice"); `rannull`/RAN-0 (URDRRAN0) → the write scheduler (pairwise nullity generalised N-way: disjoint authorities parallel, overlap refuses); `worldregion`/D16 Seam Composition (now `regionprop`-swept) → spatial composition, MESH == MONOLITH with the partition MIGRATING; `commute` → same-region concurrency (rank 0/1); `wire`+`storm` (prefix property, `stormprop`-swept) → replication under an adversarial transport with partition→prefix; `sealwrit` → authority identity; `driftgaze`+`interest` → streaming. THE CAP POSTURE, STATED (per Gilbert & Brewer, cited): the arc is CP — consistency + partition-tolerance, sacrificing availability (a stale lease / divergent replica / gapped region already refuses; correctness over responsiveness, "refuse rather than guess"). The availability cost is DECLARED (no liveness under partition/Byzantine; a migration that cannot verify refuses). Recommendation (the one load-bearing fork, offered for the operator): stay CP; treat a quorum/consensus progress layer as a named optional overlay, never folded into the certified core (it introduces a trusted majority — a different trust model). SIX FALSIFIABLE GUARANTEES named, each a ∀-law with a plant (single-writer; migration preserves the witness; MESH == MONOLITH; n-way nullity schedules correctly; partition→prefix; the attested mesh session) — several directly property-sweepable with the Tier-2 harness. RUNG ROADMAP: M1 the lease lattice / n-way nullity scheduler; M2 authority migration (lease transfer); M3 the meshed simulation (MESH == MONOLITH, generalising regionprop to migrating authorities); M4 the partitioned mesh (CAP executable); M5 the attested mesh session (the capstone — server meshing that cannot lie). Industry contrast (researched): Unity distributed authority TRUSTS the owner ("no single physics simulation governing all objects"); Star Citizen dynamic meshing is the flagship ambition "called technically impossible" — Phase M's differentiator is that authority is CERTIFIED and MESH == MONOLITH is a THEOREM (bit-identical to a single simulation), not a best-effort convergence. Grades: DECLARED / SPECULATIVE→SCOPED — nothing built; each guarantee becomes MEASURED only when its rung lands red-first. No falsifier / row / placement count change — the gate stays green at 1116 / 640. Cross-referenced from AGENTS §10 and `docs/README.md`.

**Phase M rung M1 LANDED — n-way nullity + the independence lattice (`tools/terrain/nway.py`, URDRNWY1): the certified mesh's write scheduler. 1116 → 1125 unit falsifiers, 640 → 644 rows.** The first CODE rung of Phase M, and a COMPOSITION rather than a new mechanism (the design brief's thesis, executed). `rannull`/RAN-0 proved PAIRWISE nullity (two regional edits on disjoint authorities execute concurrently, four heads equal, zero rebases); `lease` named the successor: "n-way lease scheduling and the independence lattice as a queryable allocator." M1 delivers it. THE N-WAY NULLITY THEOREM: for N regional edits whose authorities are PAIRWISE DISJOINT, the PARALLEL shard execution (`rannull.shard_apply` each against its own chunk, then one address-substituting `reunify` of all N new chunks) equals EVERY one of the N! serial-order executions, bit-for-bit, ZERO rebases — each record byte-unchanged in every order, because no shared authority moved. Discharged CONSTRUCTIVELY over all orderings (`nway_null` checks all N! against the parallel head). Overlap is `NWAY-REFUSE`, in TWO layers individually redundant and jointly load-bearing (the `first_overlap` disjointness check AND the deeper parallel≠serial divergence — proven: defanging the first still catches an overlap). THE INDEPENDENCE LATTICE (`independence_rounds`): partitions an arbitrary batch into ordered ROUNDS, each pairwise-disjoint (parallelizable); a shared chunk serializes into successive rounds; ONE round iff every edit is on a distinct chunk — the queryable allocator. THE CROSS-CHECK (anti-Goodhart): the certificate proves the SHARD path is order-independent; that it is also CORRECT is verified against `terraform`'s GLOBAL monolith (`_global_head`), a path using none of shard_apply/reunify — the gate row and the sweep assert shard-head == global-head, so a shard agreeing only with itself is caught (an off-by-one shard mutant was proven to make the sweep raise NWAY-REFUSE). N=2 AGREES WITH RAN-0 (`pair_agrees` scene: URDRNWY1 at N=2 reproduces URDRRAN0's pairwise head — a faithful generalization). The 233-byte-analogue variable-N certificate (`MAGIC | count | recs | SHA`) is evidence not authority: `check_nway` re-derives the whole proof and requires the presented bytes to reproduce, a forged/reordered certificate refuses. RED-FIRST: four pinned scenes (quad / pair_agrees / lattice / overlap) pinned AFTER the shard mutant + the two-layer overlap were proven to bite; a 150-batch seeded sweep (LCG high-bits, batch sizes 2..4, every overlapping batch refused, shard==global on every disjoint batch); a `nway` gate stage (4 rows: scenes / nullity / property / selftest) with per-row reddening. DECLARED (Phase M road ahead): authority MIGRATION mid-batch (M2); the meshed simulation MESH == MONOLITH with migrating authorities (M3); the partitioned mesh + CAP liveness cost (M4); the attested mesh session (M5). does_not_show: WHO may author (`sealwrit`); cross-process transport / failure mid-apply / wall-clock (`bench.py`); cross-placement (URDRNWY1 Python reference only). New: `tools/terrain/nway.py`, `tools/terrain/conformance_nway.txt`, `tests/test_nway.py` (9 falsifiers). Verified: gate ×2 byte-identical, GATE PASSED at 1125 / 644; doc-currency green.

**Phase M rung M2 LANDED — authority migration as lease transfer, the witness-carrying migration certificate (`tools/terrain/migrate.py`, URDRMIG1): the minimal lawful bridge from fixed certified authorities to a mobile one. 1125 → 1138 unit falsifiers, 644 → 648 rows.** M1 gave the SCHEDULE (which disjoint writes run concurrently); neither M1 nor `lease` can see a HANDOFF — and that blindness is this rung's born-red motivation, kept as a permanent falsifier (`test_lease_is_blind_to_migration`): a lease is minted from STATE (`lease_from_chunk`), a migration moves NO state, so after A→B the old steward's retained lease is BYTE-IDENTICAL to the new steward's fresh one, and `lease.admit` ALONE admits the usurper's write. Standing authority (WHO keeps writing) is therefore a different object from write capability (MAY this edit land), and M2 makes it first-class WITHOUT a new primitive: authority over a region moves by expiring the steward's standing on one node and minting it on another, preserving the equal-or-refuse discipline — and, per the ratified private slice, THE TRANSFER ITSELF IS A PROOF-PRODUCING OPERATION (Witness-Carrying Migration Certificate). THE CERTIFICATE (128 bytes): MAGIC | parent cert digest (zeros at genesis) | kx | ky | src steward | dst steward | the region's CHUNK digest at handoff | SHA-256. THE DESIGN DECISION, RAN-0's lesson applied to custody (ratified over the original sketch's world-witness pre/post pair): the certificate binds the AUTHORITY — one chunk digest, the minimal dependency closure — never the world manifest. Nothing global appears in it, so nothing global can be held by it, and two properties fall out STRUCTURAL rather than carried: (i) WITNESS PRESERVATION — `migrate` never returns a world, so pre/post manifest equality is a theorem checked in bytes (`test_witness_neutrality`), not two certificate fields; a migration that moved state is UNREPRESENTABLE, not merely refused; (ii) THE DEPENDENCY THEOREM (the user's `dependency_set ∩ changed = ∅` in its structural form) — a write certified disjoint from the region leaves the certificate bytes VALID UNCHANGED, needing no revalidation, proven in the migration diamond as byte-identical certificate transport across orders. THE STEWARD MANIFEST (55 + 40·n): STEWARD_MAGIC | W | H | C | per-region (8-byte steward tag | custody cert digest) | SHA-256 — custody's counterpart of the chunk manifest, content-addressed, TOTAL (a genesis missing or naming a foreign region refuses); custody moves the STEWARD digest, the world manifest UNTOUCHED (authority ⊥ witness, the D15 firewall lesson placed ABOVE the witness). THE CUSTODY CHAIN: each certificate's parent is the region's previous certificate digest — the chain IS the region's authority history, each link a content address (the transcript law on standing authority, as the lease chain is the write history); order is STRUCTURAL via the parent CAS — a reordered, duplicated, or forked transfer refuses on the chain ALONE, no sequence numbers to forge (`test_custody_replay`). THE MIGRATION CAS: adoption requires the certificate's chunk binding to equal the region's LIVE slot — a migration authored against a moved authority is refused, never rebased. ADMISSION IS THE CONJUNCTIVE PREDICATE (lease AND certificate) in three custody layers proven persist-style INDIVIDUALLY REDUNDANT and JOINTLY LOAD-BEARING: (1) the steward SLOT names the writer (state-free, one slot — `valid()`'s idiom); (2) the custody CHAIN re-derives from genesis and its head names the writer (evidence, never authority — a forged manifest whose slot contradicts its own cited chain refuses on the derived-data check, `_forge_slot_tag`); (3) SUCCESSION — the anamnesis trap, custody edition: old manifests and old certificates sit in the store FOREVER, integral, and integrity != truth, so a presented custody head with a KNOWN SUCCESSOR certificate refuses (retention turned into evidence) — all three gutted and the double-write LANDS (the falsifier detects it), the persist proof at custody scale. THE HANDOFF PREFIX LAW, the CP posture made executable at one region: every prefix of the handoff admits AT MOST ONE writer — minted-only (cert nowhere): src still writes, dst refuses; TORN (cert adopted, manifest not advanced): NOBODY writes, the region FREEZES (refuse rather than guess); committed: dst writes, src refuses. THE MIGRATION-DIAMOND THEOREM (the user's named capstone for M2): a write certified-disjoint from the migrating region and a migration COMMUTE in both spaces — one world head, one custody head — AND the certificate bytes are identical across orders; the overlapping half lawfully refuses (the stale certificate at adoption one way, the old steward the other) and serializes. THE ANTI-GOODHART CROSS-CHECK: the stewarded world head is verified against `terraform`'s MIGRATION-BLIND monolithic oracle (the neutral ruler cannot see custody — a migration that leaked into the witness diverges), the same discipline M1 used against `_global_head`. THE TIER-2 HARNESS GENERALIZED: a fixed-seed 120-scenario sweep mints randomized authority layouts, migration schedules, lease expiries, and concurrent writes as DATA, executes each lawfully, then faces FIVE adversaries — the double-writer (a non-steward's fully-lease-lawful write), the stale/spent lease, the duplicated transfer, the forked transfer (minted from a pre-migration snapshot), and oracle-divergence — every one refusing every scenario, with non-vacuity asserted (≥3 schedule lengths, some region re-migrated to depth ≥2, the world changed); the OFF-GATE `--explore` reseeds and SHRINKS any counterexample's schedule to its minimal failing form, ready to file as a pinned corpus scene (the wireattest off-gate→pinned discipline, now with a greedy shrinker because the schedule is data). RED-FIRST: four pinned scenes (handoff / relay / diamond / usurper) pinned AFTER the plants were proven to bite — a custody-blind admission lets the sweep's double-writer LAND, and an off-by-one shard diverges the world head from the migration-blind monolith; a `migrate` gate stage (4 rows: scenes / law / property / selftest) with per-row reddening; `tests/test_migrate.py` (13 falsifiers, each layer's redundancy demonstrated by gutting it and watching the double-write land). PRIOR ART, reviewed honestly (web, at design time — the novelty caveat the operator raised, discharged): every INGREDIENT exists — time-bounded authority with fencing (Gray & Cheriton 1989; Chubby sequencers; Kleppmann fencing tokens 2016); authority handoff carrying a state-digest certificate (PBFT stable checkpoints, Castro & Liskov 1999); transfer-mints-a-certificate (FastPay, Baudet et al. 2020; Linera microchains); lease disjointness as a machine-checked invariant (CockroachDB leader leases, SIGMOD 2026, TLA+); every-transition-carries-a-proof (zk-rollups; fraud proofs, Al-Bassam et al. 2018); dependency-closure caching (Bazel action cache; Adapton, PLDI 2014); verify-the-migrated-state (TALOS, 2025). The claim is the COMPOSITION ONLY, stated narrowly: the lease transfer itself as a certifying operation DERIVED FROM THE SAME content-addressed replay/authority/lease/witness calculus as every other law here (not a bolted-on Merkle/attestation layer), with a conjunctive admission predicate and TYPED REFUSAL — never a novelty claim on any ingredient. GRADE: the certificate and steward-manifest closed forms, round-trips, and exhaustive corruption refuses; the lease-blindness demonstration; witness neutrality and exactly-one-slot custody locality; the three-layer single-writer refusal (individually redundant, jointly load-bearing); the succession refuse with the stale-manifest trap demonstrated; custody replay determinism with structural order (reorder/dup/fork refuse); the migration CAS; the handoff prefix law (minted/torn/committed); the migration diamond with byte-identical certificate transport; the migration-blind oracle agreement; and the 120-scenario seeded sweep are MEASURED (exact, reproducible, a defect diverges). DECLARED, honestly: steward IDENTITY BINDING — tags are opaque 8-byte names; WHO may be a steward and Lamport signatures on migrations are `sealwrit` territory, keys and distribution out of scope (the brief's keyless pattern); SEMANTIC dependency witnesses — the user's richer arc (obligation sets like `requires Authority(R)/Witness(R)/Physics(R)/Lease(R)`, pending-revalidation states, and proof-preserving TRANSPORT PROOFS that rewrite a certificate mechanically when a dependency changes representation but not the property relied upon) is the NAMED SUCCESSOR (M2 ships the STRUCTURAL form — a certificate resilient to unrelated evolution because it names exactly its authority closure — and distinguishes "this data changed" from "this proof's assumptions changed" only structurally, not yet semantically); custody-head CURRENCY — the coordinator admits against the CURRENT steward manifest, the same trust anchor the world manifest already is (a partitioned custody store is M4); LIVE meshed simulation (M3). does_not_show: revocation other than by migration; cross-process transport / failure mid-handoff / wall-clock (`bench.py`); cross-placement (URDRMIG1 Python reference only — it joins the URDR write-calculus placement frontier). D5 PLACEMENT: this entry is the authoritative narrative; the per-module index is `tools/terrain/README.md`; the theorem statements are the migration-diamond, single-writer safety, witness preservation, replay determinism, migration locality, and refusal semantics enumerated above. THE ARC AFTER THIS ENTRY: `nway` proved the schedule; migrate proves the HANDOFF — standing authority is now mobile, content-addressed, and equal-or-refuse, and every transfer is an independently replayable proof that the world witness survived it. New: `tools/terrain/migrate.py`, `tools/terrain/conformance_migrate.txt`, `tests/test_migrate.py` (13 falsifiers). Next: M3 the meshed simulation (MESH == MONOLITH with migrating authorities). Verified: gate ×2 byte-identical, GATE PASSED at 1138 / 648; doc-currency green.

**Phase M rung M2.5 LANDED — the mesh reality attestation (`tools/terrain/meshattest.py`, URDRMAT1): authority migration over REAL sockets and REAL processes, judged by the same laws after the fact. 1138 → 1149 unit falsifiers, 648 → 652 rows.** M2 proved the handoff protocol with INFORMATIONAL isolation — the shard's inputs do not contain the world, but everything still ran in ONE process; the OODA review after M2 named the single missing vertical slice that converts "certified protocol" into "certified distributed SYSTEM": crossing a real process boundary. This rung crosses it, and it is not a new mesh mechanism — it is the WIREATTEST DISCIPLINE (T3.51, W5) applied to custody: sockets and wall-clock are NONDETERMINISTIC and by the house law may not live inside the gate, so the migration certificate is SERIALIZED, sent over a real TCP socket to a SEPARATE OS PROCESS, DESERIALIZED there from raw bytes, and ADOPTED by the unmodified `migrate` law running in that far process — and what crosses back into the gate is a SELF-DIGESTED TRACE, reality recorded once and re-verified forever. THE SPLIT (wireattest's, inherited): the RUN (off-gate, `python meshattest.py --run` on a NAMED host) has a COORDINATOR process mint certificates and drive two/three real steward-NODE subprocesses over TCP — a node ADOPTS a certificate it never minted (`apply_migration` from the wire bytes, in the far process, its returned steward manifest asserted equal to the coordinator's mint — reality reproduced the law), a node ADMITS a write as the region's steward, and THE OLD STEWARD NODE'S WRITE IS REFUSED after the handoff (the double-writer caught across a real socket — the differentiated claim, re-attested over reality); the CHECK (in-gate, pure) replays every recorded migration through `migrate.migrate` and every recorded write through `migrate.admit` against its own world, and reality's claims (the certificate bytes, the outcomes, the witnesses, the custody head) must MATCH what the law replays or the attestation is UNLAWFUL. THE NAMED-HOST LAW, MECHANIZED (bench_protocol's rule, inherited): a trace with an empty or missing host line REFUSES — an unnamed MEASURED is no MEASURED at all; the pinned trace lives at `spec/attest/mesh_attest.txt`, re-verified on every gate run, on every host, byte-deterministically, its host named in the gate output. THE LAWS THE CHECKER ENFORCES: genesis custody total and lawful; every recorded migration's CERTIFICATE BYTES must equal what `migrate` mints from the current world+custody (a forged or wrong-destination certificate refuses — reality may not overrule the mint); every recorded write outcome must equal the law's replayed outcome (a usurper's write recorded as ADMIT refuses — the double-writer cannot launder itself through a socket); every recorded ADMIT witness must equal the replayed world (silent drift refuses); a refused write leaves the world untouched; outcomes typed ADMIT / MIGRATE-REFUSE only; and the final world witness and custody head must equal the replayed head. RED-FIRST (L15): the SYNTHETIC in-process traces (`handoff` — A→B with a usurper refused and a disjoint region untouched; `relay` — A→B→C custody chain with a mid-chain usurper refused) and SEVEN woven forges (a usurper's write recorded ADMIT, a drifted witness, a forged certificate, a certificate to the wrong destination, an untyped outcome, a dropped migration, a drifted final witness) were each proven to bite BEFORE the real trace was pinned; the `meshattest` gate stage (4 rows: laws / forges / trace / selftest) with per-row reddening; `tests/test_meshattest.py` (11 falsifiers). THE PINNED TRACE (this run): produced on the Anthropic cloud sandbox (Linux) — 3 real migrations over TCP, 6 admits, 2 usurpers refused across the process boundary, LAWFUL; the NAMED-HOST attestation is produced by re-running `--run` on the Ally X (the same off-gate→pinned discipline wireattest uses; the gate re-verifies whatever host's trace is pinned). GRADE: the checker's laws (each with a refusing synthetic forge), trace integrity (any byte flip refuses on the self-digest), the named-host rule, determinism of the check, and the verdict-bound report digest are MEASURED; the pinned trace's verdict is MEASURED on its named host (the trace IS the host log, bench_protocol's sense). DECLARED, honestly: the run exercises LOOPBACK TCP on one machine (real sockets, real processes, real serialization and kernel buffering — but NOT cross-machine networks, NAT, or the open internet); wall-clock latency is NOT claimed (`bench.py` §3); a PARTITION during handoff (a lost certificate, a torn commit visible to a live peer) is M4's territory, NOT re-attested here — this rung attests the RELIABLE handoff crossing a real boundary, the CP posture's happy path made real. does_not_show: throughput/scale (never a measured number until the scale bench); cross-placement (URDRMAT1 attests URDRMIG1, itself Python reference only). THE ARC AFTER THIS ENTRY: M2 proved the handoff in one process; M2.5 proves it crosses a real one — the mesh is no longer only modeled, it has been executed across a socket and the record proves it met the laws. New: `tools/terrain/meshattest.py`, `spec/attest/mesh_attest.txt`, `tests/test_meshattest.py` (11 falsifiers). Next: M3 the meshed simulation (MESH == MONOLITH with migrating authorities). Verified: gate ×2 byte-identical, GATE PASSED at 1149 / 652; doc-currency green.

**Named-host confirmation (no new capability, no count change): the M2.5 mesh attestation reproduced on the Ally X.** The M2.5 entry pinned a cloud-sandbox (Linux) trace and named the re-run on the owner's Windows host as the named-host step; that step is now DONE. `python tools/terrain/meshattest.py --run spec/attest/mesh_attest.txt "Ally X"` on `DanielDillberg | Windows 11 | python 3.14.5` re-produced the attestation over REAL TCP sockets and REAL node subprocesses — 3 migrations, 6 admits, and 2 USURPERS REFUSED ACROSS THE PROCESS BOUNDARY, verdict LAWFUL — and re-pinned `spec/attest/mesh_attest.txt` (only the host line and the self-digest moved; the migration certificates, witnesses, and outcomes are deterministic and unchanged, so the trace body is byte-identical to the Linux run). The `meshattest:trace` gate row now re-verifies the Ally-X-named trace on every run, byte-deterministically, on any host. GRADE: the pinned trace's verdict is MEASURED on the NAMED host (bench_protocol's sense — the trace IS the host log), the same graduation `wireattest`'s `wire_attest.txt` carries. does_not_show unchanged: loopback TCP on one machine (not cross-machine / NAT / internet); a partition during handoff remains M4's territory.

**Phase M rung M3 LANDED — the meshed simulation, the capstone MESH == MONOLITH (`tools/terrain/mesh.py`, URDRMSH1): the certified mesh as a composed theorem. 1149 → 1159 unit falsifiers, 652 → 656 rows.** The rung the whole phase was built toward: N authorities own regions of one world and MIGRATE authority between each other over time, and a meshed simulation of concurrent per-tick writes composes to the SAME world witness a single monolithic authority would compute — bit-for-bit — or refuses. It is the answer to server meshing that cannot lie: not a best-effort convergence (the industry's flagship, whose public talks disclose no handoff or consistency semantics), but a THEOREM re-derived in bytes. M3 IS A COMPOSITION, not a new mechanism — the mesh brief's thesis, executed to its end: `nway` (M1) schedules each tick's CONCURRENT writes (the independence lattice is the concurrency certificate — a tick's writes admit in parallel iff they form ONE independence round, pairwise-disjoint regions; an overlapping batch is not schedulable and the tick refuses WHOLE); `migrate` (M2) moves authority between ticks WITNESS-NEUTRALLY (a migration returns no world) and gates EVERY write through the steward-checked conjunctive admission (lease AND custody chain; a non-steward's write refuses the WHOLE tick); `terraform` is the MONOLITH ORACLE (the anti-Goodhart neutral ruler — it applies the same writes globally, IGNORING custody entirely, so a bug in the meshed tick cannot hide inside its own answer). THE TICK, in two phases: (1) WRITES — the current stewards write their own regions concurrently, the records certified ONE independence round (M1), each admitted steward-checked (M2), and — disjoint — their order immaterial (the shard path equals the monolith, M1's cross-check); (2) MIGRATIONS — authority moves for the next tick, witness-neutral (the world manifest untouched, asserted in bytes). THE CAPSTONE THEOREM (guarantee #3): for any schedule of ticks the meshed world witness equals the monolith of the same writes, bit-for-bit. This GENERALIZES `regionprop`'s reunify == monolith (URDRRGP1) from a STATIC partition (fixed seams) to a MIGRATING one (the steward of a region changes over time): the partition of WORK is fixed (the chunk grid), the partition of AUTHORITY migrates, and the witness is invariant to BOTH — because migration is witness-neutral and concurrent disjoint writes are n-way null. WITNESS-NEUTRALITY AT SCALE, gate-checked: the SAME writes under a DIFFERENT migration schedule (no migration vs alfa→bravo mid-simulation) land the IDENTICAL world witness — authority mobility does not perturb the world. REFUSAL SEMANTICS, reject-whole (the regionprop atomicity discipline): a non-steward write, an overlapping concurrent batch (two writes on one region in one tick), and a theft migration (a source that is not the region's steward) each refuse the WHOLE tick typed MESH-REFUSE — a mesh tick admits entirely or not at all, never a partial world. RED-FIRST (L15): four pinned scenes (parallel_tick — four stewards write four quadrant regions in one concurrent tick, one independence round; migrating — writes interleaved with migrations to two more stewards; handoff_write — a region migrates A→B→C with each current steward writing it in turn; refusal — a non-steward write refuses the whole tick) pinned AFTER the plants were proven to bite — a monolith oracle that drops a write and a mesh tick that silently drops a write each diverge and raise MESH-REFUSE; a seeded 100-mesh sweep (randomized steward layouts 2..4, tick schedules, concurrent write batches, migration schedules, each asserted MESH == MONOLITH against the independent terraform monolith, non-vacuity asserted: 150 migrations, 236 genuinely-concurrent ticks, ≥2 distinct layouts, the world changed); a `mesh` gate stage (4 rows: scenes / law / property / selftest) with per-row reddening; `tests/test_mesh.py` (10 falsifiers). GRADE: MESH == MONOLITH over the corpus and the seeded sweep; witness-neutrality of migration at scale; the one-independence-round concurrency certificate; and the reject-whole refusals are MEASURED (exact, reproducible, a defect diverges). DECLARED: the partitioned mesh + the CAP liveness cost under a REAL partition (M4 — this rung is the CP posture's consistent, un-partitioned composition; a partition freezing clients at the prefix is M4's territory); the attested mesh session (M5); SCALE (the mesh's CORRECTNESS is the claim; "thousands of authorities" is MEASURED-on-a-named-host or it is not claimed). does_not_show: WHO may be a steward (`sealwrit`/genesis fiat); cross-process transport (attested separately by `meshattest`, M2.5); wall-clock (`bench.py`); cross-placement (URDRMSH1 Python reference only). THE ARC AFTER THIS ENTRY: M1 proved the schedule, M2 the handoff, M2.5 the handoff over a real socket, and M3 the WHOLE — the composed multi-authority world with authority migrating over time is bit-identical to a monolith, which is exactly the property the industry's server meshing does not have. New: `tools/terrain/mesh.py`, `tools/terrain/conformance_mesh.txt`, `tests/test_mesh.py` (10 falsifiers). Next: M4 the partitioned mesh (CAP executable — the storm prefix property at mesh scale, a partition freezing clients at the prefix, detected not silent). Verified: gate ×2 byte-identical, GATE PASSED at 1159 / 656; doc-currency green.

**Phase M rung M4 LANDED — the partitioned mesh, the Partition Prefix Theorem (`tools/terrain/partition.py`, URDRPRT1): under partition, the system refuses to invent history. 1159 → 1169 unit falsifiers, 656 → 660 rows.** M3 proved MESH == MONOLITH while CONNECTED; M4 asks what happens when the connection disappears — and the answer, as the operator's brief anticipated, is already LATENT in the laws (M1's disjointness, M2's custody CAS, the storm prefix property), so M4 composes rather than invents. THE ARCHITECTURAL CLAIM MADE EXECUTABLE, implicit since `storm`/`wire`/`chunkstate`/`reunify==monolith` and now a theorem: under partition the system REFUSES TO INVENT HISTORY — a stronger, more distinctive statement than "the cluster remains available." THE PARTITION PREFIX THEOREM: every lawful partitioned execution equals a PREFIX of the corresponding connected execution; any attempt to extend beyond that certified prefix either preserves equality or refuses. In one line: `partitioned mesh == monolith prefix` OR `PARTITION-REFUSE` — the mesh-scale version of the storm prefix property (a stalled client freezes at the authority's prefix, never a state the authority never had). THE MODEL: a partition splits the stewards (and thus the regions they own) into two SIDES from a shared CUT (the world + custody both sides agree on at partition time); each side runs its own schedule from the FROZEN cut but may only admit an operation it can VERIFY from frozen knowledge. THE FREEZE RULE (refuse rather than guess, the rung's heart): a write to a region whose steward is on the UNREACHABLE side FREEZES — the side cannot prove sole authority, so it does not speculate (the storm freeze at custody scale). CUSTODY STILL BITES (M2, inherited): every admitted write is steward-checked against the FROZEN cut custody, so even a DUPLICATED LEASE (the split-brain attack) cannot write on the side that is not the region's steward — the lease is blind, custody is not. A CROSS-PARTITION MIGRATION FREEZES: authority cannot be handed to an unreachable node. THE MIGRATION CAS refuses PARTITION-TRANSPORT FORGERY: a certificate minted on the unreachable side chains from a custody head this side's frozen custody does not contain, so its parent CAS fails — refused, never rebased. REUNIFICATION, two layers individually redundant and jointly load-bearing: because the freeze rule keeps each side writing only regions it solely owns, the two sides change DISJOINT chunk slots (M1's n-way nullity, now across the partition boundary), so reunification is well-defined and equals the monolith of the admitted writes; the SECOND layer is the overlap check — if the freeze rule is gutted and both sides change the same region (silent divergence / split-brain), reunification DETECTS the shared slot and REFUSES, so divergence is caught even if the first layer is defeated. THE FIVE ATTACKS (the operator's red-first spec, each landed): (1) SILENT DIVERGENCE — refused BEFORE it occurs (the freeze rule; the reunify overlap as backstop); (2) AVAILABILITY FORGERY — a gutted freeze rule's speculation fails at reunification against the monolith; (3) PREFIX VIOLATION — a mid-transfer region admits NOTHING beyond the shared prefix (the world stays at the cut); (4) SPLIT-BRAIN AUTHORITY — a duplicated lease admits on only the owning side, and forcing both refuses at reunify; (5) PARTITION-TRANSPORT FORGERY — a certificate from the unreachable side refuses on the migration CAS. THE PREFIX PROPERTY, checked as PATH-MEMBERSHIP (no invented history): every region's partitioned chunk is a state the CONNECTED execution genuinely passed through for that region (its cut state or an intermediate/final state) — never a third, invented state; the connected reference is itself steward-checked (a cross-write, the wrong side speculating, is not part of any legitimate schedule and is excluded, exactly as the partitioned side freezes it). RED-FIRST (L15): four pinned scenes (disjoint — each side writes only its own regions, reunify == monolith, nothing frozen; freeze — a cross-partition write frozen, the admitted world a prefix; mid_transfer — a cross migration and a beyond-prefix write both freeze, the world at the cut; split_brain — the freeze rule gutted, both sides write one region, reunify refuses) pinned AFTER the plants were proven to bite; a seeded 80-partition sweep (randomized side splits, local + cross-partition writes, cross-partition migrations, each asserted partitioned == monolith of the admitted AND the prefix property, non-vacuity: 149 froze, 312 admitted, both sides active every scenario, the world changed); a `partition` gate stage (4 rows: scenes / law / property / selftest) with per-row reddening — a gutted freeze rule makes the sweep raise; `tests/test_partition.py` (10 falsifiers, the five attacks each an explicit test). GRADE: the Partition Prefix Theorem over the corpus and the seeded sweep; partitioned == monolith of the admitted writes; the prefix property (path-membership, no invented history); the freeze rule and the reunify-overlap backstop; and the five attacks are MEASURED (exact, reproducible, a defect diverges). DECLARED, the CP AVAILABILITY COST STATED not hidden (the brief's one load-bearing fork, resolved): a mid-transfer region FREEZES — there is NO liveness guarantee under partition; a consensus/quorum PROGRESS overlay that would buy liveness by introducing a trusted majority is a NAMED, OPTIONAL, FUTURE extension, NEVER folded into this theorem (it is a different trust model — "every byte re-derived or refused" — and would be graded separately). does_not_show: real cross-machine partitions / netsplit wall-clock timing (this models the partition as a frozen-knowledge split, not a clock; `meshattest`/`bench.py` own the reality boundary); the attested mesh session (M5); cross-placement (URDRPRT1 Python reference only). THE ARC AFTER THIS ENTRY: M1 the schedule, M2 the handoff, M2.5 the handoff over a real socket, M3 the whole mesh == monolith while connected, and M4 the mesh under partition — the composed multi-authority world stays a prefix of the monolith or refuses, never a fork. New: `tools/terrain/partition.py`, `tools/terrain/conformance_partition.txt`, `tests/test_partition.py` (10 falsifiers). Next: M5 the attested mesh session (a recorded multi-authority playthrough replayed by the gate — "server meshing that cannot lie" as a proof, built ON TOP of this correctness theorem rather than replacing it). Verified: gate ×2 byte-identical, GATE PASSED at 1169 / 660; doc-currency green.

**Phase M rung M5 LANDED — the attested mesh session, and THE PHASE SEALS (`tools/terrain/meshsession.py`, URDRMSS1): the demo is not a video, it is a proof. 1169 → 1178 unit falsifiers, 660 → 664 rows.** The capstone, and — per the operator's framing, honored exactly — an EVIDENCE theorem built ON TOP of the correctness theorems (M3 mesh == monolith, M4 the partition prefix), NEVER a replacement for them: M5 proves the ENTIRE multi-authority session can be ATTESTED and REPLAYED. THE DISCIPLINE (sealsession/wireattest, applied to the whole mesh): a named SESSION is a deterministic multi-authority playthrough composing the landed laws as a sequence of EPISODES — each either a connected MESH TICK (M3: concurrent writes by the current stewards, then witness-neutral migrations) or a PARTITION episode (M4: the world splits into two sides from the live cut, each side writes only what it can verify, cross-partition ops freeze, reunification equals a prefix of the monolith) — threaded through ONE evolving state; running the session yields a chain of WITNESS CHECKPOINTS (the world witness + custody head after every episode), and the session TRACE is that chain plus its own SHA-256, a content-addressed proof object. THE CHECK (in-gate, pure): `check_session` RE-RUNS the named session through the UNMODIFIED laws (`mesh.apply_tick`, `partition.partitioned_from`, added as clean stateful hooks — pure additions, no existing digest moved) and requires every recorded checkpoint to reproduce BIT-FOR-BIT; the session's claims are exactly what the composed calculus produces, re-derivable by anyone, nothing trusted. THE CAMPAIGN, the flagship session (six checkpoints): four stewards write concurrently (M1/M3, one independence round) → a live migration alfa→echo mid-session (M2, the custody head visibly changes) → the new steward writes its region → a PARTITION episode (M4: three ops one side / two the other, one cross-write frozen, three admitted, reunified to a prefix) → post-reunification concurrent writes → the final witness; the SKIRMISH is the minimal composed timeline (two stewards, a migration, a partition, a final tick) pinned as a second session and a determinism cross-check. RED-FIRST (L15): the two sessions' checkpoint-chain digests and the sealed campaign trace (`spec/attest/mesh_session.txt`) were pinned AFTER five forges were each proven to bite — a tampered tick witness, a tampered partition witness, a forged custody head, a dropped episode, and a bumped admitted count each REFUSE (reality may not overrule the composed law), and a byte flip refuses on the self-digest; a `meshsession` gate stage (4 rows: sessions / attest / forges / selftest) with per-row reddening; `tests/test_meshsession.py` (9 falsifiers). GRADE: the named sessions' checkpoint chains and digests, the sealed trace's round-trip, the checker's laws (each with a refusing forge), trace integrity (any byte flip refuses), and determinism are MEASURED (exact, reproducible, a defect diverges). DECLARED: this is a DETERMINISTIC in-process attestation (the composed correctness laws are deterministic, so the session needs no named host — the REAL cross-process boundary is attested separately by `meshattest`, M2.5); scale (a fixed multi-authority playthrough, not a scale claim). does_not_show: wall-clock (`bench.py`); cross-placement (URDRMSS1 Python reference only). ═══ PHASE M SEALS ═══ The certified mesh is complete as a COMPOSITION, exactly as the design brief promised: M1 (`nway`) the write scheduler / independence lattice; M2 (`migrate`) authority migration as lease transfer with the witness-carrying certificate; M2.5 (`meshattest`) the handoff over a REAL socket, MEASURED on the Ally X; M3 (`mesh`) the capstone MESH == MONOLITH while connected; M4 (`partition`) the Partition Prefix Theorem — under partition, refuse to invent history; M5 (`meshsession`) the whole multi-authority timeline attested and replayable. Not one new primitive was minted — every rung is a composition of the sealed write calculus (`terraform`/`commute`/`rannull`/`lease`/`testament`/`quintessence`), the wire phase (`wire`/`storm`/`sealwrit`/`driftgaze`/`wireattest`), and the storage/streaming spine — which is the phase's whole thesis: distributed execution as a theorem obtained by composing proofs, not a new subsystem. THE INDUSTRY CONTRAST STANDS: the flagship's server meshing is a best-effort convergence whose public talks disclose no handoff or consistency semantics; here `MESH == MONOLITH` and the Partition Prefix Theorem are re-derivable in bytes. THE COVENANT ON DEBTS (restated so it cannot hide): Phase M ships PYTHON REFERENCE ONLY — placement batch #4 (a std-only Rust placement reproducing the URDRNWY1/URDRMIG1/URDRMSH1/URDRPRT1/URDRMSS1 digests) falls due at THIS seal, NO LATER, exactly as the wire phase's batch #3 did; and SCALE remains architectural direction (MEASURED-on-a-named-host or unclaimed) with the scale bench the named successor. New: `tools/terrain/meshsession.py`, `spec/attest/mesh_session.txt`, `tools/terrain/conformance_meshsession.txt`, `tests/test_meshsession.py` (9 falsifiers); mesh/partition gained pure stateful hooks. Verified: gate ×2 byte-identical, GATE PASSED at 1178 / 664; doc-currency green.

**The perception layer — WITNESSED ABSENCE as server-authoritative AoI (`tools/terrain/perception.py`, URDRPCP1): the anti-cheat Band A rung, and the sound kernel of the operator's Ø idea — built with NO NEW GLYPH. 1178 → 1190 unit falsifiers, 664 → 668 rows.** The first rung of the anti-cheat home's Band A ("server-driven AoI delivery WITH Phase M — the real ESP/wallhack mitigation", `docs/hardening_brief.md`), now buildable because Phase M sealed and the mesh gives the certified authority to decide, per client, what may be perceived. It grew from the operator's **Ø (bridge of potential)** proposal; the design pass (`docs/perception_brief.md`) kept the sound kernel of that idea and ruled honestly on the parts that could not become law. THE THESIS: witnessed absence is the D15 presentation firewall (`view_witness`) applied to RESIDENCY — TWO CHANNELS, never one packed word: (i) THE WITNESS, the authoritative world (entities at integer positions, each carrying its authority digest), exact and untouched by perception; (ii) THE RESIDENCY CHANNEL, a per-client MANIFESTED set (which entities this client may perceive), cited to the witness but structurally WALLED from it — the "structural instruction" of the operator's dual-state scalar, kept SEPARATE from the value because packing it into the 64-bit word would steal range from the exact-i64/refuse-on-overflow law or move the value's content identity, cracking determinism. A hidden entity is therefore not a zeroed record with a visibility flag but an UN-ADDRESSED ABSENCE: the client transcript is a PURE FUNCTION of the manifested set, so a wallhack replayed against it finds NOTHING for anything out of view. THE EXACT-INTEGER AoI (deterministic, no floats): an entity is manifested iff in front (dot > 0), within the wedge (|cross|·hw ≤ dot·hh, the half-angle as an integer slope), within the squared range plus the declared margin band, and NOT OCCLUDED (the integer supercover of the segment viewpoint→entity crosses no wall cell). THE THIRD SEAM THIS TARGETS (aimed at, NOT claimed as surpassed — an honesty correction after the initial draft overreached): the literature survey identified the publicly-described state of the art (VALORANT Fog of War and CS2FOW: per-tick server-side visibility, multiple line-of-sight rays, precomputed visibility structures, conservative velocity margins) and isolated three remaining seams — (1) the visibility margin is an unavoidable asymmetric leak; (2) audio and physics/hitbox are independent channels; (3) timing/bandwidth characteristics can themselves reveal impending visibility changes (a burst of updates as an entity becomes visible). URDRPCP1 is AIMED at seam (3): the transcript is CONSTANT-SHAPE, padded to a fixed capacity so its byte-length is invariant to the hidden set; the traffic shape carries no information about how many entities are hidden or about to appear. The one unavoidable leak — the pre-reveal MARGIN (the peeker's-advantage tradeoff) — is made an EXPLICIT, BOUNDED quantity, not pretended to be zero. WHETHER THIS EXCEEDS EXISTING PRODUCTION SYSTEMS IS AN EMPIRICAL QUESTION, NOT ESTABLISHED BY THESE PROOFS: the proofs establish properties of THIS protocol (witness-blindness, hidden-set invariance, constant-shape, the bounded margin), not comparative performance or completeness versus commercial anti-cheat. THE GLYPH QUESTION, ruled honestly against D1 §20: **NO new glyph — the kernel stays frozen.** Witnessed absence introduces no primitive the alphabet cannot express (the membrane already models "nothing exists until minted"; the epistemic ladder already carries "not yet proven"; absence is the refusal discipline), the residency channel is a view-layer/`tools/` object that never touches the sealed language, and the originally-proposed Ø arithmetic (division-by-Ø as an exact scalar; the digit as a positional "bridge") is unsound (division by zero is a typed refusal, not a value; the digit is a place coefficient). The honest synthesis that HONORS the idea: Ø earns a place as a NAMED VALUE in the tools/view layer (the residency/absence sentinel `PAD_EID`), not as a frozen-kernel symbol — the capability lives in `tools/`, consumes the kernel, never edits it, exactly as every rung before it. RED-FIRST (L15): four pinned scenes (sniper — a narrow wedge manifests only the corridor entity; corner — occlusion, a byte-identical transcript whether the hidden entity is there or moved; margin — the certified early-reveal band; wallhack — the verdict IS the defeat: a probe for a hidden enemy returns Ø) pinned AFTER the leak-the-hidden manifest was proven to break byte-identical invariance and raise; a seeded 120-world sweep (random worlds/walls/entities, a GUARANTEED-hidden entity behind the viewpoint whose change must leave the transcript byte-identical, a guaranteed-visible entity for non-vacuity, occlusion exercised every scenario); a `perception` gate stage (4 rows: scenes / law / property / selftest) with per-row reddening; `tests/test_perception.py` (12 falsifiers). GRADE: witness-blindness, hidden-set invariance (a change confined to out-of-wedge/occluded entities yields a BYTE-IDENTICAL transcript), the constant-shape transcript, the wallhack-probe-finds-nothing property, the certified margin band, the lawful mint (∅→1) and the citation contract (a forged citation reddens), and the seeded sweep are MEASURED (exact, reproducible, a defect diverges). DECLARED, honestly: the MARGIN is a real, bounded, DECLARED leak — pop-in cannot be avoided without leaking a margin; the rung bounds and certifies it, it does not eliminate it, and the peeker's-advantage asymmetry is latency-inherent and NOT solved; AUDIO and physics-HITBOX channels are OUT OF SCOPE (this governs the position/state channel); PASSIVE-INFORMATION cheats only — witnessed absence defeats ESP/wallhacks that read data the client should not have, it does NOT touch aim-assist, trigger-bots, or any cheat on legitimately-visible data (the honest aimbot boundary, unchanged from the README threat model); the constant-shape hardening hides the COUNT via length (content-level indistinguishability would need an encryption layer, declared out of scope). does_not_show: real line-of-sight over continuous geometry (this is an exact integer grid model); cross-placement (URDRPCP1 Python reference only). New: `tools/terrain/perception.py`, `tools/terrain/conformance_perception.txt`, `tests/test_perception.py` (12 falsifiers), `docs/perception_brief.md`. Next (declared): Band A integration with the mesh's per-client authority (the server that decides the manifested set is the mesh steward); audio/hitbox channels; cross-placement. Verified: gate ×2 byte-identical, GATE PASSED at 1190 / 668; doc-currency green.

**The closed-world (∅^∅) hardening to witnessed absence (`tools/terrain/perception.py`, URDRPCP1) — 2026-07-24. 1190 → 1193 unit falsifiers, 668 rows (no new rows — the perception stage's `perception-law` row absorbs the guarantee).** A small, named falsifier suite formalizing the operator's ∅^∅ refinement: the client's reconstructed state is a CLOSED WORLD — it holds exactly the manifested set and offers NO addressable slot (not even a null one) for any absent entity. Where the earlier rung proved the *transcript* is witness-blind and constant-shape, this proves the *reconstruction* of that transcript is closed: `reconstruct(transcript)` returns exactly `{eid: (x, y, cite)}` for the manifested set, and `is_closed_world(entities, walls, client, transcript)` holds iff `set(reconstruct(t)) == set(manifest(...))`. THE PLANT THAT BITES (L15): `_perceive_open` — the standard-engine mistake, an OPEN template that keeps a null "empty slot" per entity and so writes a padded record CARRYING every hidden entity's id — is proven to leak the hidden identities into the reconstruction (`3 in reconstruct(leaky)`) and to FAIL `is_closed_world`, while the honest `perceive` passes; a third falsifier proves the wire padding (Ø) is anonymous — it carries no absent entity's id, so nothing can be recovered from it. THE DISTINCTION THIS SHARPENS: the ∅^∅ reading is that change over an empty possibility is the empty function — the one closed map, `0^0 = 1` — NOT a reserved slot awaiting a value; the perceiver does not allocate a resting potential for the un-manifested, it holds a closed reality. HONESTY UNCHANGED: this remains a property of THIS protocol (the reconstruction is closed by construction — an un-addressed absence cannot be probed), not a comparative claim against production anti-cheat; NO new glyph — Ø stays a named view-layer value (`PAD_EID`), the kernel frozen. THE WIRE IS BYTE-IDENTICAL: the closed-world checks are assert-only over the existing sweep, so the four `perception` conformance digests are UNCHANGED (the guarantee tightens the proof, not the protocol). Grew `tests/test_perception.py` 12 → 15 falsifiers (the `TheClosedWorld` class: reconstruction-is-closed / open-template-plant-bites / padding-carries-no-identity); the `perception-law` gate row now also asserts the reconstruction is a closed world and that the open-template null-slot mistake is caught. Verified: gate ×2 byte-identical, GATE PASSED at 1193 / 668; doc-currency green.

**The tunable semantic focal lens — ANAMORPHOSIS over witnessed absence (`tools/terrain/anamorphosis.py`, URDRANA1): the perception firewall generalized from a BINARY wall to a GRADED, server-tunable dial — built with NO NEW GLYPH. 1193 → 1208 unit falsifiers, 668 → 672 rows.** Grown from the operator's proposal — *"anamorphosis that is tunable, like adjusting the angle of looking at a particle with a microscope, but for the server side… a variable semantic focal lens"* — and from the operator's constraint that it deploys as *"a simple patch to all users when needed."* THE KNIFE-EDGE (the design pass, `docs/anamorphosis_brief.md`): "tunable distortion" has a dangerous reading and a sound one, and the closed-world (∅^∅) law admits only the sound one. The DANGEROUS reading — emit a DEGRADED-BUT-PRESENT record for a marginally-visible entity ("semi-awareness gives a blurry blip") — REINTRODUCES the exact leak the perception rung closed: a wallhack reads "an enemy is roughly there," and the reconstruction is no longer a closed world. So anamorphosis NEVER distorts what is hidden; it tunes two already-lawful things — the BOUNDARY of what manifests, and the PRECISION of records that ALREADY passed the firewall. Below the tuned boundary an entity stays an un-addressed absence; above it, its record is rendered at a distance-graded precision. The microscope angle changes the RESOLUTION OF THE LEGITIMATELY-VISIBLE, never the PRESENCE OF THE HIDDEN. THE LENS `L = (reach, focus)` — two integers, the whole deployable patch: `reach` widens the manifestation boundary (a superset dial), `focus` sharpens precision (a resolution dial), both server-set and CITED into the transcript header. THE EXACT-INTEGER SCHEDULE (no floats, deterministic): awareness `a = (range² + margin + reach) − d²`; grid shift `s = clamp(COARSEST − (a >> SCALE) − focus, 0, COARSEST)`; the record's position is floored to the 2ˢ grid (`quantize(v,s) = (v >> s) << s`) — closer/higher-focus ⇒ finer, a just-revealed boundary entity enters at the coarsest precision and sharpens as it becomes central. PRIOR ART (the sound kernel maps onto established work, no superiority claim): the academic twin is the Spatial Model of Interaction's FOCUS/NIMBUS (Benford & Fahlén, CSCW 1993) — awareness as a GRADED function of an observer's focus and a target's nimbus, tuned by explicit parameters and by ADAPTERS; the precision channel is standard LOD/quantization replication; the 2025 anti-cheat survey leaves the residual leak from reduced-precision/graded visibility under-treated, and the contribution here is doing graded visibility WHILE PRESERVING the closed world. RED-FIRST (L15): four pinned scenes (focal — a close entity SHARP, a far one COARSE under one lens, the microscope's depth of field; widen — an entity beyond base range ABSENT at reach 0, MANIFESTING coarse once the server widens reach, a superset; defended — a behind entity stays ABSENT, a wallhack probe finds Ø, the reconstruction closed; reduce — at the identity lens the manifested set equals perception's and positions are EXACT) pinned AFTER the plants were proven to bite: `_perceive_graded_leak` (the semi-awareness blip) lets a wallhack recover a hidden entity AND breaks closed-world; `_perceive_covert` (exact low bits kept under a coarse claim, a reversible blur) is refused by the citation contract and unmasked by `is_lossy`; the inverted schedule breaks the monotone order; a client-forged wider/finer lens is refused. A seeded 120-world × 4-lens sweep asserts, for EVERY lens: witness-blindness, CLOSED-WORLD (the graded dial changes RESOLUTION not MEMBERSHIP), constant-shape, the citation contract, LOSSY-ONLY, hidden-set invariance (byte-identical under a hidden change), and the MONOTONE DIAL (widening only adds entities and refines precision, never swaps); non-vacuous (coarse + sharp records and boundary crossings all exercised). An `anamorphosis` gate stage (4 rows: scenes / law / property / selftest) with per-row reddening; `tests/test_anamorphosis.py` (15 falsifiers). GRADE: closed-world across the dial, the monotone dial, lossy-only quantization, the server-only cited lens, constant-shape across the dial, and the reduction to perception at `L = ⊤` are MEASURED (exact, reproducible, a defect diverges). THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** Anamorphosis introduces no primitive the alphabet cannot express (the membrane already models manifested-vs-absent; floor-division is existing exact math; the `view_witness` citation contract already exists; and the grading vocabulary is literally the epistemic ladder — `absent → coarse → fine` is a maturity ascent on the residency channel); the lens is a `tools/`/view-layer object like `PAD_EID`, consuming the kernel, never editing it. DECLARED, honestly: this inherits every perception-rung boundary (the margin is a real bounded leak; audio/hitbox out of scope; passive-info cheats only; count hidden via length, not content) and adds a NEW declared boundary — a coarse record still reveals the APPROXIMATE position of a LEGITIMATELY-VISIBLE entity (that is the point; it is data the client is allowed to have); anamorphosis BOUNDS the resolution, it does not encrypt it. THE THIRD PILLAR (declared successor, NOT built here): the operator identifies **deterministic simulation-rate decoupling — a clarity-bounded update throttle** — beyond security and network compression, the same awareness this rung computes can bound a per-entity update rate, decoupling local client compute from the global sim rate by perceptual relevance. Recorded as the correct NEXT rung (it reads this rung's `shift_of`/`awareness` as its input); deliberately not folded in because it opens a new proof surface that must be discharged on its own terms — determinism (the throttle a pure function of `(tick, awareness)`, replay byte-identical), staleness under the citation contract (a carried-forward coarse record cites the authority as of its last refresh tick, reusing the predict/reconcile machinery), and NO new timing channel (the throttle applies only to already-manifested entities, the transcript stays constant-shape, so the refresh cadence carries nothing about the hidden set). does_not_show: continuous line-of-sight (exact integer grid model); the update throttle (declared next); cross-placement (URDRANA1 Python reference only). New: `tools/terrain/anamorphosis.py`, `tools/terrain/conformance_anamorphosis.txt`, `tests/test_anamorphosis.py` (15 falsifiers), `docs/anamorphosis_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1208 / 672; doc-currency green.

**The clarity-bounded update throttle — DETERMINISTIC SIMULATION-RATE DECOUPLING (`tools/terrain/throttle.py`, URDRTHR1): the third pillar the focal lens unlocks, built with NO NEW GLYPH. 1208 → 1223 unit falsifiers, 672 → 676 rows.** The declared successor of the anamorphosis rung, now landed. Beyond (1) the SECURITY of witnessed absence (URDRPCP1) and (2) the NETWORK compression of the graded transcript (URDRANA1), the same exact-integer awareness the focal lens computes bounds a per-entity POSITION-refresh rate — decoupling local client COMPUTE from the global sim rate by perceptual relevance (the operator's third pillar: "a structural way to throttle local client computation cost relative to perceptual relevance"). THE SEPARATION THAT KEEPS IT SOUND: the throttle delays POSITION, never PRESENCE. MEMBERSHIP stays LIVE — the transcript at tick T holds EXACTLY the manifested-under-L set at tick T, so the reconstruction is a CLOSED WORLD at every tick (the throttle changes refresh RATE, never who is addressable); a departed entity is DROPPED the instant it leaves the manifested set — NO GHOSTS (a lingering last-known position would leak where an enemy WAS). POSITION is THROTTLED — a manifested entity's transmitted position is refreshed only on its clarity cadence and CARRIED (held stale) between refreshes, cited to the authority as of its last refresh tick (the predict/reconcile discipline). THE EXACT-INTEGER CADENCE (deterministic, no floats, no wall-clock — `tick` is an explicit integer of the replay, never a clock read): `rate = 2^shift` (sharp shift 0 → every tick; coarsest → every 2^COARSEST); an entity refreshes iff it just entered OR `tick mod rate == 0`. Because every rate is a power of two dividing 2^COARSEST, EVERY tick with `tick mod 2^COARSEST == 0` refreshes ALL manifested entities — so staleness is STRUCTURALLY BOUNDED by 2^COARSEST − 1, and a sharp entity is NEVER stale. RED-FIRST (L15): four pinned scenes (throttle — a coarse entity carried so the refreshed count is strictly fewer than refresh-everything, a real compute saving; live — a sharp entity live every tick; depart — an entity walks out of the wedge and is DROPPED, no ghost; bounded — max staleness within 2^COARSEST − 1) pinned AFTER the plants were proven to bite: `_run_ghost` (carry a departed entity) and `_run_membership_throttle` (delay a new coarse entity's presence) both break closed-world-per-tick; `_run_unbounded` (never refresh after entry) exceeds the staleness bound on a run longer than 2^COARSEST; the wall-clock plant (fold an external mutable value into the cadence) is a divergence source (a zero clock inert, a nonzero clock perturbs). A seeded 90-sequence × 10-tick sweep of moving worlds asserts, per tick: CLOSED-WORLD (membership live), constant-shape, hidden-set invariance (a change to the behind entity is byte-identical); and per run: bounded staleness, a REAL throttle (refreshes < manifest-total), and deterministic replay; non-vacuous (carries, departures, and stale records all exercised). A `throttle` gate stage (4 rows: scenes / law / property / selftest) with per-row reddening; `tests/test_throttle.py` (15 falsifiers). GRADE: closed-world at every tick, bounded staleness, deterministic replay, the real compute saving, no timing channel, and the reduction to anamorphosis at the identity lens are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** The throttle needs no primitive the alphabet cannot express (the focal lens's integer shift drives an integer cadence over an explicit integer `tick` — the `tick` is DATA, not a clock; the carried record reuses the existing citation/reconcile discipline); it lives in `tools/`, consuming the kernel, never editing it. DECLARED, honestly: inherits every anamorphosis/perception boundary (the margin a bounded leak; a coarse record reveals an approximate position of a legitimately-visible entity; audio/hitbox out of scope; passive-info cheats only) and adds a NEW declared boundary — a carried position is STALE by up to 2^COARSEST − 1 ticks (the cost of the compute saving; the throttle bounds and declares it, does not eliminate it; a game needing every entity live every tick sets the identity lens, no throttle). THE THREE PILLARS NOW STAND on the focal lens: SECURITY (URDRPCP1), NETWORK (URDRANA1), COMPUTE (URDRTHR1) — all three read the same exact-integer awareness and all three preserve the closed world, the throttle over TIME exactly as the lens does over SPACE. does_not_show: continuous line-of-sight (exact integer grid); adaptive/priority scheduling beyond the clarity cadence (declared successor); cross-placement (URDRTHR1 Python reference only). New: `tools/terrain/throttle.py`, `tools/terrain/conformance_throttle.txt`, `tests/test_throttle.py` (15 falsifiers), `docs/throttle_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1223 / 676; doc-currency green.

**The adaptive priority scheduler — BANDWIDTH- AND IMPORTANCE-AWARE REFRESH SCHEDULING (`tools/terrain/schedule.py`, URDRSCH1): the throttle's declared successor, now landed, built with NO NEW GLYPH. 1223 → 1239 unit falsifiers, 676 → 680 rows.** The throttle refreshes each entity on its clarity cadence; under a real bandwidth cap a tick can have MORE due entities than it can serve, and the scheduler chooses WHICH get fresh positions. THE NEW HAZARD, AND THE LAW THAT ANSWERS IT: the obvious "serve the most important first" is a trap — a far, coarse, low-importance entity that is always outranked never refreshes, and its staleness grows without bound, breaking the throttle's bounded-staleness guarantee (STARVATION). The scheduler is therefore STARVATION-FREE by construction: among the due it serves OLDEST-FIRST (age primary), with importance and eid as deterministic tiebreaks — a deferred entity only ages, so it eventually becomes the oldest and is served; importance still shapes HOW OFTEN an entity is due (the cadence) and breaks ties among equally-stale entities, so the scheduler is importance-aware without being importance-STARVED. THE SEPARATION (inherited): scheduling delays POSITION, never PRESENCE — MEMBERSHIP is the live manifested set (closed-world every tick — a deferred entity is still SHOWN, carried; a departed one DROPPED, no ghosts); only the position-refresh is scheduled. THE MODEL (deterministic, exact-integer, no wall-clock — `tick` an explicit integer): rate = 2^shift (the throttle cadence); an entity is DUE iff it just entered OR `tick − last_refresh ≥ rate`; each tick the ENTRANTS refresh (mandatory — membership needs a position) and up to `budget` DUE-known entities refresh, chosen by priority (age, importance, −eid) descending; every other manifested entity is carried and SHOWN. RED-FIRST (L15): four pinned scenes (budget — more entities due than budget 2 can serve, so the discretionary refreshes stay ≤ budget with real deferrals; priority — the refreshed set is exactly the top-budget by priority; starvefree — over a 24-tick run at the tightest budget 1 the max staleness stays within MAX_STALE + ⌈CAPACITY/budget⌉; reduce — at budget ≥ CAPACITY nothing is deferred and staleness collapses to the throttle's ≤ MAX_STALE) pinned AFTER the plants were proven to bite: `_run_static_priority` (importance-only, no age) STARVES the coarse and exceeds the bound on a long run; `_run_membership_defer` (drop a deferred entity from the transcript) breaks closed-world; `_run_over_budget` exceeds the budget; `_run_inversion` (serve the youngest) starves the oldest; the wall-clock plant (fold an external mutable value into the priority) diverges (a zero clock inert, a nonzero clock perturbs). A seeded 80-sequence × 3-budget × 12-tick sweep of contended moving worlds asserts, per tick: CLOSED-WORLD (membership live), constant-shape, hidden-set invariance; per run: budget respected, bounded staleness, priority correct, deterministic replay; non-vacuous (deferrals, departures, and stale records all exercised). A `schedule` gate stage (4 rows: scenes / law / property / selftest) with per-row reddening; `tests/test_schedule.py` (16 falsifiers). GRADE: closed-world every tick, starvation-free bounded staleness, budget respected, priority correctness, deterministic replay, and the reduction to the throttle at high budget are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** The scheduler needs no primitive the alphabet cannot express (integer priorities over the throttle's integer cadence and the lens's integer shift, under an integer budget, driven by an explicit integer `tick` — all arithmetic on data the earlier rungs already produce); it lives in `tools/`, consuming the kernel, never editing it. DECLARED, honestly: inherits every throttle/anamorphosis/perception boundary and adds a NEW declared boundary — under budget pressure a carried position may be staler than the pure cadence (up to MAX_STALE + ⌈CAPACITY/budget⌉), the cost of the bandwidth cap; bounded and declared, not eliminated (raise the budget to the no-contention corner). The budget is a per-tick refresh COUNT, not a byte meter — byte-level bandwidth accounting is the declared successor. FOUR CAPABILITIES now stand on the focal lens, all reading the same exact-integer awareness and all preserving the closed world: SECURITY (URDRPCP1), NETWORK (URDRANA1), COMPUTE (URDRTHR1), and BANDWIDTH SCHEDULING (URDRSCH1) — the scheduler resolving WHICH refresh happens WHEN the budget binds, over time, exactly as the lens resolves WHAT RESOLUTION over space. does_not_show: continuous line-of-sight (exact integer grid); byte-level bandwidth accounting (declared successor); cross-placement (URDRSCH1 Python reference only). New: `tools/terrain/schedule.py`, `tools/terrain/conformance_schedule.txt`, `tests/test_schedule.py` (16 falsifiers), `docs/schedule_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1239 / 680; doc-currency green.

**Proof-carrying BYTE ACCOUNTING — the Byte Budget Theorem (`tools/terrain/byteacct.py`, URDRBYT1): the wire refinement of the scheduler under a real byte budget, built with NO NEW GLYPH. 1239 → 1257 unit falsifiers, 680 → 684 rows.** The scheduler proves WHICH entities are served under a bounded refresh COUNT; this rung proves the same properties under a real BYTE budget, where updates have DIFFERENT serialized costs — and makes the byte total a REPLAYABLE ARTIFACT (the operator's pipeline: World → manifested → scheduled → serialized → exact byte count; the decision, the packet, and the measured byte total all deterministic consequences of the same world). THE TENSION WITH THE HARDENING, AND ITS RESOLUTION: a variable-length packet would re-open the timing/bandwidth side-channel URDRPCP1 closed, so the BYTE BUDGET `B` IS THE CONSTANT PACKET SIZE — every tick emits EXACTLY `B` bytes (variable-size delta records + anonymous padding to `B`). The emitted transcript never exceeds `B` (it equals `B`), constant-shape is preserved, and the accounting proves the USEFUL record bytes ≤ `B − OVERHEAD`; byte-accounting COMPOSES with the constant-shape hardening rather than undoing it. THE SERIALIZATION (deterministic, exact-integer, canonical): a record is `tag | eid | payload` — REMOVE (a departure), MOVE (a canonical zigzag-varint position delta, small = few bytes), FULL (an absolute position + the 32-byte authority citation, an entrant or cite change). THE BYTE BUDGET THEOREM: mandatory records (departures + entrants, which membership needs) fit first or the tick REFUSES (typed `BYTEACCT-REFUSE`, never a silent drop); discretionary position updates — the scheduler's due-known set in its priority order — are admitted as the deterministic MAXIMAL PREFIX that fits; the rest defer and age; the remaining body is anonymous padding. RED-FIRST (L15): four pinned scenes (budget — mixed-size updates under a tight byte budget with real deferrals, every packet exactly B; prefix — the admitted set is the priority prefix; account — the byte total equals the canonical re-serialization and the client replay matches the server; reduce — at a roomy budget nothing defers, reducing to the scheduler) pinned AFTER the plants were proven to bite: `_pack_firstfit` (gap-fill) and `_pack_smallest_first` (starves a large cite-change FULL forever — the variable-size starvation the byte model introduces) break the maximal prefix; `_serialize_nopad` + `_pack_overrun` exceed `B`; `_serialize_hidden` smuggles bytes into the padding (caught by the canonical re-serialization check); `_serialize_noncanonical` (a non-minimal varint) is rejected on parse; `_run_drop_departure` never emits a REMOVE and leaves a GHOST (breaks closed-world from the wire). A seeded 70-sequence × 3-budget × 12-tick sweep of mixed-size moving worlds asserts, per run: every packet exactly B, closed-world reconstructed from the WIRE alone every tick, accounting fidelity, client == server replay, priority-prefix packing, hidden-set invariance, and deterministic replay; non-vacuous (deferrals, departures, and stale records all exercised). A `byteacct` gate stage (4 rows: scenes / law / property / selftest); `tests/test_byteacct.py` (18 falsifiers). GRADE: the byte budget, the maximal prefix, variable-size starvation-freedom, canonical serialization, accounting fidelity, closed-world from the wire, and determinism are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** Canonical integer varints over the scheduler's integer priority and the lens's integer shift, under an integer byte budget, driven by an explicit integer `tick` — the packet is DATA, the byte count a deterministic function of the world; it lives in `tools/`, consuming the kernel, never editing it. DECLARED, honestly: inherits every scheduler/throttle/anamorphosis/perception boundary and adds a NEW declared boundary — under byte pressure a position update may defer (staleness up to the scheduler's bound plus the byte-deferral), the cost of the byte cap; bounded and declared (raise `B`). does_not_show: citation compression (the 32-byte digest sent whole on a FULL); cross-tick rate limiting / multi-packet fragmentation (one packet per tick); cross-placement (URDRBYT1 Python reference only). THE COMPLETED ARC — the bandwidth subsystem now reads as a stack of measurable, replayable laws on one focal lens: PERCEPTION (what may the client observe? URDRPCP1) → ANAMORPHOSIS (at what resolution? URDRANA1) → THROTTLE (how much work may be attempted? URDRTHR1) → SCHEDULE (which updates are chosen? URDRSCH1) → BYTE ACCOUNTING (exactly how many bytes are emitted, and why no lawful scheduler could admit more? URDRBYT1); each rung replaces an implementation heuristic with a measurable law. Next declared: citation compression / cross-tick rate limiting on the byte layer. New: `tools/terrain/byteacct.py`, `tools/terrain/conformance_byteacct.txt`, `tests/test_byteacct.py` (18 falsifiers), `docs/byteacct_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1257 / 684; doc-currency green.

**The DETERMINISTIC CROSS-TICK CITATION PROTOCOL — lawful historical reuse (`tools/terrain/citation.py`, URDRCIT1): the byte layer extended from individual packets to the temporal evolution of the wire, built with NO NEW GLYPH. 1257 → 1272 unit falsifiers, 684 → 688 rows.** URDRBYT1 proves every emitted packet is a deterministic witness bounded by an exact byte budget B; the remaining inefficiency is TEMPORAL redundancy — successive ticks retransmit state the client has already verified. This rung proves that historical reuse can be made DETERMINISTIC, REPLAYABLE, and CLOSED-WORLD, preserving every perception/scheduler/byte-accounting law; it is NOT an adaptive compressor but a proof framework for lawful reuse. THE HEADLINE LAW — CITED ≡ BASELINE: the client reconstructs the SAME state sequence whether the server cites history (compact CITE records) or always sends full baselines (FULL records); compression merely changes the lawful REPRESENTATION of an already-certified history, never the semantics. A citation is a content-addressed reference to state BOTH server and client have verified: a large entity update (a FULL — absolute position + 32-byte authority citation) that RETURNS to a previously-acknowledged value is re-expressed as a compact fixed-width CITE (`tag | eid | anchor-tick`, 9 bytes vs 39), reconstructing exactly the uncited transcript. FOUR STRUCTURAL LAWS keep it proof-carrying: (1) CERTIFIED CITATION — a citation may reference only ACKNOWLEDGED history; each client carries a deterministic Acknowledgment Witness (at tick t, ticks T ≤ t − ACK_LAG are certified); the encoder is forbidden from citing outside it and emits a complete baseline when no certified anchor exists — the protocol REFUSES uncertainty rather than speculating about receiver state. (2) CONSTANT-SHAPE CITATION — a CITE is FIXED-WIDTH regardless of citation age, and every packet is padded to exactly B, preserving the URDRPCP1 constant-shape transcript. (3) CLOSED-WORLD CITATION — the citation cache is a function of the MANIFESTED set; when an entity leaves and becomes an un-addressed absence (∅), its citation history is immediately and deterministically EVICTED — a historical reference cannot resurrect information perception has withdrawn. (4) CROSS-TICK RATE — compression may not postpone mandatory baselines indefinitely; every manifested entity receives a complete FULL baseline within REFRESH_INTERVAL ticks, bounding the citation-chain depth and the client's recovery cost (the URDRSCH1 bounded-freshness preserved). RED-FIRST (L15): four pinned scenes (reuse — a toggling-cite world where a return to a prior value is a compact CITE, using fewer bytes than the all-baseline reference; equiv — cited ≡ baseline reconstruction; evict — a departed entity's history is evicted, no ghost; rate — every entity baselined within the interval) pinned AFTER the plants were proven to bite: `_encode` unack cites an un-acknowledged anchor (tick − 1, inside the ack-lag) → admission REFUSES; a forged CITE for an evicted entity is a historical GHOST (unresolvable — verify and apply both refuse); `no_baseline` lets a constant-cite entity exceed REFRESH_INTERVAL without a baseline; `_serialize_shape_drift` encodes the anchor as a variable-width varint so the packet is no longer B (parse refuses). A seeded 80-world sweep asserts, per world: cited ≡ baseline, every citation certified, closed-world (with eviction), constant-shape, bounded refresh, HIDDEN-SET INVARIANCE (a change to the behind entity leaves the wire byte-identical), a real compression, and deterministic replay; non-vacuous (citations, baselines, and the rate bound all exercised). A `citation` gate stage (4 rows: scenes / law / property / selftest); `tests/test_citation.py` (15 falsifiers). GRADE: cited ≡ baseline, the four structural laws, closed-world from the wire, and determinism are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** Content-addressed references (anchor ticks / digests are data), a deterministic Acknowledgment Witness (a set of certified ticks), and integer eviction/rate rules — all composing over the existing rungs; content-addressing and view_witness citations already exist in the kernel. DECLARED, honestly: inherits every byteacct/scheduler/throttle/anamorphosis/perception boundary; the compression benefit is a reduction in USEFUL bytes (cheaper CITE records), which under a tight byteacct budget becomes fewer deferrals — here B is roomy to isolate the citation laws. does_not_show: adaptive anchor selection (the encoder cites the OLDEST certified match, a fixed rule); lossy history compaction; cross-placement (URDRCIT1 Python reference only). THE COMPLETED TEMPORAL ARC — the bandwidth subsystem is no longer merely a bandwidth optimization but a proof-carrying TEMPORAL representation of network state (every byte transmitted, every omitted byte, every historical reference, and every refresh obligation replayable, deterministic, bounded, and independently falsifiable): PERCEPTION (observe? URDRPCP1) → ANAMORPHOSIS (resolution? URDRANA1) → THROTTLE (work? URDRTHR1) → SCHEDULE (which? URDRSCH1) → BYTE ACCOUNTING (how many bytes? URDRBYT1) → CITATION (which history may be lawfully reused, refreshed within what bound? URDRCIT1); compression never alters semantics, it changes the lawful representation of an already-certified history — extending the Urðr evidence model from individual packets to the temporal evolution of the wire itself. New: `tools/terrain/citation.py`, `tools/terrain/conformance_citation.txt`, `tests/test_citation.py` (15 falsifiers), `docs/citation_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1272 / 688; doc-currency green.

**ADAPTIVE (BANDWIDTH-AWARE) REPRESENTATION SELECTION — the cheapest lawful spelling (`tools/terrain/adaptcite.py`, URDRADC1): the citation successor, now landed, built with NO NEW GLYPH. 1272 → 1285 unit falsifiers, 688 → 692 rows.** URDRCIT1 proves lawful historical reuse but cites by a FIXED rule (the oldest certified match), which can spend a 9-byte CITE where a 7-byte MOVE was equally lawful. THE INSIGHT: since a CITE is fixed-width (constant-shape), a citation's cost does NOT depend on which anchor it names — so "bandwidth-aware" is not about which ANCHOR, it is about which REPRESENTATION. An entity's update has several lawful encodings of different cost — nothing (0) < MOVE (~7) < CITE (9) < FULL (~39); the adaptive encoder picks the MINIMUM-COST lawful one, deterministically (kind order breaks ties), SUBJECT TO the mandatory baselines the rate law forces. THE HEADLINE LAW — REPRESENTATION-INDEPENDENCE: every lawful representation reconstructs the SAME state, so the client's reconstructed state sequence is IDENTICAL under the adaptive encoder, the citation rung's oldest-match encoder, and the all-baseline encoder — the optimizer cannot corrupt semantics, it can only choose a cheaper spelling of an already-certified history. This DECOUPLES optimization from correctness: the optimizer is free precisely because representation-independence makes every lawful choice safe. RED-FIRST (L15): four pinned scenes (cheaper — a world where a position oscillates while its cite stays constant, so both a MOVE and a CITE are lawful and the adaptive encoder picks the cheaper MOVE where the fixed rule spends a CITE, a strictly smaller wire; independent — adaptive ≡ oldest ≡ baseline reconstruction; optimal — each update the min-cost lawful spelling; lawful — the adaptive wire is still a closed world, constant-shape, bounded refresh) pinned AFTER the plants were proven to bite: the `suboptimal` encoder (max-cost lawful spelling) produces a strictly LARGER wire; a forged CITE to an UNCERTIFIED anchor (cheaper than a FULL) is refused by the certified law; the `drift` encoder (a forged CITE to a NON-matching anchor) is cheap but reconstructs the WRONG state, breaking representation-independence; a wall-clock choice diverges (a nonzero clock flips the selection). A seeded 80-world sweep asserts, per world: representation-independence, optimality, a real saving (adaptive ≤ oldest bytes), closed-world with eviction, constant-shape, bounded refresh, HIDDEN-SET INVARIANCE, and deterministic replay; non-vacuous (cheaper wires, citations, and moves all exercised). An `adaptcite` gate stage (4 rows: scenes / law / property / selftest); `tests/test_adaptcite.py` (13 falsifiers). GRADE: representation-independence, optimality, the real saving, the four plants, and the inherited laws are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** An integer argmin over lawful record byte-lengths with a deterministic tiebreak — pure arithmetic over data the citation rung already produces; it lives in `tools/`, consuming the kernel, never editing it. DECLARED, honestly: inherits every citation/byteacct/scheduler/throttle/anamorphosis/perception boundary; the selection is a per-update LOCAL minimum (a greedy deterministic rule), not a global multi-tick optimum — a bounded look-ahead optimizer (still deterministic and representation-independent) is the declared successor; B is roomy here to isolate the selection laws (under a tight byteacct budget the smaller wire becomes fewer deferrals). does_not_show: multi-tick global optimization; cross-placement (URDRADC1 Python reference only). THE TEMPORAL LAYER NOW HAS TWO RUNGS: URDRCIT1 proves WHICH history may be lawfully reused, and URDRADC1 proves WHICH lawful spelling is cheapest — the encoder minimizes bytes over a choice set every member of which reconstructs the same certified state. New: `tools/terrain/adaptcite.py`, `tools/terrain/conformance_adaptcite.txt`, `tests/test_adaptcite.py` (13 falsifiers), `docs/adaptcite_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1285 / 692; doc-currency green.

**The BOUNDED LOOK-AHEAD OPTIMALITY CERTIFICATE — an honest negative result (`tools/terrain/lookahead.py`, URDRLKA1): proving a multi-tick optimizer cannot beat the greedy adaptive encoder on this model, built with NO NEW GLYPH. 1285 → 1300 unit falsifiers, 692 → 696 rows.** URDRADC1 picks the per-update LOCAL minimum-cost lawful representation; the natural question is whether a bounded W-tick look-ahead could do better globally. This rung answers with a PROOF, not a hope — and the answer, honestly, is NO. THE KEY LEMMA — CROSS-TICK INDEPENDENCE: in this model every representation (nothing / MOVE / CITE / FULL) advances the client to the same state, records the same history anchor, and any FULL resets the refresh interval identically, so the cost of a tick's representation does NOT depend on which representation earlier ticks used — the inter-tick TRANSITION cost is ZERO. A minimum-cost path over independent stages is the sum of per-stage minima, so the GREEDY per-update choice is already the GLOBAL optimum. THE CERTIFICATE: a deterministic bounded Viterbi DP over a W-tick window minimises the true total cost (base + transition); on the REAL model (transition = 0) the DP total EQUALS the greedy total — a machine-checked certificate that the adaptive encoder is globally optimal and no look-ahead helps. This is an honest CONFIRMATORY result (the Ursprung no-inflation discipline applied to an optimization question): we PROVE you do not need look-ahead, rather than claim a win that is not there. THE TEETH: the certificate is only meaningful if the DP is a real optimizer — so on a SYNTHETIC COUPLED cost model (a penalty for choosing the cheapest representation twice in a row, a transition the real model does not have), the DP finds a strictly cheaper assignment than greedy's ACTUAL cost (16 vs 114 in the pinned case), and its total DIFFERS from greedy — so the DP genuinely optimises, and the real-model equality is a real measurement, not a tautology. RED-FIRST (L15): four pinned scenes (optimal — greedy certified globally optimal on the real model, non-vacuously; teeth — the DP beats greedy on the coupled model; independent — the look-ahead encoding equals the adaptive encoding and reconstructs the baseline states; bounded — an over-window search is refused) pinned AFTER the plants were proven to bite: the coupled model makes DP ≠ greedy (the certificate is not vacuous) and DP < greedy's actual cost (teeth); an over-window search raises. A seeded 80-world sweep asserts, per world: greedy-optimality on the real model (the bounded DP finds no improvement), representation-independence, hidden-set invariance of the certified wire, and deterministic certification, with the optimizer-has-teeth invariant checked once; non-vacuous (656 genuine multi-option windows certified). A `lookahead` gate stage (4 rows: scenes / law / property / selftest); `tests/test_lookahead.py` (15 falsifiers). GRADE: greedy-optimality, the DP's teeth, the coupling-detection, representation-independence, the bounded window, and determinism are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** A deterministic Viterbi over integer costs plus a lemma about the cost model — pure arithmetic and proof over data the adaptive rung already produces; it lives in `tools/`, consuming the kernel, never editing it. DECLARED, honestly: the certificate is specific to THIS cost model, whose per-tick anchors are independent — a model with BOUNDED HISTORY (where citing an old anchor risks its eviction before reuse) WOULD couple ticks and give look-ahead genuine teeth; that bounded-history optimizer (still deterministic and representation-independent) is the declared successor. does_not_show: bounded-history coupling; adaptive window sizing; cross-placement (URDRLKA1 Python reference only). THE OPTIMIZATION ARC IS NOW CLOSED WITH A PROOF: URDRADC1 picks the cheapest lawful spelling greedily, and URDRLKA1 certifies that greedy spelling is globally optimal on this model — optimization stops not at a heuristic's edge but at a proven optimum. New: `tools/terrain/lookahead.py`, `tools/terrain/conformance_lookahead.txt`, `tests/test_lookahead.py` (15 falsifiers), `docs/lookahead_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1300 / 696; doc-currency green.

**The BOUNDED-HISTORY OPTIMIZER — where look-ahead earns its teeth (`tools/terrain/boundedhist.py`, URDRBHO1): the inversion URDRLKA1 predicted, built with NO NEW GLYPH. 1300 → 1314 unit falsifiers, 696 → 700 rows.** URDRLKA1 proved that with unbounded, always-available history the per-tick representation choices are cross-tick independent, so greedy is globally optimal and look-ahead is UNNECESSARY — and it named the successor that would give look-ahead teeth: a bounded-history model. This is that model, and it delivers the inversion. THE COUPLING: a real client caches only H keyframes and must EVICT; a returning state is a compact CITE only if its keyframe is still cached, and WHICH slot to evict on a miss is a genuine multi-tick decision that COUPLES the ticks (the classic cache-replacement problem). On a cyclic access pattern with H below the cycle length, greedy LRU is PESSIMAL — it evicts exactly the keyframe about to be used next, thrashing to a near-zero hit rate — while BELADY's optimal replacement (evict the keyframe whose next use is furthest in the future) maximises hits, needing the future the SERVER has and a bounded W-tick look-ahead supplies. So on the REAL, coupled model the DP (Belady) produces a STRICTLY SMALLER wire than greedy (LRU): on the 3-cycle with H=2, LRU gets 0 hits / 960 bytes while Belady gets 11 hits / 619 bytes — look-ahead finally has TEETH. SOUNDNESS: Belady's evictions depend on the future the CLIENT does not know, so every eviction is SIGNALED on the wire (a FULL names the slot it replaces); the client mirrors the cache exactly (determinism and closed-world preserved) and the eviction-signal cost is COUNTED (the win survives paying it). THE HEADLINE LAW — REPRESENTATION-INDEPENDENCE (still): every policy — greedy LRU, look-ahead Belady, and the all-FULL baseline — reconstructs the SAME key sequence; the optimizer changes only WHICH slot is evicted and therefore the byte cost, never the reconstructed state. RED-FIRST (L15): four pinned scenes (teeth — Belady's wire strictly smaller than LRU's on a thrashing cycle; independent — every policy reconstructs the same keys; optimal — unbounded Belady == the offline optimum misses; bounded — the cache never exceeds H) pinned AFTER the plants were proven to bite: a wrong-slot CITE reconstructs the WRONG key (caught by the client mirror); an out-of-range eviction slot and an empty-slot CITE are refused; the wall-clock plant diverges. A seeded 120-sequence sweep asserts, per random recurring access pattern under a binding cache: representation-independence, bounded cache, Belady optimality, Belady never worse than LRU, and deterministic encoding; non-vacuous (look-ahead strictly beat greedy on 95 of 120 sequences). A `boundedhist` gate stage (4 rows: scenes / law / property / selftest); `tests/test_boundedhist.py` (14 falsifiers). GRADE: the teeth, Belady optimality, representation-independence, the bounded cache, and determinism are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** A bounded cache simulation with signaled evictions and integer costs — pure arithmetic and a classic replacement result over data the earlier rungs already produce; it lives in `tools/`, consuming the kernel, never editing it. DECLARED, honestly: the model abstracts an entity's keyframe accesses; the eviction-signal cost is a fixed small overhead per miss; a byte-accurate wire and the byteacct-budget interaction are declared successors; the Belady look-ahead is bounded to a W-tick window (causal-within-W). does_not_show: variable-size keyframes; shared cross-entity cache; cross-placement (URDRBHO1 Python reference only). THE OPTIMIZATION ARC IS NOW PROVEN ON BOTH SIDES OF THE COUPLING BOUNDARY: URDRLKA1 — where ticks are independent (unbounded history), greedy is globally optimal and look-ahead is provably unnecessary; URDRBHO1 — where ticks couple (bounded history), look-ahead provably beats greedy (the Belady/LRU gap). The engineering lesson is now a theorem: look-ahead's value is EXACTLY the cross-tick coupling, and this repo MEASURES that coupling rather than assuming it. New: `tools/terrain/boundedhist.py`, `tools/terrain/conformance_boundedhist.txt`, `tests/test_boundedhist.py` (14 falsifiers), `docs/boundedhist_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1314 / 700; doc-currency green.

**AUDIBLE ABSENCE — the audio channel of the anti-cheat firewall (`tools/terrain/audible.py`, URDRAUD1): witnessed absence applied to positional sound, built with NO NEW GLYPH. 1314 → 1328 unit falsifiers, 700 → 704 rows.** Chosen by an OODA/websearch review of the best next design step: the perception rung declared audio explicitly OUT OF SCOPE, and positional-audio leakage is a CURRENT, PUBLIC open problem — VALORANT and CS2 both have ongoing footstep-sound controversies, whose root fault is that engines transmit audio data for sounds a client should not be able to hear, leaking enemy position at low volume. WITNESSED ABSENCE answers it directly: a sound BELOW the audibility threshold (too quiet, too far, or wall-occluded) is an UN-ADDRESSED ABSENCE, so an audio-ESP replayed against the listener transcript finds NOTHING — no sub-threshold position leak. THE THESIS: the D15 presentation firewall (`view_witness`) applied to the AUDIO RESIDENCY channel — the witness is the world of sound EVENTS (integer position, integer source loudness, authority citation), untouched; the audible set is a per-listener view-side channel walled from it; an inaudible sound is not a zeroed record with a volume flag but an un-addressed absence. THE EXACT-INTEGER AUDIBILITY (deterministic, no floats, OMNIDIRECTIONAL — no wedge, unlike vision): a sound of loudness `L` at squared distance `d²` across `w` wall cells is audible iff `L ≥ MIN_LOUDNESS` and `d² ≤ L*RANGE_PER_LOUDNESS − WALL_PENALTY*w` (louder carries further; walls attenuate). WHAT THE LISTENER LEGITIMATELY GETS (bounded localization, not the source): for each audible sound a BUCKETED direction (one of 8 integer compass sectors — exact, no `atan2`/float) and a QUANTIZED heard loudness — the spatial cue a player is allowed to have, bounding the source to an annular sector, never a point (a declared bounded leak, as anamorphosis bounds visual precision). RED-FIRST (L15): four pinned scenes (near — a loud nearby sound is HEARD with a direction, a distant quiet one is inaudible; wall — a sound audible in the open is MUFFLED below audibility behind three walls, byte-identical whether the muffled sound moves; direction — four sounds around the listener resolve to four distinct sectors, none an exact position; esp — the verdict IS the defeat: an audio-ESP probes a distant quiet footstep and finds Ø) pinned AFTER the footstep-leak plant (`_perceive_leak` — the engine mistake, a low-volume "whisper" record for a sub-threshold sound) was proven to let an audio-ESP read a hidden enemy's direction AND break the closed world. A seeded 120-soundscape sweep asserts witness-blindness, hidden-set invariance (a change to a ground-truth-inaudible sound leaves the transcript BYTE-IDENTICAL — the audio-ESP has nothing to read), the closed-world reconstruction, constant-shape, and the citation contract; non-vacuous (inaudible / audible / wall-muffled all exercised). An `audible` gate stage (4 rows: scenes / law / property / selftest); `tests/test_audible.py` (14 falsifiers). GRADE: witness-blindness, hidden-set invariance, the constant-shape transcript, the audio-ESP-finds-nothing property, the certified audibility band, the wall-muffle, the bounded localization, the citation contract, and the closed world are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** Audible absence is the same view-layer residency channel as perception on the audio dimension — exact-integer audibility and an integer direction bucket over data the world already carries; the membrane already models manifested-vs-absent; it lives in `tools/`, consuming the kernel, never editing it. DECLARED, honestly: the AUDIBLE localization (sector + loudness) is a real, bounded, DECLARED leak — a player who can legitimately hear an enemy can roughly place them (spatial audio, fair play); the rung bounds it, it does not eliminate it. This governs the positional-audio channel only; occlusion is a simple integer wall-attenuation model, not acoustic propagation (reverb, diffraction). does_not_show: continuous acoustics (exact integer grid); the visual/hitbox channels (their own rungs); cross-placement (URDRAUD1 Python reference only). THE ANTI-CHEAT FIREWALL NOW COVERS TWO CHANNELS with one discipline: VISION (URDRPCP1, witnessed absence) and AUDIO (URDRAUD1, audible absence) — both answer the same fault (never transmit data for what a client should not perceive) and both close a seam production systems are publicly known to leave open. Next channel in the same shape: hitbox/physics exposure. New: `tools/terrain/audible.py`, `tools/terrain/conformance_audible.txt`, `tests/test_audible.py` (14 falsifiers), `docs/audible_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1328 / 704; doc-currency green.

**SERVER-AUTHORITATIVE HIT VALIDATION — the ACTIVE channel of the anti-cheat firewall (`tools/terrain/hitbox.py`, URDRHIT1): the aimbot / wall-shoot defense, built with NO NEW GLYPH. 1328 → 1344 unit falsifiers, 704 → 708 rows.** The residency channels (URDRPCP1 vision, URDRAUD1 audio) close the seam where a server TRANSMITS data a client should not have; this rung closes the DUAL seam — where a server ACCEPTS an authority claim the world does not support. It is the active-cheat frontier: wall-shooting (a hit through geometry), phantom hits (a hit where no target is), inflated hitboxes (a client asserting a bigger silhouette than the server's), off-ray snaps (an aimbot claiming a target the shooter is not on), and out-of-range claims. THE THESIS: the passive discipline turned active — witnessed absence says a hidden entity is an UN-ADDRESSED ABSENCE (no byte to read); server-authoritative validation says an unearned hit is an UN-ADDRESSABLE ADMIT (no verdict to forge). THE WITNESS is the authoritative world — targets at integer positions carrying the SERVER's AABB half-extents and an authority citation, plus the wall set; a CLAIM `(target, point)` is a client assertion walled from the decision. THE EXACT-INTEGER ADMISSION (deterministic, no floats), in a FIXED reason priority so the verdict is a pure function of the world: the point must be ON the server's box (`ex−hbx ≤ hx ≤ ex+hbx` and likewise y — a client-claimed extent is NEVER read), ON the forward aim ray (exact-integer colinear `(hx−px)·ay == (hy−py)·ax` AND forward `(hx−px)·ax + (hy−py)·ay > 0` — no `atan2`/float), WITHIN squared range, and the LINE OF FIRE must cross no wall cell (reusing `perception._occluded`, the integer supercover). The server emits a CONSTANT-SHAPE (92-byte) PROOF-CARRYING verdict — verdict code, fixed-priority reason, and (on ADMIT only) the target's authority citation (a REFUSE carries a zero-cite, leaking nothing). RED-FIRST (L15): five pinned scenes (clean — a legitimate hit on-box/on-ray/in-range/unoccluded ADMITS; wallshot — a wall on the line of fire REFUSES an otherwise-valid shot; phantom — a point on the ray but past the box REFUSES, no target there; offray — a box corner off the aim line REFUSES, the aimbot boundary; inflated — a point off the real box REFUSES though a bigger client extent would accept it) pinned AFTER each of the FIVE forgery plants (`_admit_no_box`, `_admit_no_ray`, `_admit_no_range`, `_admit_no_occlusion`, `_admit_client_extent` — each skips exactly one check or trusts the client extent) was proven to ADMIT precisely where the honest law REFUSES, and after the sweep was proven to REDDEN under a skipped-occlusion / box-blind adjudicator. THE PROOF-CARRYING TEETH: `forge_admit` rewrites a REFUSE into a sealed ADMIT with a VALID self-digest, yet `verify_verdict` still refuses it — because a verdict is lawful iff it is BYTE-IDENTICAL to a fresh authoritative adjudication of its own claim, and the server, not the client, decides. A seeded 120-arena sweep asserts server-authority (the verdict never reads a client extent), witness-blindness, determinism, the constant-shape verdict, the proof-carrying contract (a forged ADMIT never verifies), the guaranteed admit ADMITS, the guaranteed wall-shot is REFUSED (occlusion honoured), an off-ray corner is REFUSED, and an off-real-box point a client's inflated box would accept is REFUSED; non-vacuous (admit / wall / off-ray / inflated all exercised). A `hitbox` gate stage (4 rows: scenes / law / property / selftest); `tests/test_hitbox.py` (16 falsifiers). GRADE: server-authority (the verdict is a pure function of the authoritative world + claim, never of a client-supplied extent), determinism, the five refusals (each planted and proven to bite), the clean admit with its authority citation, the constant-shape verdict, and the proof-carrying contract are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** Hit validation is the same view-layer authority discipline as perception, run on the CLAIM direction instead of the RESIDENCY direction — exact-integer admission predicates and the same integer supercover occlusion over data the world already carries, adjudicated by machinery the membrane already models; it lives in `tools/`, consuming the kernel, never editing it. DECLARED, honestly: this is INSTANTANEOUS validation against the CURRENT authoritative snapshot — TEMPORAL LAG-COMPENSATION (rewinding target positions to the shooter's view-time) is the DECLARED SUCCESSOR, not solved here; without it a legitimately-aimed shot at a moving target can be wrongly refused. THE HONEST AIMBOT BOUNDARY (shared with URDRPCP1): the rung refuses geometrically-impossible claims but does NOT touch aim ASSISTANCE on a legitimately-hittable target — a lawful geometric hit is admitted whether a human or an aimbot produced the aim. does_not_show: continuous geometry / sub-cell precision (exact integer grid); real ballistics (penetration, drop, ricochet); lag-compensation (the successor); cross-placement (URDRHIT1 Python reference only). THE ANTI-CHEAT FIREWALL NOW COVERS THREE CHANNELS with one discipline: VISION (URDRPCP1) and AUDIO (URDRAUD1) on the RECEIVE side — never transmit data for what a client should not perceive — and HIT VALIDATION (URDRHIT1) on the CLAIM side — never accept an authority claim the world does not support. Next refinement in the same shape: temporal lag-compensation, which earns the hit channel its teeth against moving targets. New: `tools/terrain/hitbox.py`, `tools/terrain/conformance_hitbox.txt`, `tests/test_hitbox.py` (16 falsifiers), `docs/hitbox_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1344 / 708; doc-currency green.

**TEMPORAL LAG-COMPENSATION — the refinement that earns the hit channel its teeth against MOVING targets (`tools/terrain/lagcomp.py`, URDRLAG1): composition over URDRHIT1 (over URDRPCP1), built with NO NEW GLYPH. 1344 → 1360 unit falsifiers, 708 → 712 rows.** URDRHIT1 adjudicated a claimed hit against the CURRENT authoritative snapshot and declared its own boundary: without lag-compensation, a legitimately-aimed shot at a MOVING target can be wrongly refused — a shooter fired at what they SAW, the world as of an earlier tick, and by the time the claim reaches the server the target has left that position. This rung closes that declared successor with the universally-deployed netcode fix, made deterministic and exact-integer: REWIND the target to the shooter's view-tick and adjudicate there. THE THESIS: the temporal analogue of the geometric authority URDRHIT1 established — URDRHIT1 fixed WHERE the target is, URDRLAG1 fixes WHEN. A bounded TIMELINE (one authoritative target-set per tick, walls static across the window) is the witness; a claim carries the shooter's VIEW-TICK `vt`. The server (1) BOUNDS `vt` to `[now − MAX_REWIND, now]` — a FUTURE claim (`vt > now`) or an OVER-OLD claim (`vt < now − MAX_REWIND`) is REFUSED, the anti-abuse bound that stops a cheater backdating to an ancient favourable frame; (2) REWINDS — looks up the EXACT stored target-set at `vt` (no float, no sub-tick interpolation — determinism is structural); (3) DELEGATES to URDRHIT1's geometric admission at the rewound position. Lag-comp moves the target in TIME and does nothing else — the geometry is unchanged and UNCOMPROMISED. RED-FIRST (L15): five pinned scenes (rewind — THE TEETH: a target on the crosshair at vt=95 has drifted off-axis by now=100, so rewinding ADMITS while the no-rewind adjudicator at `now` would refuse; stale — a view-tick one past the 8-tick window is REFUSED though the buffer still holds it; future — a view-tick after the present is REFUSED; wall_at_vt — a far target behind a wall at the rewound tick is REFUSED, geometry composing; behind_cover — the DECLARED favor-the-shooter tradeoff: a target open on the shooter's screen at vt is admitted though it may have moved) pinned AFTER the three plants (`_admit_no_rewind` — adjudicate at `now`, the core teeth; `_admit_no_window` — skip the stale bound, admitting an over-old claim; `_admit_clamp_future` — clamp a future tick to `now` instead of rejecting) were each proven to ADMIT/REFUSE precisely opposite the honest law, and after the sweep was proven to REDDEN under a no-rewind and a no-window adjudicator. THE PROOF-CARRYING TEETH: the 104-byte verdict carries the view-tick AND the EXACT rewound position the server adjudicated against (auditable — an observer sees which historical snapshot was used); `forge_admit` re-seals a REFUSE into a valid-self-digest ADMIT, yet `verify_verdict` still refuses it because a fresh lag-compensated adjudication disagrees. A seeded 120-timeline sweep asserts the rewind teeth (a moved-target shot admits by rewinding while the no-rewind adjudicator refuses — non-vacuous, the target must have moved), the window bound (a stale and a future claim refused, each plant admitting), composed geometry (a wall-shadowed rewound shot refused), determinism, the constant-shape verdict, and the proof-carrying contract; non-vacuous (rewind / stale / future / wall all exercised). A `lagcomp` gate stage (4 rows: scenes / law / property / selftest); `tests/test_lagcomp.py` (16 falsifiers). GRADE: the rewind teeth, the window bound, composed geometry, determinism, the constant-shape verdict, and the proof-carrying contract are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** Lag-compensation is a temporal index over data the world already carries — a bounded history of authoritative snapshots, a tick bound, and a delegation to URDRHIT1's admission; the membrane already models the per-tick authoritative state, this rung reads a PAST one instead of the present and hands it to the existing geometric law; it lives in `tools/`, consuming hitbox and perception, never editing the kernel. DECLARED, honestly: the FAVOR-THE-SHOOTER tradeoff — a target who has stepped behind cover on THEIR OWN screen can still be hit, because the shooter's earlier view was authoritative (the well-known "killed behind cover" artifact) — is a real, BOUNDED consequence, capped by MAX_REWIND; the rung makes the window an explicit certified quantity, it does not (and cannot) pretend the latency-inherent asymmetry away. does_not_show: sub-tick interpolation (exact per-tick snapshots, no float); moving/destructible walls (static across the window); clock-authority (the view-tick is taken as given — a lying clock is the declared successor); real network transport; cross-placement (URDRLAG1 Python reference only). THE HIT CHANNEL NOW VALIDATES MOVING TARGETS HONESTLY: URDRHIT1 established WHERE a hit is lawful, URDRLAG1 establishes WHEN — rewinding to the shooter's authoritative view within a bounded window while every geometric refusal composes through. Next refinement in the same shape: clock-authority (bounding the view-tick a client may assert). New: `tools/terrain/lagcomp.py`, `tools/terrain/conformance_lagcomp.txt`, `tests/test_lagcomp.py` (16 falsifiers), `docs/lagcomp_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1360 / 712; doc-currency green.

**CLOCK-AUTHORITY — bounding the view-tick a client may assert to its server-attested latency (`tools/terrain/clockauth.py`, URDRCLK1): the rung that closes the backdating-within-the-window abuse URDRLAG1 left declared, composition over URDRLAG1 (over URDRHIT1, over URDRPCP1), built with NO NEW GLYPH. 1360 → 1376 unit falsifiers, 712 → 716 rows.** URDRLAG1 rewinds the target to the shooter's VIEW-TICK `vt` and bounds `vt` to the compensable window `[now − MAX_REWIND, now]` — but TAKES `vt` AS GIVEN. Within the window a cheater is free to pick whichever tick is most favourable (the frame a target was exposed, on their crosshair, or not yet behind cover); the window bound cannot catch it because the cherry-picked tick is inside the window and geometrically valid there. This is a real, documented class of lag-comp abuse (fake-ping / backdate cheats), and it is exactly the seam URDRLAG1's honest boundary named. This rung closes it: the admissible `vt` is bounded to what the client's MEASURED latency justifies. THE THESIS: a legitimate `vt` is not any tick in the window — it is the tick the client actually rendered, which the client's SERVER-ATTESTED latency pins to a narrow band. Per client the server holds `(lat, jitter)` in ticks, derived from the acknowledgment / round-trip stream and NEVER client-asserted (a client cannot assert its own latency to widen its band — that would just move the cheat up a level); the admissible band at server tick `now` is `[now − lat − jitter, now − lat + jitter]`, clamped inside the lag window. A claim's `vt` must fall in that band (CLOCK-CONSISTENT), checked BEFORE any rewind, or it is REFUSED with R_CLOCK; a clock-consistent `vt` is then handed to URDRLAG1, whose window+rewind and URDRHIT1's geometry compose through UNCOMPROMISED — the chain is now clock-consistency → lag-window + rewind → hitbox geometry. RED-FIRST (L15): five pinned scenes (consistent — a view-tick matching the attested latency, lat 3 band [96,98] at now=100, on a hittable target ADMITS; backdate — THE TEETH: a cherry-picked older view-tick 95, inside the window and geometrically valid on the STATIC target, is REFUSED with R_CLOCK; forward — a view-tick 100 fresher than the latency allows is REFUSED, a laggy client cannot claim the present; laggy — latency-proportional: a lat-6 client's older band [93,95] legitimately ADMITS view-tick 94, the band tracks the attested latency; wall — composition: a clock-consistent but wall-shadowed rewound target is REFUSED with R_WALL) pinned AFTER the two plants (`_admit_no_clock` — skip the band and delegate straight to lag-comp, admitting the cherry-picked backdate; `_admit_client_latency` — trust a CLIENT-ASSERTED inflated latency to widen the band, admitting a backdate the attested clock refuses) were each proven to ADMIT precisely where the honest law REFUSES, and after the sweep was proven to REDDEN under a disabled clock band. THE ATTESTATION PROPERTY is the crux: the sweep uses a STATIC on-axis target hittable at EVERY tick, so the clock band is provably the SOLE discriminator between a legitimate and a backdated view-tick (the backdate is refused for the clock, not the geometry — asserted explicitly). THE PROOF-CARRYING TEETH: the 120-byte verdict carries the ATTESTED `(lat, jitter)` and the enforced band `[lo, hi]` (auditable — an observer sees WHY the view-tick was accepted or refused), and a verdict issued under one attested clock does NOT verify under another (the clock is an authoritative input, not part of the claim); `forge_admit` re-seals a REFUSE into a valid-self-digest ADMIT, yet `verify_verdict` still refuses it because a fresh clock-bounded adjudication disagrees. A seeded 120-arena sweep asserts the clock-consistent admit, the backdating teeth (refused by the clock while the no-clock plant admits), the forward-skew refusal, the attestation property (a client-asserted latency admits the backdate), composed geometry (a wall-shadowed clock-consistent shot refused), determinism, the constant-shape verdict, and the proof-carrying contract; non-vacuous (admit / backdate / forward / attest / wall all exercised). A `clockauth` gate stage (4 rows: scenes / law / property / selftest); `tests/test_clockauth.py` (16 falsifiers). GRADE: the clock-consistent admit, the backdating teeth, the forward-skew refusal, the attestation property, latency-proportionality, composition, determinism, the constant-shape verdict, and the proof-carrying contract are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** Clock-authority is a bound on ONE field of the claim, derived from data the server already holds (the per-client latency measurement) and delegating everything geometric and temporal to URDRLAG1; the membrane already models the authoritative per-client state, this rung reads the attested latency and gates the asserted view-tick against it; it lives in `tools/`, consuming lagcomp / hitbox / perception, never editing the kernel. DECLARED, honestly: the JITTER band is a real, BOUNDED leak — a band of width `2·jitter+1` ticks the client may legitimately claim within, because network jitter is real (a zero-jitter band would false-refuse honest laggy players); the rung bounds the slack and makes it an explicit certified quantity, it does not (and cannot) eliminate it without punishing legitimate players. does_not_show: the ACCURACY of the latency estimate itself — this rung takes `(lat, jitter)` as attested truth; HOW the ack-stream measures it, and how that measurement resists a SLOW-DRIP latency forge (a client gradually inflating its apparent ping to widen its band over time), is the DECLARED SUCCESSOR; sub-tick timing; real network transport; cross-placement (URDRCLK1 Python reference only). THE LAG-COMPENSATED HIT CHANNEL IS NOW CLOSED ON BOTH AXES IT EXPOSED: URDRHIT1 fixed WHERE a hit is lawful, URDRLAG1 fixed WHEN the server rewinds to, and URDRCLK1 fixes WHICH VIEW-TICK the client may claim — bound to its attested latency, so the rewind cannot be cherry-picked. Next refinement in the same shape: the latency-estimator itself (measuring and defending `(lat, jitter)` from the acknowledgment stream). New: `tools/terrain/clockauth.py`, `tools/terrain/conformance_clockauth.txt`, `tests/test_clockauth.py` (16 falsifiers), `docs/clockauth_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1376 / 716; doc-currency green.

**THE LATENCY-ESTIMATOR — measuring and defending the attested clock clock-authority relies on (`tools/terrain/latencyest.py`, URDRLES1): the rung that closes the slow-drip latency forge URDRCLK1 left declared, composition over URDRCLK1 (over URDRLAG1, URDRHIT1, URDRPCP1), built with NO NEW GLYPH. 1376 → 1393 unit falsifiers, 716 → 720 rows.** URDRCLK1 bounds the client's asserted view-tick to a server-ATTESTED latency `(lat, jitter)` — but TAKES THAT LATENCY AS GIVEN. It named, as its declared boundary, that it does not MEASURE the latency nor DEFEND the measurement against a client that gradually inflates its apparent ping to widen its clock band over time (a slow-drip forge), eventually legalising a backdate. This rung closes it: `(lat, jitter)` is derived deterministically from the acknowledgment / round-trip stream, and the derivation is built to RESIST inflation. THE THESIS — WHY THE MINIMUM: the measurement is a round-trip (the server sends a tick-stamped ping, the client echoes it, the server reads `RTT = recv_tick − sent_tick`), and the asymmetry that makes it defensible is PHYSICAL — a cheater can DELAY an echo (inflating that sample's RTT) but can NEVER make it arrive faster than the true network path, so the MINIMUM RTT over a window is an honest floor, immune to inflation as long as one true-timed ack lands in the window. The one-way latency is `min_rtt // 2`. Three further defenses: the estimate may RISE by at most `MAX_RISE` ticks per update (a drip is rate-limited, its every step bounded and visible) but FALLS freely (a genuinely improved ping tightens the band at once — a tighter band is never a cheat); the JITTER is the bounded spread CAPPED at `MAX_JITTER` (a few delayed acks cannot widen the band without limit); and an IMPLAUSIBLE RTT — negative (an echo before its ping) or beyond `MAX_RTT` — is REFUSED, never folded in. The output is a `clockauth.clock`, fed straight into URDRCLK1. RED-FIRST (L15): five pinned scenes (honest — a steady RTT-6 stream yields the true one-way latency 3 with no jitter; inflate — THE MIN FLOOR: half the acks delayed to RTT 12 but the minimum stays 6, so the latency stays at its floor 3, the inflation does not move it; drip — THE RATE LIMIT: every ack delayed (raw latency 5) but from a low prior 2 the estimate may only rise MAX_RISE, clamped to 3; improve — FREE FALL: an improved ping (RTT 4) drops the latency immediately from a high prior 5 to 2; implausible — an RTT of 100 beyond the max is REFUSED) pinned AFTER the three plants (`_estimate_by_mean` — use the MEAN RTT so delayed acks inflate the latency; `_estimate_no_ratelimit` — publish the raw latency with no rise clamp so a drip jumps; `_estimate_no_plausibility` — fold a garbage RTT straight in) were each proven to ADMIT/inflate precisely where the honest law holds/refuses, and after the sweep was proven to REDDEN under a mean-based estimator. THE END-TO-END PAYOFF (the composition teeth, asserted every scenario): the honest estimator's clock keeps the URDRCLK1 band TIGHT and REFUSES a backdate, while a defective (mean-inflated) estimator's clock WIDENS the band and ADMITS it — tying the measurement back to the thing it protects; the min-floor is what keeps the honest band tight. THE PROOF-CARRYING CONTRACT: the 88-byte published record is bound to its exact ack window by a digest; `forge_clock` bumps the sealed latency and re-seals a valid self-digest, yet `verify_record` still refuses it because a fresh honest publish over the window disagrees, and a record presented against a DIFFERENT ack window (whose digest differs) also fails. A seeded 120-arena sweep asserts the min floor (an inflated window's latency equals the honest floor while the mean plant inflates it), the rate limit (a drip rises at most MAX_RISE while the no-ratelimit plant jumps), the jitter cap, the plausibility rejection, the end-to-end composition, determinism, and the proof-carrying record; non-vacuous (floor / drip / implausible / compose all exercised). A `latencyest` gate stage (4 rows: scenes / law / property / selftest); `tests/test_latencyest.py` (17 falsifiers). GRADE: the honest convergence, the min floor, the rate-limited rise, the free fall, the jitter cap, the plausibility rejection, the end-to-end composition, determinism, and the proof-carrying record are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** The estimator is integer arithmetic over a window of round-trip samples the server already collects, producing the `(lat, jitter)` pair URDRCLK1 already consumes; the ack stream is data the transport already carries, this rung reduces it to a defended scalar and hands it to the existing clock law; it lives in `tools/`, consuming clockauth / lagcomp / hitbox / perception, never editing the kernel. DECLARED, honestly: this BOUNDS and SLOWS band-widening — it does NOT make inflation impossible; a patient cheater who delays EVERY ack in the window can raise the minimum, and the estimate can still climb MAX_RISE per update; the rung caps the RATE and forces the inflation to be total (every sample delayed) and visible, it does not prove a cheater can never widen their band at all — that residual is the honest boundary, bounded and stated. does_not_show: the ping-SCHEDULING / sample-selection policy (this rung consumes a given window — WHEN to ping and WHICH samples to keep is the declared successor); clock-skew beyond the jitter model; real network transport; cross-placement (URDRLES1 Python reference only). THE LAG-COMPENSATED HIT CHANNEL IS NOW SELF-CONTAINED: URDRHIT1 (WHERE a hit is lawful) + URDRLAG1 (WHEN the server rewinds to) + URDRCLK1 (WHICH view-tick the client may claim) + URDRLES1 (the measured clock that bounds it), alongside vision (URDRPCP1) and audio (URDRAUD1) on the receive side. Next refinement in the same shape: the ping-scheduling policy that decides which samples the estimator sees. New: `tools/terrain/latencyest.py`, `tools/terrain/conformance_latencyest.txt`, `tests/test_latencyest.py` (17 falsifiers), `docs/latencyest_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1393 / 720; doc-currency green.

**THE PING POLICY — one invariant closing the measurement layer, stated as a falsifiable theorem (`tools/terrain/pingpolicy.py`, URDRPNG1): the scheduling / sample-selection rung feeding URDRLES1's ack window, composition over URDRLES1 (over URDRCLK1, URDRLAG1, URDRHIT1, URDRPCP1), built with NO NEW GLYPH. 1393 → 1422 unit falsifiers, 720 → 724 rows.** URDRLES1's min-floor defense rests on a single sentence — the minimum RTT is honest "AS LONG AS ONE TRUE-TIMED ACK LANDS IN THE WINDOW" — which URDRLES1 cannot itself guarantee, since it consumes a window someone else chose; and its declared residual was OPEN-ENDED (a client who delays EVERY ack raises the minimum, and the estimate may climb MAX_RISE per update, so with enough patience the band widens without bound). Both gaps live in the layer ABOVE the estimator: who pings, when, and which echoes count. THE DESIGN MOVE: rather than patch the two gaps separately, generalise the shape URDRLES1 already had — "rises slowly, falls freely", where the direction that HELPS the client is hard and the direction that COSTS them is easy — and lift it from the estimate to EVERY degree of freedom the client can touch. THE INVARIANT — **CONDITIONAL MONOTONE DISADVANTAGE: once a session floor is established, every lever the client can pull resolves against the client.** Such a client may always make their own clock band TIGHTER; no strategy makes it WIDER than honest behaviour beyond one declared constant. It is stated as a FALSIFIABLE THEOREM over an EXPLICIT client strategy space, not as a hope: `GIVEN a session floor founded on a window the client did not pad, for every strategy σ, reach(σ) ≤ reach(honest) + DRIFT_ALLOWANCE`, and `for every NON-TOTAL-DELAY σ, reach(σ) ≤ reach(honest)`, where `reach = lat + jitter` is exactly how far back into the lag window URDRCLK1 will let that client claim. **THE PRECONDITION IS LOAD-BEARING, AND ITS FAILURE IS THIS RUNG'S DECLARED RESIDUAL — THE COLD START (found by an engineering-rigor audit of this very entry's first draft, which stated the theorem UNCONDITIONALLY and was wrong to).** The session floor is only as honest as the window that SET it: a client padding EVERY ack from the moment it CONNECTS never founds an honest floor, records an inflated one, and keeps a permanently WIDER band than honest play — MEASURED at reach 6 vs honest 3 on the reference path (true RTT 6), i.e. past the theorem's own `honest + DRIFT_ALLOWANCE` bound. That client is NOT covered. WHAT STILL BOUNDS IT (also measured, asserted every sweep): padding beyond the plausibility ceiling `MAX_RTT` is REFUSED outright so the samples never enter the estimate, capping `reach ≤ cold_start_ceiling() = MAX_RTT//2 + DRIFT_ALLOWANCE + MAX_JITTER = 11`; and URDRCLK1 clamps the admissible band to the lag window regardless, so backdating never exceeds MAX_REWIND however the floor was set — a BOUNDED constant, but a LARGER one than honest play. WHY IT IS NOT MERELY UNFIXED: a client padding from connect is INDISTINGUISHABLE, FROM TIMING ALONE, from a client on a genuinely slow path (at connect the server holds no prior for it, and refusing the padded one would refuse the honest laggy one identically), so closing it needs an OUT-OF-BAND prior — a route/population baseline, a geo/AS expectation, or a trusted first measurement — which is the declared successor and a DIFFERENT KIND of evidence, not more of this one. The residual is pinned as the `coldstart` scene, asserted every sweep against its ceiling and its refusal-beyond-plausibility, and guarded by a FIXED WITNESS that raises if the gap ever stops being real — so the declared boundary cannot silently go vacuous. FOUR LAWS COMPOSE TO IT, each closing one way a strategy could have widened the band: (1) AUTHENTICATED ECHO — each ping carries a SERVER-KEYED nonce (a secret the client never holds); an echo citing a nonce the server did not send, or re-citing one already used, is REFUSED. Without it coverage is FAKEABLE: a cheater replays cheap echoes to LOOK responsive while withholding the real samples. (2) COVERAGE OR REFUSAL — fewer than MIN_SAMPLES authenticated echoes FREEZES the band (the latency may not rise, the jitter collapses to 0) and after STARVE_WINDOWS consecutive failures REFUSES outright; silence is not missing data, it is a refusal, and sustained withholding costs lag-compensation entirely. (3) THE LOWER-HALF RULE — a delay can only push an RTT UP, never down, so only the FAST HALF of the samples is trusted and the jitter is the spread of THAT half; a client who delays half their acks pushes those into the upper half where they are not read, so the jitter does not move (URDRLES1's own jitter is the FULL spread — the policy's is strictly TIGHTER, and that tightening is precisely what stops partial delay from buying reach). (4) THE SESSION FLOOR — the published latency may never exceed `session_min_rtt // 2 + DRIFT_ALLOWANCE`, the floor being the all-time minimum RTT (monotone non-increasing); a client must play honestly to play at all, so THEIR OWN EARLY TRUE-TIMED SAMPLES PIN THEM for the rest of the session, and delaying everything thereafter buys a CONSTANT, never a growing advantage — this is what converts URDRLES1's open-ended residual into a BOUNDED one, and it is the heart of the rung. SCRUTINY IS MONOTONE TOO: the ping rate jumps to MAX_RATE on any instability and is earned back at most ONE step per demonstrably stable window, floored at MIN_RATE — a client can make the server ping MORE, never less, so the sample stream cannot be thinned (and that is also where the bandwidth economy lives: pings are spent only where the measurement is uncertain). The ping placement is evenly spaced with a KEYED PHASE, so it is not a fixed cadence the client can model. RED-FIRST (L15): six pinned scenes (steady — an honest client converges and the rate DECAYS one step at a time; starve — a client answering nothing freezes then is REFUSED; replay — a cheater re-citing one nonce across the window to fake liveness gets only the single genuine echo, so the coverage deficit stands; pinned — THE SESSION FLOOR: one honest opening window records the floor, then the client delays EVERY ack forever and the band widens by at most DRIFT_ALLOWANCE and STOPS; halfdelay — THE LOWER-HALF RULE: half the acks delayed, the fast half untouched, the jitter unmoved; coldstart — the declared residual, pinned as a scene rather than a footnote) pinned AFTER the four plants (`_authenticate_none` — accept any echo, handing replay and forgery full coverage; `_step_no_floor` — forget the session floor so a patient total delay drifts unbounded; `_full_spread_jitter` — read the jitter from the whole spread so partial delay inflates it; `_step_rate_free_fall` — collapse the rate to the floor in one window, thinning the stream) were each proven to bite, TWO of them by FALSIFYING THE THEOREM ITSELF. THE REACH TABLE is the design in one glance: honest = baseline; delay_half = honest (the lower-half rule ignores the delayed half); delay_all ≤ honest + DRIFT_ALLOWANCE (pinned by the session floor); drop_half ≤ honest; drop_all / replay / forge = REFUSED. A seeded 120-client sweep (random ping secrets, true path RTTs, horizons) asserts the theorem over the whole strategy space on every client, plus the four laws, the rate floor and one-step decay, determinism, and the proof-carrying record; non-vacuous (theorem / auth / floor / half / rate all exercised). A `pingpolicy` gate stage (4 rows: scenes / law / property / selftest); `tests/test_pingpolicy.py` (29 falsifiers). GRADE: the CONDITIONAL monotone-disadvantage theorem over the strategy space, the measured cold-start residual and the ceiling that bounds it, the four laws, the ping schedule's coverage, the monotone scrutiny and its bandwidth economy, determinism, and the proof-carrying published record (the 100-byte record is bound by digest to its exact ack window; a forged widened band, or a replay against a different window, fails) are MEASURED. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** The policy is a keyed hash for ping placement, integer comparisons for authentication and selection, and a clamp — over data the transport already carries, producing the `(lat, jitter)` URDRCLK1 already consumes; it lives in `tools/`, never editing the kernel. DECLARED, honestly: (a0) THE THEOREM IS CONDITIONAL — the cold start above is its measured failure mode, bounded but strictly worse than honest, and closing it needs an out-of-band prior (the declared successor); (a) the `+ DRIFT_ALLOWANCE` is REAL — a sustained total delay still buys that one constant; the rung bounds it and makes it explicit, it does not eliminate it; (b) the SESSION FLOOR assumes the path does not permanently WORSEN mid-session — a genuine sustained route degradation beyond the allowance is capped, so that honest player receives LESS lag-compensation than their network deserves; (c) the LOWER-HALF RULE under-reads genuinely one-sided upward jitter, so an honest client on a bursty path gets a tighter band than their network deserves — (b) and (c) are deliberate fairness costs favouring the defender, bounded and stated rather than hidden. does_not_show: the transport that carries the pings; a colluding PAIR of clients; secret rotation / compromise; cross-placement (URDRPNG1 Python reference only). THE CLOCK SUBSYSTEM IS NOW CLOSED AS A UNIT UNDER ONE INVARIANT: URDRHIT1 (WHERE a hit is lawful) + URDRLAG1 (WHEN the server rewinds to) + URDRCLK1 (WHICH view-tick a client may claim) + URDRLES1 (the measured clock that bounds it) + URDRPNG1 (the measurement itself) — so that across the whole apparatus, a client can tighten their own band but never widen it. New: `tools/terrain/pingpolicy.py`, `tools/terrain/conformance_pingpolicy.txt`, `tests/test_pingpolicy.py` (29 falsifiers), `docs/pingpolicy_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1422 / 724; doc-currency green. NOTE ON PROVENANCE: this entry's FIRST draft stated the theorem unconditionally; an engineering-rigor audit probed the initial state the sweep actually tests, found it pre-seeds an honest session floor, MEASURED the cold-start gap that assumption hides, and the claim was downgraded from unconditional to conditional with the residual made a first-class tested boundary BEFORE the rung was landed on the authoritative branch. `claim != code`; the audit is the reason the shipped claim is the true one.

**THE OUT-OF-BAND PRIOR — closing the cold start with evidence the judged client does not control (`tools/terrain/oobprior.py`, URDROOB1): the successor URDRPNG1 declared, composition over URDRPNG1 (over URDRLES1, URDRCLK1, URDRLAG1, URDRHIT1, URDRPCP1), built with NO NEW GLYPH. 1422 → 1444 unit falsifiers, 724 → 728 rows.** URDRPNG1's monotone-disadvantage theorem is CONDITIONAL on a session floor founded on a window the client did not pad, and its declared residual was the COLD START: a client padding every ack from the moment it CONNECTS founds an inflated floor and keeps a permanently wider band. URDRPNG1 also stated precisely WHY it could not close this itself, and that statement contained the design: a cold-start padder is INDISTINGUISHABLE, FROM TIMING ALONE, from a client on a genuinely slow path — at connect the server holds no prior for that client, and refusing the padded one would refuse the honest laggy one identically. The missing ingredient is therefore NOT more timing evidence (no amount of it separates the two) but evidence OF A DIFFERENT KIND. THE THESIS — A RULER THE JUDGED PARTY CANNOT TOUCH: other clients on the same route have already founded honest session floors; those floors say what the route costs, and the client being judged does not control them. So `admissible = min(claimed, cohort_reference + TOLERANCE)`, the reference being the LOWER MEDIAN of PEER floors. A padder may still claim what it likes; the claim is simply not believed past what its peers demonstrate. THREE DESIGN CONSTRAINTS FOLLOW, each a law: the reference must be LEAVE-ONE-OUT (the judged client cannot feed its own ruler), ROBUST (a minority of padded peers must not move it), and PER-ROUTE (so corroborated slowness is believed rather than taxed). THE NEUTRAL-RULER RULE IS STRUCTURAL: `cohort_reference(observations, cohort_key, exclude_client)` cannot RECEIVE the judged client's own observation — the exclusion lives in the function, not a comment, so the ruler is never built from the very quantity the adversary optimises (which would be circular, and the cap theatre). EXACTLY WHAT THE EXCLUSION BUYS, MEASURED — and NOT more, this rung's first draft having assumed more and been corrected before landing: against a SINGLE inflated self-observation it is belt-and-braces (the robust median already absorbs it — law and plant agree, nothing is bought); it is LOAD-BEARING against SELF-SYBIL, a client flooding the pool with inflated rows under its OWN id, where the exclusion drops every one and the reference is UNMOVED at 6 while the including-self plant is dragged to 16 on the reference cohort; and it does NOTHING against OTHER-SYBIL (many DISTINCT fake identities), because those rows are not the judged client's — the declared residual. MEASURED, NOT ASSUMED (two properties are COUNTED rather than asserted as universals, because asserting them universally would have been FALSE — both discovered by the sweep refusing to pass): the prior NEVER HURTS a client (universal, asserted) and STRICTLY reduces a padder's reach in 86 of 120 sweep cases, since where URDRPNG1's rate limit binds before the padder's own ceiling both land on the same reach and the prior is merely redundant; and a MEAN reference is inflated by one outlier in 110 of 120 cases (it can COINCIDE with the median under integer division), which is the honest strength of the case for a robust statistic. Both carry FIXED WITNESSES in the sweep so neither can quietly become vacuous. RED-FIRST (L15): five pinned scenes (capped — a cold-start padder on a fast route claims 12, its peers demonstrate 6, so it is believed only to 8 and its band shrinks; honest_slow — FAIRNESS: a genuinely slow client whose PEERS ARE ALSO SLOW is NOT capped, the property that keeps the prior from becoming a tax on distant players and the reason the reference is per-route rather than one global constant; no_cohort — THE HONEST BOOTSTRAP: below MIN_COHORT founded peers there is NO prior and the rung falls back to URDRPNG1 alone, refusing to invent a reference from too little evidence; minority_poison — a MINORITY of padded peers cannot move the lower median; majority_poison — THE DECLARED RESIDUAL, pinned as a scene rather than a footnote) pinned AFTER the three plants (`_reference_including_self` — the Goodhart mistake, let the judged client feed its own ruler; `_reference_by_mean` — the fragile-statistic mistake; `_found_no_cap` — compute the reference then ignore it) were each proven to bite. A seeded 120-cohort sweep asserts the leave-one-out invariance under a self-sybil flood, the cap and its worth, the fairness exemption, the bootstrap, robustness, determinism, and the proof-carrying record (the 92-byte founding record is bound by digest to its exact cohort; a forged higher admissible floor, or the record replayed against a different cohort, fails). An `oobprior` gate stage (4 rows: scenes / law / property / selftest); `tests/test_oobprior.py` (22 falsifiers). GRADE: MEASURED — the leave-one-out neutral-ruler invariance, the exclusion's load-bearing case against self-sybil, the cap and the reach it removes, the never-hurts universal and the strict-help count, the fairness exemption, the bootstrap refusal, minority-poisoning robustness and the mean's fragility, determinism, and the proof-carrying record. THE GLYPH QUESTION, ruled against D1 §20: **NO new glyph — the kernel stays frozen.** The prior is a leave-one-out median over peer floors the server already holds, plus a clamp, producing the founding floor URDRPNG1 already consumes; it lives in `tools/`, never editing the kernel. DECLARED, honestly: (a) THE PRIOR IS ONLY AS HONEST AS THE COHORT — a MAJORITY-poisoned cohort (other-sybil or collusion on one route) MOVES the reference and this rung does NOT defeat it; it is pinned as a scene and witnessed in the sweep, and the successor is an IDENTITY / SYBIL-COST layer — again a different KIND of evidence, not a better statistic; (b) COHORT ASSIGNMENT is server-derived from the connection, but a client that can change apparent route (VPN, relay) chooses which baseline it is judged against — declared, not solved; (c) an honest slow client in a FAST cohort is capped and under-compensated — the same deliberate fairness trade the session floor makes, favouring the defender. does_not_show: the transport; the identity layer that would make cohorts sybil-resistant; cross-placement (URDROOB1 Python reference only). THE CLOCK SUBSYSTEM'S LAST TIMING-ONLY GAP IS NOW ADDRESSED WITH NON-TIMING EVIDENCE: URDRHIT1 (WHERE) + URDRLAG1 (WHEN) + URDRCLK1 (WHICH VIEW-TICK) + URDRLES1 (the measured clock) + URDRPNG1 (the measurement) + URDROOB1 (a prior the measured party does not control). What remains is the identity layer that would make cohorts sybil-resistant — the one place the chain still rests on an assumption rather than a check. New: `tools/terrain/oobprior.py`, `tools/terrain/conformance_oobprior.txt`, `tests/test_oobprior.py` (22 falsifiers), `docs/oobprior_brief.md`. Verified: gate ×2 byte-identical, GATE PASSED at 1444 / 728; doc-currency green.

**THE CERTIFICATE ARC'S OWN COMPOSITION — the taxonomy made unbypassable, and five design briefs recording how (`tools/terrain/{inputset,tilemin,cohort,blindscreen,autoroute}.py`; URDRINP1, URDRTMN1, URDRCOH1, URDRBLS1, URDRAUT1), built with NO NEW GLYPH. Documentation only: 1948 unit falsifiers and 836 rows UNCHANGED, gate transcript byte-identical.** Stage 8 closed the arc's last structural gap and Stage 6 now records the reasoning behind it, five briefs and this entry, each brief carrying a `<!-- brief-falsifier: ROW -->` marker naming the gate row whose reddening would refute it — a brief that merely agrees with today's code is prose, and the marker is what makes it a claim.

THE ARC IN ONE LINE. `inputset` decides where every quantity may live, by WITNESS rather than by table: a quantity belongs to the coarsest projection that determines it, proved by a pair the next-coarser level does not separate. `tilemin` carries exactly the CERT-tier fields, 3 of 3 verifiable with no occupancy. `cohort` enforces the LATTICE tier and replaces a two-thirds threshold with Menger's 1927 minimum vertex cut, so the gap is a theorem. `blindscreen` refutes every cheap proxy for that verdict — four invariants, their conjunction, and a fifth candidate, each by an equal-invariant opposite-verdict pair. `autoroute` enforces all four tiers at once by CAPABILITY PROJECTION: `projected` hands a quantity only the atoms its plan designates and replaces every other with a sentinel that refuses on use, so an undesignated read refuses BY CONSTRUCTION and nobody enumerates reads. MEASURED, one tier per line — CERT/LATTICE/HISTORY/COHORT each answer AUTOROUTE-MISSING-ATOM when their atom is withheld.

GRADE: MEASURED for every claim above, each bound to a named gate row. DECLARED for none — the briefs assert nothing the gate does not carry.

THE BOUNDARY THIS ENTRY EXISTS TO RECORD, and it is the arc's honest limit rather than a to-do. Every determination POSITIVE here is family-relative and permanently so: view determinacy in the Nash-Segoufin-Vianu sense is UNDECIDABLE for unions of conjunctive queries, so enlarging the corpus cannot convert a family-relative positive into a universal one, and only a SYNTACTIC proof can. That is why an atom leaves a plan only where a witness search AND a syntactic independence check agree. MEASURED at field granularity, the family's SEPARATION BASIS SPANS 2 OF 8 DECLARED SEMANTIC AXES — only `history` and `cohort` have an isolating witness pair; `occupancy` and all five certificate fields have ZERO, the certificate fields because they co-vary (they are derived together, so no pair differs in exactly one) and `occupancy` because `situation(cert=None)` derives a certificate from it, which the certificate rung introduced. The consequence is concrete and named: `liveness_horizon` takes a single distinct value across all 54 family members, so projecting onto NOTHING determines it by constancy and the search reports it droppable. Syntax vetoes the drop, so the PLAN is right and the SEARCH is not. Extending the basis until every declared axis has an isolating witness is the declared successor to this entry, with two controls that must hold through it — the five adopted savings survive and `route_census()` is unchanged — because a corpus that erases legitimate independence results and widens fetch plans is not an improvement.


**THE INTEGER VOXEL LATTICE — the certified quantization boundary (`tools/terrain/voxlat.py`, URDRVXF1, slice S1 of the city-replica arc): NO NEW GLYPH. 3 live rows (`voxlat:scenes`, `voxlat-law`, `voxlat-selftest`).** **THIS IS A BACKFILL, AND IT SAYS SO.** The module landed 2026-07-26 (`727df1a`) and was never graded in either volume — the only such module found by the 2026-08-05 comb, and foundational to the whole city arc, which is why it was frozen as first-next. The delta counts every other entry carries (`N → M unit falsifiers, A → B rows`) are NOT reconstructed here: recovering the gate totals as they stood at landing would be archaeology, and inventing them would be worse than omitting them. **A grade written a fortnight after the rung is not the same object as a grade written at it, and the difference is recorded rather than smoothed over.** THE THESIS: every authority path in this arc is exact integer and every scan of the real world is float, so the boundary between them is an AUTHORITY ACT and needs what every other authority act got — a canonical form, a digest, a typed refusal, and a plant that bites. An earlier draft assumed the boundary came free because a shipped tool already voxelizes splats; it does not — that voxelizer is a GPU compute shader doing float accumulation against float thresholds, exporting Float32Array. The arc must OWN the quantization. PART 1, THE LCA IDENTITY AND THE CORRECTION THAT PRODUCED IT: for Morton keys the octree common-ancestor depth is `lca_depth(a,b) = (3*levels − bit_length(a XOR b)) // 3`, and `levels` when `a == b` — the count of LEADING agreeing 3-bit groups, because Morton hierarchy lives in the HIGH bits (the root's octant is the most significant group). A handed-down version used the 2-adic valuation instead — TRAILING zeros, `ctz` — which measures agreement from the BOTTOM of the tree, where an octree has no hierarchy at all. MEASURED over every pair of a pinned 120-key corpus against an independent brute-force walk: leading form 100%, trailing form under half, and it INVERTS on the sharpest case — two leaf siblings differing in one bit are reported as diverging at the ROOT. `_lca_by_ctz` is retained as a LIVE falsifier because the error is instructive in a way this arc keeps meeting: the valuation arrived attached to a p-adic framing that was refuted PRECISELY BECAUSE it measures shared-prefix depth rather than distance, and then the refuted operation was adopted as the replacement — *refuting a mechanism and then keeping it is worse than never refuting it*. THE ZERO CASE IS THE ONE THAT MATTERS, and it is exactly where hardware diverges: x86 `BSF` leaves the destination UNDEFINED for a zero operand, `TZCNT` returns the operand size, ARM `CLZ` returns the width, and C's `__builtin_ctz(0)`/`__builtin_clz(0)` are undefined behaviour — while `a == b` (a voxel compared with itself) IS that case. A cross-platform determinism claim that ignores its own zero case is not a determinism claim; it is closed here by an explicit branch, asserted as a law, and `bit_length` is used rather than an intrinsic so the reference inherits no UB. PART 2, THE OVERFLOW THEOREM, DECIDED EXACTLY: exact-integer Akenine-Möller triangle/box overlap is division-free and branch-free by ordinary bitwise combination in a placement, so the only thing that can break it is SILENT OVERFLOW — which is how a cross-platform desync is born. The operative question is therefore not "is it exact" but "in how many bits", and a handed-down estimate answered `W > 3 + 2*log2(B) + 2k`, **the wrong exponent**. THE LAW: on the lattice [−B,B]³ with the box at the origin, the largest absolute intermediate the test forms is attained by the PLANE test and equals EXACTLY `max |n·u0| = 4*B³` where `n = (u1−u0)×(u2−u1)`. DECIDED EXHAUSTIVELY at B = 1..5 over every ordered triple of lattice points — no sampling — measuring 4, 32, 108, 256, 500 against 4*B³ = 4, 32, 108, 256, 500. The triple loop collapses by the scalar triple product identity `(f0×f1)·u0 == f1·(u0×f0)`, which makes the inner maximisation a LINEAR functional over u2 and therefore attained at a lattice corner; that collapse is what makes exhaustion cheap enough to sit in a gate at all. WHY CUBIC AND NOT QUADRATIC: the nine edge-cross-axis tests are products of TWO coordinate-scale quantities, while the plane test is a triple product — a cross product, quadratic in the bound, dotted with a position, which adds the third factor — so any estimate that stops at the nine edge tests reads the exponent off the wrong term. The analytic upper bound 192*B³ is also correct but loose by 48×; the ATTAINED constant is 4, and only exhaustion gets it. WHY THE CORRECTION IS LOAD-BEARING, and this is the entry's sharpest number: at the city-scale figures the estimate was offered for — B = 32,000 voxels (4 km at 12.5 cm), k = 12 fractional bits — the fixed-point coordinate bound is 2²⁷ and the width needed is **84 bits**, where the quadratic estimate reports **57** and concludes "fits in `uint64_t`". It does not fit at ANY k: k = 8 still needs 72. Shipping the estimate would give a lattice exact on small test scenes and silently wrong on a real city, and **the symptom would be mis-adjudicated hits at long range — indistinguishable from cheating**, which is the failure mode this entire arc exists to make impossible. THE COROLLARY THAT SIZES A SHARD: requiring `3*coord_bits + 2 ≤ 64` gives `coord_bits ≤ 20`, so a 64-bit placement admits `B * 2^k ≤ 2^20`; at k = 8 that is B ≤ 4096 voxels, a 512 m tile at 12.5 cm. **The arithmetic DERIVES a tile size rather than having one chosen for it — the partition is forced by the word, not by taste.** RED-FIRST (L15), and both plants bite under `voxlat-selftest`: the 2-adic form (correct on under half the corpus, inverting on the sharpest case) and the quadratic width estimate (claiming a 57-bit fit where the decided law needs 84 — wrong in the direction that SHIPS: invisible on small test scenes, silently corrupting on a real city, and no choice of fractional precision rescues it). GRADE — **MEASURED**: the LCA identity over its whole pinned corpus against an independent oracle; the ctz plant's exact failure count; the attained overflow maximum, decided exhaustively at every pinned bound; the exact law 4*B³; the zero-case law; Morton bijectivity on the enumerated sub-lattice; and, added by this backfill, the three rows reproducing URDRVXF1 digests under the live gate. **DECLARED**: that 4*B³ extends past B = 5 — the law is DECIDED on the pinned bounds and stated as a closed form, and the city-scale width is arithmetic on that closed form, NOT an enumeration at city scale. **does_not_show**: that the lattice is CORRECT, only that it is canonical and reproducible; any splat-to-occupancy derivation (a later rung); the render/lattice divergence bound; cross-placement — `voxlat` has no Rust or C99 port, so every figure here is single-implementation. `integrity ≠ truth`.

**The playable demo survives first contact — the fpsdemo arc (P3.1→P3.2b): `fpsdemo.rs` v0→v1.5
(URDRFPD1, ungated) + `fpsrecord.py` (URDRFPR1, gated). 2897 → 2910 unit falsifiers, 971 → 974
rows.** The arc began with a skeleton whose replay properties were verified on the named host
(v0: thirty digest checkpoints identical across three replays, the planted byte caught) and ended
with a walk a human actually took, committed as evidence. In between, first host contact refuted
one green claim after another, and each refutation was MEASURED before it was repaired — the
arc's standing instrument is the HEADLESS REPLAY: the demo's math slice compiles anywhere, so an
operator's trace plus their digest chain IS their session, examinable off-host (the v1 "ribbon"
screenshots reproduced frame-identically on the authoring container, which is how the
backface-culled floor and the byte-carry magenta were observed rather than guessed). The INPUT
ARC took five versions to record one real walk and every version retired an assumption nothing
had reported: the focusless WS_POPUP keyboard (v1: 0 keyed frames of 1800), the refused
SetForegroundWindow (v1.1, refuted by measurement), the machine itself (v1.2/v1.3: the named
host is a HANDHELD whose vendor layer holds the physical sticks and emits desktop vocabulary —
taking the foreground is exactly what unplugs the stick-mouse; pad_connected true, padded 0,
moused 1386 pinned the mechanism), the unpolled arrow keys (v1.4), and the end key that starts
the program (v1.5: Enter, still down at frame 0's poll, ended the first attempted walk at birth
— end keys now ARM on an observed release). L83 and L84 carry the durable forms; the input
matrix is not prose, it is four committed traces. THE CLOSING RUNG makes the arc's evidence
gate-read: `fpsrecord` commits eight sha256-pinned records (four traces including the one-frame
Enter-kill witness — refused as a workload BY LAW — three authoring-container chains, the named
host's log), derives every activity figure from the bytes at claim time, binds chains to traces
WITHOUT RE-EXECUTION (checkpoint schedule + the pinned static-spawn constant over each all-zero
prefix, non-vacuous on the real walk's 236 still frames), and turns CROSS-OS BYTE-IDENTITY into
a comparison of committed artifacts: the operator's Windows build and the container's Linux
build agree on the same committed walk, twenty checkpoints, digest for digest, re-derived on
every gate run. Cost stayed inside the envelope throughout: the real walk's named replay reads
raster med ~2.0-2.9 ms, worst 3.6 ms, present med ~0.27 ms at 720p against the 8.33 ms slot —
the moving workload fits with room, and those rows are PRESERVED, graded nowhere, because budget
verdicts belong to `pixelcost` and a moving-workload admission through `sealframe`'s door is a
rung not yet built. Deliberately not claimed: the camera FEEL (sensitivity, signs,
ground-tracking) has no committed verdict — a trace records what happened, never whether it
should have.

**capcost — the bounded cache's cost surface (URDRCPC1) — evidence rung over fpsdemo v1.10 — MEASURED (the two-regime law; the one-schedule law; identity under every cap on the host).** `tools/terrain/capcost.py` + gate stage `capcost` (3 rows: records / law / selftest) + red-first `tests/test_capcost.py` (17 falsifiers, seven plants proven to bite). The v1.10 host cap sweep sealed: five named-host logs of the committed walk (reach 500 at cap 0/131072/65536/32768, reach 60 at 32768; host/power/scheduler declared) and one authoring-container schedule record, all sha256-pinned, every derived number recomputed from the sealed bytes on every gate run. THE FOOTPRINT IS ARITHMETIC, NOT A LABEL: each record's prefill count must equal the sum of resident-grid areas derived from its own printed ladder (mirroring the demo's grid construction), so a different access schedule cannot wear the demo's name. THE TWO-REGIME LAW: above the ladder's live footprint (69,661 tiles at r500; 17,914 at r60) a cap rides FREE — the 131072 rail's occupancy/recomputes/evictions EQUAL unbounded (76,606/76,606/0) — while below it the cache thrashes catastrophically (65,536 < footprint: 4.7x recomputes, 57% late frames, worst rasters 93-126 ms; 32,768: 8.7x, 78% late, quarter-second worsts) — a below-footprint cap is a DEGRADED regime, never an operating point, and a relabeled record is refused by its own eviction scars. THE ONE-SCHEDULE LAW closes the arc's methodological catch: the demo prefills before walking, the harness filled on first rebase, and their FIFO counts disagreed at every eviction point though every chain was identical — the harness was taught to prefill (`hainuwele/parallel/harness_capcost.rs`, committed) and then reproduced the demo's counts EXACTLY at all five shared points; the old no-prefill counts are committed beside them as the negative control that must always differ. IDENTITY UNDER EVERY CAP: all five host chains equal the committed reach-record oracles, re-verified each run — capacity changes cost, never values (v2 R4's law, now host-proven in the demo). GRADE: the two-regime law, the one-schedule law, and cap identity are MEASURED on the named host over the committed walk. DECLARED, honestly: the 2x-footprint rule stays a CANDIDATE policy — the sealed evidence supports "the ceiling must accommodate the live footprint" and nothing stronger; reach 60 below its footprint carries container counts only (cap 16384, regime B as predicted in advance), its host timing unmeasured; the reach freeze (60 vs 120) and the cap freeze remain the operator's, to be chosen from these numbers. Provenance register 102 -> 103 (fpsdemo:schedule-counts).

**fpsdemo v1.11 — THE COMPETITIVE FREEZE (operator decision, shipped as defaults).** The reach question closes the way the ladder was built to close it: from the measured surface, by the operator. REACH 60 IS THE COMPETITIVE DEFAULT — the measured ceiling-clean operating point (reachenv grades it FITS at 120 Hz BY CEILING, zero late frames on the committed walk; the only swept reach with that property). Reach 120 stays available explicitly as HIGH-REACH/VISTA (6x original draw distance, measured viable at 120 Hz with sub-ms overruns); planetary reach is a derivation (v2 R2c), not a tuning knob. THE CACHE RAIL DERIVES: absent --cache-cap, cap = 2x the ladder's live footprint (capcost's arithmetic) — declared margin POLICY, not a proven optimum; 0 stays unbounded, explicit N honored, the log declares the policy. Container-verified before delivery: derived rail 35,828 (r60) / 139,322 (r500), both count-identical to unbounded, chains byte-identical to the committed oracles. Compatibility stated: pre-v1.7 traces replay with --reach 20. The optional r60 host capcost runs are DECLINED for now (the decision would not change); 1080p certification waits on the probe's three-cell sweep (640x360/1280x720/1920x1080), the one resolution question P2 left open — extrapolating 1080p from 720p would violate the convexity caution P2 itself recorded. Gates unmoved: this is defaults and prose plus one derivation function; identity proven against the committed records.

**skycost — the far field's price (URDRSKY1) — evidence rung over fpsdemo v1.12 — MEASURED (the label-vs-bytes law; the freeze signature; cross-OS identity for the composed sky; the price inside the competitive profile).** `tools/terrain/skycost.py` + gate stage `skycost` (3 rows: records / verdict / selftest) + red-first `tests/test_skycost.py` (14 falsifiers, six plants proven to bite). v1.12 composed the far-field channel behind the terrain (R2d adopted as a VIEW: world-fixed stars, direction-only, painted solely into pixels the terrain does not own; pixel-identity verified container-side — zero owned-pixel moves across the committed walk at reaches 60/120/compat, ~9.7M sky pixels painted, sky-on deterministic, default untouched; R2c's curvature deliberately NOT adopted with the arithmetic on record — sub-0.01-tile at shipped reaches is the drop-blind vacuity planet.py's own plant refuses). The host then priced it and this rung seals the pair: THE LABEL IS CHECKED AGAINST BYTES (a `sky off` record must CARRY the committed r60 oracle chain — the default proven standing on every gate run; a `starfield` record must carry the committed CONTAINER sky chain and differ from the oracle — cross-OS byte-identity for the composed sky, two operating systems, one starfield); THE FREEZE SIGNATURE IS WORN, not claimed (derived ladder, footprint prefill, the 2x rail + policy line, zero evictions — a record off the frozen path cannot wear the freeze's name); THE PRICE DERIVES FROM SEALED BYTES (465..717 us median per segment, ceiling 6.67 -> 6.72 ms vs the 8.33 ms slot, late 0 -> 0 — every delta POSITIVE, and the verdict: the far field rides INSIDE the competitive 120 Hz profile on the committed walk). GRADE: all three law families MEASURED on the named host over the committed walk. DECLARED: the sky at reach 120 / 1080p is unswept (the caustic law); the sky's look is recorded prose; defaulting the sky ON is the operator's decision, priced here, not made here. Meanwhile the three-cell resolution sweep ran its first pass on the host (1080p @ 120 Hz: medians 9.6-12.3 ms vs the 8.33 slot — EXCEEDS, with the convexity caution vindicated: 2.25x pixels cost 2.2-2.8x median; 1080p worsts 14.8-15.9 ms under the 16.67 ms slot — a 60 Hz candidate), awaiting its second run for the pair its reader rung will seal.

**rescell — the resolution ladder (URDRRSC1) — evidence rung over present_probe v0.5 — MEASURED (the agreement law at 120 Hz; the conservative pair law at 60 Hz; late-counter corroboration; the convexity caution as arithmetic).** `tools/terrain/rescell.py` + gate stage `rescell` (3 rows: records / verdict / selftest) + red-first `tests/test_rescell.py` (14 falsifiers, six plants proven to bite). The one resolution question P2 left structurally open, answered by measurement twice rather than extrapolation once: two independent named-host three-cell sweeps (640x360/1280x720/1920x1080, 1:1 presentation, six interleaved order-rotated passes per cell, conditions declared) sealed under sha256. THE LADDER, AGREED BY BOTH RUNS AT 120 Hz: 640x360 FITS; 1280x720 FITS with zero late frames in twelve passes — THE CERTIFIED COMPETITIVE CEILING; 1920x1080 EXCEEDS — medians 9.6..12.6 ms past the entire 8.33 ms slot before presentation is counted, all twelve 1080p passes fully late (not a marginal miss). THE PAIR EARNED ITS KEEP AT 60 Hz: run 1 alone would have graded 1080p FITS by ceiling (worsts 14.8..15.9 ms); run 2's 21.08 ms final-pass excursion makes it MARGINAL, and the conservative verdict carries with the disagreement RECORDED — a one-run FITS is exactly what the two-run protocol exists to catch, and the catch is kept as a live plant. THE CONVEXITY CAUTION VINDICATED IN BYTES: the affine 2.25x prediction from 720p undershoots the measured 1080p mean median by ~10% in BOTH runs (1096/1101 permille) — extrapolation would have argued about MARGINAL where measurement says EXCEEDS. A doctored pair that disagrees at 120 Hz REFUSES TO SPEAK rather than averaging. GRADE: all four law families MEASURED on the named host. DECLARED: input-to-photon absent by protocol (no clicks, cost rows unaffected); 960x540 unmeasured; 1080p's fidelity/photo-mode future and any 120 Hz reopening (precise target: the worst 1080p frame under 8.33 ms) are the operator's. The resolution question is closed the way the reach question closed: swept, sealed, then chosen from numbers.

**scenecost — the composed scene's price, and a verdict that knows its resolution (URDRSCN1) — evidence rung over fpsdemo v1.13.x — MEASURED (cross-OS identity for the whole composition; the replay law across runs; the price; the RESOLUTION law).** `tools/terrain/scenecost.py` + gate stage `scenecost` (3 rows: records / verdict / selftest) + red-first `tests/test_scenecost.py` (20 falsifiers, seven plants proven to bite). The visual acceptance target's first measured milestone. Eight committed artifacts: two INDEPENDENT named-host sweeps of three configurations each (baseline / +wanderer / +wanderer+starfield, fpsdemo at the frozen competitive defaults, conditions declared, the per-frame focus counter FULL in all six — a record measured partly out of foreground refuses) plus the authoring container's chains for both composed configurations. IDENTITY: the host's third-person and third-person-plus-starfield chains each equal the container's digest for digest — CROSS-OS REPRODUCTION OF THE CERTIFIED BIPED AND THE FAR FIELD, two operating systems and two compilers agreeing on a composed game frame — and the two sweeps of a configuration render identically, the replay law restated across runs (both admitted demo versions must satisfy it, so a version that moved a pixel reddens the rung rather than passing unnoticed). PRICE: wanderer and composition median deltas derived from sealed bytes, TOTAL asserted positive while individual segments are not (one reads 69 us FASTER than baseline — the negative tail is reported, not tidied), and the starfield's increment measured ON TOP of the wanderer lands inside skycost's sealed STANDALONE price, corroborating both records without licensing addition in place of measurement. THE RESOLUTION LAW, the rung's own discovery: both sweeps classify all three configurations FITS at 120 Hz and the agreement law holds, but the composition's headroom collapsed 511 us -> 6.6 us between sweeps, while the BASELINE — identical trace, identical conditions — moved its own ceiling 79.6 us across the same pair. That spread is a direct measurement of the instrument's run-to-run resolution, so a margin beneath it cannot be told from zero. The rung reports VERDICT and RESOLVED separately: terrain (1682 us) and terrain-plus-wanderer (953 us) are resolved operating points; the full composition is FITS and UNRESOLVED. GRADE: identity, price and resolution MEASURED on the named host over the committed walk. DECLARED: no other reach, resolution or refresh (the caustic law); why the ceiling moved is visible but unattributed; the look of anything is recorded prose; whether the competitive profile ships either feature ON remains the operator's decision, priced here, not made here. L86 minted (a verdict is not an operating point unless its margin is distinguishable from zero); S18 records the redirection — the control run was the ruler all along.

**worldbind — an authored world bound to certified ground, exactly or not at all (URDRWBD1) — the first joint between the two repositories — MEASURED (the numeric door; the axis door; cross-language ground; content-addressed records with content split from provenance; edit locality).** `tools/terrain/worldbind.py` + gate stage `worldbind` (3 rows: doors / world / selftest) + red-first `tests/test_worldbind.py` (21 falsifiers, seven plants proven to bite). The reconnaissance (`docs/ursprung_bridge.md`) found that Ursprung's `weltwerk` authors CAUSAL TOPOLOGY as text with geometry declared downstream, while this tree authors CERTIFIED TERRAIN with no entity layer — complementary halves never joined — and that the seam between them carries two corruptions that fail SILENTLY. This rung is the door for both, built before any format. THE NUMERIC DOOR: authored coordinates are decimal text bound for float32 on the other side; every one is parsed as an exact rational (integer mantissa, power of ten) and converted by integer arithmetic alone, no float constructed even transiently, a float-SHAPED token (1e3, nan, 0x10) refused on sight, and an unrepresentable value REFUSED RATHER THAN ROUNDED — with the property rounding would destroy asserted directly: placement is INJECTIVE, so two authored positions can never collapse to one and share a digest forever. THE AXIS DOOR: the authoring frame is y-up with ground plane (x, z), MEASURED from weltwerk's own editor (`position.set(e.x, h/2, e.z)`) rather than assumed, and this runtime is z-up; the map is declared and its determinant CHECKED, because a reflection is a different world no content digest would notice. Over the doors: GROUND FROM THE CANON, CHECKED ACROSS LANGUAGES — heights come from `heightfield.noise16` (imported, never copied) under the demo's declared parameters and must equal a committed record of the same tiles produced by the Rust demo's own stride-1 path, so an entity cannot stand on ground the renderer does not draw; CONTENT-ADDRESSED CHUNKS — entities partition by lattice position, each chunk canonical under a total order and named by its own digest, loading a verification rather than a trust; CONTENT SPLIT FROM PROVENANCE, the way this gate's reconcile line splits rowset from content — re-ordering the authored text leaves every chunk byte-identical and the content digest unmoved while the manifest's authoring digest changes, so the record distinguishes an edit from a reformat (this split was FOUND by the shuffle law failing against a manifest that mixed them); and EDIT LOCALITY — moving one entity moves exactly its chunk's digest and no other, the property the streaming design will rest on, asserted where it is cheap. The corpus is a real authored `.wrk` carried verbatim from the other repository (AGPL, same licence): a six-entity fortress with zones, health and typed relations including reversed edges. GRADE: both doors, the cross-language ground, canonicity, provenance separation and edit locality MEASURED. DECLARED: renders nothing (runtime adoption is a later rung with its own before/after); the causal ANALYSIS of the graph stays weltwerk's under its own discipline — relations are carried as data and graded not at all; addressing is established, a streaming loader is not; no collision, contact or gameplay meaning; sub-tile placement refuses at binding even though the numeric door admits it, because the door and the contract are separate things.

**worldgeom — a castle generated from what it IS, on ground it did not choose (URDRWGM1) — MEASURED (derived geometry from authored parts; the SUPPORT law with its declared-overhang case; measured tower projection; the open gate; record determinism and re-derivation).** `tools/terrain/worldgeom.py` + gate stage `worldgeom` (3 rows: castle / ground / selftest) + red-first `tests/test_worldgeom.py` (22 falsifiers, nine plants proven to bite) + the authored corpus `spec/attest/world-castle.wrk` and the generated record `spec/attest/castle-geometry.txt`, both pinned. `worldbind` bound authored POINTS; a castle is not a point, and EXTENT is where authored geometry and certified terrain actually collide — a wall does not sit on a height, it crosses a slope. Blackstone is authored as WHAT ITS PARTS ARE (walls with spans/thicknesses, towers with radii, blocks with heights, crenels and batter as flags, relations carried as data) and the mesh is DERIVED — weltwerk's own thesis carried into a substrate that admits no float: every coordinate crosses worldbind's exact door, and towers are INTEGER OCTAGONS, corner-cut squares DECLARED as exactly that because a regular octagon has no exact integer realisation. 28 authored parts become 238 prisms (10 wall, 12 tower, 6 block, 6 batter, 204 merlon). THE SUPPORT LAW, and its phrasing was EARNED: the first draft asserted `nothing floats above the terrain`, which is false of a merlon by design — a merlon stands on a wall — so restated as SUPPORT (founded below the lowest ground under its own footprint, or resting on a prism containing it) one inequality catches the floating wall AND the overhanging merlon. The machicolation then forced the third case by being refused before anyone declared it, which was correct: an overhang the authoring never claimed is indistinguishable from a mistake, so an overhang is admitted only when DECLARED and only when a corbel actually carries it. MILITARY GEOMETRY MEASURED, NOT COMMENTED: every tower touching a curtain must project beyond it (8 of 8 — the whole reason towers exist is shooting ALONG a wall rather than only away from it), and the gate passage is checked clear because it is a hole by construction rather than by subtraction. Each part is level along its length, footed below the lowest ground it crosses and standing its declared height above the highest. THE COMMITTED RECORD IS RE-DERIVED on every gate run, so the bytes the runtime loads are the bytes the gate checked. **fpsdemo v1.14** adopts it: `--castle <record>` reads the prisms and rasters top faces and side quads through the SAME projection, edge functions, z-buffer and depth fog the terrain uses, so a castle wins pixels on depth like everything else; OFF by default, and the default digest chain is byte-identical to the committed r60 oracle (verified on the authoring container). GRADE: derivation, support, projection, passage and record identity MEASURED. DECLARED: no terrain modification (the canon is immutable — no ditch, no motte; that is terraform's and is not claimed); no collision, contact or climbability; no interiors, floors, stairs or roofs (a prism is a solid, the castle a structured silhouette); no materials or lighting; the causal ANALYSIS of the relations stays weltwerk's; and the castle's RENDERING COST is unmeasured — the host's before/after is its own rung. S19 and S20 record the redirections.

**versionarc — a version that stamps evidence must be documented (URDRVRA1) — MEASURED (the required set over the committed corpus; the two independent sources of obligation; token-boundary matching in both directions; the clean control).** `tools/terrain/versionarc.py` + gate stage `versionarc` (3 rows: evidence / declared / selftest) + red-first `tests/test_versionarc.py` (20 falsifiers, seven plants proven to bite). THE EXEMPTION THAT GREW. `fpsdemo` and `present_probe` are WALL-CLOCK CLASS and deliberately ungated — no timing they produce may enter this gate, so neither carries a design brief, so the brief/falsifier/index coupling that binds every gated module cannot reach them. That exemption was granted for BEHAVIOUR, and it was correct; it became an exemption from DOCUMENTATION without anyone deciding it should, and four fpsdemo versions (v1.13.1, v1.13.2, v1.13.3, v1.14) shipped with no paragraph in their own module README while every gate run in that span was green, because nothing in the tree was entitled to look. THE DAMAGE IS NOT TIDINESS: committed records under `spec/attest/` STAMP the version that produced them and this gate re-reads those records on every run, so v1.13.2 and v1.13.3 stamp six records whose producing version was documented NOWHERE — a reader holding `fpsdemo-scene-s1-full.txt` could see which version made the bytes the gate checks and could not learn what that version was. The door therefore gates the ARTIFACT'S EVIDENCE rather than the artifact, which is a documentation law the gate may enforce without certifying a nanosecond. THE LAW: every version stamped on a committed record, plus the version the source's own title line declares, must appear as ITSELF in the `## ` section naming that artifact's code. TOKEN BOUNDARIES ARE THE MECHANISM, not a detail — a substring test lets `v1.14` satisfy `v1.1` and `v1.13.2` satisfy `v1.13`, silently and in both directions, which would report CLEAN precisely on the point increments at the end of an arc, the versions most likely to be missing; both directions ship as plants. ONE-DIRECTIONAL BY MEASUREMENT, NOT BY PREFERENCE: an ORPHANED verdict was drafted and the corpus refuted it before it was written — the fpsdemo section documents v1.1 through v1.8 and evidence stamps almost none of them, because a version can be superseded before anyone runs a measurement worth committing, and punishing that would redden the tree for its own thoroughness. THE TWO SOURCES OF OBLIGATION ARE INDEPENDENT, and starving one proves it: under an empty corpus `fpsdemo` still owes its README the version it declares while `present_probe`, which declares none, owes nothing and reports VACUOUS rather than the CLEAN it could claim by asking nothing (L61 applied to this door itself). THE CONTROL MATTERS AS MUCH AS THE CATCH: `present_probe` stamps v0.1, v0.3, v0.4 and v0.5, its section names all four, and the identical sweep reports it CLEAN — a door that refuses everything it is pointed at has measured nothing. RED-FIRST AGAINST THE REAL TREE, NOT A PLANT: run at `6d450cf` before the repair it returned `fpsdemo UNDOCUMENTED (v1.13.2 v1.13.3 v1.14)` beside `present_probe CLEAN`, and the repair in the preceding commit is what turned it. GRADE: the required set, the boundary rule, the vacuity split and the control are MEASURED. DECLARED: the register holds two artifacts, and it is the register's growth — not the sweep's cleverness — that extends coverage; a version named only in prose the section does not contain is invisible to this door. `does_not_show`: that the paragraph is TRUE, current, or useful — only that a version is not a stranger to its own README. `documented != accurate`.

**admit — the instrument reports; the gate adjudicates (URDRADM1) — MEASURED (the recomputation and its DISAGREEMENT verdict; the class table; the declared trace length; the provenance/identity digest split; the finite version exemption).** `tools/terrain/admit.py` + gate stage `admit` (3 rows: corpus / verdict / selftest) + red-first `tests/test_admit.py` (20 falsifiers) + **fpsdemo v1.15**, which emits the contract. THE DEFECT: a replay that ended early looked EXACTLY like a replay that ended — `ab_off_a` consumed 2479 of its trace's 2564 frames and printed a record identical in shape to a complete measurement (same header, same per-segment costs, same digest chain, no marker), and it survived only because three runs were compared BY HAND. That catch does not scale, and it is L85 one level up: a condition sampled once is an assumption it held throughout, and a RUN ASSUMED COMPLETE is the same mistake about the whole run. THE RULER WAS WRONG UNDERNEATH IT — `expected` was the number of rows the file happened to contain, so a partial write shrank the ruler to fit the damaged workload and the run reported complete against it; a ruler derived from the thing it measures measures nothing. v1.15 traces DECLARE their length (`# frames N`, and the loader already skipped `#` lines so every older trace still loads, as LEGACY, which the record then states) and a declaration disagreeing with its rows refuses as a FORMAT failure rather than as a short replay. THE STATUS IS A CONJUNCTION over the conditions the measurement CLASS declares, never a boolean about end-of-input: `replay` requires strict frame AND focus equality (a run that consumed every frame while drawing half of them to a background window is also not a measurement — that failure has happened here twice), `play` declares NONE because a play run yields a TRACE rather than a measurement and its last frame losing focus as the window closes is benign; a door firing on every honest session is the warning nobody reads. TWO IDENTITIES, NOT ONE — worldbind's split (S19) at the trace layer: `bytes` is PROVENANCE (which artifact), `replay_workload` is IDENTITY (which motion, over the canonical rows, so whitespace and line endings cannot move it), and an A/B compares WORKLOAD because a line-ending change is not a different walk — this repository's own `.gitattributes` says `* text=auto eol=lf`, so a bytes-only identity would refuse a legitimate pair the first time a trace crossed a checkout. The semantic digest comes from the SAME parse that feeds the replay, never a second one. THE REDUNDANCY EARNS ITS BYTES: the producer prints a verdict and this reader recomputes it, so agreement admits or rejects and DISAGREEMENT is a THIRD and more serious outcome — the two implementations of one contract have drifted, which is the failure where both halves are individually green and the pair is lying; a reader that merely confirmed the string COMPLETE was present would have rebuilt the original defect one layer up and certified it. THE EXEMPTION IS FINITE AND NAMED: 22 committed records predate the contract and are LEGACY-ADMITTED by version (probelog's precedent — a v0 log refuses by version discipline) and COUNTED, so the boundary retires at zero instead of outliving its reason (L68). The version extractor is IMPORTED from `versionarc`, because a second private copy is how two parsers drift while both keep parsing. GRADE: the recomputation, the disagreement verdict, the class table, the declared length, the digest split and the boundary are MEASURED. DECLARED: the RENDER PATH IS UNTOUCHED, so every v1.14 digest chain stands and no named-host artifact needs re-recording for this rung; the ground hole under the camera (a whole-quad discard at the near plane rather than a clip, `NEAR8` = 2 world units against a 3-unit eye and a 3-unit tile) is NOT repaired here and is the next rung, which WILL move every chain; and movement remains frame-coupled BY DESIGN — the frame is the tick, world speed falls with frame rate, and the accumulator that would smooth it consumes wall-clock into simulated state, which replay does not survive. `does_not_show`: that an admitted record's NUMBERS are right, that its declared host is true, or that two admitted records measured the same workload (comparing `replay_workload` is the caller's job). `admitted != correct`.

**castlecost — the castle's price, and the separation that outlives it (URDRCCS1) — MEASURED (the frozen verdict over a named segment set; reach-invariance of the castle delta with both controls; the presence bound from chain-identical frames; pairwise chain identity and one-variable separation).** `tools/terrain/castlecost.py` + gate stage `castlecost` (3 rows: records / separation / presence) + red-first `tests/test_castlecost.py` (22 falsifiers) + NINE COMMITTED NAMED-HOST ARTIFACTS: eight replay logs (`fpsdemo-castle-r{120,60}-{off,on}-{a,b}`) and the operator's own hand-flown approach trace (`fpsdemo-castle-walk.txt`, 2564 frames). THE SHALLOW FINDING: 238 prisms cost more than the entire rest of the scene — up to +17.5 ms on a frame whose terrain, starfield and wanderer together came to 2.3 — so the composition EXCEEDS the 8.33 ms slot at 120 Hz in every one of the seven test segments, at BOTH reaches. THE FINDING WORTH KEEPING: halving the terrain reach left the castle's price where it was (+0.6%, −8.2%, −0.9%, −3.7%, −1.1%, −0.4%, −0.5% across segments 4–10, all inside the frozen ±20% band) while the terrain arm ITSELF fell 15–20% — so WORLD REACH COST AND AUTHORED-GEOMETRY FILL COST ARE INDEPENDENT AXES, and the renderer needs a second representation rather than another cache or ladder adjustment. THE RULE WAS FROZEN BEFORE THE DATA: the segment set was named in advance of the reach-60 runs, because a verdict assembled afterwards from whichever cells agree is not a verdict — segment 15, where the operator had turned away and castle-on comfortably FITS, is exactly what a post-hoc rule would have reached for and ships as a plant. TWO CONTROLS CARRY THE FINDING: the terrain arm had to actually get cheaper (or reach-invariance would mean only that the treatment did nothing) and the castle-off scene had to fit the slot at the competitive reach (or the comparison would be between two failures). THE METHOD CONTRIBUTION — THE DIGEST CHAIN AS A COST ORACLE: the chain has only ever been an IDENTITY instrument here, but where the castle-off and castle-on runs of ONE trace produce identical framebuffer digests the castle put nothing on screen, so the cost difference in those frames is what the feature costs to HAVE rather than to SHOW. Free, because the demo already emits the digests; measured at 85,300 ns for 238 prisms projected and rejected against a 17.5 ms peak, two orders of magnitude, which is what says the optimisation target is COVERAGE and not projection. ONE-SIDED BY CONSTRUCTION: identical framebuffers prove the castle contributed nothing VISIBLE, not that it did no work — a prism z-rejected behind terrain costs time and changes nothing — so the figure is an UPPER BOUND, which is the useful direction. THESE RECORDS ARE THE FIRST CUSTOMERS OF `admit`'s EXEMPTION: v1.14 produced them, before the completeness contract, so `admit` returns LEGACY-ADMITTED and this rung pays for that BY HAND, re-deriving full frames, full focus and replay mode from the fields they do carry — a hand-check that retires the day they are re-recorded under a build that emits the contract. GRADE: the verdict, the invariance, both controls, the presence bound, pairwise chain identity and one-variable separation are MEASURED. DECLARED: no figure here is a property of the CASTLE rather than of this renderer at this version — the near-plane repair (a whole-quad discard where a single-plane clip belongs) WILL move every number in these files, and they will then remain valid records OF THAT RENDER PATH exactly as the v1.1–v1.5 chain records did. `does_not_show`: other hosts (ONE named machine, power and scheduler declared), other resolutions (720p), other castles, or other walks — the workload is ONE hand-flown approach recorded at ~50 fps while the frame rate was collapsing, so it carries the operator's own compensation for the slowdown, and a walk that spent longer facing the gate would move every delta in it. `measured != general`.

**castlecost v1.1 — the finding replicated on a changed renderer, and eight records GRADUATED (URDRCCS1) — MEASURED.** The eight cells were re-recorded under fpsdemo v1.17 with the same trace and the same frozen read rule. BETWEEN THE TWO CORPORA THE RENDERER CHANGED UNDER THE CLAIM: the near plane moved four-fold closer (2 world units to 1/2), `draw_castle` stopped discarding triangles at that plane and started clipping them, and terrain rasterisation grew 4-12% as the clip's price landed on the thing that got clipped. THE CASTLE'S OWN DELTA MOVED BY -1.3% TO +7.3%, and reach-invariance TIGHTENED from the ±8.2% it was frozen at to ±3.7% (segments 4-10: +2.8, -3.7, +3.6, +2.4, +0.8, +0.3, +1.8 percent). So the question the near-plane repair created — whether clipping instead of discarding made the castle more expensive — is answered NO by measurement rather than by argument, and the architectural claim survived the renderer changing beneath it, which is a different kind of claim from one measured once. Castle-off at reach 60 spans 1.735..6.825 ms against the 8.333 slot and still FITS with the clip's cost absorbed. AND THE EXEMPTION SHRANK ON SCHEDULE: v1.17 is at or above `admit`'s `COMPLETENESS_INTRO`, so all eight records now carry `measurement_class`, both trace identities and `replay_status`, and `admit` returns ADMITTED where it returned LEGACY-ADMITTED — the corpus's legacy count fell by exactly eight. castlecost's hand-rolled completeness proof is RETIRED and DELEGATED to the module that owns the contract, because a second implementation of completeness living here is precisely the drift `admit`'s DISAGREEMENT verdict exists to catch, one layer further out. That is the intended migration path being walked rather than another special case accumulating (L68). THE HONEST REMAINDER, asserted rather than glossed: the TRACE is still a v1.14 artifact, so every run reports `replay_declared legacy` and its expected length is a row count rather than a declaration — the graduation is real and it is PARTIAL, and `the_workload_still_predates_the_contract` is the falsifier that stops it reading as complete. Falsifiers 22 -> 24.

**observe — the object beside the digest, and only one claim made of it (URDRFBR1) — MEASURED (cross-placement reproduction of every host checkpoint) + REPORTED (the render-induced quotient census and the temporal-skip ceiling, neither promoted to an invariant).** `tools/terrain/fibre.py` + gate stage `fibre` (3 rows: reproduce / census / selftest) + red-first `tests/test_fibre.py` (23 falsifiers, five plants) + the harness `hainuwele/parallel/fibre_build.py` and `fibre_main.rs` + the committed record `spec/attest/fpsdemo-fibre-r60-off.txt` (2564 frames). THE QUESTION: the digest chain has only ever been an IDENTITY instrument here, and nobody had asked what it IDENTIFIES — when two frames share a digest, did the state repeat or did the render map throw something away? THE INSTRUMENT IS DERIVED, NOT WRITTEN TWICE: the builder slices the demo between the exact-integer helpers and the entry door, every renderer and the clipper and the digest unedited, and appends a replay main emitting the full state beside the digest at EVERY frame rather than every sixtieth. THE OBJECT WAS READ OUT OF THE CODE, NOT CHOSEN: step_cam's `(px, py, q, pitch_acc)` plus the wanderer's `av_state` and elapsed frames since its last transition, because `av_start = frame` is what makes concatenating two trace segments legal at all. THE GATE'S CLAIM IS DELIBERATELY NARROW — 43 of 43 host checkpoints reproduced, Windows/rustc against Linux/rustc, same trace and configuration — because the transcribed main is a SECOND IMPLEMENTATION that could drift and the reproduction is what catches it. THE CENSUS, REPORTED: 2564 frames, 2564 DISTINCT objects, 2309 distinct digests, 31 digest classes holding more than one object (29 phase-only, 2 also differing in qw/qz). THE TIDY REPAIR IS REFUTED BY THE CORPUS: coarsening the object by forgetting the clip phase leaves SEVEN coarse states carrying more than one digest, so phase cannot be declared semantically irrelevant. Inspecting the two survivors shows a camera standing still at one tile while the quaternion drifts by ONE ULP per frame — frames 502-508, qw 3334827641 then 3334827642 — some rendering identically and some not. So the equivalence is NOT `forget a coordinate`: it is the FIBRES OF THE RENDER MAP, state advancing in increments too small to push a projected vertex across a pixel boundary. RENDER-INDUCED OBSERVATIONAL EQUIVALENCE is the honest name — not a kernel (no group structure demonstrated) and not a projection (it forgets no nameable field uniformly), and that obstruction is why canonicalisation cannot come from normalising the object. TWO BOUNDARIES SHIP AS PREDICATES rather than as prose: the digest cannot be injective (strictly more objects than digests) and the converse `equal object implies equal digest` is UNTESTED rather than passed, because every object here is distinct and the silence would otherwise read as a result. THE TEMPORAL-SKIP CEILING rides along because it decides whether an optimisation is worth DESIGNING rather than building: a perfect free predictor could skip 215 of 2563 frame pairs — 8.4% — against a near-plane clip that cost 4-7% and was accepted without argument, and of 280 pose-unchanged frames 69 REDREW anyway because the avatar animates, so `camera unchanged` is not even the right predicate. Observable-event skipping is therefore DESIGNED, CEILING MEASURED, NOT BUILT. GRADE: the reproduction is MEASURED; the census and the ceiling are REPORTED. DECLARED: 8.4% is a property of a WALKING workload (position changes in 83.6% of these frames), not of the renderer — a standing camera would skip far more; and the sensitivity arithmetic that makes any conservative bound derivable at all comes from `projection_bound`'s near-plane floor, which was built as an overflow guard and doubles as the Lipschitz boundary. `does_not_show`: another walk, another reach, another resolution, or the castle on.

**armpair — the equality outlives the code that proved it (URDRARM1) — MEASURED (eight arm-pairs chain-identical, read off sixteen committed records) + MEASURED (the cost separation, against a null-control band derived from the records rather than declared) + DECLARED (the arm labels themselves).** `tools/terrain/armpair.py` + gate stage `armpair` (3 rows: equality / separation / selftest) + red-first `tests/test_armpair.py` (20 falsifiers, six plants) + sixteen committed records `spec/attest/fpsdemo-arm-{ref,inc}-r{60,120}-{on,off}-{a,b}.txt`. THE SITUATION: v1.19 replaced `draw_castle`'s per-pixel edge recomputation with the incremental recurrence `w(x+1,y) = w(x,y) + dwx` and RETAINED the recomputation under `--cfg castleref`, so the claim `D_reference(f) = D_incremental(f)` could be established BY REPLAY rather than argued from the algebra. It was — eight arm-pairs on the named host at 43 checkpoints each, plus all 2564 frames on the authoring container with the six census counters agreeing too, which is a SECOND observable (the digest watches output, the census watches control flow, and a seed error could in principle move one without the other). v1.20 RETIRES the arm, because the next rung needs the INCREMENTAL path as its reference and keeping both would make that equality three-way. THE PROBLEM THE RETIREMENT CREATES: once the reference is gone no gate run can re-derive the equality from source, and `retire` already caught the near-identical failure one layer in — there a retirement's REASON did not travel to a caller, here its EVIDENCE would not travel to the future. THE REPAIR: the records graduate and the claim becomes a comparison of committed bytes, the shape `fpsrecord` and `reachenv` already use; nothing compiles, renders or re-runs. NON-VACUITY IS CHECKED AS HARD AS TRUTH: sixteen records pairwise distinct by sha256, four cells carrying four DIFFERENT chains, and castle-on chains differing from castle-off so the castle genuinely drew — an equality over sixteen copies of one file would read green and mean nothing. THE INSTRUMENT DERIVES ITS OWN ERROR BAR: the change touched `draw_castle` alone, so with the castle OFF both arms ran identical code and the spread there IS measurement error — 5.7% — and every one of the fourteen castle-on readings exceeds it, castle-only raster p50 falling 15.7%..23.2% with a median of 17.9% (p95: 13.2%..25.3%, median 19.4%). THE CONTROL IS CHECKED TWICE, for SIZE and for DIRECTION, because magnitude alone cannot separate noise from a small systematic effect: fourteen control cells all leaning one way would be code layout or thermal drift wearing noise's clothes and would contaminate the reading by exactly that much. Both signs appear. THE RETIREMENT IS SWEPT, NOT NARRATED: the cfg name must appear nowhere in `fpsdemo.rs`, and the sweep is exercised against a source with the cfg PLANTED BACK IN, because reading the real file and reporting CLEAN proves only that today's file is clean. THE HOLE, NAMED RATHER THAN DISCOVERED LATER: the banner stamps version, host, power, scheduler, hz, res, mode, reach, sky, third, castle and qpf — NOT the build configuration — so nothing in the bytes proves the `ref` records came from a different binary than the `inc` records. THE ARM LABEL IS DECLARED BY THE OPERATOR; the cost separation is evidence for that declaration and never proof of it. This is the v1.15 measurement-admission defect found again where the contract does not reach, and the repair — stamping the raster path in the banner — is OWED BY the rung that next introduces an arm (v1.21, the span early-out, which needs it anyway). GRADE: equality and separation MEASURED; arm provenance DECLARED. `does_not_show`: that the optimisation is correct in general — 43 checkpoints of one walk is a sample of the input space, and the ALGEBRAIC identity held at every launch by `edge_recurrence_battery` is what covers the rest; the worst-frame column, which at n=2 per cell is a sample and moves BOTH ways here (-60.1% to +41.7%); or any verdict about the 8.33 ms slot, which the castle overruns 1.26x..2.34x on BOTH arms. This rung removed about a sixth of the castle's fill and did not fix it — the 58.97% edge-reject population is still untouched and the span early-out is where the fix has to come from.

**fpsdemo v1.21 — the row span closes early, and the census signature was PREDICTED before the build (URDRFPD1) — MEASURED (the equality and the iteration reduction, on the authoring container) + PENDING (the named host's A/B).** `hainuwele/parallel/fpsdemo.rs` + `span_break_battery` as a launch door in BOTH builds + the full-row arm retained under `--cfg castlefullrow` + `armpair.these_records_predate_the_raster_stamp`. THE TARGET, NAMED BY MEASUREMENT RATHER THAN INTUITION: v1.18's census made `edge_reject` the largest population by far (12.14 G of 20.59 G iterations, 58.97%) and v1.20's A/B confirmed the edge recurrence had removed about a sixth of the castle's fill while leaving that population ENTIRELY untouched. THE CHANGE: the inside set of a CONVEX triangle on any horizontal line is an INTERVAL, so once a row has entered the span and left it no later pixel can be inside — the loop breaks, exactly as `raster_rings` has done since the terrain existed. THE PREDICTION, REGISTERED BEFORE THE BUILD AND IN WRITING: because a break removes iterations without changing which pixels pass either test, the census had to move in one way and no other — `written`, `depth_reject`, `triangles` and `bbox_away` IDENTICAL to v1.20's digits, `visited` and `edge_reject` falling by exactly the same amount. IT DID: 4,297,902,868 removed from both, 20.87% of all visits and 35.39% of the edge-reject population, identity holding on both arms, digest chain unmoved across all 2564 frames. A prediction met is worth more than the same numbers read off afterwards, because afterwards every number looks like it was expected. THE BATTERY WAS WRONG FIRST AND THE RED-FIRST CHECK CAUGHT IT — a version counting inside-runs per row could not be falsified by any mutation of its own inputs, because removing the winding fix makes the region EMPTY rather than gapped and an empty row reads zero runs; a property no mutation can violate is a THEOREM, not a check, and a battery restating a theorem is decoration. The shipped battery asserts what `draw_castle` actually depends on: the break-based traversal visits exactly the pixel set the full scan visits, row by row, compared by count AND positional checksum so two different sets of equal size cannot pass — and dropping the `entered` guard reddens it. CONTRACT HYGIENE, DELIBERATELY NOT THE VARIABLE: the banner now carries `| raster <path>`, appended after `qpf` so every committed parser's header regex is untouched, discharging the debt `armpair` recorded — and `armpair` now asserts that its OWN sixteen records lack the field, so the weaker declared-arm claim cannot be reused once stronger records exist; that law must be REPLACED and not relaxed when it reddens. THE RESIDUAL IS THE NEXT RUNG, MEASURED RATHER THAN ESTIMATED: 7.85 G edge rejects survive, 48.15% of what remains, and they are the LEADING runs — pixels left of the span, which a break cannot reach because the row has not entered yet. That boundary is solvable in CLOSED FORM rather than scanned, and it is a separate rung. GRADE: the equality and the iteration reduction are MEASURED on the container over all 2564 frames; the nanosecond cost on the named host is NOT_MEASURED until the A/B runs. DECLARED: the full-row arm retires by the v1.20 protocol once that equality is green on the host, not before.

**fpsdemo v1.22 + armpair v1.1 — the second arm retires, the arc closes, and the retirement sweep was wrong in the way `retire` had already written down (URDRFPD1 / URDRARM1) — MEASURED (both equalities, both separations) + UNDERDETERMINED (the compound) + DECLARED (generation 1's arm labels only).** Sixteen more committed records `spec/attest/fpsdemo-arm2-{span,fullrow}-*` + `armpair` gaining a SECOND GENERATION + `castlefullrow` retired from `hainuwele/parallel/fpsdemo.rs`. THE ARC, COMPLETE: recomputation -> incremental (v1.19, retired v1.20) -> span early-out (v1.21, retired v1.22), each transformation carrying an exact replay equality, an independent launch-time falsifier, a measured cost separation against its own derived null band, and permanent record-vs-record evidence that survives the deletion of the code. The source now contains only the optimised implementation. THE IMPROVEMENT OVER v1.20, AND IT IS THE ONE v1.20 NAMED AS OWED: generation 2's arm identity is DERIVED, not declared. v1.21 stamps `| raster <path>` into the banner, so `armpair` checks that every record's stamp is one of the two arms, AGREES with the key it is filed under, and that each cell holds exactly one of each — a mislabelled commit reddens instead of quietly re-pointing a pair, which generation 1 structurally could not notice. Generation 1 KEEPS its weaker law (its banners cannot name their arms) because bytes with no field cannot be retro-fitted with a stronger claim. THE PRICE, MEASURED: castle-only raster p50 down 10.9%..26.1%, median 15.9%, p95 median 19.0%, against a DERIVED null band of 3.8% with both signs present; and for the first time in the arc a frozen segment fits, r60 seg 6 at 0.98x the 8.33 ms slot, with every other cell still 1.10x..2.12x over and late-frame counts unmoved. A MEASUREMENT NOBODY PAID FOR: generation 1's `inc` arm and generation 2's `fullrow` arm are THE SAME RASTERISER run in different sessions, so their disagreement IS the session drift — -4.0%..+2.3% on castle-only p50, derived rather than assumed — which is exactly why the compound recompute-to-span figure (23.2%..44.2%, median 32.9%) stays UNDERDETERMINED and appears in no scene and no gate row: n=2 on the drift does not license promoting it. THE DEFECT THIS RUNG REBUILT ON PURPOSE, and the finding worth keeping: v1.20's retirement sweep searched the source for the cfg name as RAW TEXT. That read CLEAN while nothing mentioned it. v1.22 retired a second arm and — as this tree's protocol requires — EXPLAINED the retirement in the comment beside the code it removed, and the sweep called that explanation a restoration. `retire`'s own docstring had said it in as many words: a retired name in a comment is a MENTION, a call is a CALL, and a text sweep punishes exactly the documentation the law wants written. READING IT WAS NOT ENOUGH; the defect had to be rebuilt to be believed, which is the second time in this arc a checker was refuted by its own subject rather than by argument. The sweep now matches ATTRIBUTE POSITION (`#[cfg(..)]`, `#[cfg_attr(..)]`, `cfg!(..)`) and ships a falsifier pointing the OPPOSITE way from the usual one: a prose paragraph naming both retired cfgs must read CLEAN. DECLARED: Rust offers no AST here the way `retire` has Python's, so a SHAPE is matched rather than parsed — a cfg smuggled in through a macro expansion or a build script would pass, and that bound is written into the law. `does_not_show`: that either optimisation is correct in general (43 checkpoints of one walk is a sample; the algebraic identities in `edge_recurrence_battery` and `span_break_battery` cover what records cannot); the worst-frame column; or any verdict about the slot, which `castlecost` owns.

**voxref — rung zero of the voxel arc, the observable frozen before any reduction exists (URDRVXF1) — ESTABLISHED (determinism, partition, order-irrelevance, all recomputed by the gate) + REPORTED (the coarseness census).** `tools/terrain/voxref.py` + gate stage `voxref` (3 rows: contract / partition / order) + red-first `tests/test_voxref.py` (17 falsifiers) on a dedicated `voxel-rung-zero` branch. THE MISTAKE THIS RUNG EXISTS TO NOT MAKE: the castle arc's observable was the framebuffer digest, inherited from the demo's replay DNA for an unrelated purpose, and it happened to be external to `recompute -> incremental -> span`. That was LUCK. The failure mode when it is not luck is circular and quiet — optimise the representation, define the observable around the optimised representation, prove the optimised representation preserves the observable — and nothing in that proof looks wrong. A voxel world makes the trap easy, because visible faces, depth ordering and work counts are all OUTPUTS of the reductions about to be built. WHAT IS FROZEN: `O_t = (H_C(colorbuffer), H_Z(depthbuffer))`, coverage an exact top-left PARTITION, draw order observationally irrelevant. UNLIKE THE CASTLE ARC THE GATE RECOMPUTES THE OBSERVABLE rather than comparing committed records — the reference is pure integer Python, small enough (12^3 voxels, 96x72, 4848 primitives) to render its whole trace inside the gate, so determinism and order-irrelevance are LIVE checks and not archaeology. BOTH DIGESTS, AND NEITHER DOMINATES: a CONSTRUCTED witness gives two renders whose colour buffers are byte-identical and whose depth buffers are not — the exact shape of an occlusion bug dropping a face behind a same-coloured neighbour, and exactly what a colour-only observable calls a pass — and a MIRROR witness exhibits the reverse, so neither digest is a function of the other. BOTH ARE CONSTRUCTED, WHICH IS A REPAIR: the first version read the property off the corpus, two declared frames happening to share a depth digest, and then this gate's own `magicuniq` stage caught the module's MAGIC colliding with `voxlat`'s — the rename changed the world seed string and the coincidence evaporated, which is the difference between a structural claim and a property of one hash. THE PARTITION REPLACES AN ACCIDENTAL ASSUMPTION INHERITED FROM THE CASTLE: `w >= 0` on three edges is a COVER, a shared edge satisfies both triangles, and the castle never noticed because the loser failed a strict depth test and both triangles carried one colour. A voxel world's seams would have inherited that and paid for it. The top-left bias makes every pixel claimed exactly once and the CONTROL is what makes the law mean something: the same sample without the bias  double-claims 50 pixels. AND A PARTITION ALONE IS NOT ENOUGH — a finding from building rather than from writing. It fixes pixels shared by ADJACENT triangles and does nothing about two DISTINCT faces that are geometrically COINCIDENT, of which the naive reference has 1058 pairs because every two adjacent solid voxels contribute two faces in one plane. Those tie at exactly equal depth and a plain `<` hands the pixel to whoever drew first, so draw order would still have been observable through a door the top-left rule does not close. The depth test therefore compares `(depth, face_key)` with the key derived from the voxel's own coordinates and face index — SCENE IDENTITY, not traversal — and the corpus is checked to CONTAIN coincidences, since a world without them would pass the order law with the tiebreak never consulted. THE TRACE IS DESIGNED, NOT RECORDED: buried, floor-grazing, section seam, wall-flat, open air, oblique, corner, edge-on — because the castle's evidence is 43 checkpoints of a human walk, `armpair`'s does_not_show says so, and a reduction wrong only where the trace never goes passes forever. THE DEPTH WITNESS READ GREEN FOR NOTHING AT FIRST: its quads were wound backwards, so both renders drew nothing and the law compared two empty buffers; it now refuses typed if the witness renders no pixel, which is the same class of defect as the empty-castle equality caught two rungs earlier and the second time it has appeared in this arc. GRADE: determinism, partition and order-irrelevance ESTABLISHED (recomputed every gate run, each with a control proving the check bites); the census REPORTED. `does_not_show`: performance — this renderer draws every face of every solid voxel including buried ones and is slow on purpose; that the observable is fine ENOUGH; and anything about face culling, greedy meshing, T-junctions or LOD seams, which are the FIRST REDUCTION's problem and are deliberately absent, since a meshing equivalence holds only over properly tiled uniform-resolution coplanar regions (a T-junction leaves a pixel claimed by nobody, and a partition renders that as a crack) and validating a semantic foundation with the first optimisation depending on it is the circularity this rung was built to avoid.

**voxcoarse — how coarse the frozen observable is, measured before anything leans on it (URDRVXC1) — MEASURED (the fibre structure over a declared lattice) + DECLARED (the lattice itself).** `tools/terrain/voxcoarse.py` + gate stage `voxcoarse` (3 rows: census / binding / selftest) + red-first `tests/test_voxcoarse.py` (18 falsifiers) + the committed record `spec/attest/voxref-coarseness.txt` (1728 rows), on the `voxel-rung-zero` branch. WHY: `voxref` froze O_t and reported a census over its eight adversarial frames — eight states, eight distinct observables — and labelled it PLUMBING RATHER THAN EVIDENCE, which was honest and insufficient. Eight frames designed to be maximally different from one another are close to the worst possible sample for detecting collisions; injectivity on them establishes that on those eight none was seen and nothing about the size or structure of the fibres of `state -> O_t`. An obligation later reductions must preserve, whose discriminating power nobody measured, is an obligation of unknown strength. THE LATTICE WAS DECLARED BEFORE THE ANSWER WAS KNOWN and is regular rather than curated: 6 positions per axis sweeping from well outside the world to buried inside it, 8 forward directions, 1728 states. THE RECORD IS THE ARTIFACT AND THE GATE RE-DERIVES FROM IT: rendering 1728 states is minutes and not a gate budget, so the census is generated once and committed — but a committed table of digests could describe any renderer at all, so every figure is re-derived from the rows at claim time (L75) AND a declared sample is RE-RENDERED through the live `voxref` every run and must reproduce the record's digests, with the collision witnesses re-rendered too so a fabricated collision cannot survive. THE RESULT, AND IT NEEDED DECOMPOSING RATHER THAN REPORTING: over the whole lattice, 1728 states -> 558 distinct observables, largest fibre 1125. Read alone that is a devastating verdict on the observable and it is nothing of the kind — the fibre is THE EMPTY VIEW, because the lattice reaches far outside the world and every position out there looking away writes background everywhere and no depth. The empty view is identified by RENDERING AN EMPTY PRIMITIVE LIST, a category derivable from the renderer, rather than by taking the biggest fibre and naming it afterwards, which would be reading the answer off the data; and the lattice is NOT redesigned to remove those states, because choosing states after seeing which ones collide is choosing the answer. OVER THE 603 STATES THAT SEE SOMETHING: 557 distinct, 68 (11.3%) colliding, largest fibre 13, and 535 uniquely identified — so the observable is SHARP wherever it has anything to look at, and 13 is the worst genuine collision class the reduction rungs inherit. A REAL DEFECT, NOT A PLANTED ONE, KEPT AS A REGRESSION: the census took its state count from `len(rows)` under both censuses, but the exclude-empty filter removes FIBRES and not ROWS, so the collided ratio was divided by 1728 instead of by 603 and read 3.9% where the answer is 11.3%. A denominator that does not move with its numerator is the quietest way to publish a wrong ratio. GRADE: the fibre structure over this lattice is MEASURED; the lattice is DECLARED. `does_not_show`: coarseness over the whole camera state space — the lattice is finite and regular and its limits are visible in its own result; coarseness under any distribution of real camera motion; whether a collision corresponds to semantically equivalent VIEWS, since two cameras that both see one flat face at the same distance produce the same bytes and whether that ought to count as one observation is a question this census does not answer; and anything at all about reductions — no culling, no meshing, only the reference's own observable, which is what the reduction rungs must now preserve.

**voxray — the geometric oracle, and the reduction that made it necessary (URDRVXR1) — ESTABLISHED (the oracle's own invariants and the shared camera contract) + REPORTED (its correspondence with the reference) + OPEN (the residue's mechanism).** `tools/terrain/voxray.py` + gate stage `voxray` (3 rows: oracle / correspondence / selftest) + red-first `tests/test_voxray.py` (24 falsifiers) + the committed record `spec/attest/voxref-oracle.txt`, on the `voxel-rung-zero` branch. WHY IT EXISTS: `voxref`'s laws are all TRUE and all still pass, and every one of them is a property of the MACHINERY — none asks whether the machinery draws the right thing, because until a second computation exists there is nothing to be right about. THE FIRST REDUCTION ASKED. Face culling changed O_t on six of eight declared frames; tracing one pixel gave an unculled winner of voxel (2,1,10)'s INTERIOR top face — solid directly above it — beating voxel (2,1,11)'s own exposed face, which is not surprising but impossible. Two hypotheses were tested: the orientation test SELECTS THE WRONG WINDING (confirmed on a single voxel from six directions, since the screen-space Y inversion reverses projected orientation) and the 1/z perspective hypothesis, which was REFUTED — interpolating 1/z changes nothing and the same six frames differ. THE ORACLE'S AUTHORITY IS DELIBERATELY NARROW: which solid voxel does a camera ray enter first, through which face, at what t. Exact integer voxel traversal, t carried as a rational and compared by cross-multiplication, no triangles, no edge functions, no depth buffer, no tolerance anywhere. It shares exactly two things with the reference and both are the SCENE rather than the renderer: occupancy and the camera basis. IT AUDITS ITSELF, because an oracle nobody checked is an opinion: first-hit-is-first verified by INDEPENDENT fine sampling, the hit lies on its reported face and inside the voxel on the other axes, entry direction agrees with the face normal, a miss traverses nothing, an interior origin gets NO invented face, and the probe set contains both hits and misses. THE CAMERA IS A SHARED AUDITED CONTRACT and this is the strongest part: a pixel's ray projects back to within ONE pixel of itself, 70.15% of them exactly. THE LAW WAS FIRST WRITTEN AS AN EXACT INVERSION AND WAS FALSE — composing a Q16 basis with two floor divisions is not an involution, and the residual lands systematically at (-1,+1), (0,+1) or (-1,0), the signature of `//` truncating toward negative infinity rather than of anything geometric. The distribution is PINNED so the claim cannot silently strengthen. THE VERDICT ON THE DISPUTED PIXEL IS A THIRD ANSWER: voxel (2,0,10) face +z, one step nearer in y than either arm — BOTH were wrong, which is why an oracle was needed rather than a tiebreaker. CORRESPONDENCE, REPORTED AND NEVER CERTIFIED: 17.4% as committed, 87.0% with the six face windings reversed. The reversal is carried in THIS module as DECLARED DATA rather than as an edit to `voxref`, because `voxref.render` takes a primitive LIST — so both arms go through the IDENTICAL rasteriser, differ in nothing but corner order, and the whole finding reproduces from the committed tree instead of from an uncommitted change. THE SINGLE DIRECTIONAL CLAIM is that the reversal moves the reference CLOSER to geometric truth; it does not claim the reversal is correct. GRADE: the oracle's invariants and the camera contract are ESTABLISHED (recomputed every gate run, each with a plant); the correspondence is REPORTED; the residue's mechanism is OPEN. `does_not_show`, and these are bounds rather than footnotes: THE RESIDUE IS AN UPPER BOUND ON DEFECT, not a measurement of it, since up to a pixel of ray/sample offset is folded into every figure; no mechanism is established for what survives the winding reversal, and turning 13% into a single depth-bug claim is exactly the inflation this ladder refuses; ONE DECLARED FRAME IS NOT COMPARABLE AT ALL — `floor_flat`'s eye begins inside a solid voxel, so the oracle returns that voxel with NO entry face for every ray, a legal answer in a DIFFERENT SEMANTIC DOMAIN from 'which surface does the camera see', and which definition an interior origin deserves is an open question, so the frame is excluded BY DERIVATION rather than by hand; and THE TRACE LABELS ARE WRONG — the frame named `floor_flat` is the buried one and the frame named `buried` is not inside anything, recorded here as a metadata defect rather than repaired, because renaming would move `voxref`'s pinned scene for a cosmetic reason. NOTHING about performance, and nothing about the observable: the oracle answers in voxel/face/t and deliberately cannot produce a framebuffer.

**voxmicro — qualifying the oracle, and turning a residue into a distribution (URDRVXM1) — ESTABLISHED (the elementary scenes, the origin-semantics law, the interior-face theorem and its detector) + MEASURED (the residue decomposition and the impossible-pixel counts) + OPEN (what the surviving `not_covered` / `depth_rejected` mechanism actually is).** `tools/terrain/voxmicro.py` + gate stage `voxmicro` (4 rows: scenes / labels / residue / selftest) + red-first `tests/test_voxmicro.py` (26 falsifiers) + the committed record `spec/attest/voxref-micro.txt` (124 rows), on the `voxel-rung-zero` branch. WHY IT EXISTS: `voxray` ended with 13% disagreement and was careful to call it an UPPER BOUND on the reference's defect rather than a measurement. That caution was correct and it was also the end of the road — an upper bound on a residue is not a diagnosis, and three unrelated things were stacked inside this one number: a real unexplained defect, the ~1px ray/sample offset the projection-inversion law had already measured and pinned, and one whole declared frame excluded outright because the oracle answered in a different semantic domain. THE EXCLUDED FRAME IS A SEMANTICS QUESTION WITH A RIGHT ANSWER, AND THE ARGUMENT IS NOT ABOUT THE RENDERER: with the eye inside matter, `opaque` semantics return the containing voxel with no entry face for EVERY pixel at t = 0 regardless of direction, which `the_opaque_origin_is_direction_blind` proves — and an oracle whose output does not depend on the ray carries no information about the camera and cannot separate a correct renderer from a broken one. `transparent` semantics free the eye's OWN cell, that one cell and not the connected run it belongs to, restoring direction-dependence; `the_two_origins_agree_off_solid` proves the choice moves nothing anywhere else, pixel for pixel, on every scene and every declared frame. All eight frames are comparable for the first time. It also happens to agree with what the reference does from inside matter and THAT AGREEMENT IS NOT THE REASON — a semantics adopted because it matches the renderer under test is the circularity this arc exists to avoid. THE SAMPLING OFFSET IS SUBTRACTED FIRST AND BY NAME: a disagreement counts as `sampling_shift` when the rasteriser's answer at this pixel is the oracle's answer at one of the eight neighbours — exactly the bound the inversion law measured, no wider, so the subtraction cannot absorb a defect two pixels away. It is an UPPER bound on what sampling explains, which is the conservative direction: every other class becomes a LOWER bound on real defect. WHAT SURVIVES IS TRACED THROUGH THE REFERENCE'S OWN PIPELINE by instrumenting its inner loop — not generated, near-clipped, degenerate, backfacing, off-screen, rasterised but not covering this pixel, or covering it and beaten on depth — with a DECLARED fate ladder whose one arbitrary step (`backface` above `degenerate`) is stated as arbitrary, `unknown` required to be zero in every row, and a plant that removes the `depth_rejected` branch and shows exactly those pixels arriving in `unknown`. THE NUMBERS: over all eight frames the reversed-winding disagreement is 6266/55296 (11.3%); at most 3391 of it (54.1%) is the sampling offset; the remainder is 1765 `depth_rejected`, 1097 `not_covered`, 11 `degenerate`, 2 `phantom`. The as-committed arm decomposes as 46554 `backface` of 46897, which is the winding defect named rather than inferred. AND ONE FIGURE OWES THE ORACLE NOTHING AT ALL: a face with solid on the far side of its own outward normal is sandwiched between two solid cells, so from any eye outside the solid set every ray reaching it has already passed through one of them — no exterior camera can see an interior face, at any resolution, under any sampling rule. The reference awards pixels to 14032 of them as committed and STILL 2040 with the winding reversed. That number needs no oracle, no exclusion and no ray/sample caveat, and it is what turns 'a second defect is real and unexplained' from an inference into a measurement. TWENTY-FOUR ELEMENTARY SCENES, FIFTY-SIX EXPECTATIONS WRITTEN BEFORE THEY RAN, of which fifty-five held exactly as written; the fifty-sixth demanded that moving the ray ORIGIN by one unit leave the picture unchanged, which is false under perspective and had nothing to do with the boundary being probed, and is RESTATED with a control on both sides rather than relaxed. One scene records a limitation rather than a result: `voxref.basis` refuses a forward vector parallel to +z by construction, so the two z faces are never seen head-on ANYWHERE, in the micro-scenes or in the declared trace. LABELS BECOME CLAIMS: each of the eight frame names now carries one checkable assertion evaluated every gate run, because renaming the two `voxray` found wrong would have prevented nothing — the proof is that a THIRD was wrong too, `wall_flat`'s comment saying 'from the adjacent air voxel' when the wall is three voxels away. The control is that restoring an old name reddens the row. AND THE COVERAGE GAP IS STATED RATHER THAN LEFT AS AN ABSENCE: no declared frame stands on anything, so grazing incidence over a supported surface is covered only by `edge_on`, which floats one cell above the slab; two micro-scenes derived from the real world by a canonical scan and pinned supply the vantage, and moving the frozen trace belongs to the re-freeze rung. GRADE: the scenes, the two origin laws, the interior-face theorem and the instrument's agreement with the reference are ESTABLISHED (recomputed every gate run, each with a plant); the residue decomposition and the impossible-pixel counts are MEASURED; the mechanism behind the surviving `not_covered` and `depth_rejected` pixels is OPEN. `does_not_show`: NOTHING about performance; NO claim that the oracle is RIGHT — it is audited and qualified, which is much weaker than correct; NO claim that the reversed winding is the correct repair, since 2040 impossible pixels survive it; and the micro-scenes are deliberately trivial, so a reduction right on all of them can still be wrong on a full lattice. AND A BLIND SPOT IN THIS GATE'S OWN COVERAGE LAW, FOUND BY BUILDING AND RECORDED AS A ROW: `specfreeze.lattice`'s clause (e) requires every module carrying both `scene_result` and `SCENES` to sit in its sealed partition or its post-seal register. This module's first draft used `SCENES` for its micro-scene register and the clause noticed — but `voxref`, `voxray` and `voxcoarse` all pin conformance scenes through `scene_result`/`golden` and define no `SCENES` at all, so the clause has never seen one of them. It asked about this module on the strength of an attribute NAME. The register is renamed to the tree's convention, the module is entered in the post-seal register with its reason, and `the_sibling_modules_escape_the_coverage_clause` asserts the gap so it reddens when the clause is fixed to key on `golden` — a different rung. THE ROW `voxmicro-residue` IS SCHEDULED FOR RETIREMENT: it asserts the reference is still wrong, so the repair rung will redden it, which is the same retention-then-retirement shape `armpair` used.

**voxevent — does the visible surface grow with the geometry or with the incidences (URDRVXE1) — ESTABLISHED (the subdivision ladder's exactness, the degeneracy counts, the tie convention) + MEASURED (the growth rates and the incidence census) + UNDERDETERMINED (any compression claim, because the census is censored by the ray budget past the first rung).** `tools/terrain/voxevent.py` + gate stage `voxevent` (4 rows: ladder / degeneracy / ties / selftest) + red-first `tests/test_voxevent.py` (24 falsifiers) + the committed record `spec/attest/voxref-incidence.txt` (36 rows), on the `voxel-rung-zero` branch. WHY IT EXISTS: a rasteriser's cost scales with primitives and a camera observes a LOWER ENVELOPE, whose complexity Sharir (1994) bounds at O(n^(2+eps)) in three dimensions while the arrangement of viewpoints above it is tight at Theta(n^4 k^2) orthographic and Theta(n^6 k^3) perspective for k convex polyhedra of total complexity n (Aronov, Broennimann, Halperin, Schiffenbauer 2001). A world of 803 solid voxels IS k convex polyhedra, so that ceiling is not an object anyone enumerates — which is the real justification for `voxcoarse` sampling the viewpoint space rather than constructing it, and it is now written where that module can be read. The gap between those two exponents is the only compression axis worth building on and nothing here had measured where a real scene sits in it. IT READS THE ORACLE AND NEVER THE RASTERISER, because `voxmicro` left 2040 pixels the reference still awards to faces no exterior camera can see, so a compression law taken from the renderer today would be a law about a wrong answer; `voxray.first_hit` is audited, qualified, and now takes its occupancy AND its lattice as parameters, so a finer lattice is posed to the same oracle rather than to a second one. THE INSTRUMENT IS PROVED EXACT BEFORE IT IS READ: splitting every solid cell into s^3 cells of the same material multiplies primitives by s^3 and moves NOT ONE WORLD POINT — same entry point, same face, and a fine cell contained in the coarse one, at all four scales — with every comparison by cross multiplication, because `first_hit` returns UNREDUCED rationals whose representation depends on the cell size and the first probe was wrong in exactly that way. MERGED REGIONS ARE COUNTED AND NEVER BUILT: the merge relation (same face index, adjacent along an in-plane axis, taken to connected components) is greedy meshing's own, defined on the visible set alone with no knowledge of the subdivision, and letting the merged representation become the thing rendered would be the reduction redefining the observable that `voxref` was frozen to prevent. SEVEN PREDICTIONS WERE WRITTEN BEFORE THE LADDER RAN AND THREE MISSED. P5's wall camera CLIPPED THE WALL IT WAS PREDICTING ABOUT — 96 pixels of a 72-pixel frame, so a row of faces never entered the visible set and five of twenty-five corners went with them — repaired by making the scene meet the claim's own visibility precondition rather than by relaxing the count. P7 ASSERTED THE WRONG THING ENTIRELY: a simultaneous crossing decides which axis STEPS, not which face the eventual hit reports, and a ray tying at every step alternates until something stops it; rebuilt so the tie itself resolves the hit, the CONTROL came out sharper than the law — with only the losing candidate solid the ray MISSES IT ENTIRELY, because the tie sent the traversal down the other axis and out of the column, so the convention `voxray` never declared decides which cell is REACHED and not which label is printed, and 20.1% of declared rays enter through an edge or a corner at the finest scale against 1.8% at the coarsest. AND P3 MISSED FOR A REASON THAT CHANGES WHAT THE RUNG IS ABOUT: visible faces grew 22.4x rather than the predicted 30-70, and the shortfall is not geometry — a frame has W*H rays and no more, so the count of DISTINCT faces any census can observe is capped by the ray budget however fine the lattice becomes. The hit count is IDENTICAL at every scale (46685 over the eight frames, the same rays finding the same solid) while the share of hits landing on a distinct face climbs from 1.7% to 37.9%, so past the first rung the census measures the SAMPLER as much as the scene. That is `sealframe-cost-surface`'s two-axis defect arriving from the opposite direction — it held scene complexity still and varied resolution, this holds resolution still and varies complexity — and the second axis is unavailable here because W and H belong to a frozen contract. THE CLAIM IS THEREFORE THE FIRST RUNG AND NOT THE LAST: EIGHT TIMES THE PRIMITIVES MOVES THE MERGED VISIBLE REGIONS BY THREE PER CENT, measured where the visible set still covers each face densely; the 512x figure (visible faces 22.4x, merged regions 6.81x) is REPORTED and is not the claim. FOUR INCIDENCE-DEGENERATE FAMILIES, each with its count predicted before it ran: k coplanar faces in a row collapse 4k corner instances onto exactly 2(k+1) screen positions because the sharing is structural rather than projective; a k x k wall gives (k+1)^2 corners with four faces meeting at every interior one; a cube down its body diagonal puts three concurrent projected edges through a single point; and a block viewed along a lattice diagonal sends rays into edges and corners instead of faces. GRADE: the ladder's exactness, the degeneracy counts and the tie convention are ESTABLISHED (recomputed every gate run, each with a plant); the growth rates and the incidence census are MEASURED; any compression claim is UNDERDETERMINED, because the far end of the ladder is censored. `does_not_show`: NOTHING about performance — no renderer runs in this module at all; NO BOUND, since a growth rate measured on one scene family says nothing about the family that was not built, and Zhang, Everett, Lazard, Weibel and Whitesides (2008) measured the 3D visibility skeleton at roughly C*k*sqrt(nk) against a proven tight worst case of Theta(n^2 k^2) using random and structured scenes — what is new here is the deliberate degenerate construction against a byte-exact observable, which is a measurement and not a theorem; NO CERTIFICATE, because counting how few regions the visible surface has is not producing a witness that proves the rest invisible, and that rung waits for the reference repair; and NO SECOND AXIS.

**voxcand — a repair candidate, and it is not the reference (URDRVXD1) — ESTABLISHED (the two fixes, the transcription binding, the preservation chain, the non-authority guards) + MEASURED (what the candidate buys) + RED (fact three: visibility correctness is NOT achieved) + OPEN (the residual's mechanism).** `tools/terrain/voxcand.py` + gate stage `voxcand` (4 rows: arms / facts / preservation / selftest) + red-first `tests/test_voxcand.py` (26 falsifiers) + the committed record `spec/attest/voxref-candidate.txt` (32 rows), on the `voxel-rung-zero` branch. WHY IT EXISTS AND WHY IT REFUSES TO LAND: `voxmicro` proved the reference awards 2040 pixels to faces sandwiched between two solid cells, which no exterior camera can see at any resolution under any sampling. A repair landed in `voxref` moves O_t on every frame and invalidates `voxcoarse`'s 1728-state census and `voxevent`'s subdivision ladder, both of which are measurements OF the committed observable — and a PARTIAL repair invalidates them twice. So this rung establishes the repair and declines to promote it: `the_committed_reference_is_untouched` asserts every gate run that `voxref` still declares the committed winding and still reproduces its pinned contract digest, and `the_candidate_digest_is_not_an_observable` proves the candidate's hashes live in a different MAGIC namespace, so a candidate figure cannot be pasted where a frozen O_t is expected and pass unnoticed. THE SECOND DEFECT, ISOLATED: `voxref.render` computes `w = edge(...) + top_left_bias` and then feeds those same values into `d = (za*w1 + zb*w2 + zc*w0) // area`. The bias exists to decide which of two triangles OWNS a shared edge — it is a coverage rule, not a barycentric coordinate, and using it as one displaces the interpolated depth by up to one edge unit, which at grazing incidence is enormous. THE 2x2 REFUTED THE PREDICTION THAT MOTIVATED IT, and that is the finding: impossible pixels are 14032 committed, 2040 with the winding reversed alone, 14655 with the weights unbiased alone — WORSE than committed, and moving not one agreeing pixel, 8399 either way — and 661 with both; oracle agreement goes 8399 -> 50547 of 55296 (15.2% -> 91.4%). Correcting the barycentric coordinates of a renderer that draws the WRONG FACES does not make it draw the right ones; it gives those faces more accurate depths and lets more of them win, so the weight fix is CONDITIONAL on the winding fix and a bundled before/after would have reported 'the repair helps' while hiding that one half is actively harmful in isolation. PERSPECTIVE CORRECTION WAS TESTED AND IS REFUSED ON EVIDENCE, which is a result rather than an omission: with the bias still present it improved the total (2040 -> 1430) while REGRESSING `oblique` (300 -> 577) and `corner` (100 -> 265), and that regression is what sent the search from the interpolation to the weights; once the weights are unbiased it changes NO WINNER at any pixel of any declared frame, the two arms identical frame by frame, and against the oracle's exact t it ties on depth VALUES — 71.9% within one camera unit either way, mean absolute error 5.89 against 6.10. A tie costing an exact rational per pixel is not an improvement, and the verdict is pinned so the hypothesis cannot quietly return. PRESERVATION IS RE-RUN RATHER THAN ASSUMED, because a repair that fixed visibility and broke the partition would be a worse renderer with a better number: under the candidate the trace is still deterministic, no pixel of a quad is claimed twice WHILE dropping the bias still double-claims on the same sample (the partition with its control, not a sample without shared edges), draw order is still observationally irrelevant under both declared permutations, and both constructed witnesses still exhibit their pairs so neither digest has become a function of the other. THE FOUR FACTS: projection correctness PASS (unchanged), face orientation PASS (all six single-voxel axis scenes at ZERO impossible pixels — orientation demonstrated rather than argued), preservation PASS, and visibility correctness RED — 54 impossible pixels survive across three of the twenty-three renderable micro-scenes (`aperture` 38, `pair_oblique` 12, `world_standing` 4) and 661 across the declared trace. `the_third_fact_is_still_red` ASSERTS THE FAILURE so it cannot be forgotten, mis-read or rounded to zero, and it reddens on the day the residual closes — which is the day the frozen census may be regenerated and not one rung before; `the_census_may_not_be_regenerated_yet` states that consequence as a law rather than as a plan. GRADE: the two fixes, the byte-for-byte transcription binding to `voxref.render`, the preservation chain and the non-authority guards are ESTABLISHED (recomputed every gate run, each with a plant); what the candidate buys is MEASURED; fact three is RED by assertion; the residual's mechanism is OPEN. `does_not_show`: NOTHING about performance; NO claim that the candidate is CORRECT — it is measurably closer and measurably still wrong, and those are different claims; NO mechanism for the residual, whose leading hypothesis is the integer flooring of projected vertex positions (which would also account for the ~6-unit mean depth error and the `not_covered` population) and which has no controlled experiment behind it yet, a named hypothesis being not a finding; and NO observable, by construction.

**voxtie — is a disagreeing pixel a defect, or a sample on an event surface (URDRVXT1) — ESTABLISHED (the classifier, the ladder's binding, the resolvable ceiling, the oracle's freedom from isolated answers) + MEASURED (the classified population and the two further defects) + REFUSED (the carve-out) + OPEN (which tie rule, which is a design decision and not a bug fix).** `tools/terrain/voxtie.py` + gate stage `voxtie` (4 rows: census / ladder / ties / selftest) + red-first `tests/test_voxtie.py` (22 falsifiers) + the committed record `spec/attest/voxref-tie.txt` (1225 rows), on the `voxel-rung-zero` branch. WHY IT EXISTS: `voxcand` left 661 impossible pixels, two further candidate fixes take that to 152, and thirteen survive on one two-cell scene — an inviting number, and declaring them a permanent geometric degeneracy with a contract carve-out would have been the tempting move. THE ONE-SIDED LIMIT TEST REFUSED IT. Perturbing the ray a thousandth of a pixel either side in both screen directions, and the camera by one world unit along each of six directions, ONE of the thirteen never changes the oracle's answer: there is no ambiguity to appeal to, and the rasteriser awards that pixel to a face sandwiched between two solid cells anyway because `(depth, face_key)` prefers key 4710 to 5576. A carve-out written on the count would have buried a bug. SO THE RESIDUAL IS CLASSIFIED RATHER THAN TALLIED, into three classes with a declared priority: DEGENERATE, where the exact ray enters through an edge or corner and the ORACLE ITSELF is resolving by the convention `voxevent` named, so geometric uniqueness is not available; BOUNDARY, a clean single-plane entry whose answer differs across the sample, meaning the pixel lies on a visibility event surface and the two programs land on opposite sides of it; and STABLE, where the oracle gives one answer under every perturbation, so a disagreement IS a defect. TWO PERTURBATIONS BECAUSE THEY ANSWER DIFFERENT QUESTIONS: moving the screen sample tells you which side of a projected edge an integer pixel falls on, moving the CAMERA tells you whether the viewpoint sits on a boundary in viewpoint space, and conflating them would put a screen-space artefact into a viewpoint-space census — a distinction that matters the moment this feeds propagation work. THE NUMBERS OVER THE DECLARED TRACE: 1137 disagreeing pixels classify as 378 stable, 198 boundary, 561 degenerate; among the 152 impossible-face pixels, 80 are STABLE against 71 degenerate and 1 boundary. The carve-out is refused outright — stable disagreements outnumber event-surface ones and dominate them eighty to one on impossible faces — and the two-cell scene was misleading exactly because it was two cells. THAT LAW WAS FIRST WRITTEN WITH A THRESHOLD AND THE DATA MISSED IT BY THREE PIXELS: the claim was "a third of the disagreements are stable", 378 of 1137 is 33.25%, and `stable * 3 > total` reads 1134 against 1137. A fraction chosen because it sounded like the answer is a law fitted to a hope, and the repair was to DELETE the fraction rather than move it, since what the measurement supports is a comparison between two classes and not a number invented for it. THE ORACLE IS EXONERATED BY ITS OWN PATTERNS: the one-sided sequence is recorded per pixel rather than summarised, because `A->B->B`, `A->B->C` and `A->B->A` are three different situations and only the third would indict the oracle — and across the whole population the only patterns occurring are `AAA`, `AAB` and `ABB`, so the exact value ALWAYS equals one of the two sides. The oracle is a function with no isolated answers, and it is not uniformly right-continuous either; it takes whichever side the exact ray falls on, which is what a well-defined function does at a discontinuity. TWO FURTHER DEFECTS ISOLATED BY THE LADDER AND NEITHER APPLIED: the projection's axes round in OPPOSITE DIRECTIONS, since `voxref` computes `cy - (cu*F)//cf` and negates AFTER the floor so screen Y rounds toward +inf where X rounds toward -inf — which is also the mechanism behind the (-1,+1), (0,+1), (-1,0) bias `voxray` measured in its round-trip profile three rungs ago and could not explain; and the projected VERTEX is quantised to whole pixels, where carrying it at 1/64 closes the `aperture` scene entirely and 1/256 buys nothing further, so the floor is measured rather than assumed. Level zero of the ladder is REQUIRED to reproduce `voxcand`'s candidate arm exactly, keeping the chain bound from `voxref.render` through two transcriptions. FOUR TIE ORDERINGS MEASURED AND NONE ADOPTED, and the useful output is a CEILING rather than a ranking: the oracle's answer is among the tied candidates at exactly ONE of the thirteen exact-depth ties, so no tie rule can score above 1/13 however cleverly it orders them, because everywhere else the correct face is not competing at that pixel at all and the disagreement is about COVERAGE. The committed `(depth, face_key)` scores ZERO while the geometric-ray-parameter and normal-opposition orderings both reach the ceiling, so the committed rule is arbitrary AND worse than available — which turns "change the tiebreak" into a bounded proposal rather than an open-ended hope. Every candidate preserves draw-order independence, checked by reversing the candidate list, because that is the property the committed rule was built for and no replacement may cost it. GRADE: the classifier, the ladder binding, the resolvable ceiling and the oracle's freedom from isolated answers are ESTABLISHED (recomputed every gate run, each with a plant); the classified population and the two further defects are MEASURED, with the POPULATION pinned rather than the count since two different sets of 1137 pixels would tally the same and digest differently; the carve-out is REFUSED; and which tie rule to adopt is OPEN. `does_not_show`: NOTHING about performance; NO adopted tie rule, because the geometric-parameter ordering computes a ray/plane intersection inside the rasteriser and makes it partly a ray tracer, so choosing it is a decision about what the reference IS rather than a bug fix; NO promotion, since `voxref` is untouched here as in `voxcand` and the frozen census stays frozen; and NO generalisation past the declared trace and the tie scenes, because a classifier's verdict is about the samples it was given.

**voxfate — condition the population, then ask what broke it (URDRVXS1) — ESTABLISHED (the conditioning, the instrument's binding to the ladder, the demonstrated contamination, the census cache's invalidatable key) + MEASURED (the conditioned fate distribution and the minimal counterexample) + OPEN (WHY the coverage displaces, which is the next rung's binary experiment).** `tools/terrain/voxfate.py` + gate stage `voxfate` (4 rows: conditioned / counterexample / cache / selftest) + gate stage `rowtext` (2 rows: messages / identity) + red-first `tests/test_voxfate.py` (22 falsifiers) and `tests/test_rowtext.py` (13 falsifiers) + the committed record `spec/attest/voxref-fate.txt` (402 rows), on the `voxel-rung-zero` branch. WHY IT EXISTS: `voxmicro` answered the fate question three rungs ago and the answer was a SPREAD — some pixels lost to coverage, some to depth, some to a sub-pixel sampling offset, some to nothing nameable — and a spread is not a mechanism, it is what a mechanism looks like once it has been averaged with two other mechanisms. THE POPULATION WAS THE PROBLEM. `voxmicro` classified the WHOLE residual, which is three unrelated things stacked: pixels where the rasteriser is wrong, pixels sitting on a visibility event surface where two correct programs may legitimately land on opposite sides, and pixels whose ray enters a lattice cell through an edge or a corner, where the ORACLE ITSELF is answering by the convention `voxevent` named rather than by geometry. `voxtie` separated them into 378 stable / 198 boundary / 561 degenerate, and stable means the oracle is unchanged at the exact sample, at a thousandth of a pixel either side in both screen directions, and under six single-axis camera perturbations — the pixels where a disagreement admits no appeal to ambiguity. THE FATE QUESTION IS ASKED OF THOSE 378 AND OF NOTHING ELSE, AND THE ANSWER DOES NOT SPLIT: not_covered 318 (84.1%), depth_rejected 58 (15.3%), phantom 2 (0.5%); and of the 80 stable pixels awarding an IMPOSSIBLE face — one sandwiched between two solid cells, which needs no oracle to be called wrong — 78 are not_covered. The two populations that could have disagreed do not. THE LAW ASSERTS DOMINANCE AS A MEASUREMENT AND REDDENS ON A SPREAD, because the interesting outcome was the other one: had the fates spread evenly, the honest move would have been to preserve the spread and say the reference has several independent defects, not to pick the largest bar and call it the mechanism. `voxmicro`'s SAMPLING BRANCH IS DISABLED HERE AND THAT IS THE WHOLE METHODOLOGICAL POINT: that branch tests first whether the rasteriser's answer at a pixel equals the oracle's answer at an integer NEIGHBOUR and subtracts those as explained by the measured <=1px offset, which on THIS population is circular — these pixels are already known stable under sub-pixel perturbation, so a neighbour agreeing is not evidence that sampling explains anything, it is the question restated. THE CONTAMINATION IS DEMONSTRATED RATHER THAN ARGUED: the same classifier runs over the same pixels with the branch enabled, 269 of the 378 are absorbed into `sampling_shift`, and the coverage signal disappears entirely — so both distributions are pinned in the golden, because a claim that the conditioning matters is worth nothing without the unconditioned run beside it. AND THE TWO FACTS TOGETHER SAY SOMETHING NEITHER SAYS ALONE: sub-pixel stable, yet the rasteriser's answer equals the oracle's answer ONE WHOLE PIXEL OVER at 269 of 378 — that is not a sub-pixel ambiguity being resolved differently, it is a whole-pixel COVERAGE DISPLACEMENT, the fill rule claiming pixels for a face that the ray through the integer coordinate does not meet. THE SURVIVING DEFECT IS COVERAGE: not depth ordering, which `voxcand`'s winding fix addressed and which accounts for 58 pixels here; not the projection, whose axis asymmetry and vertex quantisation `voxtie` isolated and closed; not the tie convention, whose ceiling `voxtie` measured at 1 of 13. AND THE MINIMAL COUNTEREXAMPLE IS THE PIXEL THIS WHOLE ARC STARTED FROM: six rungs ago a face-culling experiment disagreed with the reference at frame 4, pixel (10, 0), and tracing it produced an unculled winner of voxel (2,1,10)'s INTERIOR top face beating voxel (2,1,11)'s own exposed face — impossible rather than merely surprising, and the reason an oracle had to exist at all; `voxray` then found the oracle names a THIRD answer there, voxel (2,0,10), which neither arm had. That pixel is now the first instance of the dominant class: the oracle's face is generated, is front-facing, is on screen, reaches the pixel loop, and does not claim the pixel. The investigation returns to where it began with the mechanism named. THE INSTRUMENT IS A THIRD TRANSCRIPTION of the same rasteriser loop and is REQUIRED to reproduce `voxtie`'s ladder winner on every declared frame, because a chain of transcriptions is exactly how a rung ends up measuring a fourth renderer nobody declared. THE CENSUS CACHE IS THE MAINTENANCE THIS RUNG MADE LOAD-BEARING: eleven oracle queries per disagreeing pixel, six `voxtie` laws and a golden that each need the whole census, and now a second module conditioning on it — materialised once per process on a key of five DECLARED inputs (world digest, level, sub-pixel denominator, viewpoint epsilon, semantic version) and NOT on `this run`, which would be a cache that cannot be invalidated. It is deliberately PROCESS-LOCAL: one surviving across runs would introduce exactly the stale-record failure mode this tree spends its rungs eliminating, and would do it while the records are still being established. TWO COSMETIC DEFECTS ARE RETIRED IN THE SAME CYCLE AND GIVEN DETECTORS, because the gate transcript is a certified artefact and its defects are defects: FIVE ROWS PRINTED A LITERAL DOUBLE PER CENT SIGN — the escape a `%`-formatted string uses to emit one per cent sign, which in a string that is never the left operand of a `%` is not an escape but two characters, and the transcript said `93%%` and `20.1%%` for four rungs. The detector is STRUCTURAL rather than textual: it walks each `record()` call's detail EXPRESSION and collects the string constants that are not a format string, so the same two characters are correct in one branch of a conditional and a defect in the other — which is exactly the shape of the fifth instance, found by the detector and not by the sweep that retired the other four. AND ONE STAGE NAMED THE WRONG MODULE: `voxlat`'s docstring and its row both claimed URDRVXF1, which is `voxref`'s magic, while `voxlat.py` declares URDRVOX1 — a copied header that had a reader chasing the wrong file. That detector is DELIBERATELY NARROW, reading only the FIRST parenthesised identity in a stage docstring and comparing it only against the module the stage is named for: 101 stages claim an identity and 96 are checkable, while a broad `no stage may mention a foreign magic` rule flags thirty of which twenty-nine are legitimate prose cross-references — `voxcand` SHOULD say it transcribes URDRVXF1 — so the broad rule would have been a detector whose output is a list to ignore. BOTH LIVE POPULATIONS ARE NOW EMPTY, WHICH IS WHY BOTH ROWS CARRY PLANTS: a detector with nothing to find is indistinguishable from one that cannot find, and the plants are the retired defects themselves with their corrected forms beside them as controls. GRADE: the conditioning, the instrument's binding, the demonstrated contamination and the cache key are ESTABLISHED (recomputed every gate run, each with a plant); the conditioned distribution and the counterexample are MEASURED, with the POPULATION pinned rather than the counts; and WHY the coverage displaces is OPEN. `does_not_show`: NOTHING about performance; NOT WHY the coverage displaces — this rung localises the class and does not explain it, and the candidates are the half-open top-left convention, the bounding-box derivation and the sample point the fill rule effectively uses, a binary experiment deliberately not attempted here because a rung that localised AND explained in one step would have no falsifiable middle; NO generalisation past the declared trace; and NO repair, since `voxref` is untouched as in `voxcand` and `voxtie` and the frozen census stays frozen.

**voxfill — the fill rule, one variable at a time, and the arm that won was not on the list (URDRVXL1) — ESTABLISHED (the algebraic classification, the control arm's binding, the bbox elimination, order-independence under every arm, the convention control's validity) + MEASURED (the arm ledger both ways, the 2x2, the tie decomposition) + REFUTED (this rung's own leading hypothesis, by its own control) + OPEN (which convention the contract should declare).** `tools/terrain/voxfill.py` + gate stage `voxfill` (4 rows: rejections / arms / conventions / selftest) + red-first `tests/test_voxfill.py` (31 falsifiers) + the committed record `spec/attest/voxref-fill.txt` (355 rows), on the `voxel-rung-zero` branch. WHY IT EXISTS: `voxfate` left the defect named but not explained — 318 of the 378 stable disagreements are `not_covered`, meaning the oracle's face was generated, front-facing, on screen, reached the pixel loop and did not claim the pixel. Three mechanisms inside a fill rule can do that and each is varied ALONE: drop the top-left bias (edge ownership), pad the candidate box (which pixels enter the loop), move the sample point (where the rule reads the triangle). EVERY REJECTION IS CLASSIFIED ALGEBRAICALLY BEFORE ANY ARM RUNS, by reading the three edge functions of the ORACLE'S OWN FACE at the exact sample point: 215 `bias_only` (every rejecting edge has e == 0, so the sample sits EXACTLY on the edge and only the top-left bias rejects it), 100 `outside` (some rejecting edge has e < 0), 3 `bbox` (never entered the candidate loop). That classification PREDICTS the arms rather than summarising them, which is the difference between an experiment and a sweep. THE 100 ARE OUTSIDE BY ALMOST NOTHING: 99 fall short by less than one pixel and typically by a single SUB-PIXEL UNIT at S=64, which is the quantum of the floor in the projection itself — and the distance is compared as an EXACT INTEGER by squaring, `e*e < S*S*(dx*dx + dy*dy)`, taking no square root and constructing no float, because a measurement that rounds cannot be evidence about a defect one sub-pixel unit wide. THE BOUNDING BOX IS ELIMINATED BY A STRONGER STATEMENT THAN THE ONE FIRST WRITTEN. The law began as `the bbox class is EMPTY, so padding can rescue nothing`, and the data refused it at three pixels. All three sit exactly one pixel right of the box; padding ADMITS all three; all three still FAIL the edge test. So the box is not merely tight, it is a conservative SUPERSET of the pixels the edges accept, and `wide_bbox` moves not one pixel anywhere on screen. THE REPAIR WAS TO MEASURE WHAT THE PADDING ACTUALLY ADMITTED rather than to shrink the claim: without that check, `the arm changed nothing` would have been green whether the padding reached those pixels or not — the vacuity failure mode this tree spends its rungs eliminating. THE OWNERSHIP ARM LOOKS LIKE THE ANSWER AND ITS LEDGER SAYS OTHERWISE: `inclusive` rescues 204 of the 318 and drops impossible faces 152 -> 59, but net agreement is only +58, from +396 GAINED against 338 LOST — and 236 of those losses are pixels that become EXACT-DEPTH TIES, because dropping the bias turns the partition into a cover and hands them to the `(depth, face_key)` rule `voxtie` measured at ZERO of its own resolvable ceiling. Gained and lost are reported separately and NEVER netted into one number, since a rung reporting only the rescue would be reporting the half of the ledger it chose; and the combined arm is deliberately NOT RUN, because a two-variable change explains nothing. ONE BELIEF IS CORRECTED ALONG THE WAY: draw order stays unobservable under EVERY arm, including the one that abolishes the partition, because the `(depth, face_key)` tiebreak already deletes draw order on a WRITTEN DATUM. The top-left rule was introduced to stop draw order reaching the screen; it is not what stops it. THE DECIDING EXPERIMENT VARIES THE ORACLE'S CONVENTION TOO, WHICH NO EARLIER RUNG OF THIS ARC HAS DONE. The oracle's ray through pixel (px, py) is DERIVED from the rasteriser's own projection, so a convention error is invisible to any experiment that holds one side fixed — and every rung so far has held the oracle fixed and asked what the rasteriser got wrong. Corner sample: 45550 against a corner-ray oracle, 41861 against a centre-ray one. Centre sample: 42744 and 46567. BOTH CONSISTENT PAIRINGS BEAT BOTH MIXED ONES, which is the shape of a CONVENTION error and not of a renderer defect — neither side is wrong alone. THE MECHANISM: the reference's projection FLOORS, mapping a screen position into a pixel REGION, while its sample point is that region's CORNER, and the oracle inherited the corner convention from the projection's ALGEBRA rather than from its ROUNDING; the two are inconsistent by half a pixel. THE STRONGEST NUMBER NEEDS NO ORACLE AT ALL: `impossible` counts pixels awarded to a face sandwiched between two solid cells, a property of the rasteriser alone that no convention choice can argue with, and it falls 152 -> 4 on the SAMPLE POINT ALONE. AND THAT REFUTES THIS RUNG'S OWN LEADING HYPOTHESIS: re-run `inclusive` with the conventions aligned and it is WORSE, 46560 against 46567 with impossible faces 7 against 4 — the same single change, opposite sign. The 215 `bias_only` pixels were an artefact of sampling a floored triangle at its corner, not evidence against the top-left rule, WHICH IS EXONERATED. The arm that won looked like the weakest of the three when scored against the misaligned oracle, because it moves the whole picture and the misaligned oracle counted that as 3739 losses. The instrument is a FOURTH transcription of the same rasteriser loop and its control arm is REQUIRED to reproduce `voxtie`'s ladder winner on every declared frame. GRADE: the classification, the control arm's binding, the bbox elimination, order-independence and the convention control's validity are ESTABLISHED (recomputed every gate run, each with a plant); the arm ledger, the 2x2 and the tie decomposition are MEASURED, with the POPULATION pinned rather than the counts; the ownership hypothesis is REFUTED; and which convention the contract should declare is OPEN. `does_not_show`: NOTHING about performance; NOT that centre sampling is CORRECT — it is better on both metrics under the pairing that assumes it, and adopting it changes what the ORACLE is, reaching every record derived from `voxray`, which is a contract decision of exactly the kind `voxtie` refused to take by default; NOT that the coverage diagnosis survives the convention change, since the population itself was selected under the corner convention and re-deriving it under the centre convention is the next rung; NOT why the one remaining whole-pixel `outside` rejection survives; and NO repair, since `voxref` AND `voxray` are both untouched and the frozen census stays frozen.
